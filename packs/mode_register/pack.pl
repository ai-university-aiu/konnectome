% State the fact: name(mode_register) — the hybrid automaton every konnectome construct now carries.
name(mode_register).
% State the fact: version('0.2.0') — slice 41 adds the departure lookup keyed on AGENCY, so a table
% governed by two agencies cannot answer with the row that merely happens to be declared first.
version('0.2.0').
% State the fact: the title names the construct and the corpus reading it realizes.
title('mode_register — the hybrid automaton: mode register, per-mode transfer functions, transition table, fault regimes (konnectome build slice 39)').
% State the fact: the author is the parent organization ai-university-aiu.
author('ai-university-aiu', 'ai.university.aiu@gmail.com').
% State the fact: requires([]) — the register imports only SWI-Prolog standard libraries.
requires([]).
% State the fact: layer(0) — base infrastructure; every construct at every grain holds a register.
layer(0).
