% State the fact: name(running_prediction) — the corpus's own phrase for what this construct holds.
name(running_prediction).
% State the fact: version('0.1.0') — slice 67 builds the carrier and corrects the obstacle behind it.
version('0.1.0').
% State the fact: the title names the construct and the corpus chapter it realizes.
title('running_prediction — the carrier that RETAINS an efference copy across the sensory feedback lag and releases it against the reading it was waiting for, so a prediction made on one tick can be judged on another: LAYER_10 the deep cerebellar nuclei, and Chapter 51.2.1 (konnectome build slice 67)').
% State the fact: the author is the parent organization ai-university-aiu.
author('ai-university-aiu', 'ai.university.aiu@gmail.com').
% State the fact: requires the scheduler whose convention derives the lag, and the copy it retains.
requires([tick_engine, efference_copy]).
% State the fact: layer(0) — base infrastructure; the carrier holds time and decides nothing.
layer(0).
