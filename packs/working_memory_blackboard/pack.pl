% State the fact: name(working_memory_blackboard) — the small erasable surface the present thought is written on.
name(working_memory_blackboard).
% State the fact: version('0.2.0') — slice 74 declares the maintenance period (DECISION-23) and the
% lifetime it implies, which is what let the cognitive cycle wire the board at last.
version('0.2.0').
% State the fact: the title names the construct and the corpus chapter it realizes.
title('working_memory_blackboard — the present-thought blackboard: gated admission, decaying slots, weakest eviction, caller-supplied rehearsal (konnectome build slice 49)').
% State the fact: the author is the parent organization ai-university-aiu.
author('ai-university-aiu', 'ai.university.aiu@gmail.com').
% State the fact: requires([tick_engine]) — the board reads the scheduler's SINGLE duration conversion
% and restates no wall-clock constant of its own. AUTO-FIX AT SLICE 74 under the Sixteenth
% Commandment: this line read requires([]) while its own comment said "one konnectome sibling", a
% contradiction standing in one sentence since slice 49; the dependency was always real and only the
% declaration was missing.
requires([tick_engine]).
% State the fact: layer(0) — base infrastructure; the board is written by attention and read by everything.
layer(0).
