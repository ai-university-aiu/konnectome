% State the fact: name(plasticity_engine) — the three-factor learning rule that changes the weights.
name(plasticity_engine).
% State the fact: version('0.1.0') — the first delivery, konnectome build slice 6.
version('0.1.0').
% State the fact: the title names the construct, its architecture component, and its rule.
title('plasticity_engine — the plasticity engine (Architecture Component 8); the three-factor learning rule of Section A2.5 (konnectome build slice 6)').
% State the fact: the author is the parent organization ai-university-aiu.
author('ai-university-aiu', 'ai.university.aiu@gmail.com').
% State the fact: requires([neuromodulator_bus]) — reads dopamine from the bus as the third factor.
requires([neuromodulator_bus]).
% State the fact: layer(0) — base infrastructure; a same-layer edge to the bus is allowed.
layer(0).
