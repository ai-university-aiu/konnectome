% State the fact: name(renewal_loop) — the corpus's own engineering name for the self-feeding maintenance daemon.
name(renewal_loop).
% State the fact: version('0.1.0') — slice 53 builds the durable tier as a process, unwired.
version('0.1.0').
% State the fact: the title names the construct and the corpus chapter it realizes.
title('renewal_loop — the durable weight tier held as a MAINTAINED PROCESS and not a stored number: a self-feeding synthesis loop that latches above a threshold and collapses permanently if the loop is cut (konnectome build slice 53)').
% State the fact: the author is the parent organization ai-university-aiu.
author('ai-university-aiu', 'ai.university.aiu@gmail.com').
% State the fact: requires([]) — the loop imports only SWI-Prolog standard libraries.
requires([]).
% State the fact: layer(0) — base infrastructure; the loop maintains a value and runs nothing else.
layer(0).
