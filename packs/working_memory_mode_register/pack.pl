% State the fact: name(working_memory_mode_register) — the four postures the present-thought blackboard can be in.
name(working_memory_mode_register).
% State the fact: version('0.1.0') — slice 50 builds the register and the first real downward assignment.
version('0.1.0').
% State the fact: the title names the construct and the corpus chapter it realizes.
title('working_memory_mode_register — Entry 16''s four modes over the one blackboard, and the first downward assignment that addresses an individual rather than a kind (konnectome build slice 50)').
% State the fact: the author is the parent organization ai-university-aiu.
author('ai-university-aiu', 'ai.university.aiu@gmail.com').
% State the fact: requires([]) — the register imports only SWI-Prolog standard libraries and konnectome siblings.
requires([]).
% State the fact: layer(0) — base infrastructure; a register declares what a construct can be and runs nothing.
layer(0).
