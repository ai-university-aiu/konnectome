% State the fact: name(neuromodulator_bus) — the broadcast channel of global brain-chemical levels.
name(neuromodulator_bus).
% State the fact: version('0.4.0') — slice 41 adds the MODE THROW, a fourth key shape on which one
% write sets the mode of every construct of one kind, and with it konnectome's own recorded decision
% that a thrown mode is a COMMAND at the discrete grain rather than a bias on a threshold.
version('0.4.0').
% State the fact: the title names the construct and the architecture component it realizes.
title('neuromodulator_bus — the neuromodulatory broadcast bus and state announcement service (Architecture Component 4) (konnectome build slices 4 and 35)').
% State the fact: the author is the parent organization ai-university-aiu.
author('ai-university-aiu', 'ai.university.aiu@gmail.com').
% State the fact: requires([]) — the bus imports only SWI-Prolog standard libraries.
requires([]).
% State the fact: layer(0) — base infrastructure; the broadcast bus sits beneath every region.
layer(0).
