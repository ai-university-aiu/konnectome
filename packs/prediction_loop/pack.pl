% State the fact: name(prediction_loop) — the expectation store and comparator loop for prediction and prediction error.
name(prediction_loop).
% State the fact: version('0.2.0') — the closure delivery, konnectome build slice 12: the export half Wall-2 demanded.
version('0.2.0').
% State the fact: the title names the construct, the manuscript rung, and the build slices.
title('prediction_loop — the expectation store, the comparator, and the prediction-error loop; Rung One object permanence, the surprise of a wrong expectation, Appendix 6 (konnectome build slice 11); the export half under Causalontology 4.0.0 — forecasts as predicted_occurrence records, surprises as prediction_error records (konnectome build slice 12)').
% State the fact: the author is the parent organization ai-university-aiu.
author('ai-university-aiu', 'ai.university.aiu@gmail.com').
% State the fact: requires([archetype, causal_core]) — reuses the comparator archetype rule and PrologAI's Causalontology core.
requires([archetype, causal_core]).
% State the fact: layer(0) — base infrastructure; same-layer edges to archetype and the Causalontology core are allowed.
layer(0).
