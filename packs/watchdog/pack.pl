% State the fact: name(watchdog) — the windowed history that gives the supervisor something to judge.
name(watchdog).
% State the fact: version('0.1.0') — the first delivery, konnectome build slice 45.
version('0.1.0').
% State the fact: the title names the construct and the observation it closes.
title('watchdog — the first real watchdog: a windowed per-construct mode history, read as supervisor readings (konnectome build slice 45)').
% State the fact: the author is the parent organization ai-university-aiu.
author('ai-university-aiu', 'ai.university.aiu@gmail.com').
% State the fact: requires([]) — the watchdog imports only SWI-Prolog standard libraries and one sibling.
requires([]).
% State the fact: layer(0) — base infrastructure; any construct at any grain may be watched.
layer(0).
