% State the fact: name(override_controller) — vital drives preempt deliberation; the safety property.
name(override_controller).
% State the fact: version('0.1.0') — the first delivery, konnectome build slice 7.
version('0.1.0').
% State the fact: the title names the construct, its architecture component, and its safety role.
title('override_controller — the override controller (Architecture Component 7); vital drives seize control, the inviolable safety property (konnectome build slice 7)').
% State the fact: the author is the parent organization ai-university-aiu.
author('ai-university-aiu', 'ai.university.aiu@gmail.com').
% State the fact: requires([]) — the override controller imports only SWI-Prolog standard libraries.
requires([]).
% State the fact: layer(0) — base infrastructure; a small, high-priority component beneath the selector.
layer(0).
