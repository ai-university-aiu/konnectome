% State the fact: name(connection_graph) — the connectome as data: directed weighted delayed interfaces.
name(connection_graph).
% State the fact: version('0.2.0') — slice 9 adds the bus-modulated update (gain set by the bus).
version('0.2.0').
% State the fact: the title names the construct and the architecture component it realizes.
title('connection_graph — the connection graph (Architecture Component 3); directed weighted interfaces and signal propagation (konnectome build slice 3)').
% State the fact: the author is the parent organization ai-university-aiu.
author('ai-university-aiu', 'ai.university.aiu@gmail.com').
% State the fact: requires the relay archetype rule and the neuromodulatory bus (for gain).
requires([archetype, neuromodulator_bus]).
% State the fact: layer(0) — base infrastructure; a same-layer edge to archetype is allowed.
layer(0).
