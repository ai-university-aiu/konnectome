% State the fact: name(master_register) — Layer 11's own construct: the register of global states.
name(master_register).
% State the fact: version('0.1.0') — the first delivery, konnectome build slice 46.
version('0.1.0').
% State the fact: the title names the construct and the corpus layer it realizes.
title('master_register — the master mode register: a named global state plus a sub-mode, and the downward assignment (konnectome build slice 46)').
% State the fact: the author is the parent organization ai-university-aiu.
author('ai-university-aiu', 'ai.university.aiu@gmail.com').
% State the fact: requires([]) — the master register imports only SWI-Prolog standard libraries and siblings.
requires([]).
% State the fact: layer(0) — base infrastructure; the state above everything sits beside the bus it is announced on.
layer(0).
