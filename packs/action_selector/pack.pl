% State the fact: name(action_selector) — the referee that releases exactly one action and never invents one.
name(action_selector).
% State the fact: version('0.1.0') — the first delivery, konnectome build slice 5.
version('0.1.0').
% State the fact: the title names the construct, its architecture component, and its inviolable invariant.
title('action_selector — the action selector (Architecture Component 6); releases exactly one candidate, never invents one (konnectome build slice 5)').
% State the fact: the author is the parent organization ai-university-aiu.
author('ai-university-aiu', 'ai.university.aiu@gmail.com').
% State the fact: requires([]) — the selector imports only SWI-Prolog standard libraries.
requires([]).
% State the fact: layer(0) — base infrastructure; the referee sits beneath the regions it arbitrates.
layer(0).
