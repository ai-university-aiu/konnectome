% State the fact: name(supervisor) — the watcher that publishes warnings and never admits a fault as a mode.
name(supervisor).
% State the fact: version('0.1.0') — the first delivery, konnectome build slice 42.
version('0.1.0').
% State the fact: the title names the construct and the corpus directive it realizes.
title('supervisor — the supervisor channel: fault regimes watched and published as warnings, never admitted as modes (konnectome build slice 42)').
% State the fact: the author is the parent organization ai-university-aiu.
author('ai-university-aiu', 'ai.university.aiu@gmail.com').
% State the fact: requires([]) — the supervisor imports only SWI-Prolog standard libraries and its siblings.
requires([]).
% State the fact: layer(0) — base infrastructure; every construct at every grain may be watched.
layer(0).
