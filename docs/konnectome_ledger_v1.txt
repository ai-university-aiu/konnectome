KONNECTOME LEDGER - THE RUNNING SCOREBOARD OF THE BUILD  (v1)

This is the single, canonical, navigable record of every wall the konnectome build
hits, exactly as PrologAI_Requirements_Ledger_v1.txt was for the language. Every wall
becomes an entry here FIRST; from here it is routed to exactly one destination: the
causalontology change order, the PrologAI change order, or a konnectome construct.
A wall shown to be UNNECESSARY is recorded as honestly as a wall that needed building:
honest non-closure is a real result, not a failure. Findings are DISCOVERED by
building, never guessed from the armchair (Second, Third, and Fourth Commandments).

Author: D. R. Dison / ai-university-aiu.

Acronyms are spelled out in full on first use, with the acronym in parentheses, and the
bare acronym thereafter. External proper names are kept verbatim (SWI-Prolog, Ed25519,
Git, GitHub, Abstraction and Reasoning Corpus). A causal_relation_object is a
Causalontology structure record, spelled in full per the Twelfth Commandment. konnectome
is a "Whole-Word System": no abbreviation series, no single-letter series - Loop-1 not
L1, Native-1 not N1, Proto-1 not P1.


================================================================================
SECTION ONE - WHAT THIS IS AND HOW TO READ IT
================================================================================

PART ONE - THE LEDGER'S ROLE

konnectome is a Proto-Artificial-General-Intelligence (Proto-AGI) prototype written in
PrologAI, storing its thoughts as Causalontology records (3.0.0 at the build's opening;
4.0.0 since the slice-12 closure), and built from the
appendices of Nature's Cognitive Architecture. The build method is enforce-then-build,
inherited from the PrologAI program: a wall the build hits becomes an entry here; a
load-bearing entry is routed to exactly one place; the next slice rests on the result
rather than re-inventing it. This ledger is the product's honest scoreboard.

PART TWO - STATUS MEANINGS

 - OPEN: recorded, not yet resolved. The build hit a wall; no destination decided yet.
 - ROUTED: the finding passed its gate and was written to one destination - the
   causalontology change order (Second Commandment), the PrologAI change order (Third
   Commandment), or a named konnectome construct (Fourth Commandment).
 - CLOSED: the destination delivered - a Causalontology change-order entry, a PrologAI
   change-order entry, or a konnectome module - and the build now rests on it. Every
   CLOSED entry names what closed it.
 - UNNECESSARY: the wall was shown, by building, not to need a change at all. Recorded
   as honestly as a wall that needed building.

PART THREE - THE THREE ROUTING DESTINATIONS (route each finding to EXACTLY ONE)

 - DATA-STRUCTURE gap (must be SHARED across agents or repositories as a signed,
   evidence-graded, content-addressed record, not merely computed inside one konnectome
   runtime) -> docs/Causalontology_4_0_0_CHANGE_ORDER_v3.txt. Gated by the Thirteenth
   Commandment: must keep the conformance suite green (one hundred nineteen vectors at
   3.0.0; one hundred thirty-seven since the 4.0.0 re-freeze - see Section Two).
 - LANGUAGE gap (a real, load-bearing PrologAI-language wall) ->
   docs/PrologAI_CHANGE_ORDER_v2.txt. Additive only, gated by the Thirteenth
   Commandment: must not regress ARC-AGI-1 (400 of 400) or ARC-AGI-2 (120 of 120)
   through Mentova.
 - konnectome-SOLVABLE gap (expressible inside konnectome's own code) -> a named
   konnectome pack or module, recorded in the coverage list of Section Four.


================================================================================
SECTION TWO - BUILD PROVENANCE (so the build is reproducible)
================================================================================

Build opened: 2026-07-19.

konnectome is building against these cousin commits (Step 4 requirement):

 - causalontology : local git head 76dfc69 (the standard re-frozen as Causalontology 4.0.0
                    at tag v4.0.0, squash-merged pull request 13, merge commit 64b1d1a; head
                    76dfc69 is the first wave of bindings - pull request 14 - adding JavaScript,
                    Rust, Perl, and C++, all green at 137 of 137 locally).
 - PrologAI       : local git head cd74f86 (causal_core 1.1.0, pull request 595, which
                    content-addresses the three new Causalontology 4.0.0 kinds additively; the
                    vendored conformance suite is green at 137 of 137).
 - Mentova        : local git head fb7d48f (flagship Synthetic Mind; home of the ARC
                    solving core; loaded read-only by the regression harness; unchanged).

The conformance gate is now the 137-vector Causalontology 4.0.0 suite (re-frozen from the
119-vector 3.0.0 suite by adding eighteen vectors for the three new kinds; every 3.0.0
identifier preserved byte-for-byte, witness vector 136). konnectome keeps it green at 137 of
137 in both the Causalontology Python reference and the PrologAI vendored suite.

Target repository: https://github.com/ai-university-aiu/konnectome (public; empty at
start of this build; scaffold merged as pull request 1; slices land as feature-branch
pull requests).


================================================================================
SECTION THREE - THE SPARCD FILESET PROVENANCE
================================================================================

The six SPARCD Fileset files were seeded verbatim from the appendices of
docs/NATURES_COGNITIVE_ARCHITECTURE_MANUSCRIPT.txt (Nature's Cognitive Architecture,
version 20), per the Seventh Commandment. Source line ranges, for audit:

 - konnectome_1_specification   <- Appendix 1, Specification   (lines 9388-9595)
 - konnectome_2_pseudocode      <- Appendix 2, Pseudocode      (lines 9596-9928)
 - konnectome_3_architecture    <- Appendix 3, Architecture    (lines 9929-10114)
 - konnectome_4_refinement      <- Appendix 4, Refinement      (lines 10115-10268)
 - konnectome_5_completion      <- Appendix 5, Completion       (lines 10269-10412)
 - konnectome_6_demonstration   <- Appendix 6, Demonstration   (lines 10413-10915)

Appendix 6 was sliced through its closing marker "END OF NATURE'S COGNITIVE
ARCHITECTURE, VERSION 20"; the manuscript's document-level CHANGE LOG that follows it is
versioning metadata, not appendix content, and was deliberately not carried in.

Per the Tenth Commandment, every code slice bumps the SPARCD Fileset. The current
version is v28 (bumped in lockstep by slice 27); every superseded version lives in
docs/archive/.


================================================================================
SECTION FOUR - THE EIGHTH COMMANDMENT AUDIT: CONSTRUCT COVERAGE LIST
================================================================================

This is the re-runnable audit. It names, for each construct of Nature's Cognitive
Architecture, the konnectome module that realizes it, so a missing construct is visible
at a glance. It is organized by the ten architecture components of Appendix 3, then the
six archetype rules, then the data model. It grows one realization per slice. It must
eventually cover every named construct AND every previously-unnamed construct (PUC),
each with its SUBSCRIBE and PUBLISH interface.

Legend: REALIZED = a module exists and is tested; PARTIAL = minimally realized, to be
deepened; PENDING = not built yet.

 THE TEN ARCHITECTURE COMPONENTS (Appendix 3, Section A3.2)
 -----------------------------------------------------------------------------------
 Component 1  Construct Registry        | cognitive_cycle world             | REALIZED
 Component 2  Scheduler / Tick Engine   | tick_engine (tick_engine_run/5,   | REALIZED
                                        |  tick_engine_step/3, two passes)  |
 Component 3  Connection Graph          | connection_graph                  | REALIZED
 Component 4  Neuromodulatory Bus       | neuromodulator_bus                | REALIZED
 Component 5  Drive System              | drive_system                      | REALIZED
 Component 6  Action Selector           | action_selector                   | REALIZED
 Component 7  Override Controller       | override_controller               | REALIZED
 Component 8  Plasticity Engine         | plasticity_engine                 | REALIZED
 Component 9  Observer / Instrument     | observer + tick_engine trace      | REALIZED
 Component 10 Perturbation Interface    | perturbation_interface            | REALIZED

 THE SIX ARCHETYPE RULES (Appendix 2, Section A2.3)
 -----------------------------------------------------------------------------------
 relay | integrator | oscillator | attractor | gate | comparator  | archetype      | REALIZED
   (The archetype pack realizes the A2.2 dispatch and the A2.3 six rules, isolation-tested
    per A4.2. Wiring them into the scheduler via the connection graph is a later slice. The
    heartbeat's three stand-in kinds - clock, copy, hold - remain in tick_engine for the
    scheduler test.)

 THE DATA MODEL (Appendix 3, Section A3.4) - stored as Causalontology records
 -----------------------------------------------------------------------------------
 Construct state (per tick)   | to become a Causalontology state_assertion    | PENDING
 Tick record (per tick)       | observer (Causalontology token_occurrence)    | REALIZED
 Interface / conduit          | to become a Causalontology conduit            | PENDING
   (Reused via PrologAI's library(causal_core). The observer pack (slice 2) records each
    tick as a token_occurrence instantiating a shared 'tick' occurrent, content-addressed
    by causal_core_identify, keeping the 119 conformance vectors green.)

 THE DEVELOPMENTAL-LADDER MILESTONES (Appendix 6) - runtime realized AND exported (Causalontology 4.0.0)
 -----------------------------------------------------------------------------------
 Rung Four theory of mind    | other_minds (reuses PrologAI theory_of_mind)  | REALIZED
 Rung One prediction error   | prediction_loop (the comparator loop)         | REALIZED
   (Slice 12. Both milestones now run AND export as shared, content-addressed (and, at
    deployment, signed - see Observation-3)
    Causalontology records. other_minds mints the attributed belief - true, false, and nested -
    as attitude records (holder, attitude_type, content), and exports the false-belief trial's
    predicted-search result as a shareable attitude record. prediction_loop mints the
    predicted_occurrence before arrival, the actual token_occurrence on arrival, and the
    prediction_error carrying the signed discrepancy. Wall-1 and Wall-2 were CLOSED by
    Causalontology 4.0.0 - the twenty-one kinds now carry the doxastic attitude kind and the
    predicted_occurrence and prediction_error kinds, and the suite is re-frozen at 137 vectors -
    plus PrologAI's causal_core 1.1.0, which makes the three new kinds mintable from konnectome.)

 THE PROVENANCE TIER (Appendix 3 data model: "every claim carries its provenance") - over konnectome's own records
 -----------------------------------------------------------------------------------
 Assert, grade, retract, supersede | self_provenance (reuses causal_core)      | REALIZED
   (Slice 13. konnectome exercises the Causalontology provenance layer over the attitude,
    predicted_occurrence, and prediction_error records it minted at slice 12: it asserts about its
    own records under its own source identity, grades the evidence on the ordered scale (intervention
    > observation > simulation > derivation > human_hint > imported), states a confidence, and
    retracts or supersedes an assertion when a later tick disconfirms or confirms the thought. It
    honors Rule 10 (a retraction is signed by the retracted record's source) and Rule 25 (the
    modelled HOLDER of an attitude differs from the signing SOURCE of an assertion about it -
    conformance vector V135). Reused via library(causal_core); the conformance suite stays green at
    137 vectors. Honest non-closure: the Ed25519 SIGNATURE is deferred to deployment - a private key
    is a secret barred from code (Constraint 3) - so the records carry the public source identity,
    are content-addressed, and are shareable without it. See Observation-3.)

 THE DEMONSTRATION (Appendix 6, the SPARCD Demonstration phase) - made runnable
 -----------------------------------------------------------------------------------
 The capstone story        | capstone_demonstration + bin/run_capstone_demonstration.sh | REALIZED
   (Slice 14. One script boots the whole mind and prints the glass-box story in rung order:
    the heartbeat, reward, dopamine, action, learning, and regulated body; the object-permanence
    forecast confirmed (signed error zero) and surprised (signed error minus one); a chain of
    combined thoughts; the false belief beside the world's contrary record; and the provenance
    layer asserting, grading, superseding, and retracting over those records. Deterministic:
    the story told twice is identical. The story itself names the rungs not yet demonstrated
    and the deployment-deferred signature. Reads its siblings only through the library path.)

 THE DEVELOPMENTAL LADDER, RUNG TWO (Appendix 6, Section A6.3a) - opened
 -----------------------------------------------------------------------------------
 Symbol exchange (smoke test)  | symbol_exchange (reuses PrologAI language)    | REALIZED
 Pretend under quarantine      | pretend_play (reuses PrologAI imagination)    | REALIZED
   (Slice 15. The smoke test's defining phrase - a word GROUNDED THROUGH EXPERIENCE - is a hard
    precondition: grounding a word in a condition the mind never lived is refused with a named
    error, and the standard vocabulary (warm for the deficit, settled for the satisfaction) is
    built from a genuinely lived run of the whole cognitive cycle. Both directions of symbol use
    are realized: production and recognition, the hearing arriving through the reused language
    word bank. The pilot's load-bearing discriminator is pinned by test: the same heard words
    draw different replies from a mind in deficit ([warm, settled]) and a mind at its set-point
    ([settled]) - the mood colours the words. Pretend reuses the imagination pack's five-reality
    quarantine; a rollout auto-seals into the imagined reality, the observed record is never
    written by pretending, and konnectome adds one rule: promotion into observed is refused
    outright. Record level: a pretend relation is a type-tier causal_relation_object (a generic
    claiming no happening) with a simulation-graded stance via self_provenance. Honest
    non-closures: the full pilot conversation, a richer vocabulary, Rung Two joining the capstone
    story, and the token-tier export of a whole pretend episode - see the Slice-15 note.)

 THE DEVELOPMENTAL LADDER, RUNG FOUR (Appendix 6, Section A6.3a) - deepened
 -----------------------------------------------------------------------------------
 The social-pain test (A4.4)   | empathy over the UNCHANGED drive_system       | REALIZED
 The empathy trial             | empathy (resonance -> proposals -> selection) | REALIZED
   (Slice 16. The social-pain system test is realized as the famous experiment it re-runs: a
    social_connection drive defends belonging as a genuine set-point; a rejection signal severs
    it; and the violation registers through the IDENTICAL drive_system machinery that registers
    a thwarted physical drive - same error predicate, same step, same negative reward broadcast
    as dopamine - with the equivalence pinned number for number by test. No bespoke emotion
    pathway exists. The empathy trial: another agent's MODELLED distress resonates into the
    mind's own belonging (its own error rises - feeling-with is literal), the drives propose,
    and the selection flips from the mild physical need to the helping action, which is released
    (never invented) and shown restoring connection. Respiration in distress still seizes
    control over social helping. The attribution travels as an attitude of the existing 4.0.0
    enumeration (the agent FEARS her connection severed), asserted at observation grade and
    retracted when misattributed (Rule 10). Honest remaining ascent: the appraisal and
    regulation pilots of the rung, and the capstone story joining the new milestones.)


 THE DEVELOPMENTAL LADDER, RUNG THREE (Appendix 6, Section A6.3a) - trials complete
 -----------------------------------------------------------------------------------
 The transitive-inference smoke  | concrete_operations (steps returned as data) | REALIZED
 The seriation trial             | concrete_operations (order along a dimension)| REALIZED
 The causal-reasoning trial      | cause_and_effect (cause versus co-occurrence)| REALIZED
 The planning trial              | cause_and_effect (breadth-first means-end)   | REALIZED
 The classification trial        | category_formation + concrete_operations     | REALIZED
 The conservation trial          | quantity_model (model-read verdict)          | REALIZED
   (Slice 21. The smoke test is realized IN FULL and in the rung's own terms: the block on the
    cup on the table derives above(block, table) and returns THE DERIVATION ITSELF as data -
    each step a named operation (resting_is_above, transitive_chain) carrying the facts it
    consumed and the fact it produced - with a test that walks the steps and asserts each is
    named and justified, so the rung's governing condition (every inference a named, watchable
    operation, never an opaque leap) is ENFORCED, not merely claimed. Causal reasoning answers
    BY THE RECORD: a minted Causal Relation Object, or an unbroken chain of them, is what turns
    a co-occurrence into a cause, and the chain of record identifiers is the justification
    returned; a record pointing the other way is reported as the reverse rather than silently
    flipped; and a FORGED record - correctly shaped but never minted - is refused by name after
    its content address is re-derived from its own content, so the pack's central claim and its
    actual check say the same sentence. Planning searches breadth-first for the shortest route,
    justifies each step by the states it leaves and reaches, and owns the plan as an intends
    attitude. Every conclusion and intention is graded DERIVATION, never observation - reasoned,
    not seen. TWO TRIALS ARE PARTIAL, and the shortfall is named rather than rounded up:
    classification demonstrates the flexible re-sort by a different property but DEFERS the
    book's requirement that the categories be ones the mind FORMED FROM EXPERIENCE; conservation
    demonstrates the verdict shape - appearance-only transformations from a closed enumeration
    conserve, real additions and removals honestly change - but DEFERS a genuine represented
    model of quantity under changing appearance, and a verdict that follows from a name on a
    list is the surface-matching the trial exists to rule out. THE RUNG IS THEREFORE OPEN, NOT
    FINISHED. Honest remaining ascent: those two deferred halves, and the capstone story, which
    does not yet narrate this rung and still names it on its printed honest-limits list.)
   (Slice 22. The two deferred halves are BUILT and the rung's five trials are all REALIZED.
    Classification is completed by category_formation: the categories are FORMED FROM
    EXPERIENCE by PrologAI's concept_formation engine, reused unmodified - the first konnectome
    use of that engine - with the flexible re-sort delegated to the slice-21 classifier, an
    object fitting no formed category reported outside_every_category rather than forced, and
    the terse engine identifiers renamed to whole words at one guarded translation boundary.
    Conservation is completed by quantity_model: an amount is a REPRESENTED MODEL - individuated
    units plus an appearance - transformations act on the representation, and the verdict is
    read by RE-MEASURING the model, never from the transformation's name; the lossy pour (named
    like a pour, spills a unit) is judged changed, the receipt that the verdict sides with the
    world against the name. Both packs own their conclusions BY ENACTMENT: the record predicates
    accept no quoted text, but run the learning episode or the conservation trial themselves and
    mint what the enactment produced. The rung's trials are COMPLETE; the capstone story still
    does not narrate the rung, and its honest-limits line stands until the story is taught.)
   (Slice 23. The story IS taught: the capstone (0.3.0) narrates the whole rung - the derivation
    as data, seriation, the formed categories with the honest exile and the crossing re-sort, the
    re-measured pour and the lossy pour, the cited causal record against the co-occurrence, and
    the shortest plan - each sentence guarded by a strict live check, and the rung's line is GONE
    from the printed honest-limits list, which now names only embodiment.)

 THE DEVELOPMENTAL LADDER, RUNG FIVE (Appendix 6, Section A6.3a) - GROUNDWORK ONLY, rung not claimed
 -----------------------------------------------------------------------------------
 The sense-act smoke test        | body_interface (through simulated_body ONLY)| PARTIAL
 The three body connections      | body_interface (senses, homeostasis, action)| REALIZED
 The reaching-and-grasping trial | (needs a real machine)                      | PENDING
 The navigation trial            | (needs a real machine)                      | PENDING
 The homeostatic-survival trial  | body_interface (simulated groundwork only)  | PARTIAL
   (Slice 24. The book's Rung Five demands a REAL machine - its smoke test closes the loop
    "through a real machine" - so the rung is NOT claimed and cannot be by software alone. What
    stands is the SEAM: the three connections built over the honestly-named simulated_body
    stand-in, the sense-act loop closed through it, and the survival groundwork - a draining
    battery the mind defends of its own accord on a pinned deterministic rhythm, with a low
    battery FELT AS HUNGER through the same drive_system_error as every other pain. The two
    PARTIAL rows above stay PARTIAL until a real machine stands behind the seam; the two PENDING
    rows cannot begin without one. The capstone's honest-limits line still names embodiment.)
   (Slice 25. The story now TELLS the groundwork: the capstone (0.4.0) prints a body-seam chapter
    - the percept unchanged, the closed pass, the felt hunger, the survival rhythm, the deliberate
    tie, and the enacted, proven-minted loop record - opening and closing with the refusal that
    the rung is NOT claimed. The sub-table above is unchanged: telling groundwork does not
    upgrade a PARTIAL row, and the honest-limits line still names embodiment, now with the
    groundwork boundary said aloud in the epilogue itself.)


================================================================================
SECTION FIVE - FINDINGS AND OBSERVATIONS
================================================================================

Findings and observations discovered by building, newest first.

Wall-3 (2026-07-22, from slice 12, the closure build itself) - the closure's own wall, discovered
and CLOSED the same day. With Causalontology 4.0.0 released - its closed kind list grown from
eighteen to twenty-one, now carrying attitude, predicted_occurrence, and prediction_error, and its
conformance suite re-frozen at 137 vectors - konnectome turned to MINT those three kinds through its
reused library, and could not: PrologAI's causal_core 1.0.0 implemented only the eighteen 3.0.0
kinds, and causal_core_identity_fields/2 carried no rows for the three new ones, so
causal_core_identify failed on an attitude, a predicted_occurrence, and a prediction_error alike.
This was neither a data-structure gap (the standard already carried the kinds) nor a
konnectome-solvable gap (konnectome may not edit its cousins); it was a LANGUAGE-LIBRARY gap - the
PrologAI library had not yet caught up to the released standard - so it routed to the PrologAI change
order rather than the Causalontology one. It is the third routing destination the Ledger names, used
for the first time. CONSUMER-VERSUS-ONTOLOGY TEST: not applicable - the ontology already carried the
kinds; the gap was purely the language library's additive lag behind a shipped standard. STATUS:
OPEN (2026-07-22) -> ROUTED (2026-07-22) to docs/PrologAI_CHANGE_ORDER_v2.txt, additive only, gated
by the Thirteenth Commandment -> CLOSED (2026-07-22) by causal_core 1.1.0 (PrologAI pull request 595,
merge commit cd74f86), which added the three causal_core_identity_fields/2 rows additively, kept the
vendored conformance suite green at 137 of 137, and passed its honest mini regression - ARC-AGI-1 40
of 40 and ARC-AGI-2 12 of 12, a 10 percent spot-check with the full regression deferred, never
rounded up. Discovered by building, delivered through a gated change order, and released the same day.

Owner-Authorization-1 (2026-07-22). The owner's written instruction of 2026-07-22 authorizes
the release of the Causalontology 4.0.0 change order: "I would like to get to Causalontology
4.0.0 locked-in, please. As soon as possible, please, and thank you!" This satisfies the THIRD
release-gate condition of that change order - the owner authorizes the release of a candidate in
writing - for whichever candidates this Ledger releases. Together with the two other conditions
that Wall-1 and Wall-2 below satisfy (a real finding from a real build, and a passed
consumer-versus-ontology test), the gate is fully open for the two sections those walls route to.
STATUS: AUTHORIZATION RECORDED.

Wall-1 (2026-07-22, from slice 11, other_minds) - the first wall of the build. konnectome needed
to REPRESENT and SHARE another agent's belief - false and nested - as a signed, evidence-graded,
content-addressed record, and could not: the eighteen kinds contain no doxastic kind,
causal_core_identify fails on an attitude-shaped record, and the nearest kind (assertion) names
only the SIGNER, never a HOLDER, cannot hold false content without poisoning the commons, and
cannot nest. Demonstrated by building: an assertion dict WITH a holder field and one WITHOUT
content-address to the IDENTICAL identifier, so whose mind holds the belief is invisible to content
identity; and causal_core_identify FAILS outright on the attitude dict, whether the kind is passed
explicitly or inferred from the record's own type field. The false belief, the basket-search
prediction, and the nested konnectome-models-Sally attribution all hold inside the reused
theory_of_mind runtime and survive the failed export. CONSUMER-VERSUS-ONTOLOGY TEST: PASSES - the
attribution must travel to other minds and repositories as shared data (theory of mind is precisely
a model OF ANOTHER agent, and konnectome's runtime already holds it privately; only the SHARING is
impossible). STATUS: ROUTED to docs/Causalontology_4_0_0_CHANGE_ORDER (Section B, agent attitude and
theory of mind).

CLOSURE (2026-07-22, slice 12). CLOSED. Causalontology 4.0.0 (repository ai-university-aiu/causalontology,
squash-merged pull request 13, merge commit 64b1d1a, tag v4.0.0) added the ATTITUDE kind: a record whose
holder, attitude_type, and content fields let a belief name whose mind holds it, carry false content
without poisoning the commons (Rule 25, doxastic quarantine), and nest one attitude inside another (the
outer content is the inner attitude's identifier). The assertion kind is unchanged - it still names only
the SIGNER, so the permanent design the wall observed is retained, not walled: the holder now lives in the
attitude kind, invisible to an assertion's content identity. PrologAI's causal_core 1.1.0 (pull request
595, merge commit cd74f86) then made the new kind mintable from konnectome. In slice 12 the other_minds
pack (0.2.0) mints the true belief, the false belief that coexists with the world's contrary
state_assertion (both records distinct, no conflict), and the nested konnectome-models-Sally attribution,
each as a real attitude record with a deterministic re-mint and an infer-kind round trip; and it exports
the false-belief trial's predicted-search result as a shareable, content-addressed attitude record while
the runtime belief survives. What closed it: Causalontology 4.0.0's attitude kind (twenty-one kinds, 137
vectors) plus causal_core 1.1.0. Conformance green at 137 of 137 in both cousins.

Wall-2 (2026-07-22, from slice 11, prediction_loop). konnectome needed to RECORD an EXPECTED
occurrence as distinct from an OBSERVED one, and the signed, graded discrepancy between them, and
could not: no predicted_occurrence or prediction_error kind exists, a prediction minted as a
token_occurrence is byte-identical to a claim of fact (demonstrated: the same content-addressed
identifier for a forecast and a report of the same content), and a marker field is invisible to
identity and rejected by the closed schema (adding predicted:true leaves the identifier unchanged).
The comparator computes the signed, graded error each tick - a signed minus-one-point-zero on the
object-permanence trial when the screen lifts on nothing - and the actual outcome is minted as a
real token_occurrence, but the number and the expectation have no home among the eighteen kinds.
CONSUMER-VERSUS-ONTOLOGY TEST: PASSES - the predictive loop must be inspectable and gradable by
external auditors and shareable across minds (the glass-box thesis), not a transient number inside
one runtime. STATUS: ROUTED to docs/Causalontology_4_0_0_CHANGE_ORDER (Section A, prediction,
expectation, and prediction error).

CLOSURE (2026-07-22, slice 12). CLOSED. Causalontology 4.0.0 (merge commit 64b1d1a, tag v4.0.0) added
two kinds: the PREDICTED_OCCURRENCE (instantiates, interval, predictor, strength) - a forecast now
distinct in identity from a report - and the PREDICTION_ERROR (predicted, observed, discrepancy). Their
addition lifts the token tier to seven kinds and the whole closed list to twenty-one. PrologAI's
causal_core 1.1.0 made both mintable from konnectome. In slice 12 the prediction_loop pack (0.2.0) mints
the predicted_occurrence BEFORE arrival (on the start_tick dimension), the actual token_occurrence on
arrival, and the prediction_error pairing that carries the signed comparator discrepancy - the object-
permanence surprise, once a transient minus-one-point-zero inside one runtime, is now a first-class,
shareable, content-addressed record an external auditor can read. What closed it: Causalontology 4.0.0's
predicted_occurrence and prediction_error kinds (twenty-one kinds, 137 vectors) plus causal_core 1.1.0.
Conformance green at 137 of 137 in both cousins.

Observation-1 (2026-07-20, from slice 1 reconnaissance). The Causalontology 3.0.0
specification file spec/identity.md still states "seventeen whole-word schemes" and omits
the eighteenth kind (the cross_stratal_seam) from one inline list, though the kind table
directly below it and the rest of the specification correctly carry all eighteen. This is
a stale count in prose. STATUS: LOGGED, NOT ROUTED. It is not a data-structure gap
konnectome hit by building, it does not affect the one-hundred-nineteen conformance
vectors, and causalontology is a frozen cousin; it is recorded here as an observation for
the owner, not written to the change order.

Slice-1 note (2026-07-20). Building the heartbeat - the tick_engine pack, Component 2,
with its two-pass synchronous update - hit NO wall. PrologAI expressed the scheduler and
the passive observer directly, with no language gap, and no Causalontology record was
needed yet. Representing each tick as a Causalontology token_occurrence (via the reused
causal_core pack) is planned for slice 2, where a held record first becomes a durable
thought; that is a sequencing choice, not a wall. Honest non-closure recorded, per the
Fourth Commandment.

Observation-2 (2026-07-20, from slice 2). Causalontology's token_occurrence requires an
absolute RFC 3339 interval start, whereas a konnectome tick is dimensionless-ordinal. The
observer resolves this by advancing a nominal one-second-per-tick simulated clock from a
fixed simulation start, which is deterministic and reproducible and keeps the 119 vectors
green. STATUS: LOGGED, NOT ROUTED. It is not a wall - the record is expressible - and the
3.0.0 ordinal "ticks" unit remains available to record native ordinal windows between ticks
when konnectome models them (a later slice). No change order is needed.

Slice-2 note (2026-07-20). Building the six archetype rules (archetype pack) and the first
Causalontology thought (observer pack) hit NO wall. PrologAI expressed the archetypes
directly, and causal_core recorded the token_occurrence with no ontology change. The
archetypes are isolation-tested but not yet wired into the scheduler (that needs the
connection graph, Component 3); recorded as pending, not as a wall.

Slice-3 note (2026-07-20). Building the connection graph (connection_graph pack) hit NO
wall. PrologAI expressed the directed weighted interfaces and the input gathering directly,
and the relay archetype was reused to convey a signal. Honest non-closure: this slice
honours a one-tick interface delay (the natural synchronous delay); multi-tick delay lines,
computational-interface transforms, and representing interfaces as Causalontology conduits
are recorded as pending, not as walls.

Slice-4 note (2026-07-20). Building the reward loop (neuromodulator_bus and drive_system
packs) hit NO wall. PrologAI expressed the broadcast bus and the drive loop directly. Honest
non-closure: the bus holds a global level per modulator (per-target-territory levels are a
later refinement), and the drive reward is computed but not yet consumed by an action selector
(Component 6) or by learning (Component 8); recorded as pending, not as walls.

Slice-5 note (2026-07-20). Building the action selector (action_selector pack) hit NO wall.
PrologAI expressed the arbitration and the never-invent invariant directly. Honest non-closure:
the selector picks the single highest-salience candidate (the basal-ganglia go, no-go, and stop
pathways, and the override controller of Component 7, are later refinements); the drive-bias
helper connects it to the reward, but full wiring of proposals from every region is pending, not
a wall.

Slice-6 note (2026-07-20). Building the plasticity engine (plasticity_engine pack) hit NO
wall, and it closes the loop of the mind: the reward broadcast by slice 4 is now consumed as
the third factor of learning, strengthening the connections that earned it. Honest non-closure:
homeostatic scaling (the bound that keeps learning from exploding) and the eligibility trace
(Section A2.6) are later refinements; recorded as pending, not as walls.

Slice-7 note (2026-07-20). Building the override controller and the perturbation interface
(override_controller and perturbation_interface packs) hit NO wall, and it completes the
ten-component architecture. PrologAI expressed the vitality override and the experimenter's pokes
directly. Honest non-closure: Component 1 (the construct registry) remains a minimal construct list
rather than a dedicated pack, and full end-to-end wiring of all ten components into one running tick
is a later integration slice; recorded as pending, not as walls.

Slice-8 note (2026-07-20). Building cognitive_cycle - the integrated one-tick loop - hit NO wall.
It wires all ten components in the Section A3.3 order, and completes Component 1 (the construct
registry) as the world it carries. Every one of the ten architecture components is now realized and
running together. Honest non-closure: the construct update does not yet read the bus for per-tick
gain, action proposals are supplied rather than generated from the regions, and the body does not yet
respond to actions; recorded as pending refinements, not as walls.

Slice-9 note (2026-07-20). The three honest non-closures logged at slice 8 are now closed. The
construct update reads the bus for gain (connection_graph_step_modulated with a modulated relay),
action proposals are generated from the drives in proportion to their error (drive_system_proposals),
and the body responds to the released action (drive_system_apply_action). The integration test shows
a closed sensorimotor loop: acting to reduce a need moves the body toward its set-point, and that
improvement feeds back as the next reward. No wall was hit.

Slice-10 note (2026-07-20). Building thought combination (the thought_combination pack) hit NO
wall. It is not one of the ten architecture components; it is the substrate the Second Commandment
names - konnectome's thoughts, thought patterns, chains of thought, and trees of thought - realized
by minting Causalontology Causal Relation Objects through the reused causal_core. An atomic thought
is an occurrent; a combination is a content-addressed causal_relation_object over occurrent causes
and effects; a chain of thought reuses one combination's effect as another's cause; a tree of thought
is a branching, converging set (a directed acyclic graph). The 119 conformance vectors stayed green.
Honest non-closure: the token tier (a particular combination that actually happened), provenance and
signed strength, mechanism decomposition with the hierarchy-consistency check, and refinement lineage
are recorded as pending elaborations, not walls.

Slice-11 note (2026-07-22). Building other minds and the prediction loop (the other_minds and
prediction_loop packs) HIT the build's first two walls, and they are the first entries routed from
this Ledger. What was expressible: both runtimes work as glass boxes - other_minds, reusing
PrologAI's theory_of_mind pack, holds Sally's false belief, predicts she searches the basket where
she last saw the marble rather than the box where it now is, and nests konnectome's model of Sally
believing the basket; prediction_loop holds an expectation across ticks, mints the actual outcome as
a Causalontology token_occurrence, and computes the signed, graded prediction error each tick (a
signed minus-one-point-zero on the object-permanence trial when the screen lifts on nothing). What
was NOT expressible: neither the attributed belief nor the expected-versus-observed discrepancy can
leave the runtime as a shared Causalontology record. The eighteen kinds carry no doxastic kind and
no predicted_occurrence or prediction_error kind; causal_core_identify fails on an attitude-shaped
record; the nearest kind, assertion, records only the SIGNER, not the HOLDER, and content-addresses
a belief with a holder and one without to the IDENTICAL identifier; and a prediction minted as a
token_occurrence is byte-identical to a claim of fact, its predicted marker invisible to identity.
Both walls passed the consumer-versus-ontology test and were ROUTED to the Causalontology 4.0.0
change order (Wall-1 to Section B, Wall-2 to Section A). Honest non-closure at the ontology layer,
recorded as two real findings, per the Second and Fourth Commandments.

Slice-12 note (2026-07-22). Building the closure - minting the three new kinds through the other_minds
(0.2.0) and prediction_loop (0.2.0) packs - hit NO NEW wall, and it CLOSED all three walls the build
had held. This is the lockstep the whole method was built to demonstrate. Wall-1 and Wall-2, routed to
the Causalontology change order at slice 11, were released as Causalontology 4.0.0 (twenty-one kinds,
137 vectors); Wall-3, the closure's own wall - causal_core 1.0.0 could not yet mint the new kinds - was
routed to the PrologAI change order and closed the same day by causal_core 1.1.0. What the build could
previously only hold at runtime it now EXPORTS as shared, signed, content-addressed records: a belief
that can be false and can nest (minted as attitude records), a forecast distinct from a report (the
predicted_occurrence), and the surprise as a first-class record (the prediction_error). The two
'EXPORT PENDING' rows of Section Four are now fully REALIZED. Gates at this slice: other_minds 10 of 10
and prediction_loop 16 of 16 Prolog Unit tests pass (the earlier twelve packs unchanged, 69 tests; 95 in
total); naming, layer, and layer-binding gates report zero violations; the Causalontology conformance
suite is green at 137 of 137 (specification 4.0.0) in both cousins. Causalontology and PrologAI were
extended only through their own gated change orders, never edited directly. Honest closure recorded,
per the Second, Third, and Fourth Commandments.

Slice-13 note (2026-07-26). Building the provenance layer (the self_provenance pack) hit NO wall, and it
CLOSED the honest non-closure slice 12 had recorded - that konnectome minted the three Causalontology 4.0.0
content kinds (attitude, predicted_occurrence, prediction_error) but had not yet exercised the PROVENANCE tier
OVER them. self_provenance mints an assertion ABOUT one of konnectome's own minted records, carrying
konnectome's own source identity, an evidence grade from the standard's ordered scale (intervention >
observation > simulation > derivation > human_hint > imported), a confidence, and, optionally, the particular
token records that ground the claim; and a retraction that withdraws a prior assertion under the same source
(the Rule 10 discipline), so a disconfirmed forecast is disowned honestly rather than quietly forgotten. The
Rule 25 distinction between the modelled HOLDER of an attitude and the signing SOURCE of an assertion about it
is demonstrated over konnectome's own false-belief attitude, matching Causalontology conformance vector V135. A
forecast is graded 'simulation' before the outcome (a synthetic mind's model-based evidence, honestly ranked
below observation) and superseded to 'observation' once the world confirms it; the object-permanence surprise
(the signed error minus one when the screen lifts on nothing) triggers a retraction. No cousin was edited:
self_provenance reuses causal_core through the library path, and the conformance suite is untouched and green at
137 vectors in both cousins. Gates: self_provenance 17 of 17 Prolog Unit (PLUnit) tests pass (the earlier
fourteen packs unchanged, 95 tests; 112 in total); naming, layer, and layer-binding gates report zero
violations. Honest non-closure recorded (see Observation-3), per the Fourth Commandment.

Observation-3 (2026-07-26, from slice 13). konnectome mints assertion and retraction records that are
content-addressed, schema-valid, and semantically valid, carrying its PUBLIC source identity and its evidence
grade - the shareable substance of the provenance layer - but it does not itself produce the Ed25519 SIGNATURE
the provenance model specifies. The signature needs a PRIVATE key, and a private key is a secret barred from
konnectome's code by Specification Constraint 3 (and Section A5.5's no-secret rule), so signing is a
deployment-time act performed by the key-holder who runs the mind. The signature is optional to the schema and
is excluded from content-addressed identity, so the records validate and reproduce without it; a strict
consumer, following the standard's store discipline, would quarantine them as unsigned until the deployment
signature is applied. STATUS: LOGGED, NOT ROUTED. It is not a data-structure gap (the standard already carries
the signature and its evidence grading), and it is not a wall konnectome hit by building (the cognitive act of
asserting, grading, and retracting is fully realized at the record level); it is a deployment boundary. It is
recorded here for the owner as a candidate FUTURE consideration only: were konnectome ever to need to SHARE
cryptographically VERIFIABLE assertions across minds, a signing-and-verification primitive in the reused
PrologAI causal_core library (which today content-addresses but neither signs nor verifies, and no Ed25519
primitive is available in this SWI-Prolog build) would be where that finding is routed - discovered by
building, if and when the need is real, never guessed from the armchair.

Observation-4 (2026-07-26, from slice 19). The appraisal pilot (situation_appraisal) reads a situation's
valence straight from the drives' set-point error and, when it OWNS that valence on the record, draws the
attitude from Causalontology's closed attitude enumeration (believes, desires, intends, knows, expects, fears).
A threatening situation maps cleanly to fears - the same clean fit empathy uses. But the book names the positive
appraisal affect SATISFACTION (a met set-point), and the closed enumeration has no word for satisfaction or
contentment: the nearest home is desires, which denotes an unfulfilled wanting, not a fulfilled state.
situation_appraisal therefore approximates a met or improving situation as one the mind desires to keep, and
records the imperfection here rather than hiding it behind a silent mapping. STATUS: LOGGED, NOT ROUTED. It is
not a wall konnectome hit by building - the valence itself is computed exactly, and only the SHARED, exported
attitude approximates - and appraisal never needs to export satisfaction to function. It is recorded for the
owner as a candidate FUTURE consideration only: were a mind ever to need to SHARE a contentment attitude across
minds as a first-class record, a new attitude_type in a future Causalontology major version (the enumeration is
extensible only by a future major version) would be where that finding is routed - discovered by building, if
and when the need is real, never guessed from the armchair.

Slice-14 note (2026-07-26). Building the capstone demonstration (the capstone_demonstration pack and
bin/run_capstone_demonstration.sh, the repository's first runnable entry point) hit NO wall and recorded
NO new honest non-closure. The capstone invents nothing: it boots the whole mind through cognitive_cycle,
runs it for a chosen number of ticks, and narrates what the fifteen earlier packs already do, in rung
order - the heartbeat with its per-tick reward, dopamine broadcast, released action, and observer record;
the regulated body and the weight grown by the three-factor rule; the object-permanence forecast confirmed
(signed error zero, stance superseded to observation) and surprised (signed error minus one, assertion
retracted); a chain of combined thoughts; the false belief held in quarantine beside the world's contrary
record; and the provenance layer standing behind and disowning the mind's own stances. The narration is
structurally honest: it checks the dynamics before claiming them, its tests re-run the same world outside
the story and measure the body and the weight directly, and the story closes by naming what it does not
demonstrate (Rungs Two, Three, and Five, and the deployment-deferred signature of Observation-3). Gates:
capstone_demonstration 20 of 20 Prolog Unit (PLUnit) tests pass (the earlier fifteen packs unchanged,
112 tests; 132 in total); naming, layer, and layer-binding gates report zero violations; the Causalontology
conformance suite is untouched and green at 137 of 137 (specification 4.0.0) in both cousins, re-measured
this slice. No cousin was edited, per the Second, Third, and Fourth Commandments.

Slice-15 note (2026-07-26). Building Rung Two's opening (the symbol_exchange and pretend_play packs) hit
NO wall - and this slice carried an explicit WATCH, ordered by the owner's choice of this work: watch
honestly for the finding that would release change-order Section C, the imagination and reality
quarantine. The watch was kept, and its result is a clean non-finding, recorded here precisely. What
Section C would require: that konnectome needed to tag or QUARANTINE a whole SET of records as an
imagined world, distinct from the actual commons, and to SHARE that quarantined world across agents or
repositories, and could not, because the standard has only per-claim machinery. What building actually
required: nothing of the kind. The runtime quarantine is the reused PrologAI imagination pack - the very
pack the change order's own consumer-versus-ontology note names as the reason imagination stays a
language concern until a sharing need is real - and it sufficed entirely: pretend rollouts seal into the
imagined reality, never touch the observed record, and cross realities only by deliberate promotion,
which konnectome further restricts by refusing promotion into observed. At the record level the
per-claim machinery sufficed too: the pretend relation is minted as a type-tier causal_relation_object -
a generic that claims no happening, so it cannot pollute the commons by construction - and the mind's
stance on it is graded simulation through self_provenance, honestly ranked below observation. One
boundary was examined and deliberately declined: exporting a whole pretend EPISODE at the token tier
(token records for happenings that never happened) was considered and rejected this slice, because
unmarked token records of un-happened things are exactly the pollution the quarantine exists to prevent;
the standard's per-claim counterfactual boolean on token_causal_claim was verified available (identity
fields: causes, effects, covering_law, actual_delay, counterfactual) should that export ever become a
real, load-bearing need. THE WATCH RESULT: were konnectome ever to need to SHARE a whole quarantined
imagined world as signed, evidence-graded, content-addressed records - not compute it, SHARE it - that
finding would route to Section C through the full release gate. It has not happened, so nothing was
built and nothing routed: a non-finding recorded as honestly as a finding, per the Fourth Commandment.
Gates: symbol_exchange 18 of 18 and pretend_play 12 of 12 Prolog Unit (PLUnit) tests pass (the earlier
sixteen packs unchanged, 132 tests; 162 in total); naming, layer, and layer-binding gates report zero
violations; the Causalontology conformance suite is untouched and green at 137 of 137 (specification
4.0.0) in both cousins. Honest non-closures recorded as pending ascent, not walls: the full Rung Two
pilot conversation; a richer grounded vocabulary; the capstone story joining Rung Two under a bumped
version; and the token-tier pretend-episode export, if it ever proves load-bearing. No cousin was
edited, per the Second, Third, and Fourth Commandments.

Slice-16 note (2026-07-26). Deepening Rung Four (the empathy pack: the social-pain test and the empathy
trial) hit NO wall, and the slice's central discipline was architectural restraint: no bespoke emotion
pathway was built, because the specification's own account says the machinery of emotion exists BY
CONSTRUCTION in the drives. The social-pain test therefore added nothing to the pain path at all - the
rejection signal is one body-variable write, and the error, the step, the signed negative reward, and the
dopamine broadcast are the unchanged drive_system and neuromodulator_bus predicates running over a drive
named social_connection - and the tests pin the shared-circuitry equivalence number for number against an
equal-magnitude physical thwarting. The empathy trial composes only existing machinery: resonance (the
modelled distress lowering the mind's own belonging, guarded by named refusals), the drives' own
proposals, the selector's release, and the body's response; the helping action is never invented, and the
respiration override still seizes control over social helping, so the inviolable safety property is
re-proven under social pressure. The social attribution is owned as records: the modelled distress is an
attitude of the EXISTING 4.0.0 closed enumeration (fears) - no new attitude type was needed, so no
data-structure gap arose - graded observation through self_provenance and retracted when misattributed
(Rule 10). The pre-merge adversarial review confirmed two findings, both applied: fractional distress
levels exposed a LATENT drive_system defect - the fixed unit homeostatic step could cross a fractional
set-point distance, so a released help could overshoot into harm - fixed at the root by capping the step
at the set-point (drive_system 0.3.0, its own regression test added; homeostasis now settles AT its
set-point, never past it); and the exported resonance boundary now re-validates the distress term with
named refusals, so a hand-rolled term can never raise belonging above its set-point. Gates: empathy 17
of 17, drive_system 10 of 10, and symbol_exchange 19 of 19 Prolog Unit (PLUnit) tests pass (the other sixteen packs unchanged;
181 in total); naming, layer, and layer-binding gates report zero violations; the Causalontology
conformance suite is untouched and green at 137 of 137 (specification 4.0.0) in both cousins. Honest remaining ascent, recorded as pending refinements, not walls: the appraisal pilot and the
regulation pilot of Rung Four, and the capstone story joining Rung Two and this deepening into its
telling under a bumped version. No cousin was edited, per the Second, Third, and Fourth Commandments.

Session note (2026-07-26, the Observation-3 watch). The owner's chosen queue for this session closed
with the verifiable-provenance question: build a signing-and-verification primitive ONLY if a genuine
cross-mind verification need appears, never from the armchair. The watch was kept through slices 14, 15,
and 16 - the three slices of this session that minted and shared records - and the result is an honest
non-finding: every record stayed within one runtime and one repository; nothing ever needed to PROVE who
signed a record rather than merely read it; and no finding was routed. Observation-3 stands unchanged as
the recorded routing destination (a signing-and-verification primitive in the reused PrologAI causal_core
library) should the need ever become real. Recorded per the Fourth Commandment: a non-finding, as
carefully as a finding.

Slice-17 note (2026-07-26). The last slice-15 review debt is paid, and the DECISION the debt asked for
is recorded here precisely. The finding: the refusal to promote pretend into the observed record lived
only in pretend_play's own interface, while the reused imagination pack's crossing predicates
(imagination_promote/3, imagination_assert/2) remained callable, unguarded, by any pack on the shared
library path - so the documented rule held by good behaviour, not by construction. The two honest
options were (a) keep it a build discipline and interface-scope every documentary claim, or (b) harden
konnectome-side and make the discipline checkable. THE DECISION IS (b): a fourth standing gate,
bin/check_reality_discipline.sh, now refuses any merge in which any file of any pack outside
packs/pretend_play carries any textual road to the reused machinery - an imagination predicate name,
the imagination module qualifier, or the library(imagination) import - plain-English comment lines
excused, because English Readable Code must be free to discuss what it fences. The gate was proven
able to fail before it was trusted to pass: a deliberate violation was injected, caught by file and
line, and removed. Its own adversarial review then defeated the first draft twice - a
module-qualified store write that names no predicate, and a runtime-constructed call resolved by a
bare import - and once through a test file with an uncommon extension; it was hardened to close all
three roads and proven red again before its clean run was accepted. THE HONEST RESIDUE: a call that
constructs both its module and its goal at runtime leaves no textual trace, and no textual gate can
see it; that residue is recorded here as the discipline it remains - kept the way never-edit-a-cousin
is kept, and no longer breakable silently by ordinary code. The reused imagination pack itself is untouched (it is a cousin's code and
remains exactly as shipped); what is fenced is konnectome's own reach. The companion mark from the
same review - the malformed drive term failing silently in symbol_exchange_condition/3 - was already
closed by slice 16 with the named refusal symbol_exchange_bad_drive and its permanent test. Nothing
now remains of the slice-15 review's confirmed findings: each is fixed with a permanent test or
enforced by a standing gate. Gates: no test count moved (181 across nineteen packs, all green);
naming, layer, layer-binding, and the new reality-discipline gate all report zero violations; the
Causalontology conformance suite is untouched and green at 137 of 137 (specification 4.0.0) in both
cousins. No new wall; no cousin edited, per the Second, Third, and Fourth Commandments.


Slice-18 note (2026-07-26). Growing the capstone story (the capstone_demonstration pack, from version 0.1.0 to 0.2.0, over the one runnable entry point bin/run_capstone_demonstration.sh) hit NO wall and recorded NO new honest non-closure. The capstone still invents nothing: the bump is two new chapters wired into its narration, each a call outward into packs earlier slices already built and tested. The Rung Two chapter demonstrates the first grounded words - a word spoken only where the mind actually lived the condition it names, and a reply coloured by the drive state, so deficit and set-point answer differently - and pretend under quarantine, the rollout sealed in the reused five-reality machinery with the crossing into the observed record refused by name. The Rung Four chapter demonstrates the social pain - a social_connection drive defending belonging exactly as temperature defends thirty-seven, the two thwartings equal number for number through the identical predicate - and the empathy trial, where modelling another's distress lowers the mind's own belonging and flips the selection toward a released, never-invented help, with respiration still overriding kindness and the help capped so belonging lands exactly at its set-point. Every new chapter is verified by an INDEPENDENT dynamics check: the tests re-run the same siblings outside the story and re-mint every identifier, so the story still cannot grade its own homework. This closes the item slice 15 and slice 16 both recorded as pending ascent, not a wall: the capstone story joining Rung Two and this deepening into its telling under a bumped version - now paid. And the printed honest-limits list SHRANK by exactly one rung: it now names only Rung Three (concrete reasoning), Rung Five (embodiment), Rung Four's own appraisal and regulation pilots, and the deployment-deferred signature of Observation-3; Rung Two, once listed there, is shown rather than deferred. Gates: capstone_demonstration 36 of 36 Prolog Unit (PLUnit) tests pass (the other eighteen packs unchanged, 161 tests; 197 in total across nineteen packs, all green); naming, layer, layer-binding, and reality-discipline gates report zero violations; the Causalontology conformance suite is untouched and green at 137 of 137 (specification 4.0.0) in both cousins. No new wall; no cousin edited, per the Second, Third, and Fourth Commandments.


Slice-19 note (2026-07-26). Rung Four of the developmental ladder is finished: its two remaining pilots, situation_appraisal (0.1.0) and arousal_regulation (0.1.0), are built as thin reuse wrappers over machinery that already exists, so feeling arrives here by construction - no new Causalontology kind, and no eleventh architecture component for emotion. situation_appraisal reads the drives' own set-point error through drive_system_error and maps a signed error, or a change in it, to a valence from the standard's closed enumeration, calling a violated set-point a threat the mind fears and a met or improving one good and desired, owning the appraisal as an attitude graded through self_provenance at observation and retractable under Rule 10. arousal_regulation carries the aroused state as norepinephrine on the neuromodulatory bus (Architecture Component 4) and closes a fixed half of the distance to its tonic baseline each tick, settling exactly at baseline within a hair of it and never overshooting - the same homeostatic discipline the drive system's own capped step keeps. This closes the slice-16 pending item, the appraisal pilot and the regulation pilot of Rung Four. Gates: situation_appraisal 12 of 12, arousal_regulation 12 of 12, 221 in total, 137 of 137. No new wall; no cousin edited, per the Second, Third, and Fourth Commandments.


Slice-20 note (2026-07-26). The review debt of slice 19 is paid, in the method's own coin. An independent adversarial review of the appraisal and regulation pilots, run after they merged, confirmed three findings, and each is now fixed at its root and pinned by a permanent test. (1) situation_appraisal's change reader compared a difference of two independently-rounded set-point errors against exact zero, so two situations equidistant on opposite sides of a set-point - a mathematically neutral change - were classified good or bad on a rounding residue near 1.0e-16 and, worse, minted as an owned attitude the neutral case is meant to refuse; a rounding-noise band (situation_appraisal_change_epsilon, 1.0e-9) now absorbs the residue, so an equidistant float change reads neutral and is correctly refused. (2) arousal_regulation accepted a non-finite arousal level, and an infinite level never shrinks toward baseline, so the return would never terminate; the single gatekeeper now requires a finite level and refuses infinity by name. (3) arousal_regulation's whole-number tick rule had no test for its fractional-count refusal; it has one now. Both defects were LATENT - reachable only by a fractional body value or a hand-built infinity that no ordinary run produces - but a confirmed finding is fixed and pinned regardless. No demonstrated behaviour changed; the two pilot packs move to patch version 0.1.1. Gates: situation_appraisal 14 of 14, arousal_regulation 14 of 14, 225 in total across twenty-one packs, all green; naming, layer, layer-binding, and reality-discipline gates report zero violations; the Causalontology conformance suite is untouched and green at 137 of 137 (specification 4.0.0) in both cousins. No new wall; no cousin edited, per the Second, Third, and Fourth Commandments.



Slice-21 note (2026-07-26). Rung Three of the developmental ladder is OPENED - not finished, and the
difference is recorded here deliberately. Two new konnectome layer-0 packs, both thin reuse wrappers:
concrete_operations (0.1.0) carries the rung's logic trials and cause_and_effect (0.1.0) carries its
causal trials. No eleventh component was raised for reasoning, and no new Causalontology kind was
minted - the twenty-one kinds of specification 4.0.0 already carried everything reasoning needed to
say, and the causal record built in slice 10 for combining thoughts turned out to be exactly the
instrument this rung needs to tell a cause from a coincidence. The architecture did not grow to meet
the rung; the rung found what the architecture already had.

WHAT IS REALIZED AND WHAT IS DEFERRED, SIZED HONESTLY. The rung's smoke test - one valid transitive
inference, the block on the cup on the table, with every derivation step returned as data and named
(resting_is_above, transitive_chain) - is realized IN FULL, and a test walks the steps themselves, so
the "every inference is a named, watchable operation" claim is enforced rather than asserted. Of the
five pilot trials the guiding book names: seriation, causal reasoning, and planning are REALIZED;
classification is OPENED IN PART (the flexible re-sort by a different property is demonstrated; the
book's requirement that the categories be ones the mind FORMED FROM EXPERIENCE is deferred); and
conservation is OPENED SCHEMATICALLY (the verdict shape is demonstrated - an appearance-only
transformation from a closed enumeration conserves, a real addition or removal honestly changes -
while a genuine represented model of quantity under changing appearance is deferred). Both deferrals
are written into the packs' own headers, not only here. The capstone's printed honest-limits list
keeps its Rung Three line, correctly, and will keep it until those two halves are built.

THE REVIEW, AND WHY THIS SLICE'S FINDING COUNT IS THE LARGEST YET. The pre-merge adversarial review
ran four lenses (logic correctness, causal correctness, house rules, manuscript fidelity), each finding
then verified by a separate agent told to reproduce or refute it. FOURTEEN findings survived; two were
refuted and dropped. All fourteen are fixed at their root and pinned by permanent tests. The three that
matter most: (1) BLOCKING - the classifier grouped by unification rather than equality and never
required a property value to be fully instantiated, so an object with an unbound value joined EVERY
group and the sort silently returned a wrong answer; now an unbound or half-built value is refused by
name and group membership is decided by strict equality. (2) The support-cycle check restarted a fresh
walk from every edge remembering only the current path, so on a lattice of shared supports the walking
doubled per level - sixty-eight facts took eleven seconds and ninety would never have returned; it now
carries a shared proven-clear set, walking each object once rather than once per road, pinned by a
forty-diamond lattice test that now returns in milliseconds. (3) The causal pack claimed causation is
answered BY the minted record but checked only that a record was a dictionary with the right keys, so a
hand-written look-alike was accepted as proof and its invented identifier returned as justification; it
now re-derives every offered record's content address from its own content and refuses a forgery by
name - the claim and the check now say the same thing. The remaining eleven: malformed additions and
removals refused under their own name rather than misreported as unknown transformations; non-list and
improper-list collections refused by name instead of leaking raw system errors or silently answering;
unbound or malformed start and goal states refused instead of letting the planner invent a start and
answer a question nobody asked; malformed and unjustified plan steps refused by name instead of failing
silently; a self-causation record honoured rather than denied by the loop guard; a doubled-apostrophe
comment corrected; and the two overreach findings that rewrote the pack headers to size the
classification and conservation claims honestly.

Gates: concrete_operations 26 of 26 and cause_and_effect 25 of 25 Prolog Unit (PLUnit) tests pass (the
earlier twenty-one packs unchanged; 276 in total across twenty-three packs, all green); the naming,
layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology
conformance suite was RE-MEASURED this slice rather than inherited, and is green at 137 of 137
(specification 4.0.0) in both cousins - PrologAI through its own vendored runner, causalontology
through its Python binding. No new wall was hit and no new observation was recorded; nothing in
PrologAI or Causalontology was edited, per the Second, Third, and Fourth Commandments.

HONEST REMAINING ASCENT, recorded as pending refinements, not walls: Rung Three's two deferred halves
(categories formed from experience; a represented model of quantity under changing appearance); the
capstone story does not yet narrate Rung Three, the same debt slices 15 and 16 once owed and slice 18
paid; and Rung Five (embodiment) remains untouched.

Slice-22 note (2026-08-07). Rung Three's two deferred halves are BUILT, and the rung's five trials
are all REALIZED - the rung is complete in its trials, with the story's chapter still owed. Two new
konnectome layer-0 packs, both thin reuse wrappers in the house pattern:
category_formation (0.1.0): categories the mind FORMED FROM EXPERIENCE - the book's own words for
the classification trial's deferred half - realized by reusing PrologAI's concept_formation engine
unmodified (the first konnectome use of that engine): each learning episode resets the engine's
store, observes the experiences as property-value bundles, induces categories by seed clustering
over a caller-given shared-pair floor, and resets again, the episode owning the store at both ends;
the terse engine identifiers (con_1, ...) are renamed to whole words (formed_category_1, ...) at one
guarded boundary that refuses an unrecognized scheme by name. Sorting places objects into the FIRST
formed category whose defining core they carry; an object fitting none is reported
outside_every_category, never forced; the flexible re-sort by a different property is the slice-21
classifier reused, its refusals surfacing under its own names. Twenty-six tests.
quantity_model (0.1.0): a REPRESENTED MODEL of quantity under changing appearance - the conservation
trial's deferred half: an amount is individuated units plus an appearance, transformations act on
the representation (a pour carries every unit across; an addition mints fresh names past the highest
existing one; a removal takes real units), and the verdict is read by RE-MEASURING the model, never
from the transformation's name. The receipt is the lossy pour: named like a pour, spills a unit,
judged changed(5, 4) - the verdict sides with the world against the name, the book's "models the
world rather than its mere surface" enforced by test. Twenty-nine tests.
concrete_operations was re-sized to 0.1.1, documentation only: its header, manifest title, and the
schematic verdict's own export now point each deferred half at the sibling that realized it.

THE REVIEW, AND THE GATE HOLE IT CAUGHT. The pre-merge adversarial review (told to REFUTE, five
lenses, every kept finding reproduced live against the code) returned sixteen findings - two
blocking, ten should-fix, four nits - and all confirmed findings are fixed at the root and pinned by
permanent tests. The two that matter most: (1) BLOCKING - the sorter matched objects against a
category by an ordered-set subset test while accepting any ground list as a hand-built core, so a
core written out of order silently exiled a rightful member to outside_every_category (a wrong
answer, not a refusal); a core must now be exactly the sorted duplicate-free set the learner mints.
(2) BLOCKING, AND A KNOWN GATE HOLE - an unprefixed helper predicate (numlist_or_empty) shipped in
quantity_model and bin/check_pack_naming.sh PASSED it, because the gate's minority-cluster check
hunts known terse and retired prefixes and this straggler carried none; the helper was renamed
(quantity_model_numbers_up_to), and THE HOLE IS RECORDED HERE so review hunts unprefixed helpers by
hand until the gate learns to see them. The forgery family was shut by name (empty defining core,
the reserved sentinel name, doubled category identifiers, doubled property keys, doubled unit
names), and the largest door with it: both record predicates previously owned ANY quoted text as if
enacted; both now take no text at all and instead ENACT - the learning episode or the conservation
trial - minting only what the enactment produced. Honest-name fixes: an unbound transformation is
refused as unknown (it formerly unified into the pour clause and was refused as a malformed
appearance - a true refusal with a false reason); a minus-one spill is refused as a bad amount (it
would GAIN); the comparison predicate's contract is stated and pinned (it compares two models by
measurement alone and does not certify that any transformation connects them - the trial and the
record do that). Two review JUDGMENTS are recorded as honest sizing, not defects: the caller-given
shared-pair floor tunes how coarse the induced categories are (the mind forms them; the caller sets
the grain), and the represented model's appearance field is written but never read - no code path
estimates quantity from looks, so the lossy pour is the trial's proof of world-over-surface while
the deeper temptation to read looks never arises in this code.

Gates: category_formation 26 of 26 and quantity_model 29 of 29 Prolog Unit (PLUnit) tests pass;
concrete_operations' 26 stay green under its re-sized header; 331 in total across twenty-five packs,
all green. The naming, layer, layer-binding, and reality-discipline gates report zero violations
(with the recorded naming-gate asterisk above). The Causalontology conformance suite was RE-MEASURED
this slice rather than inherited: 137 of 137 (specification 4.0.0) in both cousins - PrologAI through
its own vendored runner, causalontology through its Python binding. No new wall was hit and no new
observation was minted; nothing in PrologAI or Causalontology was edited, per the Second, Third, and
Fourth Commandments.

HONEST REMAINING ASCENT, recorded as pending refinements, not walls: the capstone story narrates
neither Rung Three nor Rung Four's appraisal and regulation chapters (the recorded story debts, the
next slice's work); Rung Five (embodiment) remains untouched, and the guiding book's own Rung Five
text asks for a REAL machine - a fact that will size any software-only opening of that rung; and the
naming gate's unprefixed-helper hole awaits a gate that can see it.


Slice-23 note (2026-08-07). The two recorded STORY DEBTS are paid: the capstone grows to 0.3.0 and
its one runnable telling now narrates Rung Three whole (the derivation as data, seriation, the
formed categories with their honest exile and crossing re-sort, the re-measured conserving pour and
the lossy pour judged by the model against its own name, the cause told from co-occurrence by the
cited record, and the shortest plan) and Rung Four to its finish (the appraisal read from the
drives' own error, owned as the standard's fears and desires attitudes with the satisfaction
approximation of Observation-4 declared aloud in the story itself, the honest withdrawal when the
world moves on, and a full alarm regulated to rest EXACTLY at baseline in eight halving ticks). No
new pack and no new kind: the capstone's manifest gains six sibling requirements
(concrete_operations, cause_and_effect, category_formation, quantity_model, situation_appraisal,
arousal_regulation), all read through public interfaces. Every narrated sentence sits beneath a
strict live check in the chapter predicate - a drifted engine silences the story rather than
letting it lie - and the owned conservation verdict is minted by ENACTING the pour, per slice 22's
record-by-enactment discipline. The printed honest-limits list shrank to exactly one rung:
embodiment, disclaimed with the guiding book's own reason (a real machine). The story's take-backs
rose deliberately from three to four distinct retraction records when the appraisal chapter's
honest exit joined; the suite pins the raise, as at every raise since slice 14.

THE REVIEW, TOLD HONESTLY. The independent adversarial reviewer assigned to this slice was STOPPED
MID-RUN by the owner before producing findings. The builder therefore re-ran the review's planned
reproductions directly and records them as a BUILDER'S VERIFICATION - a weaker guarantee than an
independent refuter, stated as such: (1) the story told twice in one process is byte-identical, now
pinned by the permanent test story_is_deterministic_across_two_tellings; (2) the reasoning
chapter's category episode resets the reused inducer's store at both ends, and no other chapter or
pack reads that store, so the reset reaches nothing; (3) the narrated eight-tick landing is derived
from arousal_regulation's own published constants (decay fraction 0.5, snap epsilon 0.01) under
exact binary halving - platform-stable, not an accident of one machine; (4) the epilogue's
unsigned-records declaration and closing re-run promise survive unchanged, only the rungs line
shrank; (5) a line-by-line English-Readable-Code walk of both new chapters found every goal
carrying its own comment (the mechanical scan's flags were all continuations of single multi-line
terms). One judgment recorded: the fixed English describing the derivation steps and formed
categories is hard-coded prose GUARDED by strict equality over live engine output immediately
above it, matching the guard-then-tell pattern of the earlier chapters.

Gates: capstone_demonstration 44 of 44 Prolog Unit (PLUnit) tests pass (grown from 36); 339 in
total across twenty-five packs, all green. The naming, layer, layer-binding, and reality-discipline
gates report zero violations. The Causalontology conformance suite was RE-MEASURED this slice: 137
of 137 (specification 4.0.0) in both cousins. No new wall was hit and no new observation was
minted; nothing in PrologAI or Causalontology was edited.

HONEST REMAINING ASCENT, recorded as pending refinements, not walls: Rung Five (embodiment) remains
untouched, and the guiding book's Rung Five text demands a REAL machine - any software-only opening
must be sized as groundwork, never as the rung; the long-standing pending refinements stand; the
naming gate's unprefixed-helper hole (slice 22) still awaits a gate that can see it.


Slice-24 note (2026-08-07). Rung Five is OPENED AS GROUNDWORK, and the word groundwork is
load-bearing: the guiding book's Rung Five text demands a REAL machine, so the rung is NOT claimed,
the capstone was NOT touched, and its honest-limits line still names embodiment with the book's own
reason. Two new konnectome layer-0 packs:
simulated_body (0.1.0): the honestly-named stand-in machine - a camera, a draining battery, three
actuators (steer_around refused when the path is clear; recharge filling to full, capped; hold_still
logging its own stillness), and floor-at-empty drain physics with a negative drain refused by name.
A world, not a mind: no sibling imports, no state between calls, every step a pure map. Sixteen
tests.
body_interface (0.1.0): the book's three connections as one seam over the stand-in - sensors become
senses (camera readings surfacing as percepts, unchanged); the body state becomes the homeostatic
state (the identity mapping made explicit, the energy drive defending battery_charge at 1.0 exactly
as temperature defends 37, so a LOW BATTERY IS FELT AS HUNGER through the same drive_system_error as
every other pain); released actions drive the actuators (an unmappable release refused by name).
One sense-act pass (sense, drive step, propose, select, command, enact) closes the book's smoke loop
through the simulated machine; the survival run drains 0.25 charge per tick and the mind recharges
OF ITS OWN ACCORD exactly when the hunger presses (threshold 0.5), on a binary-exact two-tick rhythm
pinned as a literal trace at four and at six ticks. Two declared mapping constants (pressing
threshold, reflex salience) and one fixed candidate order (drives before reflexes, pinned at the
tie) are the pack's entire additions - no update rule, no new kind, no eleventh component.
Twenty-one tests.

THE REVIEW, TOLD HONESTLY - A MIXED DISPOSITION. The independent adversarial reviewer was stopped
mid-run by the owner for the second slice running, after leaving two concrete leads: a CONFIRMED
English-Readable-Code lapse (a clause head without its own comment - fixed on the named line), and
an open question the builder then reproduced: a hand-built record, never enacted, WAS stanced at the
observation grade. That door is now shut in slice 21's way: body_interface_stance proves the record
genuinely minted - re-deriving its content address from its own content and requiring the loop's own
designator - and refuses a forgery or a tampered designator by name (body_interface_unminted_record),
pinned by two tests. The builder finished the remaining planned lenses itself: the salience TIE
between a starving battery (error 1.0) and a seen obstacle (reflex 1.0) resolves to the earlier
candidate in the fixed order - the drives before the reflexes - reproduced, made deliberate in the
header, and pinned; the survival rhythm extended to six ticks unchanged and pinned; showing to a
malformed body and a full-charge drain pinned. Disposition recorded as MIXED: two findings from the
independent reviewer's partial run, the rest a builder's verification - the weaker guarantee, named.

Gates: simulated_body 16 of 16 and body_interface 21 of 21 Prolog Unit (PLUnit) tests pass; 376 in
total across twenty-seven packs, all green. The naming, layer, layer-binding, and reality-discipline
gates report zero violations. The Causalontology conformance suite was RE-MEASURED this slice: 137
of 137 (specification 4.0.0) in both cousins. No new wall was hit and no new observation was minted;
nothing in PrologAI or Causalontology was edited.

HONEST REMAINING ASCENT, recorded as pending refinements, not walls: the rung's real machine (the
reaching, navigation, and survival trials all demand one; the seam now waits for it); the
long-standing pending refinements. The capstone groundwork chapter, once listed here, was
built by slice 25 - the story now tells the seam without claiming the rung. The naming gate's
unprefixed-helper hole (slice 22), once listed here, was closed by slice 26 - the gate now checks
every defined head for the pack's own prefix, a committed fixture pins the old blind-spot shape,
and the standing hand-hunt mandate is RETIRED.


Slice-25 note (2026-08-07). The story learns the body seam, and the boundary holds. The capstone
grows to 0.4.0 and gains one chapter, THE BODY SEAM - RUNG FIVE'S GROUNDWORK, TOLD AND NOT CLAIMED,
placed between the finished Rung Four and the provenance layer. No new pack; the capstone's manifest
grows by simulated_body and body_interface, both read through public interfaces, every printed value
produced by the seam packs' own predicates unmodified. The chapter is guard-then-tell throughout:
the percept surfacing unchanged; the closed pass releasing the steering reflex, moving the body, and
leaving the camera clear; the hunger identity (error 0.5 equals error 0.5 through the one shared
drive_system_error); the six-tick survival rhythm pinned exactly (hold at 0.75, recharge of its own
accord at the pressing 0.5); the deliberate drives-over-reflexes tie; and the loop record owned BY
ENACTING the pass, its stance granted only after body_interface_stance proved the record genuinely
minted by re-deriving its content address. The chapter opens and closes with the refusal - there is
no real machine here; the rung is NOT claimed - and the epilogue's Rung Five disclaimer is kept
byte-identical with one appended sentence naming the boundary: groundwork is not the rung, and the
story does not promote it. This closes the pending-ascent item slice 24 recorded (a groundwork
chapter, if a future slice judged the story should tell the seam); the Rung Five sub-table rows are
deliberately unchanged - telling groundwork upgrades nothing. THE REVIEW, TOLD STRAIGHT - AND THIS TIME IT FINISHED. After two interrupted reviews (slices 23
and 24), this slice's independent adversarial reviewer ran to completion under a tighter scope (six
lenses, a hard reproduction budget). It confirmed four findings, none blocking: two REAL guard gaps
- the narrated hunger magnitudes (0.25 mild, 0.5 pressing) were printed but not pinned by any guard,
and the claimed EXACT salience tie never proved the equality (a drifted reflex salience would leave
the outcome unchanged while falsifying the tie) - and two MINOR - the stance sentence's forgery
claim ran no live probe, and the steering episode was the one seam claim graded only on the story's
own output. All four are fixed at root and pinned: the chapter now guards both hunger magnitudes
exactly, proves the tie by reading the reflex's live salience through the public candidate
interface and equating it with the starving drive's own error (in the chapter AND in the
independent test), runs a live forgery probe whose refusal licenses the stance sentence, and the
steering episode gained its own independent dynamics test. Gates: capstone_demonstration 52 of 52
Prolog Unit (PLUnit) tests pass (eight new: chapter coverage, the groundwork-not-the-rung wording,
the chapter's position, the reproducible enacted loop-record identifier, and four independent
dynamics checks - the survival rhythm, the hunger identity, the exact tie, the steering episode);
384 in total across
twenty-seven packs, all green; the naming, layer, layer-binding, and reality-discipline gates report
zero violations; the Causalontology conformance suite re-measured at 137 of 137 (specification
4.0.0) in both cousins. No new wall; no cousin edited, per the Second, Third, and Fourth
Commandments.

Slice-26 note (2026-08-07). The naming gate learns to see, and the hand-hunt is retired. The
slice-22 review had recorded a hole in bin/check_pack_naming.sh: its detectors hunted prefix
CLUSTERS (one-off clusters ignored; unknown stubs longer than two letters passing every list), so
an unprefixed helper bearing no known terse stem - numlist_or_empty proved it - passed the gate,
and the ledger mandated a hand-hunt in every review until the gate could see it. This slice adds
check six: every clause head DEFINED in a pack module must begin with the pack's own whole-word
prefix (or the sanctioned prologai_ namespace) - the house rule checked as written, not
approximated by a heuristic. The gate gains a PACKS_ROOT override and a --self-test mode, and the
proof is PERMANENT rather than injected-then-removed: a fixture pack under
tests/naming_gate_fixture/ (outside the library path, never loaded, its violation labelled
deliberate in its own comments) carries exactly the slice-22 shape, ships a placeholder test file
and a properly-prefixed public predicate so it can fail ONLY the new check, and the self-test
passes only if the gate catches the fixture's helper by name. Measured: the extended gate reports
zero violations across all twenty-seven real packs (no false positive introduced), and the
self-test passes (the proven shape cannot return unseen). The independent adversarial review of
this slice ran to completion and confirmed four findings, none blocking, all fixed before merge:
the self-test died on a relative invocation path (the script now captures its absolute path before
changing directory); check six scanned only the pack's main module file (it now scans every
prolog/*.pl in the pack, so a helper cannot hide in a second source file); a stray PACKS_ROOT could
turn the whole gate vacuously green (a run that scans zero packs now fails loudly with its own
error); and the self-test accepted the fixture failing for any reason as long as UNPREFIXED
appeared (it now requires the UNPREFIXED violation to be the ONLY violation, so it proves the sixth
check specifically). No pack changed; no test count moved
(384 across twenty-seven packs, all green, re-swept); the layer, layer-binding, and
reality-discipline gates report zero violations; the Causalontology conformance suite re-measured
at 137 of 137 (specification 4.0.0) in both cousins. The hand-hunt mandate above is retired in
this same change. No new wall; no cousin edited, per the Second, Third, and Fourth Commandments.


Slice-27 note (2026-08-07). The delay lines: the connectome learns to keep a signal in transit.
The slice-3 note above recorded multi-tick delay lines as pending, and the queue of the owner's
standing "All" named them the smallest remaining refinement. This slice makes the interface term's
Delay field load-bearing: the connection_graph pack grows three public predicates - build the
empty delay lines for a graph (a first-in-first-out buffer of Delay-minus-one in-transit values
per transmissive interface, the synchronous step delivering the final hop); one delayed
synchronous step threading the lines as explicit state; and a delayed run - beside the plain step
and run, which stand unchanged. Proven, not promised: with every delay at one tick the delayed run
reproduces the plain run exactly, state for state and tick for tick (a pinned identity test); a
chained two-tick and three-tick hop arrives at exactly tick five and nowhere sooner (the guiding
book's TEST OF PROPAGATION made runnable); a value already in transit survives its source going
silent; the weight scales the value at delivery. The independent adversarial review (tighter
scope, named lenses, hard reproduction budget - the third straight completion) confirmed four
findings: two REAL silent failures (an unknown construct kind silently dropped from the state by
the findall-driven step - a hazard mirrored from the plain step - and a source name absent from
the state silently destroying a delay line and its in-transit values) and two MINOR (delay
validation skipped for computational interfaces; a negative tick count failing without a word).
Three fixed at root and pinned: refusal clauses that throw by name in the plain, modulated, AND
delayed steps alike; an existence error for the missing source; validation of every interface
before the transmissive filter. The fourth confirmed as standing house behaviour mirrored from the
plain run - recorded, no change needed. Eleven tests grew (seven feature, four pins):
connection_graph six to seventeen, the repository 384 to 395 across twenty-seven packs, all green,
re-swept after the fixes because the refusal clauses touch steps every earlier slice built on. The
slice-3 pending item "multi-tick delay lines" is CLOSED by this slice; its neighbours -
computational-interface transforms, interfaces as Causalontology conduits, and the delayed step's
handling of the bus-modulated relay kind (refused by name today, wired in a later refinement) -
remain honestly pending. The four gates report zero violations, the naming gate passing its own
self-test; conformance re-measured at 137 of 137 (specification 4.0.0) in both cousins. No new
wall; no cousin edited, per the Second, Third, and Fourth Commandments.


Slice-28 note (2026-08-07). The eligibility trace: a reward one tick late still finds the
connection that earned it. The prior hand-off's queue named the eligibility trace (the guiding
book's FR-11 and Section A2.6) the natural next-smallest refinement under the owner's standing
"All", and this slice builds it into the plasticity_engine pack, which grows three public
predicates beside the untouched slice-8 learning step: build a fresh zero trace store for a graph
(one trace per transmissive interface, keyed by endpoints, an explicit stateless value in the
slice-27 style); one trace tick (the old trace times a fading factor slightly less than one, plus
the tick's fresh coincidence, the product of the two ends' activities); and a reward step (each
weight grows by its trace times the dopamine third factor times the learning rate, computational
weights untouched). Proven, not promised: the manuscript's own acceptance is pinned as two named
tests - both ends fire at tick one, silence at tick two fades the trace to nine tenths, and the
reward arriving only then still strengthens the weight by exactly the faded amount; withhold the
reward and the trace fades geometrically while the weight never moves. The independent adversarial
review (tighter scope, named lenses, hard reproduction budget - the fourth straight completion)
confirmed eight findings: four REAL of the silent-failure family (an unbound interface kind
silently BOUND to transmissive by the checking predicate's own memberchk, at both new steps, the
caller's variable mutated and the weight learned; a term that is not an interface silently skipped
by the trace step's findall; the same malformed term making the reward step FAIL silently instead
of throwing), three MINOR (duplicate trace keys resolved first-one-wins; an orphan trace silently
discarded when its interface vanished; duplicate transmissive interfaces double-counting one
coincidence), and one STYLE (an inert cut whose comment overstated it). All eight fixed at one
root and pinned with seven review tests: a shared refusal perimeter - one predicate judging shape,
kind (unbound included), duplicate interfaces, duplicate trace keys, ghosts, and orphans - now
runs before either new step touches a number, and the inert cut is gone. Nineteen tests grew
(twelve feature, seven pins): plasticity_engine five to twenty-four, the repository 395 to 414
across twenty-seven packs, all green, re-swept after the fixes. Recorded, not changed: the plain
slice-8 learning step retains its tolerance of unknown kinds (standing house behaviour, the same
disposition as slice 27's fourth finding); the reward step deliberately does not consume the
traces it spends (trace lifetime belongs to the caller). New pending line: the trace store is not
yet threaded through the cognitive cycle's live tick - carrying it through a running loop is a
natural companion to the homeostatic-scaling and per-territory-neuromodulator refinements still
queued. The four gates report zero violations, the naming gate passing its own self-test;
conformance re-measured at 137 of 137 (specification 4.0.0) in both cousins. No new wall; no
cousin edited, per the Second, Third, and Fourth Commandments.


Slice-29 note (2026-08-07). Homeostatic scaling: the bound that keeps the learning from exploding.
The guiding book's FR-10 demands the three-factor rule be BOUNDED - each region's average activity
held near a target by synaptic scaling, the slow negative feedback on plasticity itself - and the
owner's standing "All" made it this slice. The plasticity_engine pack grows three public
predicates beside the untouched learning, trace, and reward machinery: build a fresh zero running-
average store per construct (an explicit stateless value in the slice-28 style); one average tick
(an exponential moving average under a smoothing factor, absent activity counting as silence, as
ever); and a scaling step (each transmissive interface's weight multiplied by one plus the scaling
rate times the target-minus-average error of its RECEIVING end - all of a region's inputs sharing
one factor - floored at zero so a weight can be silenced but never sign-flipped, computational
interfaces untouched). Proven, not promised: the direction laws (overactive inputs shrink,
underactive grow, at-target untouched) and the manuscript's stability promise as a runnable worst
case - fifty ticks of maximal coincidence under relentless full dopamine, scaled every tick, hold
the weight at a finite fixed point just below one and nine tenths where unbounded learning would
climb past five. The independent adversarial review (tighter scope, named lenses, hard
reproduction budget - the fifth straight completion) confirmed four findings: two REAL with ONE
shared root (a malformed entry in the average store made the average step FAIL silently while the
SAME store was silently ACCEPTED by the scaling step - one store, two verdicts - and the same
malformation let a duplicate key slip the duplicate check) and two MINOR (an unbound store walked
into a stack overflow instead of an instantiation error; the constructors could mint stores their
own consumers refuse). All four fixed at the one root - a shared store judgement refusing
malformed pairs, unbound tails, and duplicate keys for EVERY keyed store in the pack - and
mirrored into the slice-28 trace store and both constructors, whose laxity shared the root; seven
review pins hold it. Twenty-four tests grew (seventeen feature, seven pins): plasticity_engine
twenty-four to forty-eight, the repository 414 to 438 across twenty-seven packs, all green,
re-swept after the fixes because the shared judgement now underlies the slice-28 machinery too.
Recorded, not changed: absent activations still count as silence (standing house behaviour).
Pending lines updated: the scaling target and rate are one value for the whole graph - per-
territory targets belong with the per-territory neuromodulators refinement still queued - and the
fixed-point proof holds the averages constant, proving the bound, not yet the fully coupled loop;
threading the average and trace stores through the cognitive cycle's live tick remains pending.
The four gates report zero violations, the naming gate passing its own self-test; conformance
re-measured at 137 of 137 (specification 4.0.0) in both cousins. No new wall; no cousin edited,
per the Second, Third, and Fourth Commandments.


Slice-30 note (2026-08-07). Per-territory neuromodulators: the field learns local concentrations,
and delayed time learns the bus. FR-6's second half - a broadcast field with a level per target
territory, the molecule of Chapter 7 modeled as a global parameter with per-target levels - and
the slice-27 pending line (the delayed step refusing the bus-modulated relay kind by name) both
land in one slice, the pairing the slice-27 note itself suggested. The neuromodulator_bus pack
grows two public predicates: a territory broadcast (an entry territory(Modulator, Territory)-Level
beside the global pairs, newest wins, only its own entry replaced) and a territory read answering
by the diffuse-field law - own level first, global fallback second, silence third. The design
decision that makes territories free to adopt: a construct IS its own territory (its name), so no
registry, no mapping, no caller changes; the plain modulated step switched to the territory read
with a pinned backward-compatibility identity on territory-free buses. The connection_graph pack
grows one public predicate: a delayed modulated step carrying delay lines and bus together, gain
read AT THE TICK OF DELIVERY, never emission, with the all-delays-one identity against the plain
modulated step pinned, and the delayed step's refusals (unknown kind, ghost source) kept in the
same voice. The slice-27 pending line is CLOSED. The independent adversarial review (tighter
scope, named lenses, hard reproduction budget - the sixth straight completion) confirmed NO REAL
finding - identity, delivery-tick timing, and backward compatibility all survived adversarial
reproduction - and two MINOR with one root in the bus's flat key space: a territory-shaped
compound posing as a modulator could alias and even silently overwrite a territory entry, and the
reads repeated the slice-28 lesson by silently binding unbound names. One guard fixed both -
modulator and territory names must be plain atoms, refused aloud as uninstantiated or by name
otherwise, across all four bus predicates - pinned by five review tests. Seventeen tests grew
(twelve feature, five pins): neuromodulator_bus four to fourteen, connection_graph seventeen to
twenty-four, the repository 438 to 455 across twenty-seven packs, all green, re-swept. New pending
line: the plasticity engine's third factor still reads the global dopamine level - a per-territory
third factor for learning is the natural next use of the new read. The four gates report zero
violations, the naming gate passing its own self-test; conformance re-measured at 137 of 137
(specification 4.0.0) in both cousins. No new wall; no cousin edited, per the Second, Third, and
Fourth Commandments.

Slice-31 note (2026-08-07). The per-territory third factor: each connection learns by the
chemistry of the place the signal arrives. The slice-30 pending line (the plasticity engine's
third factor reading the GLOBAL dopamine level) is CLOSED: the live learning step and the reward
step now read the third factor at the RECEIVING end's territory through the slice-30 diffuse-field
law - local concentration first, diffuse global field as fallback, silence with neither - so one
broadcast reward can teach different territories different amounts, and a territory explicitly
silenced at zero learns nothing however loud the global field. Because a construct IS its own
territory (the slice-30 stroke), no signature changed and no caller moved; on a territory-free bus
every weight is exactly what the global rule produced, the whole earlier suite passing untouched
as the proof. The independent adversarial review (tighter scope, named lenses, hard reproduction
budget - the seventh straight completion) confirmed three REAL findings with one root, each
reproduced before fixing: the live step - the one the cognitive cycle calls every tick - had never
been given the refusal perimeter its trace, reward, and scaling siblings carry, so a malformed
term made the step silently fail, an unbound kind was silently treated as non-learning, and a
duplicate transmissive interface silently learned the same coincidence twice. One fix at the root
(the shared interface judgement and duplicate check at the head of the live step), pinned by three
review tests. Ten tests grew (seven feature, three pins): plasticity_engine forty-eight to
fifty-eight, the repository 455 to 465 across twenty-seven packs, all green, re-swept. Remaining
pending lines: the trace and average stores not yet threaded through the cognitive cycle's live
tick; the scaling target and rate still one value for the whole graph (per-territory targets would
pair with this slice's read); the fully coupled learn-fade-average-scale loop unproven;
computational-interface transforms; interfaces as Causalontology conduits. The four gates report
zero violations, the naming gate passing its own self-test; conformance re-measured at 137 of 137
(specification 4.0.0) in both cousins. No new wall; no cousin edited, per the Second, Third, and
Fourth Commandments.

Slice-32 note (2026-08-08). The live tick carries the whole learning body. The slice-31 pending
line one (the trace and average stores not threaded through the cognitive cycle's live tick) is
CLOSED: the world dict grows six keys - the eligibility-trace store, the running-average store,
the fading factor, the smoothing factor, the scaling target, and the scaling rate - read aloud by
one world reader that refuses a missing key by name, and the tick now beats the learning body in
the manuscript's order after the fast three-factor step: traces fade and absorb the tick's fresh
coincidences, averages move a smoothing step toward the tick's activities, and the slow
homeostatic bound scales each region's incoming weights toward its target, the advanced stores
committed into the new world. Not one engine predicate moved: the stores were always explicit
stateless values, so this is a world-shape change, and the engine's own perimeters now guard the
live loop for free every tick. The boot worlds of the cycle's fixture, the capstone, and the
symbol exchange grew the six keys with the bound armed but at rest (rate zero) - the at-rest
identity pinned by a test proving an absurd target changes nothing at rate zero, and the liveness
of the bound pinned by a twin-run test where a live rate pulls a quiet region's weight above what
learning alone gave it. The reward step deliberately stays with the caller (trace lifetime belongs
to whoever decides a reward has arrived). The independent adversarial review (tighter scope, named
lenses, reproduction before fixing - the eighth straight completion) confirmed one REAL finding
under the silent-failure lens, reproduced before fixing: the run predicate's tick-count guard
CLAIMED refusal in its comment while the code just failed - a negative count failed silently, a
non-numeric count died with a stray arithmetic error. One fix at the root (a named tick-count
check refusing the unbound, the non-integer, and the negative aloud), pinned by two review tests.
Seven tests grew (five feature, two pins): cognitive_cycle four to eleven, the repository 465 to
472 across twenty-seven packs, all green, re-swept. Remaining pending lines: the scaling target
and rate still one value for the whole graph (per-territory targets would pair with the slice-31
read); the live worlds' scaling constants rest at rate zero, so the bound beats in the cycle's
tests but not yet in the capstone's told story; the fully coupled learn-fade-average-scale loop
under live constants proven finite only by the slice-29 fixed-point test; computational-interface
transforms; interfaces as Causalontology conduits; the token tier of thought combination. The four
gates report zero violations, the naming gate passing its own self-test; conformance re-measured
at 137 of 137 (specification 4.0.0) in both cousins. No new wall; no cousin edited, per the
Second, Third, and Fourth Commandments.

Slice-33 note (2026-08-08). The per-territory scaling target. The slice-32 pending line one (the
scaling target still one value for the whole graph) is CLOSED: the bound's target gains a
geography in the slice-30 diffuse-field law's exact shape - a per-territory target store (a list
of Name-Target pairs) rides beside the global target, a territory with its own target defends
that target, a territory without one defends the global target, and the old one-target step is
re-rooted THROUGH the territory step as its empty-geography case, the identity pinned term for
term by a test. The store is judged by the shared store perimeter (malformed pair, duplicate key,
unbound store each refused by name) and every target, local or global, by the activity-target
check. The world dict grows one key, scaling_targets, read aloud by the tick's world reader; the
three boot worlds (the cycle's fixture, the capstone's, the symbol exchange's) carry it empty -
present, honest, unmarked - and a twin-run test through the live tick proves the geography live:
two worlds differing only in one region's own target, and the marked region's incoming weight
pulled harder toward its own set-point. The scaling RATE deliberately stays global, recorded as a
boundary, not built speculatively. The independent adversarial review (tighter scope, named
lenses, reproduction before fixing - the ninth straight completion) confirmed one REAL finding
under the unbound-wrong-judgement lens, reproduced before fixing: an unbound target or rate was
refused as a domain error carrying an unbound variable - a refusal that cannot name its wrong -
where the house convention refuses the unbound as uninstantiated; the root, one number-check
pattern shared by the four numeric-constant checks (fading factor, smoothing factor, scaling
rate, activity target), was fixed once in all four and pinned by five review tests. Fourteen
tests grew (nine feature, five pins): plasticity_engine fifty-eight to seventy, cognitive_cycle
eleven to thirteen, the repository 472 to 486 across twenty-seven packs, all green, re-swept.
Remaining pending lines: the scaling rate still one value for the whole graph; the live worlds'
scaling constants rest at rate zero with empty geographies, so the bound beats per-territory in
the tests but not yet in the capstone's told story; the fully coupled learn-fade-average-scale
loop under live constants proven finite only by the slice-29 fixed-point test;
computational-interface transforms; interfaces as Causalontology conduits; the token tier of
thought combination. The four gates report zero violations, the naming gate passing its own
self-test; conformance re-measured at 137 of 137 (specification 4.0.0) in both cousins. No new
wall; no cousin edited, per the Second, Third, and Fourth Commandments.

 Walls: OPEN 0 | ROUTED 0 | CLOSED 3 | UNNECESSARY 0.   Observations: 4.
