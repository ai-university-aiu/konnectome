APPENDIX 1. SPECIFICATION

A1.0 THE PURPOSE OF THIS PHASE

The Specification phase defines the problem completely before any code is written.

Its restraint is its point: not one line of program code appears here.

What appears is a complete, plain-language statement of what the system must do, the qualities it must have, the scenarios it must serve, the boundaries it must respect, and the criteria by which it will be judged finished.

In the language of SPARC, this appendix is the deliverable traditionally named specification.md.

A1.1 PROJECT OVERVIEW

PROJECT NAME: Nature's Cognitive Architecture, referred to hereafter as the System.

PROJECT GOAL: to build a running software model of the human cognitive architecture at the grain established in this book, meaning the grain of roughly one hundred brain regions, forty named interfaces, the principal chemical messengers, and the dynamics, drives, and plasticity that make them change over time.

The System is not a model of individual neurons or synapses; it is a model of the regional architecture and its control loops.

TARGET AUDIENCE: three audiences, in order.

First, the independent researcher or student who wishes to build and study a whole-architecture cognitive model on modest hardware.

Second, the research laboratory with serious compute that wishes to scale the model and test it against real datasets.

Third, the implementation engine, human or artificial, that will construct the System from this specification.

WHAT THE SYSTEM IS NOT, STATED UP FRONT: it is not a large language model, and it is not trained on text.

It is a mechanistic, glass-box architecture whose every part corresponds to a named neuroscientific construct and whose every operation is inspectable.

This is a deliberate contrast to the dominant paradigm, and its rationale is developed in Appendix 6.

A1.2 FUNCTIONAL REQUIREMENTS

A functional requirement, labelled with the prefix FR, states what the System must DO.

Each is drawn directly from the chapter cited.

FR-1, THE CONSTRUCT REGISTRY. The System must represent every named construct of Chapters 2, 3, and 7, meaning each region, each interface, and each molecule, as a distinct, inspectable entity carrying its established function and its specification block (its dynamical archetype, its state variables, the drives it serves, its plasticity rule, and its modulators).

FR-2, STATE. Every region must hold a state consisting at minimum of an activation, a gain, and an adaptation variable, as defined in Chapter 8, and, where its archetype requires, a tuning or content variable and a phase variable.

FR-3, THE UPDATE RULE. The System must be able to compute, for every construct, its next state from its current state, its inputs, and the ambient neuromodulator levels, according to the dynamical archetype assigned to it in Chapter 8. This is the core operation: next_state = update(current_state, inputs, neuromodulators).

FR-4, THE TICK. The System must advance in discrete time steps, called ticks. One tick is one synchronous update of every construct at once, as specified in Chapter 8, Section 8.4. The System must be able to run for an arbitrary number of ticks.

FR-5, THE INTERFACES AS CONDUITS. The System must route signals between regions through the interfaces of Chapters 3 and 4, each carrying a delay and a weight, and each being either transmissive (passing its signal unchanged) or computational (applying its named transform), per the Perfect Wire Test of Chapter 3.

FR-6, THE NEUROMODULATORY BUS. The System must represent the neuromodulators of Chapter 7 not as point-to-point connections but as broadcast fields with a level per target territory, and must make those levels available to every construct's update rule and every learning rule.

FR-7, THE DRIVES. The System must represent the homeostatic drives of Chapter 9, each with a monitored variable, a set-point, a computed error, and a corrective bias on action selection. The System must compute reward as the reduction of drive error, per the homeostatic reinforcement learning unification of Chapter 9.

FR-8, ACTION SELECTION. The System must arbitrate among competing drive-proposed actions through a selection mechanism corresponding to the basal ganglia of Chapter 2, which selects exactly one action by releasing its inhibition while suppressing the rest, and which generates no actions of its own.

FR-9, THE OVERRIDE HIERARCHY. The System must enforce the override ordering of Chapter 9 and Chapter 12, in which older and more vital drives can preempt newer deliberative processes, such that, for example, the respiration drive cannot be suppressed to the point of self-termination.

FR-10, PLASTICITY. The System must change the weights of its interfaces over time according to the three-factor learning rule of Chapter 10: a weight changes in proportion to the product of the activity at its two ends and a neuromodulatory third factor, bounded by homeostatic scaling that holds each region's average activity near a target.

FR-11, THE ELIGIBILITY TRACE. The System must implement the eligibility trace of Chapter 10, so that a reward arriving shortly after an action can strengthen the connection that earned it.

FR-12, CONSOLIDATION. The System must support a distinct slow-timescale plasticity, corresponding to the structural plasticity and sleep replay of Chapters 5 and 10, in which important recent changes are consolidated during a dedicated offline phase.

FR-13, INSPECTABILITY. At any tick, the System must be able to report the full state of any construct, the level of any neuromodulator, the error of any drive, and the weight of any interface. Nothing is hidden. This is a hard requirement, not a convenience, and it is the property that distinguishes this System from opaque models.

FR-14, PERTURBATION AND LESION. The System must permit any construct to be disabled, any interface to be cut, and any neuromodulator to be raised or lowered, so that the System can reproduce the lesion and pharmacological experiments that are the evidential basis of the science. The System that cannot be given a simulated Parkinson's disease by depleting dopamine has not faithfully implemented this book.

FR-15, THE LARGE-SCALE NETWORKS. The System must represent the large-scale cognitive networks of Chapter 2, Section 2.10 (the default mode, salience, dorsal attention, frontoparietal control, cingulo-opercular, language, sensorimotor, visual, auditory, and limbic networks) as named groupings over the regions of the registry, each carrying its member nodes, its subscribe-and-publish couplings, and its functional signature, including the characteristic anticorrelations (for example, the default mode network deactivating as the dorsal attention and frontoparietal networks engage).

FR-16, THE COGNITIVE SYSTEMS AND PATHWAYS. The System must represent the cognitive systems and pathways of Chapter 2, Section 2.11, as named subgraphs and loops over the connection graph: the cortico-basal-ganglia-thalamo-cortical loops, the direct, indirect, and hyperdirect pathways, and the Papez circuit; and it must represent the glia of Section 2.11.5 (astrocytes, microglia, and oligodendrocytes) and the tripartite synapse as modulatory participants that gate plasticity and connectivity rather than as signal-carrying regions.

FR-17, THE TWO COMMUNICATION PRIMITIVES. The System must distinguish the two messaging primitives established in Chapters 3 and 7: a DIRECTED channel (send and receive) for a point-to-point interface, whose publisher is wired to a single subscriber; and a BROADCAST topic (publish and subscribe) for a volume-transmission interface and for every neuromodulatory and hormonal field, whose publisher addresses no one and whose subscribers are defined by the receptor they carry. A construct's inputs are what it subscribes to; its outputs are what it publishes.

A1.3 NON-FUNCTIONAL REQUIREMENTS

A non-functional requirement, labelled with the prefix NFR, states a QUALITY the System must have.

NFR-1, GLASS-BOX TRANSPARENCY. Every state, every weight, every signal must be a named, human-readable quantity tied to a named construct. No part of the System may be an uninterpretable numerical blob. This is the System's defining virtue and its central contrast with the prevailing paradigm.

NFR-2, DETERMINISM AND REPRODUCIBILITY. Given the same initial state and the same inputs, the System must produce the same trajectory. Any randomness must be seeded and recorded, so that any run can be replayed exactly.

NFR-3, SUBSTRATE INDEPENDENCE. The specification must be expressible without commitment to any programming language or data structure, so that it can be implemented in any language. This requirement is the subject of Appendix 6 and is a first-class goal, not an afterthought.

NFR-4, SCALABILITY BY GRAIN. The System must run at the regional grain on modest hardware (a personal computer), and its design must not preclude later refinement to finer grain given greater compute. The architecture must scale down to be buildable at home and scale up to be serious in a laboratory.

NFR-5, MODULARITY. Each construct must be a self-contained module with a clean interface, so that constructs can be added, replaced, or refined without rewriting the System. This mirrors the encapsulation thesis of the science itself: the brain is modular, and so must be its model.

NFR-6, BIOLOGICAL FIDELITY, BOUNDED. The System must be faithful to the neuroscience at the regional grain, and must not claim fidelity it does not have. Where the System simplifies, the simplification must be documented, in the discipline kept throughout this book.

NFR-7, VERIFIABILITY. Every requirement here must be checkable by an automated test, as developed in Appendices 4 and 5. A requirement that cannot be tested is not a requirement; it is a hope.

A1.4 USER SCENARIOS

A user scenario is a short plain-language account of what a user wants to accomplish.

SCENARIO 1, THE STUDENT. A student loads the System, runs it for a thousand ticks with a simple sensory input, and watches the activation of the visual regions, the value computed by the orbitofrontal cortex, and the action selected by the basal ganglia, tick by tick, to understand how a decision forms.

SCENARIO 2, THE LESION EXPERIMENT. A researcher depletes the System's dopamine to twenty percent of normal and observes the System's action selection slow and freeze, reproducing the bradykinesia of Parkinson's disease, then restores dopamine and observes recovery. The researcher has run a controlled experiment on a transparent brain.

SCENARIO 3, THE LEARNING TASK. A researcher presents the System with a stimulus repeatedly paired with a reward, and observes the relevant interface weights strengthen by the three-factor rule, so that the System comes to predict the reward from the stimulus. The researcher then presents the stimulus without reward and observes extinction. The System has learned and unlearned.

SCENARIO 4, THE DRIVE CONFLICT. A researcher raises the System's hunger drive and its threat drive simultaneously and observes the override hierarchy resolve the conflict in favour of threat, then lowers the threat and observes hunger resume control of behaviour. The researcher has watched motivation arbitrate.

SCENARIO 5, THE SCALE-UP. A laboratory with serious compute connects the System to a real dataset, refines selected regions to finer grain, and runs the System for millions of ticks to test whether cognitively meaningful behaviour emerges. This scenario is the bridge to Appendix 6 and to the AGI question.

A1.4bis USER PERSONAS

The SPARC Specification phase asks for personas, the fictional profiles of typical users, so the requirements are grounded in who will actually use the System. Three personas follow, matching the three audiences of Section A1.1.

PERSONA 1, THE STUDENT-BUILDER (Maya). A graduate student in cognitive science with modest hardware and strong curiosity but limited systems-programming experience. She wants to build the System at home, watch a decision form tick by tick, and reproduce a famous experiment to understand the mechanism. Her need drives the inspectability, the modest-hardware grain, and the one-hour-to-first-experiment documentation goal.

PERSONA 2, THE LABORATORY RESEARCHER (Dr. Okafor). A computational neuroscientist with serious compute who wants to scale the System, connect it to real datasets, refine selected regions to finer grain, and run controlled lesion and pharmacological experiments as reproducible science. His need drives the scalability-by-grain, determinism-and-reproducibility, and perturbation requirements.

PERSONA 3, THE IMPLEMENTATION ENGINE (an artificial or human builder). The agent, human or artificial, that constructs the System from this specification in a chosen language on a chosen substrate. Its need drives the substrate independence, the language-agnostic pseudocode, and the explicit acceptance criteria that make "done" checkable rather than judged.

A1.4ter INTERFACE AND INTERACTION GUIDELINES

The System has no consumer graphical interface, but it is not without a user experience: it is a scientific instrument, and its interaction surface is the observer and the experiment controls. The SPARC User-Interface-and-User-Experience guidelines are stated here in the terms that apply to such an instrument.

GUIDELINE 1, THE OBSERVER IS THE PRIMARY INTERFACE. The user reads the System through the observer of Appendix 3, which reports the full state of any construct, network, drive, and neuromodulator at any tick. The observer's output must be human-readable and named, never an uninterpretable numerical blob, so that a person can follow a decision as it forms.

GUIDELINE 2, EXPERIMENTS ARE THE PRIMARY CONTROLS. The user acts on the System through the perturbation interface: lesion a construct, cut an interface, or shift a neuromodulator. Each control must be a single, named, reversible operation, so that running the Parkinson experiment is one legible act, not a reconfiguration.

GUIDELINE 3, LEGIBILITY OVER CONVENIENCE. Where a choice arises between a faster interaction and a more inspectable one, the instrument chooses inspectability, consistent with the System's defining virtue. The interface exists to make the glass box readable, not to hide it behind a dashboard.

GUIDELINE 4, REPRODUCIBILITY IS PART OF THE EXPERIENCE. Every interaction is recorded with its seed and its tick, so that any session a user runs can be replayed exactly by another user, which for a scientific instrument is the whole of a good user experience.

A1.5 TECHNICAL CONSTRAINTS

CONSTRAINT 1: the specification must remain language-agnostic and data-structure-agnostic through Appendix 5. Language and data-structure choices belong to implementation, discussed in Appendix 6, not to specification.

CONSTRAINT 2: the reference implementations named by the author are a Causalontology 2.0.0 data layer, a Python Minimum Viable Product, and a PrologAI implementation. The specification must be compatible with all three and must privilege none.

CONSTRAINT 3: no secret, credential, or environment-specific value may appear anywhere in the eventual code, per SPARC quality rules.

CONSTRAINT 4: the eventual code must observe the SPARC quality bounds: files of five hundred lines or fewer, functions of fifty lines or fewer, comprehensive input validation, modular design with clean interface boundaries, and self-documenting code.

CONSTRAINT 5: the eventual code must use the structured commit-message convention SPARC prescribes, with the prefixes feat for a feature, test for a test, fix for a bug fix, docs for documentation, arch for an architecture change, quality for a refactor, and deploy for a deployment change, so that the history of the build is itself legible.

A1.6 ASSUMPTIONS

ASSUMPTION 1: the regional grain is sufficient to produce cognitively interesting behaviour. This is a hypothesis, not a certainty, and testing it is part of the project's purpose.

ASSUMPTION 2: the six dynamical archetypes of Chapter 8 are sufficient to describe the dynamics of every construct at the regional grain. Where a construct resists all six, that is a finding, and the archetype set is extended, in the manner the science itself was extended.

ASSUMPTION 3: the three-factor learning rule of Chapter 10 is sufficient for the System to learn at the regional grain. This too is a hypothesis under test.

ASSUMPTION 4: the modeling commitments marked throughout the science chapters as THIS FRAMEWORK'S CHOICE are adopted as the specification's baseline, and may be revised in Refinement.

A1.7 ACCEPTANCE CRITERIA

The System is accepted as a faithful implementation of this specification if and only if all of the following hold, each of which is made into an automated test in Appendix 5.

Each is labelled with the prefix AC, for Acceptance Criterion, and numbered.

AC-1: every named construct of Chapters 2, 3, and 7 is present in the registry with its specification block.

AC-2: the System advances by ticks, and one tick updates every construct exactly once.

AC-3: a signal presented at a sensory region propagates through the correct chain of interfaces to produce an action, in the correct number of ticks given the interface delays.

AC-4: depleting dopamine slows and then halts action selection, and restoring it recovers, reproducing the Parkinsonian signature.

AC-5: a stimulus repeatedly paired with reward comes to predict that reward through weight change, and the change reverses under extinction.

AC-6: two simultaneous drives are arbitrated in the order specified by the override hierarchy, and the respiration drive cannot be voluntarily suppressed to self-termination.

AC-7: every state, weight, drive error, and neuromodulator level is reportable at every tick.

AC-8: a full run is exactly reproducible from its seed.

AC-9: the System runs at the regional grain on a personal computer within a stated time budget per thousand ticks.

AC-10: every large-scale network of Chapter 2, Section 2.10, is present in the registry as a named grouping over its member regions, with its couplings and its functional signature.

AC-11: every cognitive system and pathway of Chapter 2, Section 2.11, is present as a named subgraph or loop, the glia are present as modulatory participants, and each interface and field is marked as a directed (send-and-receive) channel or a broadcast (publish-and-subscribe) topic.

A1.8 REFLECTION

The discipline of SPARC requires that each phase reflect on its own choices.

Three deserve note.

The decision to make INSPECTABILITY a hard functional requirement (FR-13) and the System's defining quality (NFR-1) is the most consequential in the specification, and it is a deliberate wager against the prevailing direction of the field.

The dominant models are powerful and opaque.

This System trades raw power for total transparency, on the bet that a mind we can inspect is worth more, scientifically and in the long run practically, than a mind we cannot.

If that bet is wrong, the System will be an educational instrument rather than a competitive intelligence.

If it is right, it is a path the opaque models cannot follow.

The decision to specify at the REGIONAL GRAIN (Assumption 1) is the specification's largest risk.

It may be that cognition requires finer detail than one hundred regions can hold.

The specification mitigates this by requiring that the design scale to finer grain (NFR-4) without being rebuilt, so that the grain can be refined if the hypothesis fails, rather than the System abandoned.

The decision to add DEMONSTRATION as a sixth phase reflects a conviction that a specification of a mind carries an unusual obligation.

An ordinary specification may end at Completion, with a working system.

A specification that claims a path toward general intelligence owes its reader more: an honest account of what it is, what it is not, and what it might become.

Appendix 6 discharges that obligation, and its confident, well-founded case for the path to general intelligence is part of the specification, not an afterword to it.



KONNECTOME REALIZATION LOG

Slice 1 - the heartbeat - 2026-07-20 - pack tick_engine, version 0.1.0.

The scheduler requirement - a deliberately dumb tick engine that keeps time by a two-pass synchronous update - is now realized in code by the tick_engine pack (predicate tick_engine_run/5).

Gates at this slice: 7 of 7 tick_engine Prolog Unit (PLUnit) tests pass; the naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited; konnectome reuses them through the library path.

This log grows one entry per code slice, per the Tenth Commandment. The full DSPARCD Fileset (the SPARCD Fileset until the Constitution's version 8) was bumped in lockstep from version 1 to version 2, and the version 1 files were archived under docs/archive/ in the same change, per the Ninth Commandment.


KONNECTOME REALIZATION LOG - SLICE 2

Slice 2 - the six archetype rules and the first Causalontology thought - 2026-07-20 - packs archetype (0.1.0) and observer (0.1.0).

The six archetype rules and the observer's Causalontology recording are now realized in code: the archetype pack realizes the six dynamical rules, and the observer pack realizes the per-tick token_occurrence record.

Gates at this slice: archetype 7 of 7 and observer 6 of 6 Prolog Unit (PLUnit) tests pass; naming, layer, and layer-binding gates report zero violations; the Causalontology 119-vector conformance suite passes 119 of 119. Nothing in PrologAI or Causalontology was edited; konnectome reuses causal_core through the library path.


KONNECTOME REALIZATION LOG - SLICE 3

Slice 3 - the connection graph - 2026-07-20 - pack connection_graph (0.1.0).

The connection-graph requirement - directed, weighted, delayed interfaces that supply each construct's input - is realized by the connection_graph pack.

Gates at this slice: connection_graph 4 of 4 Prolog Unit (PLUnit) tests pass (tick_engine 7, archetype 7, observer 6 unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited; connection_graph reuses the archetype pack.


KONNECTOME REALIZATION LOG - SLICE 4

Slice 4 - the reward loop - 2026-07-20 - packs neuromodulator_bus (0.1.0) and drive_system (0.1.0).

The reward loop is realized: the drive system computes reward as the total reduction of drive error, and the neuromodulatory bus broadcasts it as dopamine.

Gates at this slice: neuromodulator_bus 4 of 4 and drive_system 5 of 5 Prolog Unit (PLUnit) tests pass (the earlier four packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 5

Slice 5 - the action selector - 2026-07-20 - pack action_selector (0.1.0).

The action-selector requirement - release exactly one candidate action, never inventing one - is realized by the action_selector pack.

Gates at this slice: action_selector 6 of 6 Prolog Unit (PLUnit) tests pass (the earlier six packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 6

Slice 6 - the plasticity engine - 2026-07-20 - pack plasticity_engine (0.1.0).

The learning rule is realized: the plasticity engine changes connection weights by the three-factor rule, closing the loop from reward to memory.

Gates at this slice: plasticity_engine 5 of 5 Prolog Unit (PLUnit) tests pass (the earlier seven packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 7

Slice 7 - completing the architecture - 2026-07-20 - packs override_controller (0.1.0) and perturbation_interface (0.1.0).

The last two components are realized: the override controller enforces the vitality safety property, and the perturbation interface administers experiments. All ten architecture components now have a realization.

Gates at this slice: override_controller 4 of 4 and perturbation_interface 4 of 4 Prolog Unit (PLUnit) tests pass (the earlier eight packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 8

Slice 8 - one running tick - 2026-07-20 - pack cognitive_cycle (0.1.0).

The whole System runs: cognitive_cycle wires all ten components into one integrated tick, realizing the one-tick data flow the specification requires.

Gates at this slice: cognitive_cycle 4 of 4 Prolog Unit (PLUnit) tests pass (the earlier ten packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 9

Slice 9 - hardening the loop - 2026-07-20 - packs connection_graph (0.2.0), drive_system (0.2.0), cognitive_cycle (0.2.0).

The loop is hardened into a closed sensorimotor cycle: the construct gain is set by the neuromodulatory bus, the drives propose actions, and the body responds to the action taken.

Gates at this slice: connection_graph 6 of 6, drive_system 9 of 9, and cognitive_cycle 4 of 4 Prolog Unit (PLUnit) tests pass (the other eight packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 10

Slice 10 - thought combination - 2026-07-20 - pack thought_combination (0.1.0).

Thought combination is realized: konnectome combines thoughts by minting Causal Relation Objects, the substrate for chains and trees of thought (the Second Commandment).

Gates at this slice: thought_combination 7 of 7 Prolog Unit (PLUnit) tests pass (the earlier eleven packs unchanged); naming, layer, and layer-binding gates report zero violations; the Causalontology 119-vector conformance passes 119 of 119. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 11

Slice 11 - other minds and prediction, the wall-finding build - 2026-07-22 - packs other_minds (0.1.0) and prediction_loop (0.1.0).

This slice touches two milestones the specification carries through the developmental ladder of Appendix 6: the social milestone (Rung Four, modelling another agent's belief, including a false and a nested one) and the prediction milestone (Rung One, the object-permanence surprise, the signed prediction error the comparator computes). Both are realized at runtime and are inspectable there, honouring the glass-box quality (NFR-1); but the shareable, substrate-independent record the specification's reproducibility posture assumes - a named record another party can read - cannot be minted, because Causalontology 3.0.0 has no kind for a belief or a prediction. That is the first requirement the build could not satisfy at the data-structure layer, and it is recorded as two walls rather than smuggled into konnectome's own code.

Gates at this slice: other_minds 8 of 8 and prediction_loop 10 of 10 Prolog Unit (PLUnit) tests pass (the earlier twelve packs unchanged, 69 tests); naming, layer, and layer-binding gates report zero violations; the Causalontology 119-vector conformance suite is untouched and green. Two walls were hit, recorded as Wall-1 and Wall-2, and ROUTED to the Causalontology change order (Sections B and A). Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 12

Slice 12 - the closure: minting the new kinds - 2026-07-22 - packs other_minds (0.2.0) and prediction_loop (0.2.0).

This slice closes the two walls slice 11 opened, and it does so exactly where the specification said it would. Slice 11 honoured the glass-box quality (NFR-1) at runtime but could not honour the reproducibility posture (NFR-2) or the substrate-independence requirement (NFR-3) at the data layer: a belief and a prediction could be inspected inside one runtime but not written down as a named record another party could read and replay. Causalontology 4.0.0 added the three kinds that were missing - a doxastic ATTITUDE kind and the PREDICTED_OCCURRENCE and PREDICTION_ERROR kinds, growing the closed list to twenty-one and re-freezing conformance at one hundred thirty-seven vectors with every prior identifier preserved byte-for-byte - and PrologAI's causal_core 1.1.0 made them mintable from konnectome. Now the attributed belief - true, false, or nested - and the expected-versus-observed discrepancy are minted as shared, signed, content-addressed records, so a run's social milestone (Rung Four) and predictive milestone (Rung One) are reproducible by another mind and not merely observable in this one. The specification's own account of what a finished milestone requires for these two Appendix 6 rungs is now met at both layers, runtime and record. The closure met a third wall of its own - PrologAI's causal_core still implemented only the eighteen earlier kinds, so the new records could not be minted - which was routed to the PrologAI change order and closed the same day by causal_core 1.1.0; the substrate-independence requirement was thus honoured through the cousins' own gated processes, never by editing them.

Gates at this slice: other_minds 10 of 10 and prediction_loop 16 of 16 Prolog Unit (PLUnit) tests pass (the earlier twelve packs unchanged); naming, layer, and layer-binding gates report zero violations; the Causalontology conformance suite is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. Wall-1, Wall-2, and Wall-3 are CLOSED. Causalontology and PrologAI were extended through their own gated change orders, never edited directly.


KONNECTOME REALIZATION LOG - SLICE 13

Slice 13 - the provenance layer: the mind grades and, when wrong, disowns its own thoughts - 2026-07-26 - pack self_provenance (0.1.0).

This slice closes the honest non-closure slice 12 left open: konnectome minted the three new Causalontology 4.0.0 CONTENT records - the attitude, the predicted_occurrence, and the prediction_error - but had not yet exercised the PROVENANCE layer OVER them. No konnectome assertion yet vouched for one of its own records, graded the evidence behind it, or withdrew it when a later tick proved it wrong. The self_provenance pack does exactly that, and in doing so it reaches the deepest reading of the glass-box quality (NFR-1): a mind we can inspect must show not only WHAT it holds but HOW SURE it is and WHY, and must be seen to correct itself on the record. The pack mints an ASSERTION about one of konnectome's own minted records - carrying the subject, konnectome's own source identity, an evidence grade from the standard's ordered scale (intervention, then observation, then simulation, then derivation, then a human hint, then an import), a confidence, and a timestamp, and optionally the particular token records that are its evidence - and a RETRACTION that withdraws a prior assertion, signed by the same source (the Rule 10 discipline), so a disconfirmed forecast is disowned honestly rather than quietly forgotten. Because an assertion and a retraction are content-addressed exactly as every other konnectome record, a graded, self-corrected thought is reproducible by another mind (NFR-2) and reportable at any tick (FR-13). The forecast the mind makes before the outcome is graded 'simulation' - a synthetic mind's model-based evidence is honestly ranked below observation - and, once the world confirms it, the stance is superseded to 'observation'; when the world disconfirms it (the object-permanence screen lifting on nothing, the signed error a minus one), the assertion is retracted. This is the honest self-correction the book's scientific posture demands, realized as shareable data.

Honest non-closure recorded this slice (konnectome ledger, slice 13): the Causalontology provenance model SIGNS an assertion with an Ed25519 signature over its canonical bytes, but producing that signature needs a PRIVATE key, and a private key is a secret that may never appear in konnectome's code (Constraint 3, and the no-secret rule of Section A5.5). So konnectome carries its PUBLIC source identity - a deterministic, secret-free stand-in - and defers the cryptographic signing act to deployment, where the key-holder signs the minted bytes. The signature is optional to the schema and is not identity-bearing, so the records konnectome mints are schema-valid, semantically valid, content-addressed, and shareable without it; a strict consumer would quarantine them as unsigned until the deployment signature is applied. This is neither a data-structure gap (the standard already carries the signature) nor a blocking language wall (the cognitive act - asserting, grading, retracting - is fully realized at the record level); it is a deployment boundary, recorded honestly rather than papered over with a secret in the source.

Gates at this slice: self_provenance 17 of 17 Prolog Unit (PLUnit) tests pass (the earlier fourteen packs unchanged, 95 tests; 112 in total); naming, layer, and layer-binding gates report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; the slice-12 honest non-closure is CLOSED; one new honest non-closure (the deferred signature) is recorded, not routed. Nothing in PrologAI or Causalontology was edited; self_provenance reuses causal_core through the library path.


KONNECTOME REALIZATION LOG - SLICE 14

Slice 14 - the capstone: the mind runs, and its own records tell the story - 2026-07-26 - pack capstone_demonstration (0.1.0), and the first runnable entry point, bin/run_capstone_demonstration.sh.

This slice realizes the specification's own closing promise: that the System, once built, can be WATCHED. The glass-box quality (FR-13, NFR-1) has until now been honoured pack by pack, each capability inspectable in its own tests; the capstone honours it whole. One script boots the mind from a declared world - a too-warm body, one drive defending a set-point of thirty-seven, one learnable interface, the respiration override armed at rest - runs it for a chosen number of ticks, and prints a story in which every claim is carried by a record: the reward and its dopamine broadcast per tick, the released action, the observer's token_occurrence identifier, the learned weight, the object-permanence forecast and its signed error in both a confirming and a disconfirming world, a chain of combined thoughts, the false belief attributed to Sally beside the world's contrary record, and the provenance layer asserting, grading, superseding, and retracting over those very records. Reproducibility (NFR-2) is demonstrated in the strongest available form: the story told twice is the same story, word for word and identifier for identifier, because every instant is fixed and every record content-addressed. Comprehensive input validation (Constraint 4) holds at the entry point: a tick count that is not an integer of at least six is refused as a named, inspectable error. And the specification's honesty discipline is enforced structurally: the narration checks the dynamics before claiming them - the story will not say the body settled or the weight grew unless the run achieved it - and it closes by naming the rungs it does NOT demonstrate and the signature its records do NOT yet carry.

Gates at this slice: capstone_demonstration 20 of 20 Prolog Unit (PLUnit) tests pass (the earlier fifteen packs unchanged, 112 tests; 132 in total); naming, layer, and layer-binding gates report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins, re-measured this slice. No new wall was hit and no new honest non-closure was recorded; the slice-13 deployment-signature non-closure stands, and the story itself narrates it. Nothing in PrologAI or Causalontology was edited; the capstone reads its siblings through the library path.


KONNECTOME REALIZATION LOG - SLICE 15

Slice 15 - Rung Two begins: words grounded in lived experience, and pretend under quarantine - 2026-07-26 - packs symbol_exchange (0.1.0) and pretend_play (0.1.0).

This slice opens Rung Two of the developmental ladder, and it opens it exactly where the specification's own account of the rung says to open: at the smoke test, and at the pilot's one load-bearing property. The smoke test is the single-symbol exchange, and its defining phrase - a word the System has GROUNDED THROUGH EXPERIENCE - is realized as a hard precondition, not a metaphor: symbol_exchange refuses, with a named error, to ground a word in any condition the mind has not actually lived through a real run of the whole cognitive cycle. The run that grounds the standard vocabulary is the same too-warm-to-settled world the capstone boots, lived tick by tick, so the word warm is grounded in a deficit the mind genuinely passed through and the word settled in a satisfaction it genuinely reached. Both directions of symbol use are realized and tested: production (the condition finds its word) and recognition (the heard word finds its condition, arriving through the reused language word bank so the hearing demonstrably changes the mind's state). The pilot's discriminator - the System answers differently when a drive is satisfied than when the same drive is in deficit, so its mood colours its words - is realized as data: the same incoming words draw the two-word reply of a mind in deficit and the one-word reply of a mind at its set-point. The second milestone, pretend, is realized under the reused reality quarantine with one konnectome rule added: pretend may never be promoted into the observed record. What this slice honestly does NOT claim: the full pilot conversation - a sustained back-and-forth through a text or speech front-end - remains future work; the rung is OPENED, not closed, and the speech is childlike exactly as the rung's own account says an honest opening should be.

Gates at this slice: symbol_exchange 18 of 18 and pretend_play 12 of 12 Prolog Unit (PLUnit) tests pass (the earlier sixteen packs unchanged, 132 tests; 162 in total); naming, layer, and layer-binding gates report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; the Section C watch was kept and its result is recorded in the ledger: the per-claim machinery sufficed, no world-level sharing need arose, and Section C remains held. Nothing in PrologAI or Causalontology was edited; the rung reuses the language and imagination packs through the library path, exactly as other_minds reuses theory_of_mind.


KONNECTOME REALIZATION LOG - SLICE 16

Slice 16 - Rung Four deepens: the social-pain test and the empathy trial - 2026-07-26 - pack empathy (0.1.0).

This slice realizes the two Rung Four trials the owner chose, and it realizes them on the specification's own deepest claim about emotion: that the System has the machinery of feeling BY CONSTRUCTION - drives that can be satisfied or thwarted - so no bespoke emotion pathway may be invented beside them. The social-pain test is realized exactly as the famous experiment it re-runs demands: a social-connection drive defends belonging as a genuine set-point, a social-rejection signal severs it, and the violation is registered by the IDENTICAL machinery that registers a thwarted physical drive - the same error predicate, the same step, the same negative reward broadcast as dopamine - with the equivalence pinned number for number in the tests: the broken heart and the broken arm share circuitry. The empathy trial is realized as the rung's own text words it: another agent's MODELLED distress moves the System's own affective state - resonance lowers the mind's own belonging, so the feeling-with is literal, felt through its own body - and shapes its behaviour toward help, with the helping action proposed by the drives themselves and RELEASED by the selector, never invented (FR-8 honoured under emotional pressure). The safety ordering survives feeling: respiration in distress still seizes control over social helping, so empathy never outranks breath. And the attribution is owned: the modelled distress is minted as a shareable attitude of the standard's own closed enumeration - the agent FEARS her connection severed - asserted at observation grade, and retracted when misattributed. What is honestly not claimed: the appraisal and regulation pilots of the rung remain future work.

Gates at this slice: empathy 17 of 17, drive_system 10 of 10, and symbol_exchange 19 of 19 Prolog Unit (PLUnit) tests pass (the other sixteen packs unchanged; 181 in total), the counts carrying two review-born fixes and one paid debt: drive_system (0.3.0) now caps the homeostatic step at its set-point, so a fractional distance settles exactly and help can never overshoot into harm - pinned by its own regression test; naming, layer, and layer-binding gates report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; the remaining ascent of the rung (the appraisal and regulation pilots, and the capstone joining the new milestones into its telling) is recorded as pending, not hidden. Nothing in PrologAI or Causalontology was edited; empathy reads its siblings through the library path.


KONNECTOME REALIZATION LOG - SLICE 17

Slice 17 - the reality-discipline gate: the rule made checkable - 2026-07-26 - bin/check_reality_discipline.sh (no pack version moved).

Slice 15 recorded, and slice 16 partly paid, a review debt; this slice pays what remained, and in doing so it extends the specification's honesty requirement from CLAIMS to ENFORCEMENT. The claim in question: pretend may never be promoted into the observed record. The truth behind it: the refusal lived only in pretend_play's own interface, while the reused imagination pack's crossing predicates remained callable, unguarded, by any pack on the shared library path - so the claim held by good behaviour, not by construction. The specification's answer is the same one it gives every silent failure mode: make the invariant checkable. A fourth gate now enforces, at build scope, that no file of any pack outside pretend_play carries any textual road to the reused machinery - a predicate name, the imagination module qualifier, or the library(imagination) import - so ordinary code cannot reach around the wrapper, and the guarantee the documents state is a property a merge must prove for every road a merge can see. The one road no textual gate can see - a call that constructs both its module and its goal at runtime - is recorded in the ledger as the discipline it remains.

Gates at this slice: no pack changed and no test count moved - the suite stands at one hundred eighty-one Prolog Unit (PLUnit) tests across nineteen packs, all green. The naming, layer, and layer-binding gates report zero violations, and they are joined by a fourth: the new reality-discipline gate (bin/check_reality_discipline.sh) reports zero violations, and was proven able to fail - a deliberate violation was injected, caught, and removed before its first clean run was accepted. The Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited - the imagination pack remains exactly as reused, and the discipline that fences it is konnectome's own.


KONNECTOME REALIZATION LOG - SLICE 18

Slice 18 - the capstone speaks and feels: Rung Two's first words and Rung Four's deepening join the one runnable story - 2026-07-26 - pack capstone_demonstration (0.2.0).

This slice extends the capstone's honouring of the specification's own closing promise - that the System, once built, can be WATCHED whole (FR-13, NFR-1) - from the rungs slice 14 could show to the rungs the intervening slices earned. The one runnable command still boots the mind from a declared world and prints a story in which every claim is carried by a record, and it now carries two more chapters of that story in rung order. Rung Two is demonstrated at the two properties the specification's account of the rung makes load-bearing: a word is spoken only where the mind actually lived the condition it names, and a reply is coloured by the drive state, so a mind in deficit and a mind at its set-point answer the same words differently; and pretend is shown sealed in the reused five-reality quarantine, with the crossing into the observed record refused by name, so the System imagines without lying. Rung Four's deepening is demonstrated at the specification's deepest claim about emotion - that feeling is present BY CONSTRUCTION, in drives that can be satisfied or thwarted - with a social_connection drive defending belonging exactly as the temperature drive defends thirty-seven, the two pains proven equal through the identical predicate, and an empathy trial in which modelling another's distress lowers the mind's own belonging and flips its selection toward a released, never-invented help, with respiration still overriding kindness and the help landing belonging exactly at its set-point. Reproducibility (NFR-2) is preserved in its strongest form - the longer story told twice is the same story, identifier for identifier - and the specification's honesty discipline is visible in the one list that shrank rather than grew: the printed catalogue of what is NOT demonstrated now names only Rung Three, Rung Five, Rung Four's own appraisal and regulation pilots, and the deployment-deferred signature, because Rung Two, once listed there, is now shown.

Gates at this slice: capstone_demonstration thirty-six of thirty-six Prolog Unit (PLUnit) tests pass (the other eighteen packs unchanged, one hundred sixty-one tests; one hundred ninety-seven in total across nineteen packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited - the capstone reads its siblings through the library path.


KONNECTOME REALIZATION LOG - SLICE 19

Slice 19 - Rung Four finished: the appraisal and regulation pilots - 2026-07-26 - packs situation_appraisal (0.1.0) and arousal_regulation (0.1.0).

This slice finishes Rung Four of the developmental ladder by realizing its two remaining pilots, and the binding claim the specification makes is that both are honoured as thin wrappers over machinery that already exists: no eleventh architecture component is raised for emotion, and no new Causalontology kind is minted, so feeling here is feeling by construction rather than by a bespoke engine. situation_appraisal is required to judge a situation as good or bad for the mind's OWN goals, and the requirement is exact about where that judgement comes from - it is read straight off the drives' own set-point error through drive_system_error, so a violated set-point counts as a threat the mind fears and a met or improving one counts as good, something the mind desires; the appraisal must be owned on the record as an attitude drawn from the standard's own closed enumeration of fears and desires, graded through self_provenance at the moment of observation, and retractable under Rule 10 when the world later corrects it. arousal_regulation is required to take an aroused state carried as norepinephrine on the neuromodulatory bus and bring it back toward its tonic baseline over successive ticks, with each tick closing a fixed half of the remaining distance and, within a hair of baseline, settling exactly at baseline and never overshooting - the same homeostatic discipline the drive system's own capped step already keeps. Both packs are required to read their siblings only through the library path, to edit no cousin, and to add no update rule, so the whole of Rung Four is closed without disturbing anything beneath it.

Gates at this slice: situation_appraisal twelve of twelve and arousal_regulation twelve of twelve Prolog Unit (PLUnit) tests pass (the earlier nineteen packs unchanged; two hundred twenty-one in total across twenty-one packs, all green); the four standing gates - naming, layer, layer-binding, and reality-discipline - report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins; no new wall was hit; and no cousin was edited.


KONNECTOME REALIZATION LOG - SLICE 20

Slice 20 - the pilots' review debts paid: appraisal's rounding boundary and regulation's finiteness - 2026-07-26 - packs situation_appraisal (0.1.1) and arousal_regulation (0.1.1).

Slice 20 pays the review debt slice 19 left on the record. The independent adversarial review of the appraisal and regulation pilots, run after they merged, confirmed three findings, and each is now fixed at its root and pinned by a permanent test. First, the appraisal pilot's change reader compared a difference of two independently-rounded set-point errors against exact zero, so two situations equidistant on opposite sides of a set-point - a mathematically neutral change - were read as a good or bad turn on a rounding residue near one part in ten thousand trillion, and, worse, minted as an owned attitude the neutral case is meant to refuse; a rounding-noise band now absorbs that residue, so an equidistant float change reads neutral and is correctly refused before it can become a false record. Second, the regulation pilot accepted a non-finite arousal level, and an infinite level never shrinks toward baseline, so the return would never terminate; the single gatekeeper now requires a finite level and refuses infinity by name. Third, the regulation pilot's whole-number tick rule had no test for its fractional-count refusal; it has one now. No demonstrated behaviour changed - every earlier claim still holds number for number, and every prior test still passes - and the two pilot packs move to patch version 0.1.1. The lesson the method keeps: a finding confirmed by an adversarial review is fixed at its root and pinned, even when the defect is latent - here reachable only by a fractional body value or a hand-built infinity that no ordinary run produces.

Gates at this slice: situation_appraisal fourteen of fourteen and arousal_regulation fourteen of fourteen Prolog Unit (PLUnit) tests pass (the four new tests pin the three confirmed findings; the other nineteen packs unchanged; two hundred twenty-five in total across twenty-one packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 21

Slice 21 - Rung Three opened: the concrete-reasoning trials - 2026-07-26 - packs concrete_operations (0.1.0) and cause_and_effect (0.1.0).

WHAT THE PHASE REQUIRED. Rung Three of the developmental ladder is the concrete operational stage: logic, reasoning, and rules applied to concrete, grounded situations, with every inference open to inspection. The rung's smoke test is stated in the guiding book as a single valid transitive inference - the block is on the cup, the cup is on the table, therefore the block is above the table - with every derivation step shown. Its pilot battery names five trials: classification (sort a set of perceived objects into categories and re-sort them by a different property on request), seriation (order objects along a dimension), conservation (an amount unchanged when its appearance changes), causal reasoning (use the Causalontology causal record to tell a genuine cause from mere co-occurrence in a small event chain), and planning (compose a short action sequence that reaches a given goal state). The requirement that governs all of them is the architecture's own claim: every reasoning step must be a named, watchable operation, never an opaque leap.

WHAT THIS SLICE SPECIFIES, AND WHAT IT HONESTLY DEFERS. Slice 21 requires the smoke test in full, and opens the five trials at differing depths, each sized honestly rather than claimed whole. The transitive inference is realized completely: the derivation returns its steps as data, each a named operation carrying the facts it consumed and the fact it produced. Seriation, causal reasoning, and planning are realized. Classification is opened in part: the flexible re-sort over a chosen property is demonstrated, while the book's requirement that the categories be ones the mind FORMED FROM EXPERIENCE is deferred, and the deferral is written into the pack's own header rather than papered over. Conservation is opened schematically: the verdict shape is demonstrated - an appearance-only transformation drawn from a closed enumeration conserves the amount, a real addition or removal honestly changes it - while a genuine represented model of quantity under changing appearance remains open. The rung is therefore recorded as OPENED, not finished, and the honest-limits list keeps its Rung Three line until the two deferred halves are built.

A FURTHER REQUIREMENT, DISCOVERED IN REVIEW. A record offered as causal evidence must be checked to have been truly minted, not merely shaped like a record. The specification of the causal trial says causation is answered BY the record; that requirement is empty if any dictionary carrying the right keys can answer it. Slice 21 therefore requires every offered record to re-derive its own content address from its own content, and refuses a forgery by name.

Gates at this slice: concrete_operations twenty-six of twenty-six and cause_and_effect twenty-five of twenty-five Prolog Unit (PLUnit) tests pass (the earlier twenty-one packs unchanged; two hundred seventy-six in total across twenty-three packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 22

Slice 22 - Rung Three finished in its trials: the two deferred halves built - 2026-08-07 - packs category_formation (0.1.0) and quantity_model (0.1.0).

WHAT THE PHASE REQUIRED. Slice 21 opened Rung Three and recorded two honest deferrals against the guiding book's own words. The classification trial asks the mind to "sort a set of perceived objects into categories the System has formed from experience, and re-sort them by a different property on request" - and slice 21 built only the re-sort, deferring the formed-from-experience half. The conservation trial asks the mind to confirm "a quantity is unchanged when only its appearance changes - the same water in a taller, thinner glass is still the same amount", and the book names it "a direct test of whether the System models the world rather than its mere surface" - and slice 21 built only the verdict shape, whose answer followed from a transformation's NAME on a closed list, which is exactly the surface. This slice specifies the two missing halves.

WHAT THIS SLICE SPECIFIES. First, category formation: the mind observes a stream of experiences, each a bundle of property-value pairs, and induces its own categories from what the experiences share - each category carrying the defining core its members have in common and the members that earned it. The categories are FORMED, never handed down; the sorting then places objects into the mind's own categories, an object fitting none is reported outside every category rather than forced, and the flexible re-sort by a different property remains the reused sibling classifier, so one collection serves both halves of the trial. Second, the represented model of quantity: an amount is represented as individuated units plus an appearance; a transformation acts on the representation; measurement counts the units the model holds; and the conservation verdict compares the measured quantity before and after, NEVER reading the transformation's name. The specification's decisive requirement is the lossy pour: a transformation that calls itself a pour but spills a unit must be judged changed, because the verdict must side with the world against the name - that single requirement is what separates modelling the world from matching its surface.

A FURTHER REQUIREMENT, DISCOVERED IN REVIEW. A conclusion offered for the record must be minted FROM an enacted episode, not from quoted text. The review showed both packs' record predicates would own any hand-written sentence as if it had been formed or measured; the specification now requires the record predicate itself to enact the learning episode or the conservation trial and to mint what that enactment produced, so the claim and the record cannot come apart. In the same family, a hand-built category or model must meet the learner's own invariants - a sorted, non-empty defining core; distinct identifiers; distinct unit names - or be refused by name, because each of those look-alikes was shown to drive an answer silently off the specification's claims.

Gates at this slice: category_formation twenty-six of twenty-six and quantity_model twenty-nine of twenty-nine Prolog Unit (PLUnit) tests pass, and concrete_operations' twenty-six stay green under its re-sized header (the earlier packs otherwise unchanged; three hundred thirty-one in total across twenty-five packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 23

Slice 23 - The story learns its owed chapters: Rung Three told, and Rung Four finished in the telling - 2026-08-07 - pack capstone_demonstration (0.3.0).

WHAT THE PHASE REQUIRED. Two story debts stood on the record. Slice 19 finished Rung Four's trials but left its appraisal and regulation chapters untold; slices 21 and 22 completed Rung Three's five trials but left the whole rung untold. The capstone's printed honest-limits list named both debts, correctly, because in this build a capacity is demonstrated only when the one runnable story tells it over live dynamics a reader can watch and re-run.

WHAT THIS SLICE SPECIFIES. The capstone grows to 0.3.0 and learns two chapters, each under the standing rule that the story cannot grade its own homework: every narrated claim must be checked against the reused engines' live output inside the chapter, and re-derived independently in the test suite outside the narration. The Rung Three chapter must tell all five trials and the smoke inference: the derivation returned as data with its named steps; seriation; the categories the mind formed from its own experience, the honest exile of an object fitting none, and the flexible re-sort that crosses the formed partition; the conserving pour re-measured at five and the lossy pour judged changed by the model against its own name; the cause told from co-occurrence by the record, with the record's identifier cited; and the shortest plan. The Rung Four finish must tell the appraisal read straight from the drives' own error - threatened, satisfied, good, and bad - owned as the standard's attitudes with the satisfaction approximation declared aloud (ledger Observation-4), graded at observation, and withdrawn when the world moves on; and the regulation of a full alarm, halving each tick and resting exactly at baseline, never overshooting. The honest-limits list must then shrink to exactly one rung: embodiment, disclaimed with the guiding book's own reason - the rung asks for a real machine.

A REQUIREMENT CARRIED FORWARD, NOT INVENTED HERE. The conservation verdict the story owns must be minted by ENACTING the pour - the record-by-enactment discipline slice 22 built - so the told verdict and the enacted trial cannot come apart in the telling either.

Gates at this slice: capstone_demonstration forty-four of forty-four Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; three hundred thirty-nine in total across twenty-five packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 24

Slice 24 - Rung Five opened as groundwork: the body interface, and the sense-act loop through the honestly-named simulated body, the rung itself not claimed - 2026-08-07 - packs simulated_body (0.1.0) and body_interface (0.1.0).

WHAT THE PHASE REQUIRED, AND WHAT IT REFUSES TO PRETEND. The guiding book's Rung Five is the embodied rung, and its text is unambiguous: the sense-act smoke test is closed "through a real machine", and its trials are "a real, end-to-end trial in a body" - reaching and grasping, navigation, and homeostatic survival, the last being the one that most fully exercises the whole architecture, a robot defending its own body state "of its own accord ... the difference between a tool that is driven and an agent that is self-driven". No software slice can round itself up to a robot, and this one does not: the rung is NOT claimed, the capstone's honest-limits line keeps naming embodiment with the book's own real-machine reason, and everything this slice builds is specified as GROUNDWORK - the seam a real machine would one day plug into, built and tested today.

WHAT THIS SLICE SPECIFIES. First, an honestly-named stand-in: a simulated body carrying a camera, a draining battery, and three actuators, whose every predicate is a deterministic map from one body term to the next - a world, not a mind. Second, the book's three connections, as an interface over that stand-in: the machine's sensors become the mind's senses (camera readings surfacing as percepts, unchanged); the machine's body state becomes the mind's homeostatic state (the identity mapping made explicit, so that a low battery is FELT AS HUNGER by the very drive_system_error every other pain passes through, the energy drive defending a full charge exactly as the temperature drive defends thirty-seven); and the mind's released actions drive the actuators (an outcome no actuator carries refused by name). Third, the loop and the run: one sense-act pass - sense, drive step, propose, select, command, enact - and a survival run in which the battery drains every tick and the mind recharges of its own accord exactly when the hunger presses, on a deterministic two-tick rhythm pinned as an exact trace.

A REQUIREMENT SHARPENED IN REVIEW. The loop's outcome is owned by enacting the loop, and the stance granting the OBSERVATION grade must PROVE the record it grades was genuinely minted: the stance re-derives the record's content address from its own content and requires the loop's own designator, refusing a hand-built look-alike and a tampered designator alike by name - because observation is the scale's strongest self-grade this build grants, and a never-enacted record must never earn it.

Gates at this slice: simulated_body sixteen of sixteen and body_interface twenty-one of twenty-one Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; three hundred seventy-six in total across twenty-seven packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 25

Slice 25 - The story learns the body seam: Rung Five's groundwork told, and the rung not claimed - 2026-08-07 - pack capstone_demonstration (0.4.0).

WHAT THE PHASE REQUIRED. Slice 24 left a deliberate absence on the record: the body seam was built and tested, and the one runnable story said nothing about bodies at all, so the boundary between groundwork and rung stayed structurally visible. The prior hand-off named the next honest step - teach the story to TELL the seam without claiming the rung - and warned that the chapter would need the same guard-then-tell discipline and careful wording as every chapter before it.

WHAT THIS SLICE SPECIFIES. The capstone grows to 0.4.0 and learns one chapter, placed between the finished Rung Four and the provenance layer. The chapter must open by refusing the pretense in its own words - there is no real machine here - and must keep embodiment on the printed honest-limits list; the epilogue must gain a sentence naming the boundary explicitly: groundwork is not the rung, and this story does not promote it. Under those refusals the chapter must tell the seam's five facts over live dynamics, each beneath a strict guard: the camera's reading surfacing as the mind's percept unchanged; one closed sense-act pass releasing the steering reflex, moving the body, and changing what the camera reads next; the hunger identity - a battery at half charge and a body half a degree from thirty-seven reading equal through the identical drive_system_error; the survival rhythm - hold at the mild hunger, recharge OF ITS OWN ACCORD at the pressing one, on the pinned two-tick rhythm; and the deliberate tie - a starving battery outranking a seen obstacle at equal salience. The loop's outcome must be owned by enacting the loop, and the stance the story prints must be one the interface itself proved genuinely minted.

Gates at this slice: capstone_demonstration fifty-two of fifty-two Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; three hundred eighty-four in total across twenty-seven packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 26

Slice 26 - The naming gate learns to see: the unprefixed-helper hole closed, and the hand-hunt retired - 2026-08-07 - tooling bin/check_pack_naming.sh, with a committed fixture and a self-test.

WHAT THE PHASE REQUIRED. Slice 22's review found the build's most self-referential defect: the automated naming gate - built to enforce the Whole-Word System - waved through an unprefixed helper predicate (numlist_or_empty proved it), because its detectors hunted prefix CLUSTERS: one-off clusters were ignored, and an unknown stub longer than two letters passed every list. The ledger recorded the hole and mandated a hand-hunt in every review until the gate could see it. A standing mandate for hands and eyes is a standing admission that a rule is not yet checkable.

WHAT THIS SLICE SPECIFIES. A sixth check, stated as the house rule actually reads: every clause head DEFINED in a pack module must begin with the pack's own whole-word prefix (or the sanctioned prologai_ shared namespace) - not merely the dominant cluster, not merely known terse stems, every head. And the gate must be proven able to fail before it is trusted to pass, permanently: a fixture pack carrying exactly the slice-22 shape of the violation is committed under tests/naming_gate_fixture/ (outside the library path, never loaded), and a --self-test mode runs the gate against it and succeeds only if the fixture is caught by name. The ledger's hand-hunt mandate is retired in the same change.

Gates at this slice: the extended naming gate reports zero violations across all twenty-seven packs AND passes its own self-test - the committed fixture's unprefixed helper is caught by name; the layer, layer-binding, and reality-discipline gates report zero violations; three hundred eighty-four Prolog Unit (PLUnit) tests across twenty-seven packs, all green and unchanged by this slice; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 27

Slice 27 - The delay lines: the connectome learns to keep a signal in transit - 2026-08-07 - pack connection_graph (grown).

WHAT THE PHASE REQUIRED. The guiding book gives every interface a DELAY set by its length and myelination, and states the acceptance plainly: a signal presented at a sensory region must appear at its target after exactly the number of ticks the interface delays dictate, and nowhere sooner (the TEST OF PROPAGATION, and acceptance criterion AC-3). Slice 3 built the connection graph honouring only the natural one-tick delay of the synchronous update and recorded multi-tick delay lines as an honest non-closure, pending. The prior hand-off's queue named the delay lines the smallest pending refinement, and the owner's standing choice - all of them - made it this slice.

WHAT THIS SLICE SPECIFIES. A delay line per transmissive interface: a first-in-first-out buffer holding the values still in transit along that interface. For a delay of D ticks the buffer holds D minus one values, because the synchronous step itself delivers the final hop; each tick the source's emission enters the back and the front value is delivered, weighted at delivery. Four laws bound the design. First, exact arrival: a signal emitted at one end arrives after exactly D ticks, and chained hops arrive at the SUM of their delays, and nowhere sooner. Second, backward compatibility as an identity, not a hope: with every delay at one tick the delayed run must reproduce the plain synchronous run exactly, state for state and tick for tick. Third, persistence: a value already in transit is preserved even if its source changes afterward - the line is memory of what was sent, not a window onto the sender. Fourth, refusal aloud: a delay below one tick is refused by name (a signal never arrives before it is sent), and - sharpened in review - an unknown construct kind or a source name absent from the state is refused by a thrown error, never answered by a silently shrunken state or a silently vanished line.

Gates at this slice: connection_graph seventeen of seventeen Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; three hundred ninety-five in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 28

Slice 28 - The eligibility trace: a reward one tick late still finds the connection that earned it - 2026-08-07 - pack plasticity_engine (grown).

WHAT THE PHASE REQUIRED. The guiding book's functional requirement FR-11 states the demand plainly: the System must implement the eligibility trace of Chapter 10, so that a reward arriving shortly after an action can strengthen the connection that earned it. Section A2.6 gives the procedure - a fading memory of recent coincidence, one per interface - and Section A2.9 gives its acceptance: reward a connection one tick after it was active and assert the weight still strengthens; withhold the reward and assert the trace fades. Slice 8 built the three-factor learning rule that this trace deepens; the prior hand-off's queue named the eligibility trace the natural next-smallest refinement, and the owner's standing choice - all of them - made it this slice.

WHAT THIS SLICE SPECIFIES. A trace per transmissive interface, held as an explicit stateless value beside the interfaces - never inside them - built at zero, advanced each tick by the manuscript's own arithmetic (the old trace multiplied by a fading factor slightly less than one, plus the tick's fresh coincidence, the product of the two ends' activities), and spent when the reward arrives (the weight grows by the trace multiplied by the dopamine third factor and the learning rate). Three laws bound the design. First, the bridge law: activity at one tick and reward at a later tick still strengthen the weight, in exact proportion to the faded trace - the manuscript's bridge between fast coincidence and slower reward, made runnable. Second, honest decay: with the reward withheld the trace fades geometrically toward zero and the weight does not move at all - a trace is a memory, not a slow-release reward. Third, refusal aloud, through one shared perimeter: a fading factor at or above one or below zero, a term that is not an interface, an unbound or unknown interface kind, a duplicate transmissive interface, a duplicate trace key, a ghost trace, and an orphan trace are each refused by a thrown error that names the offender - never answered by a silently wrong number or a silent failure.

Gates at this slice: plasticity_engine twenty-four of twenty-four Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; four hundred fourteen in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 29

Slice 29 - Homeostatic scaling: the bound that keeps the learning from exploding - 2026-08-07 - pack plasticity_engine (grown).

WHAT THE PHASE REQUIRED. The guiding book's functional requirement FR-10 does not stop at the three-factor rule: the weight changes must be BOUNDED by homeostatic scaling that holds each region's average activity near a target, and the closing line of the pseudocode's Section A2.5 says it as one sentence - apply homeostatic scaling so that each region's average activity stays near its target. Chapter 10 gives the mechanism its name, synaptic scaling, and its shape: a slow negative feedback on plasticity itself - a region too active has ALL its inputs scaled down, a region too quiet has them scaled up - so the fast, selective, dopamine-gated strengthening can never explode. The hardening plan of the book names the failure modes this bound must prevent: runaway excitation and weight explosion.

WHAT THIS SLICE SPECIFIES. A running average per construct, held as an explicit stateless value in the slice-28 style - a store of name-keyed numbers, born at zero, moved each tick as an exponential moving average under a smoothing factor - and a scaling step that spends the store into the weights: each transmissive interface's weight is multiplied by one plus the scaling rate times the target-minus-average error of its RECEIVING end, so all of a region's inputs share one factor, exactly as the book demands. Three laws bound the design. First, the direction law: an overactive region's inputs shrink, an underactive region's inputs grow, and a region exactly at target is untouched. Second, the stability law, proven as a running test: fifty ticks of maximal coincidence under relentless full dopamine, scaled every tick, hold the weight at a finite fixed point instead of the unbounded climb the learning alone would take - the manuscript's promised bound made runnable. Third, refusal aloud, extending the shared perimeter: a smoothing factor that would freeze or overshoot the average, a scaling rate at or above one, a negative activity target, a malformed or duplicate store entry, an unbound store, and a receiving region absent from the averages are each refused by a thrown error naming the offender - and the scaling factor floors at zero, so a weight can be silenced but never sign-flipped.

Gates at this slice: plasticity_engine forty-eight of forty-eight Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; four hundred thirty-eight in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 30

Slice 30 - Per-territory neuromodulators: the field learns local concentrations, and delayed time learns the bus - 2026-08-07 - packs neuromodulator_bus and connection_graph (grown).

WHAT THE PHASE REQUIRED. The guiding book's functional requirement FR-6 does not stop at a global broadcast: the neuromodulators are broadcast fields WITH A LEVEL PER TARGET TERRITORY, available to every construct's update rule and every learning rule, and the molecule profile of Chapter 7 says how - a modulator is modeled not as an edge but as a global parameter with per-target levels, diffusing from a small source nucleus to vast territories. And slice 27 left one refusal standing as a recorded pending line: the delayed step refused the bus-modulated relay kind by name, so neuromodulation and multi-tick time had never met. The owner's standing choice made both halves this slice.

WHAT THIS SLICE SPECIFIES. First, the bus grows local concentrations: beside the global Modulator-Level pairs, a territory entry carries a modulator's level in one named territory, and a territory read answers by the diffuse-field law - a territory with its own level reads that level; a territory without one falls back to the global broadcast; with neither, silence. A territory broadcast replaces only its own entry, and a later global broadcast never shadows or erases a territory's own level. Second, each construct IS its own target territory: a bus-modulated relay reads its modulator's level in the territory of its own name, in the plain modulated step and the new delayed modulated step alike - one semantics across both times. Third, delayed time learns the bus: a delayed modulated step carries the delay lines and the bus together, the gain read AT THE TICK OF DELIVERY, never at emission - a signal in transit is modulated by the chemistry of the moment it arrives, as a synapse is. Two laws bound it: with every delay at one tick the delayed modulated step reproduces the plain modulated step exactly, and every refusal of the delayed step - the unknown kind, the ghost source - is refused in the modulated variant with the same voice. And sharpened in review: a modulator or territory name is a plain atom, refused aloud if unbound or compound, so the two key shapes of the bus can never collide or be silently bound.

Gates at this slice: neuromodulator_bus fourteen of fourteen and connection_graph twenty-four of twenty-four Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; four hundred fifty-five in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 31

Slice 31 - The per-territory third factor: each connection learns by the chemistry of the place the signal arrives - 2026-08-07 - pack plasticity_engine (grown at its root).

WHAT THE PHASE REQUIRED. The guiding book's functional requirement FR-6 gives every learning rule access to the broadcast fields WITH A LEVEL PER TARGET TERRITORY, and slice 30 built that field - but left one honest boundary standing in its own log: the plasticity engine's third factor still read the GLOBAL dopamine level, so a territory could modulate its relays but not its learning. The ledger recorded the per-territory third factor for learning as the natural next use of the slice-30 read, and the owner's standing choice made it this slice.

WHAT THIS SLICE SPECIFIES. The three-factor rule keeps its shape - sending activity times receiving activity times the third factor, scaled by the learning rate - but the third factor is now read at the RECEIVING end's territory through the slice-30 diffuse-field law: a territory with its own dopamine concentration learns by that local level; a territory without one learns by the diffuse global broadcast; with neither, no learning. The same law governs both moments of learning: the live step, where coincidence learns at once, and the reward step, where a trace is spent - each interface reads the chemistry of the place its signal arrives, at the moment it is spent. Three laws bound the design. First, the locality law: a local half concentration beats a global full one, and a territory explicitly silenced at zero does not learn under any global reward - the local level wins, exactly as the diffuse-field law says. Second, the fallback law: on a bus with no territory entries every weight change is exactly what the global rule produced, so the old behaviour is the exact fallback, pinned by every pre-slice test passing unchanged. Third, refusal aloud: a receiving name that is not a plain atom is refused by the bus's own guard before a weight moves, and - sharpened in review - the live step now judges its interfaces as loudly as the trace, reward, and scaling steps always did: a malformed term, an unbound or unknown kind, and a duplicate transmissive interface are each refused by name, never silently mislearned.

Gates at this slice: plasticity_engine fifty-eight of fifty-eight Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; four hundred sixty-five in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 32

Slice 32 - The live tick carries the whole learning body: traces, averages, and the slow bound beat in the loop - 2026-08-08 - pack cognitive_cycle (grown; the boot worlds of capstone_demonstration and symbol_exchange grown to carry the stores).

WHAT THE PHASE REQUIRED. The guiding book's Section A3.3 tick is the loop of the whole mind, and Sections A2.5 and A2.6 give learning a body wider than one weight update: an eligibility trace fading beside every learnable interface, a running average pacing every region, and the slow homeostatic bound that keeps the fast learning finite. Slices 28 and 29 built that body and slice 31 gave it a geography - but the cognitive cycle's live tick still called only the fast three-factor step, so the trace and average stores existed only where a test harness remembered to carry them. The ledger recorded the threading as pending line one, the natural next-smallest slice, and the owner's standing choice made it this slice.

WHAT THIS SLICE SPECIFIES. The world dict grows six keys - the eligibility-trace store, the running-average store, the fading factor, the smoothing factor, the scaling target, and the scaling rate - and the tick spends them in order after the fast learning step: the traces fade one step and absorb the tick's fresh coincidences, the averages move one smoothing step toward the tick's activities, and the slow bound scales each region's incoming weights toward its target, the advanced stores committed into the new world beside the moved body. Three laws bound the design. First, the at-rest law: with the scaling rate at zero the bound is armed but at rest - the target is scenery and the learned weights are exactly what learning alone produces, so every earlier world's behaviour is reproduced by giving it resting constants. Second, the liveness law: with a live rate the bound beats - a quiet region's incoming weight is pulled up where the resting bound left it alone. Third, refusal aloud: a world missing any key the tick reads is refused by the missing key's name, never half-run, and the reward step deliberately stays with the caller - trace lifetime belongs to whoever decides a reward has arrived.

Gates at this slice: cognitive_cycle eleven of eleven Prolog Unit (PLUnit) tests pass, and the grown boot worlds of capstone_demonstration (fifty-two of fifty-two) and symbol_exchange (nineteen of nineteen) re-swept green (the earlier packs unchanged; four hundred seventy-two in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 33

Slice 33 - The per-territory scaling target: each region defends the activity level its own territory owes - 2026-08-08 - pack plasticity_engine (grown at its root; cognitive_cycle grown to read the geography; the boot worlds of capstone_demonstration and symbol_exchange grown to carry it).

WHAT THE PHASE REQUIRED. The guiding book's homeostatic bound (Chapter 10 and the closing line of Section A2.5) is each region defending ITS OWN average activity - regions differ, and a set-point that is one region's health is another's silence - yet slices 29 and 32 built and threaded the bound with one target and one rate for the whole graph, and the ledger recorded that honestly as a pending line: per-territory targets would pair naturally with the slice-31 territory read. The owner's standing choice made it this slice.

WHAT THIS SLICE SPECIFIES. The bound's target gains a geography, in the slice-30 diffuse-field law's exact shape: a per-territory target store - a list of Name-Target pairs - rides beside the global target, a territory with its own target defends that target, and a territory without one defends the global target, so the old one-target behaviour is the exact fallback under an empty store. Three laws bound the design. First, the locality law: a region with its own target is scaled toward it and no other, so one graph can hold a loud sentinel and a quiet archive without either being wrong. Second, the fallback law: under an empty geography the territory step's answer is identical, term for term, to the old global step - and the old step IS the territory step under an empty store, one root, one behaviour. Third, refusal aloud: a malformed target pair, a duplicate target key, an indefensible (negative or non-numeric) target, and an unbound store are each refused by name before a weight moves - and, sharpened in review, an unbound target, rate, fading factor, or smoothing factor is now refused as uninstantiated rather than under a domain wrong it cannot name.

Gates at this slice: plasticity_engine seventy of seventy Prolog Unit (PLUnit) tests pass and cognitive_cycle thirteen of thirteen, with the grown boot worlds of capstone_demonstration (fifty-two of fifty-two) and symbol_exchange (nineteen of nineteen) re-swept green (the earlier packs unchanged; four hundred eighty-six in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 34

Slice 34 - The bound turned live in the told story: the capstone world carries a live rate and a small geography, and the story tells the bound's work beside a rest twin - 2026-08-09 - pack capstone_demonstration (grown; no other pack changed).

WHAT THE PHASE REQUIRED. Slice 33 built the per-territory scaling target and proved it in the engine and the live tick, but the ledger recorded the honest gap as pending line one: every boot world still carried its geography empty and its rate at zero, so the bound beat per-territory in the tests and not yet in the capstone's told story - the one place the whole mind tells itself to a reader. The owner's standing choice made turning the bound live in that story this slice.

WHAT THIS SLICE SPECIFIES. The capstone's boot world arms the slow bound for real: the diffuse activity mark stays at one half, the relay b gains its own mark of eight tenths in a one-entry geography, and the scaling rate rises from zero to the slow five hundredths - live, but slow enough that every claim the story already made still holds. Three laws bound the design. First, the told-work law: the story may claim the bound's work only by showing it, so the heartbeat chapter re-lives the same ticks in a rest twin - the identical world with the bound at rest, rate zero and no geography - and tells both final weights, claiming the difference as the bound's own work only under a live check that the live weight really stands above the rest one. Second, the untouched-regulation law: the bound scales interface weights and never the body, so the temperature still lands at its defended thirty-seven and every older chapter tells exactly the story it told. Third, refusal aloud, sharpened in review: an unbound tick count is refused as uninstantiated rather than under a named wrong carrying a variable no reader could name, the same house convention slice 33 laid in the numeric-constant checks.

Gates at this slice: capstone_demonstration sixty of sixty Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; four hundred ninety-four in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 35

Slice 35 - The two-shift switchboard laid: the global operating state rides the bus, online by default, and nothing yet reads it - 2026-08-09 - pack neuromodulator_bus (grown; no other pack changed).

WHAT THE PHASE REQUIRED. The Eighth Commandment analysis named the two-shift principle - online computation alternating with offline consolidation - the single most central gap the Vision Set records, and the Ninth Commandment survey found four independent literatures converging on the same demand (coverage bug two). The Vision analysis ordered the family smallest first, and the smallest honest step is the state itself: before any governor can throw a switch and before any offline works can run, there must be a switch to throw, announced where every subscriber already listens. That place is settled by the corpus: the Layer 11 catalogue's Chapter 16 names the neuromodulatory broadcast system the state announcement service - the physical implementation of thrown from above - and its Chapter 11 says the announced selection is a single binary value held decisively at one of two positions with almost no time spent halfway.

WHAT THIS SLICE SPECIFIES. The neuromodulator_bus carries a GLOBAL OPERATING STATE beside its chemical levels: exactly two values, online and offline, and no third; a bus that never heard an announcement operates online, the waking default, so every bus built before this state existed behaves exactly as it always did; the newest announcement wins, as it does for every level on this bus; and the state's key is a third key shape - the compound global(operating_state) - which the atom-only modulator guard and the territory pair shape can never alias, so the three kinds of traffic on the one bus cannot collide even by malice. Three laws bound the design. First, the pinned-identical law: no existing predicate changes and nothing yet reads the state, so all four hundred ninety-four prior tests stand untouched beside the nine that grew. Second, the no-halfway law, the flip-flop's snap made a domain check: a state that is neither online nor offline is refused aloud by its own name. Third, refusal before invention, sharpened in review: an unbound state is refused as uninstantiated before any wrong pretends to name it, and an unbound bus is refused at the read rather than silently bound into a partial list carrying an invented state.

Gates at this slice: neuromodulator_bus twenty-three of twenty-three Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; five hundred three in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 36

Slice 36 - The reward learns its address: the tick spends the eligibility traces the drives reward, and capture consumes the spent tags - 2026-08-09 - packs plasticity_engine and cognitive_cycle (grown; no other pack changed).

WHAT THE PHASE REQUIRED. Slice 28 built the eligibility trace and its reward step, and every completion document since slice 32 has carried the same honest line: the reward step still waits for its caller. The wait was deliberate - trace lifetime belongs to whoever decides a reward has arrived - and slice 32 wired everything BUT that decision into the live tick, because the tick did not yet own it. But the tick has owned it all along: its own first step is the drive system declaring the tick's reward and broadcasting it as dopamine. The Vision analysis ordered slice family one smallest first, and after the switch of slice 35 the smallest honest step is item (b): the reward step wired into the live tick. The design authority is the Layers 4-5 slow-variables dossier, Part Two, the tagging and capture machinery, read in full: the synaptic tag is a decaying local eligibility mark that entitles exactly the synapses that earned a change to capture the plasticity products a reward ships (Chapter 5, whose konnectome note names the eligibility trace outright); the products are a broadcast cargo captured only at tagged synapses and degraded if unclaimed (Chapter 6); and the law that binds both chapters is that capture CONSUMES - a used address must expire, so a synapse cannot wrongly capture a later, unrelated shipment.

WHAT THIS SLICE SPECIFIES. A reward-capture step joins the plasticity engine beside the slice-28 reward step, and the live tick calls it: where the receiving territory's dopamine is nonzero, cargo arrived - the standing trace is spent into the weight exactly as the reward step spends it, AND the spent tag is consumed to zero; where that dopamine is exactly zero, nothing shipped - the weight stands bit-identical and the tag stands, left to fade on the trace step's own clock. The third factor is signed: a punishment ships cargo too, spending the tag downward and consuming it just the same. In the tick the capture runs on the traces as they stood at the tick's start, BEFORE the tick's fresh coincidence is folded in, so a reward one tick late finds the trace that earned it and a tick's own coincidence is never double-counted against its own reward. The older reward step keeps its slice-28 meaning untouched for callers who manage trace lifetime themselves.

Gates at this slice: plasticity_engine eighty-two of eighty-two and cognitive_cycle sixteen of sixteen Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; five hundred eighteen in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 37

Slice 37 - The night watchman takes the post: the two-process governor rides the live tick and throws the slice-35 switch, its first legitimate thrower - 2026-08-09 - pack two_process_governor (new) and pack cognitive_cycle (grown; the fixture worlds of symbol_exchange and capstone_demonstration seat the watchman; no behaviour elsewhere changed).

WHAT THE PHASE REQUIRED. Slice 35 laid the switch and slice 36 finished the day shift, and both left the same standing line: the switch is wired to nothing, and the first thing entitled to throw it is the two-process governor - item (c) of slice family one, the smallest remaining step. The design authority is the Layer 11 state machinery, Chapters 11 through 16, read in full again this session: Chapter 14 specifies Process S, the homeostatic sleep pressure, a debt that rises across waking and is paid down only by sleep, whose konnectome note names the sleep_pressure_accumulator half of the two_process_scheduler; Chapter 15 specifies Process C, the circadian drive, a twenty-four-hour wave of alerting drive set deliberately AGAINST the rising debt, so alertness can be high exactly when the debt is greatest - the whole-organism control law the corpus calls its cleanest - whose konnectome note names the circadian_clock_governor as the scheduler's other half; Chapter 11 specifies the sleep-wake flip-flop the two processes bias - a single binary selection, thrown decisively, scheduled from outside by S and C; Chapter 13 specifies the stabiliser that widens the switch's basin so a noisy mid-band value cannot chatter it; and Chapter 16 says the decision is worthless if it stays in the switch - the selection is announced on the broadcast bus every subscriber already reads.

WHAT THIS SLICE SPECIFIES. A new pack, two_process_governor, holds the scheduler as a pure engine: a governor term carrying the standing sleep pressure, the circadian phase, and six whole-word parameters (the pressure rise, the pressure discharge, the day length, the circadian amplitude, the sleep threshold, and the wake threshold). One step advances both processes one tick and selects the operating state: a waking tick adds the rise and a sleeping tick pays the discharge, floored at zero, because a debt cannot go below paid (Process S); the phase advances one tick and wraps at the day length, and the day wave rises linearly from silence at midnight to the full amplitude at midday and back - an honestly declared triangle simplification of the corpus's smooth waveform (Process C); the flip-flop judges the balance, the pressure LESS the opposing drive, and snaps offline only at or past the sleep threshold, snaps online only at or below the wake threshold, and HOLDS its current state everywhere between - the hysteresis band between the two thresholds is Chapter 13's stabiliser in its simplest form, and an inverted band is refused at build time by name, because a governor whose wake threshold stands above its sleep threshold chatters. The default parameters live the corpus's day at one tick per hour: rise one against discharge two over a twenty-four-tick day, amplitude four, thresholds sixteen and two - a long consolidated waking, then a consolidated night, roughly the corpus's sixteen and eight. The governor never touches the bus itself: exactly as Process S and Process C sit outside the flip-flop they bias, it subscribes to the standing selection and publishes its next one to its caller, and the live tick - the loop that owns the bus - does the announcing at the tick's end, after the body has responded and before the observer records. The world dict gains one required key, governor, refused aloud by name when missing, like every key the tick reads. Deliberately, nothing yet READS the thrown state: offline behaviour first diverges in item (d), so this slice is behaviour-pinned-identical everywhere but the bus's state entry and the governor's own key.

Gates at this slice: two_process_governor twenty of twenty, cognitive_cycle twenty-one of twenty-one, symbol_exchange nineteen of nineteen, and capstone_demonstration sixty of sixty Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; five hundred forty-three in total across twenty-eight packs, all green); the naming gate reports zero violations across all twenty-eight packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 38

Slice 38 - The night crew takes the dark hours: the tick reads the switch the watchman throws, and the offline works run - interleaved replay against catastrophic forgetting, and the renormalisation shift - 2026-08-09 - pack offline_consolidation (new), pack cognitive_cycle (grown), and pack neuromodulator_bus (one additive export); the fixture worlds of cognitive_cycle, symbol_exchange, and capstone_demonstration seat the three new keys, and the online day is behaviour-pinned identical.

WHAT THE PHASE REQUIRED. Slice 37 put a watchman on post and left the sharpest edge in the build: the switch is now WRITTEN every tick by a legitimate hand, and still READ by nothing. Item (d) of slice family one is the first legitimate reader, and the analysis named its proof in advance - the offline works, interleaved replay plus the bound at a raised rate, proven by a catastrophic-forgetting rest-twin telling. The design authority is the Layer 11 sleep-state machinery, read in full this session: Chapter 7, the deep works, which names systems consolidation by INTERLEAVED REPLAY the flagship computation of the whole volume and states the problem it solves by name - catastrophic forgetting, the failure in which teaching a network something new erases what it already knew - and pairs it with synaptic renormalisation, the downscaling of the day's potentiation, under the konnectome note offline_consolidation_state; Chapter 6, the spindled light sleep, whose konnectome note names a queue of freshly encoded traces and discrete gated transfer events; Chapter 8, the paradoxical theatre, read and deliberately NOT built this slice; and Chapter 9, the ninety-minute wheel, whose ordering law is likewise read and deferred.

WHAT THIS SLICE SPECIFIES. A new pack, offline_consolidation, holds the night crew as a pure engine that never touches the bus or the world. It carries the hippocampal day as a memory store - an oldest-first list of activation snapshots, each remembered by a waking tick, refused aloud for an unbound store, a malformed pair, a non-numeric activity, or a construct named twice. It builds the night's batch by INTERLEAVING: the store splits into an older half and a newer half, the odd extra staying with the OLD because the standing cortex outweighs one day's hippocampus, and the two halves alternate newest-work-first. One replay round strengthens by the DAY'S OWN three-factor product with the third factor held at one, because the night's low-aminergic climate is permissive rather than dopamine-driven. The replay rate is specified as A DOSE PER NIGHT TICK, SHARED ACROSS THE BATCH, not a dose per remembered pattern: what one night delivers must not grow with how much the mind remembers, or the bound it is set against cannot bound - Chapter 7 is explicit that the night's NET effect is renormalisation with targeted strengthening of the replayed traces, and interleaving decides WHICH traces the dose defends. One whole night tick runs the replay FIRST and the raised homeostatic bound SECOND - the order is the mechanism, and the refreshed weight is what renormalises - reusing the day's own per-territory scaling law at the night's steeper rate. In the live tick, the standing operating state read off the bus at the tick's OPENING is judged against the bus's own two-position domain and then chooses between two whole programs. The DAY program is the slice-37 waking tick unchanged, and then the day REMEMBERS: the tick's updated activation pattern joins the memory store. The NIGHT program closes the sensory gate - no drive step, no proposals, no graph update, no released action, no fast learning, no capture, an unmoved body and unmoved traces and averages - and runs the night works instead, declaring a zero reward and an offline_works outcome. The world dict gains THREE required keys, each refused aloud by name when missing: memories, replay_rate, and offline_scaling_rate. Both night rates are judged on EVERY tick, waking or sleeping alike, and a night bound set BELOW the day's is refused by name, the inverted-hysteresis refusal's sibling. The timing law of slice 37 holds to the letter: the selection is made from a tick's completed day and governs the NEXT tick's opening state.

Gates at this slice: offline_consolidation twenty-seven of twenty-seven and cognitive_cycle thirty-four of thirty-four Prolog Unit (PLUnit) tests pass, with neuromodulator_bus twenty-three of twenty-three, symbol_exchange nineteen of nineteen, and capstone_demonstration sixty of sixty unchanged through the reseated worlds (the earlier packs unchanged; FIVE HUNDRED EIGHTY-THREE in total across twenty-nine packs, all green); the naming gate reports zero violations across all twenty-nine packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 39

Slice 39 - Every construct becomes a hybrid automaton: the register of one, stated proudly - 2026-08-09 - pack mode_register (new), packs connection_graph and tick_engine (rewired so every construct's rule is read THROUGH its own register); no world dict key added, no number changed, and the engines pinned behaviour-identical against a main worktree for every well-formed construct kind.

WHAT THE PHASE REQUIRED. The owner's judgment of 2026-08-09, recorded in the ledger, holds that construct MODES AT EVERY LEVEL are a chief and critical aspect of the konnectome synthetic mind rather than one refinement among five, and the deep read ordered by that judgment - docs/konnectome_mode_register_design_authority_v1.txt, four parallel readers over the fourteen MODES companions plus the Layer 6 chapter template - is the design authority for this slice and for the family it opens. Its single most important finding is that konnectome does not have to design a mode register: the corpus specifies one, in identical words, beside every layer. STATE is the continuous instantaneous condition; MODE is the discrete operating regime; and the formalism is the HYBRID AUTOMATON - a mode register, per-mode transfer functions, a transition table, and fault watchdogs - under the admission rule that a regime enters the register only if the healthy system uses it. The design authority's Part Nine names this slice the real first build of the family, and the reason it is first is the corpus's own: A REGISTER OF ONE IS FIRST-CLASS, NOT DEGENERATE. The Layer 7 volume says "a register of one, stated proudly" three separate times, and the corpus states the interpretation directly - the length of a register is a statement of what the system trusts the component to decide. konnectome's existing single-rule constructs are therefore not a debt to be repaid; they are faithful models of mode-poor components, and this slice re-expresses them as what they already are.

WHAT THIS SLICE SPECIFIES. A new pack, mode_register, holds the corpus's formalism under the corpus's own name: hybrid_automaton(CurrentMode, Register, Transfers, Transitions, Faults). The CURRENT MODE is the atom naming the regime the construct is in right now, and it is the index into everything else. The REGISTER is a list of mode_entry(FormalName, CoinedName, Gloss) terms carrying the corpus's three fields and no more - the rule is not here, the parameters are not here, and the entry and exit conditions are not here, because keeping them out is exactly what makes the register readable as a list of what a construct can BE. The PER-MODE TRANSFER FUNCTIONS are the parallel block of transfer(FormalName, Rule) terms, one per register entry, carrying the law that holds while that mode is current; konnectome's Rule is its existing construct kind term, which is already where its parameters live. The TRANSITION TABLE is a list of transition(Trigger, Direction-from, Direction-to, Timescale, Agency) rows: timescale is mandatory because the corpus's transitions span fifteen orders of magnitude and a register that models them as instantaneous edges discards the corpus's principal quantitative content, and AGENCY is mandatory because it is the corpus's answer to who governs the machine. Guards are deliberately NOT a column, because the corpus folds them into the trigger clause and konnectome must not design a column the corpus cannot populate. The FAULT block is a list of fault(BoundarySignature, WarningCondition, Watchdog) terms, present and empty, because the Layer 6 template forbids by name the admission of faults as first-class operating modes and requires that they be watched by a supervisor process konnectome has not built yet.

Every one of the four blocks is judged before an automaton exists, and each fault is refused ALOUD and by name: a register of zero (which the corpus records nowhere, at any grain); a store that is a hole, a partial list, or not a list at all; an entry that is not a three-field mode entry; a mode named by anything but an atom; two entries sharing a name; a current mode that is a hole, or one the register does not declare; a transfer for an undeclared mode; two rules under one mode name; a declared mode carrying NO rule at all; a malformed transition row; a transition running to a mode outside the register; and a malformed fault entry. The lookup itself refuses an unbound mode key, because a membership check on a hole would bind that hole to the store's first entry and answer with a rule belonging to something else.

And then the specification's actual claim: EVERY CONSTRUCT KIND KONNECTOME RUNS TODAY IS RE-EXPRESSED AS A REGISTER OF ONE, and the two engines that dispatch on construct kinds - the tick engine's heartbeat and the connection graph's four steps - now read every construct's rule THROUGH that register before applying it. The six kinds and their single modes are the free-running clock (the Metronome), the committed copy (the Echo), the held value (the Kept Reading), the clamped input (the Clamped Window), the tonic relay (the Faithful Relay), and the modulated tonic relay (the Listening Relay); each entry carries the corpus's formal name, its vivid coined name prefixed "the", and a one-clause gloss. The per-mode transfer function is the construct kind term ITSELF, which is precisely why the conversion cannot change a number. A kind konnectome does not run has no register and passes through the resolver unchanged, so each engine's own domain refusal remains the sole authority on what that engine accepts, naming exactly the kind the caller wrote. Two corpus fields are declared omitted rather than forgotten: the per-mode CONFIDENCE tag, deferred by name in the design authority's Part Nine, and the per-mode admitted-as-fault flag, which belongs with the supervisor channel.

WHAT THIS SLICE DELIBERATELY DOES NOT SPECIFY, so the gaps are decisions. The naming resolution that the design authority's Part Nine listed as its slice one is recorded here as a decision rather than built, because it was VERIFIED this slice that there is nothing to rename: no directed-versus-broadcast topology flag exists in packs/*/prolog at all, and none exists in causalontology either - the interface term is interface(From, To, Weight, Delay, Kind) whose Kind is transmissive or computational, a different axis entirely. The decision recorded is therefore threefold: the word MODE is reserved for the corpus sense throughout konnectome code; packs/archetype's existing gate mode vocabulary is already corpus-correct and must NOT be renamed; and any future directed-versus-broadcast flag enters as TOPOLOGY. The gate archetype's genuine two-mode register, the bus throwing a transition, the supervisor channel, the stabiliser as a separate construct, and the master register widened past the online-and-offline binary are the design authority's slices three through seven and are not built here.

Gates at this slice: mode_register forty of forty Prolog Unit (PLUnit) tests pass at birth, with connection_graph grown from twenty-four to twenty-six and tick_engine from seven to eight (the earlier packs unchanged; SIX HUNDRED TWENTY-SIX in total across THIRTY packs, all green); the naming gate reports zero violations across all thirty packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 40

Slice 40 - The gate archetype re-expressed as its genuine two-mode register - 2026-08-09 - pack archetype (the gate given a register of TWO, per-mode transfer functions that really differ from each other, and an explicit transition table carrying trigger, direction, timescale and agency); no world dict key added, no number changed, and every well-formed gate answer pinned byte-identical against a main worktree.

WHAT THE PHASE REQUIRED. Slice 39 gave every construct a register of ONE, and the design authority - docs/konnectome_mode_register_design_authority_v1.txt - named the slice after it in its Part Nine: THE GATE ARCHETYPE RE-EXPRESSED as its genuine two-mode register, with an explicit transition table, validated against corpus entries whose registers are two. The reason this is the next slice rather than a later one is the design authority's own: of everything konnectome had built in thirty-eight slices, the gate archetype's binary flip is "a genuine two-mode register in the corpus's own sense and is the natural seed of the whole design", and its existing vocabulary is corpus-correct and must not be renamed. A register of one is first-class, but it is also the one register length that cannot catch a mistake, because with a single mode the register block, the transfer block and the current-mode slot have nothing to disagree about. Two modes is where the formalism starts doing work.

WHAT THIS SLICE SPECIFIES. packs/archetype's gate is given the four blocks of the hybrid automaton, built and judged by mode_register's own constructor so that no second authority on register validity comes into existence. THE REGISTER holds exactly two entries, in the corpus's three-field entry schema: open - the Open Sluice - passes what arrives at it onward, unchanged, for as long as the gate stands open; and closed - the Dropped Shutter - blocks what arrives at it, passing nothing onward, for as long as the gate stands closed. The formal names are the gate's own existing vocabulary, which the design authority's Part Six forbids renaming. THE PER-MODE TRANSFER FUNCTIONS are two rules that genuinely differ - gate_pass and gate_block - and the slice's central claim is that they are APPLIED rather than merely filed: a gate really is a different machine in each of its two modes, which is the one thing a register of one could not demonstrate. THE TRANSITION TABLE carries two explicit rows, each with all four corpus fields and no guard column: a supra-threshold switching drive closes an open gate, and the same drive at the same one threshold opens a closed one; the timescale of both is one_tick, and the agency of both is self_selected, because nothing above throws this gate yet and the bus-throws-a-transition slice is the next one. THE FAULT BLOCK is present and permanently empty, because the Layer 6 template forbids by name the admission of faults as first-class modes and konnectome has no supervisor channel yet.

THE CURRENT MODE BELONGS TO THE INSTANCE, NOT TO THE KIND, and this slice is where that distinction is settled rather than deferred. Slice 39's registers of one could be looked up by construct kind alone, because a mode that never changes is a property of the kind. The moment a mode can CHANGE, two gates of the same kind stand in different modes at the same instant, so the current mode arrives as an argument and the register is built around it: archetype_gate_automaton(+CurrentMode, -Automaton). Everything else in the register - the entries, the transfer block, the table, the fault block - remains per kind and is stated once.

AND THE OBSERVABILITY THE STANDING OWNER REQUEST DEPENDS ON. The analysis's Theme One addendum requires from the first mode-register slice onward that a construct's current mode be an externally observable value the world carries, never a private field readable only by running the construct's rule. This slice honours it exactly: a watcher holding nothing but a gate's mode name can build that gate's automaton and read back both the mode and the rule that holds right now, without running the gate. That is pinned by a test named for the purpose.

VALIDATED AGAINST A CORPUS ENTRY WHOSE REGISTER IS TWO. The Layer 8 modes volume's Entry 36, the mutual-inhibition flip-flop, coined the Either-Or Latch, is the corpus's own nearest relative of konnectome's gate: "a hysteretic toggle whose modes are simply its two self-holding states". Its register holds Mode A - the First Latch and Mode B - the Second Latch. Its per-mode transfer functions describe what holds while each state is realised. Its transition table carries a bias thrown from above on the circadian and homeostatic timescales and a supra-threshold input to the losing pool that flips it near-instantaneously. Its fault block names two regimes - instability and a stuck switch - and a watchdog. konnectome's gate was read block by block against that entry, and the one place the two shapes differ is DECLARED rather than smoothed over.

THE DECLARED DIFFERENCE: KONNECTOME'S GATE IS A SYMMETRIC TOGGLE, NOT A HYSTERETIC LATCH. The corpus's latch turns on at one boundary and off only at a lower one, and the corpus is explicit that the hysteresis is supplied by a STABILISER that is "not part of the switch" and publishes a stability bias of its own. konnectome's gate compares one switching drive against ONE threshold in both directions, so its two transition rows honestly share a single trigger. This slice does NOT invent the second threshold: the design authority already files the stabiliser as its own later slice, and this slice promises to change no number. The transition table records the shared trigger, the transfer functions record what each mode actually does, and the gap is written in the ledger as an observation rather than papered over here.

Gates at this slice: archetype grown from seven Prolog Unit (PLUnit) tests to twenty-nine, all passing (the other twenty-nine packs unchanged; SIX HUNDRED FORTY-EIGHT in total across THIRTY packs, all green); the naming gate reports zero violations across all thirty packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 41

Slice 41 - The bus throws a transition - 2026-08-09 - packs neuromodulator_bus, archetype and mode_register (a broadcast channel that carries a MODE rather than a level, the gate's transition table given its second agency, and a departure lookup keyed on that agency); no world dict key added, no number changed, and every well-formed archetype answer pinned byte-identical against a main worktree.

WHAT THE PHASE REQUIRED. The mode-register design authority - docs/konnectome_mode_register_design_authority_v1.txt - names this slice fourth in its Part Nine: THE BUS THROWS A TRANSITION, so that one write produces many coordinated register transitions. Slice 39 gave every construct a register of one; slice 40 gave the gate a genuine register of two and made its transition table the sole authority on direction, and it said in as many words that when a broadcast throws a transition the throw would be a ROW in that same table rather than a second path into the gate. This slice is where that promise is kept. It is also where the design authority's Part Seven item 1 - the COMMAND VERSUS BIAS question - had to be answered, because the whole broadcast semantics of three later families hangs on it.

WHAT THIS SLICE SPECIFIES. THREE PACKS MOVE, AND EACH MOVES BY THE SMALLEST AMOUNT THAT MAKES THE MECHANISM REAL.

neuromodulator_bus GAINS A FOURTH KEY SHAPE. Its keys were a plain atom (a modulator level), territory/2 (a level in one territory), and global/1 (the operating state). The fourth is mode_throw(ConstructKind), and on it one write sets the mode of every construct of that kind at once. Four predicates carry it: a write, a read, a withdrawal, and the domain check the write comes through. THE CHANNEL IS ADDRESSED TO A KIND, NEVER TO AN INDIVIDUAL, because a broadcast that could name one construct would be a wire and not a broadcast. It carries no territory, because no reading has yet asked for a throw that reaches some territories and not others; that gap is a decision and not an oversight. And IT MOVES NOTHING BY ITSELF: it publishes, and each construct's own transition table decides whether it has anywhere to go.

SILENCE IS GIVEN A NAME, AND THE NAME IS RESERVED. A throw channel nobody has written reads back the atom no_mode_thrown rather than failing, exactly as an unwritten modulator reads as zero and an unannounced operating state reads as online. The bus REFUSES a write of that name aloud, so a register that happened to declare a mode of the same name could never be commanded by accident and a reader can never mistake silence for an instruction.

archetype GIVES THE GATE'S TRANSITION TABLE ITS SECOND AGENCY. Two new rows join the two slice 40 wrote: the same two directions, under the trigger broadcast_mode_throw, at the timescale one_tick, with the agency broadcast_thrown. The AGENCY field, which the corpus says must never be omitted and which read self_selected on every row konnectome had ever written, now carries two values - which is the whole of what "the bus throws a transition" means in the corpus's own schema. Beside them sits the application: a predicate that takes a standing throw and the mode a gate stands in and answers where that gate stands afterwards.

mode_register GAINS A DEPARTURE LOOKUP KEYED ON AGENCY. Until this slice every row in every konnectome table was self_selected, so "the first row leaving this mode" had exactly one answer and an unkeyed lookup could not be wrong. The moment a second agency writes rows into the same table that stops being true, and a lookup that did not name its agency would answer with whichever row happened to be DECLARED FIRST - correct today by accident of ordering and silently wrong the day anyone reorders the rows. The agency is therefore named in the lookup, in the one place both of the gate's routes come through, and TWO ROWS UNDER ONE AGENCY LEAVING ONE MODE ARE REFUSED ALOUD rather than resolved in favour of the earlier one.

THREE SILENCES, AND THEY ARE NOT THE SAME SILENCE. A gate reading an unwritten channel stands where it stands and keeps its own agency. A gate commanded to be the mode it is ALREADY in stands where it stands too, with no transition - and that is what makes one standing broadcast safe to read on every tick rather than only on the tick it was written, which a bus with no clock of its own must have. A gate commanded to a mode its own register does not declare is neither of those: it is refused aloud, by name.

THE COMMAND-VERSUS-BIAS DECISION, MADE HERE AND RECORDED AS KONNECTOME'S OWN. The design authority records that the corpus is genuinely silent: keeps it in the Pinpoint Report, set from above reads as COMMAND, and biases it toward flashing reads as BIAS, sometimes in the same volume. The Theme Two read raised the same question independently for the offline broadcast, and the Theme Four read raised it a third time; the only concrete evidence any of the three found is Layer 8 Chapter 16's "neuromodulators move the circuit between regimes by scaling recurrent gain", which is bias in FORM - a gain scalar, not a mode name - and which arrives at a different grain from the one where the decision must be made. It is therefore evidence and not an instruction. THE CORPUS DOES NOT DECIDE THIS, SO KONNECTOME DOES, AND THE DECISION IS: A THROWN MODE IS A COMMAND AT THE DISCRETE GRAIN, AND BIAS IS THE CHANNEL KONNECTOME ALREADY HAS.

Three reasons, none of them the corpus's, all of them konnectome's. FIRST, IT UN-CONFLATES TWO OPERATIONS THE DESIGN AUTHORITY SAYS ARE CURRENTLY CONFLATED: its Part Eight records that continuous modulation and discrete throw are different operations on the same channel and that konnectome's bus-modulated relay runs them together. konnectome already owns bias, built and running - relay_modulated reads a level off this same bus and scales its gain where it stands. Had the throw also been a bias, konnectome would own two bias mechanisms, no command mechanism, and the conflation would be permanent. SECOND, A COMMAND COSTS NO INVENTED NUMBER AND A BIAS COSTS AT LEAST ONE: a bias moves a threshold, and the corpus supplies konnectome no threshold offset at this grain, so one would have to be made up - in a family whose standing rule is that no number in the pack is konnectome's invention. THIRD, COMMAND IS THE REVERSIBLE CHOICE: a bias channel can later be added BESIDE this one as a second agency writing rows into the same table, and nothing built on the command has to move; the reverse is not true.

AND THE CONFLICT RULE THAT FOLLOWS FROM IT, ALSO KONNECTOME'S OWN. The design authority's Part Seven item 2 records that the corpus shows preemption in one place and negotiated arbitration in another and states no general rule. konnectome takes PREEMPTION: where a construct's self-selected transition and a standing throw disagree, the throw wins. The reason is konnectome's own and is not a claim about biology - preemption is the rule that can be READ without running the construct, which the owner's standing visualization request demands of every register slice in this family. NEGOTIATED ARBITRATION IS DEFERRED BY NAME rather than denied: it is the shape a later slice would need if two sources ever threw at one kind in one tick, and this channel deliberately holds exactly ONE standing throw per kind, the newest replacing the older exactly as every other broadcast on this bus does, so that case cannot arise unnoticed.

THREE FAMILIES INHERIT THIS DECISION, which is why it is written at this length rather than as a line: the remainder of the mode-register family, the Theme Two offline-broadcast work, and the Theme Four cognitive cluster. A later session that wants to revisit it should revisit it as a decision, in the open, and not discover it as an assumption.

Gates at this slice: mode_register grown from forty Prolog Unit (PLUnit) tests to forty-five, neuromodulator_bus from twenty-three to thirty-six, and archetype from twenty-nine to forty (the other twenty-seven packs unchanged; SIX HUNDRED SEVENTY-SEVEN in total across THIRTY packs, all green); the naming gate reports zero violations across all thirty packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the capstone demonstration runs and its story is byte-identical to main's. No new wall was hit; nothing in PrologAI or Causalontology was edited. HONESTLY STATED: the Causalontology conformance suite was NOT re-measured this slice, because this slice touched neither cousin; the last measured figure is Tungsten Sluicewright's one hundred thirty-seven of one hundred thirty-seven, and a session that needs the number should re-measure rather than quote it.


KONNECTOME REALIZATION LOG - SLICE 42

Slice 42 - The supervisor channel: fault regimes watched and published as warnings, never admitted as modes - 2026-08-09 - pack supervisor (new) and pack archetype (the gate's fault block filled from the corpus's own latch entry); no world dict key added, no number invented, and every archetype answer pinned byte-identical against a main worktree.

WHAT THE PHASE REQUIRED. The mode-register design authority's Part Nine slice five, and the closing of an IOU three slices old. The Layer 6 chapter template states the admission rule for a mode register directly: a regime enters the register only if the HEALTHY system uses it, and the ways a component BREAKS are watched by supervisor processes that publish WARNINGS - never listed as regimes the component operates in. konnectome has honoured that since slice 39 by leaving every fault block present and EMPTY, which was correct and was also a debt: the block could not honestly be filled until something existed to do the watching. This slice builds the watcher, and the blocks stop being empty.

WHAT THIS SLICE SPECIFIES. A NEW PACK, supervisor, AT LAYER ZERO, and one existing block filled.

THE CHANNEL IS ITS OWN CHANNEL AND NOT A KEY ON THE NEUROMODULATOR BUS, and the reason is recorded so a later slice does not merge them out of tidiness. The bus is Architecture Component 4: the broadcast field of global brain-chemical levels, the one place the architecture permits a global value, read by every construct's update and learning rule. A supervisor warning is not a chemical and is not addressed to the constructs at all - IT IS ADDRESSED OUTWARD, to whoever can act on a component that has left its design envelope. Putting it on the chemical bus would have made every construct's update rule a potential reader of its own failure report, a coupling nothing has asked for. The channel carries one report per construct kind, newest replacing older, so a warning cannot outlive the condition that raised it.

A REPORT CARRIES TWO BLOCKS AND AN UNWRITTEN CHANNEL READS AS NEITHER CLEAN NOR ALARMED. supervisor_report(Warnings, Unwatched) is the term, and a construct kind nobody has watched reads back as two empty blocks - which is the honest statement that no watch has happened, not a clean bill of health. That is the same read-with-a-named-default idiom the bus established three times over, and it is chosen here for the same reason: a default that looks like good news is how a silence becomes a lie.

WHAT THE SUPERVISOR MAY NOT DO, and it is the shortest list in the repository: IT MAY NOT MOVE ANYTHING. It reads a fault block, compares readings against caller-supplied allowances, and publishes. It has no path to a construct's mode, no row in any transition table, and no agency in the corpus's sense. A supervisor that could move a construct would be a controller, and konnectome has spent two slices establishing that constructs are moved only by rows of their own registers.

WHY THE DISTINCTION IS WORTH A PACK RATHER THAN A CONVENTION, stated once and at length because the whole slice rests on it. A mode and a fault look alike from a distance - both are named conditions a construct can be in - and the entire difference is in what the system is entitled to DO about them. A MODE HAS A TRANSFER FUNCTION: while it holds, the construct runs a different machine, on purpose, and its behaviour in that mode is part of the design. A FAULT HAS NO TRANSFER FUNCTION AND NEVER WILL: it is a boundary the construct was not designed to cross, and the only correct response is to say so aloud to somebody who can act. If a fault were admitted as a mode, THE SYSTEM WOULD ACQUIRE A DESIGNED BEHAVIOUR FOR BEING BROKEN, and would then run it silently and indefinitely - which is exactly the failure the corpus's directive exists to prevent. Written as a convention that is a sentence people forget; written as supervisor_faults_are_not_modes/1 it is a refusal that fires, and it fires on the way into every watch so a malformed register can never be reported on as though it were well formed.

THREE OUTCOMES, NOT TWO, AND THE THIRD IS THE POINT. A fault regime is WARNED (a reading crossed its allowance), CLEAN (a reading did not), or UNWATCHED (no watchdog reported at all). A two-outcome design loses the third, and losing it is precisely how a system comes to believe that silence means health. An unwatched regime is reported as unwatched, by name, carrying the watchdog that did not report, beside the warnings rather than hidden among them.

KONNECTOME INVENTS NO THRESHOLD, AND THAT IS A SHAPE RATHER THAN AN OMISSION. Each reading arrives carrying its own ALLOWANCE, supplied by the caller that owns the watchdog. The supervisor compares and reports; it does not decide how much oscillation is too much, because the corpus supplies no such number at this grain and this family's standing rule is that no number in the pack is konnectome's invention. The boundary case is pinned rather than left to the next reader: a value exactly AT its allowance has not crossed it.

THE GATE'S TWO FAULT REGIMES ARE THE CORPUS'S OWN. They are taken from the Layer 8 modes volume's Entry 36, the mutual-inhibition flip-flop coined the Either-Or Latch - the same entry slice 40 validated the gate against block by block, which names exactly two fault regimes and a watchdog. konnectome takes both and invents no third. OSCILLATION is the latch failing by never settling; STUCK SWITCH is the same latch failing the opposite way, by settling and never leaving. Neither signature is a mode name, and that is CHECKED rather than intended.

Gates at this slice: the new supervisor pack ships TWENTY-SEVEN Prolog Unit (PLUnit) tests and archetype grows from forty to forty-two (the other twenty-nine packs unchanged; SEVEN HUNDRED AND SIX in total across THIRTY-ONE packs, all green); the naming gate reports zero violations across all thirty-one packs and passes its own self-test; the layer, layer-binding and reality-discipline gates all report zero violations; the capstone story is byte-identical to main's. WATCH-3 WAS EXAMINED AND REMAINS A WATCH: the design authority's Part Ten filed the supervisor channel as a routing candidate IF a published warning must be a shareable record rather than a konnectome-internal signal. It is not. Every warning this slice produces is computed inside one runtime from readings that runtime was handed, and travels to nobody; it fails the consumer-versus-ontology test and routes nowhere. Nothing in this slice releases the held sections of the causalontology change order. No new wall was hit; nothing in PrologAI or Causalontology was edited, and conformance was not re-measured because neither cousin was touched.


KONNECTOME REALIZATION LOG - SLICE 43

Slice 43 - The ticks-per-second convention: one hundred ticks to the nominal second, declared and refused rather than rounded - 2026-08-09 - pack tick_engine (three exported predicates, no new pack); no world dict key added, no number invented in any construct, and the capstone story pinned byte-identical against a main worktree.

WHAT THE PHASE REQUIRED. Family Four item 2 of the version 2 vision-set analysis, promoted ahead of the stabiliser on the Iridium hand-off's one scheduling suggestion. KONNECTOME HAS NO SECONDS AND THE CORPUS COUNTS IN THEM. Every quantity in the cognitive cluster the corpus specifies is wall-clock - ignition near three hundred milliseconds, admission in the low hundreds, unrehearsed decay in seconds, a board wiped on task switch in under a second - and a settling time read as confidence is wall-clock too, while konnectome's tick is dimensionless-ordinal by the observer's own written declaration. None of that cluster is buildable to the corpus's numbers until a ticks-per-second convention is declared, and the convention is konnectome's own decision, not the corpus's.

WHY IT JUMPED THE QUEUE, recorded because a promotion is itself a decision. The item arrived from THREE independent directions: beneath Theme Four and Theme Five in the version 2 analysis, and again from a direction nobody predicted when slice 42 found that a watchdog cannot have a window without a unit to measure one in - which is why OBSERVATION-9 was left open rather than closed. The stabiliser, the queue's front, publishes a scalar whose grains must be stated, and a grain argued in seconds cannot be stated in a build with no seconds. THE RULE THIS APPLIES: A CHEAP ITEM THAT ARRIVES FROM A THIRD INDEPENDENT DIRECTION HAS STOPPED BEING A QUEUE ITEM AND HAS BECOME A DEPENDENCY.

WHAT THIS SLICE DELIVERS, AND IT IS DELIBERATELY SMALL. No new pack. tick_engine gains three exported predicates: tick_engine_ticks_per_second/1, the convention stated once and in one place, and the two conversions tick_engine_ticks_from_milliseconds/2 and tick_engine_milliseconds_from_ticks/2. This is a DECISION RECORDED WITH TESTS ASSERTING IT, in the shape of slice 39's naming-collision resolution and slice 41's DECISION-1 - a decision closes a question, where a mechanism answers one.

DECISION-2: ONE HUNDRED TICKS TO THE NOMINAL SECOND, SO ONE TICK IS TEN NOMINAL MILLISECONDS, AND THE SCHEDULER STAYS DIMENSIONLESS. It is konnectome's own decision and must never be presented as the corpus's instruction. The corpus says nothing whatever about how often a synthetic mind should update, because it describes a continuous system and not a scheduler: this is not a silence to be resolved by reading harder, it is a question the source does not contain.

THE THREE REASONS, ALL KONNECTOME'S OWN. FIRST, EVERY CORPUS CONSTANT BECOMES A WHOLE NUMBER OF TICKS - three hundred milliseconds is thirty ticks exactly, one hundred is ten, one second is one hundred, half a second is fifty. A rate leaving a constant between two ticks would force a rounding at every site that used it, AND A ROUNDING IS AN INVENTED NUMBER WEARING A SMALL COAT: the constant leaves the corpus as the corpus's and arrives in the pack as konnectome's, with nobody having decided anything. SECOND, IT IS THE COARSEST RATE THAT ACHIEVES THE FIRST, and coarsest is the honest choice rather than the convenient one, because the corpus states these quantities to a hundred-millisecond grain and a millisecond tick would claim a precision the source does not have. THIRD, IT COSTS THE RUNNING MACHINE NOTHING AND IS THEREFORE REVERSIBLE: no existing behaviour moves by a single count, and every duration konnectome ever expresses in real units passes through ONE conversion predicate, so a session that must restate the rate has one predicate to change and one test to re-read rather than a search across thirty-one packs.

THE REFUSAL IS THE LOAD-BEARING HALF. A DURATION THAT IS NOT A WHOLE NUMBER OF TICKS IS REFUSED ALOUD with domain_error(whole_tick_duration, Milliseconds) and is NEVER ROUNDED. Rounding is exactly how invented numbers enter a careful build - a caller asks for five milliseconds, is handed one tick without being told, and a number that was the corpus's has silently become konnectome's. The refusal forces that decision into the open where it can be recorded. Both conversions also refuse an UNBOUND argument, the unbound-wrong-judgement lens paying for an ELEVENTH SLICE RUNNING (33 through 43).

WHAT DECISION-2 DOES NOT DECIDE. It does not make konnectome real-time: nothing sleeps, nothing measures, nothing consults a wall clock, and a tick is not ten milliseconds long - ten milliseconds is what a tick is CALLED when a duration must be stated, which is pinned by a test asserting the heartbeat still reads five after five ticks. It does not decide that any particular corpus constant is right. It does not decide the observer's stamp, which keeps its own nominal one-second-per-tick carrier and is now numerically at odds with the convention - recorded as OBSERVATION-10 rather than quietly fixed, because restating that stamp would re-mint every content-addressed token occurrence identifier in the repository. It does not decide a window length for any watchdog: it supplies the UNIT a window can be stated in, and OBSERVATION-9's closer must still choose the number and say why. And it does not decide whether a construct may hold a rate of its own.

Gates at this slice: tick_engine grows from eight Prolog Unit (PLUnit) tests to fifteen (the other thirty packs unchanged; SEVEN HUNDRED AND THIRTEEN in total across THIRTY-ONE packs, all green); the naming gate reports zero violations across all thirty-one packs and passes its own self-test; the layer, layer-binding and reality-discipline gates all report zero violations; the capstone story is byte-identical to main's. No new wall was hit, and nothing routes to either change order: a conversion between two of konnectome's own units is computed inside one runtime and travels to nobody, so it fails the consumer-versus-ontology test of the Second Commandment, and it needed no language feature PrologAI lacks. Nothing in this slice releases the held sections of the causalontology change order. Nothing in PrologAI or Causalontology was edited, and conformance was not re-measured because neither cousin was touched - the figure remains four sessions old and must be re-measured before it is quoted.


KONNECTOME REALIZATION LOG - SLICE 44

Slice 44 - The stabiliser: hysteresis published as a scalar, and the gate becomes the corpus's latch - 2026-08-09 - pack stabiliser (new) and pack archetype (the gate gains a second boundary); no world dict key added, no number invented, no number changed, and the capstone story pinned byte-identical against a main worktree.

WHAT THE PHASE REQUIRED. The mode-register design authority's Part Nine slice six, and the closer OBSERVATION-8 named when it was opened. THE CORPUS IS EXPLICIT THAT HYSTERESIS IS NOT PART OF THE SWITCH: the Layer 11 reading states in one sentence that "this is a stabiliser, not a switch, so it has no poles of its own", and that it publishes a scalar stability bias onto the configuration bus. konnectome's hysteresis was two constants inside the two-process governor's parameter block and one symmetric threshold inside the gate - the stickiness buried in the switch, which is the arrangement the corpus names and rejects. Slice 40 found the gap and recorded honestly that it could NOT be fixed where it was found, because adding a second constant inside archetype_gate/4 would have modelled the corpus wrongly while appearing to fix it.

WHAT THIS SLICE SPECIFIES. A NEW PACK, stabiliser, AT LAYER ZERO, and one existing rule given a second boundary.

WHAT A STABILITY BIAS IS, WITH ITS UNITS STATED, because a scalar without stated units is an invitation to misuse. IT IS HOW FAR APART THE TWO BOUNDARIES SIT: half the width of the hysteresis band, expressed in the units of whatever quantity the switch it steadies compares. A gate comparing a switching drive reads it as an offset on the gate's threshold; a governor comparing a sleep-pressure balance reads it as the half-width of its band about a midpoint. One meaning, two consumers, no conversion.

DECISION-1 IS APPLIED AND NOT RE-OPENED. A hysteresis scalar is a BIAS, so it travels as a LEVEL under a plain-atom key on the channel konnectome has had since slice 4, and never as a thrown mode. That is DECISION-1 applied rather than a new decision, and it is proved rather than asserted: the published value reads back through the bus's ORDINARY level read, with nothing about it needing the stabiliser to interpret it.

AND THE STABILISER'S AGENCY IS STATED, because after slice 41 a publisher has two shapes available and they are not the same thing. Its agency is broadcast_bias, AND IT IS AN AGENCY IN NO TRANSITION TABLE, which is the sharpest available statement of what a bias is. Since slice 40 a construct is moved ONLY by a row of its own register's table. THE STABILISER MOVES NOTHING. It changes where a boundary sits, after which the construct's own row fires, or does not, by its own rule. A test reads the gate's table, checks it is not empty so the test cannot pass vacuously, and asserts the stabiliser's name is absent.

DECISION-3: THE PUBLISHED SCALAR GOVERNS THE CONSTRUCT GRAIN ONLY. The version 2 analysis named this the sharpest ambiguity of its visit, because THE CORPUS ANSWERS THE HYSTERESIS QUESTION DIFFERENTLY AT TWO GRAINS - the Layer 11 reading puts the stickiness OUTSIDE the switch at the construct grain, while Layer 8 Chapter 16 gives winner-take-all's transfer function as a hysteretic max operation with an explicitly hysteretic Commitment Mode, stickiness INSIDE the motif. Both are literal. konnectome's three reasons are its own: the two readings stop conflicting once the grain is named; a bias reaching inside a transfer function would make a motif's own law depend on a global value, which is the coupling this build has spent slices avoiding; and it is the reversible choice, because a roster can be extended where a scalar already reaching everywhere could not be withdrawn. THE DECISION IS A REFUSAL AND NOT A COMMENT: the motif grain is turned away by name.

OBSERVATION-8 IS CLOSED, AND AT A BIAS OF ZERO NOTHING MOVES. archetype_gate_hysteretic/5 gives the gate two boundaries where it had one, and at a bias of zero the two coincide and the latch is the old symmetric toggle exactly, in every combination of mode and drive. Because the bus answers zero for a bias nobody has published, every switch in the repository can consult the stabiliser without any of them changing behaviour - which is what made this slice safe to build and why the capstone story is byte-identical.

THE MAPPING KONNECTOME CHOSE, AND THE ONE IT REJECTED. The corpus states hysteresis for a LEVEL comparator, and archetype's gate compares a SWITCHING DRIVE, which is a push rather than a level; the two do not map by themselves. konnectome chose: LEAVING A MODE COSTS THE RAISED BOUNDARY, WHICHEVER MODE IT IS, so over the whole range between the threshold and the threshold plus the bias both modes are stable and history decides which is realised - the corpus's own definition of the property, quoted almost word for word. THE REJECTED READING IS RECORDED because it is the one a later session will reach for first: making turn-on cost the threshold plus the bias and turn-off cost the threshold MINUS it reads exactly like the corpus's higher-and-lower boundaries, and with a push-drive it makes CLOSED ABSORBING over the whole middle range. A gate that settles and never leaves is the corpus's own STUCK SWITCH fault signature, listed in this pack's fault block since slice 42, and a hysteresis design that manufactures a fault regime is not a hysteresis design.

THE GOVERNOR'S TWO CONSTANTS ARE NOW DERIVED RATHER THAN DECLARED, AND NOT ONE NUMBER CHANGED. Sixteen and two are restated as what they always were - a band about a midpoint, a midpoint of nine and a half-width of seven - because replacing them with different numbers would be konnectome inventing where it had no need to. The suite proves the derivation reproduces the original block field for field AND that a governor standing on the derived band runs an identical twenty-four-tick day, state for state, because identical parameters are only evidence and identical behaviour is the proof.

AN HONEST INTERACTION, PINNED RATHER THAN LEFT TO BE DISCOVERED. The two consumers have DIFFERENT FLOORS. A gate reads a bias of zero as "no hysteresis" and behaves as it always did; the governor cannot, because its own guard has refused a flat or inverted band since it was built, and a flat band chatters. The same scalar is therefore legal at zero for one consumer and refused at zero by the other, and a test pins the governor's refusal by its own name. That is not a defect: it is what it looks like when two constructs share a scalar and each keeps its own domain.

WHAT THIS SLICE DELIBERATELY DID NOT BUILD. THE CORPUS GATES THE STABILISER BY CIRCADIAN PHASE AND HUNGER, AND NEITHER GATING IS BUILT: hunger has no construct in konnectome at all, and gating by phase would require a function the corpus does not supply at this grain, so the scalar is published by a caller who chooses it and the gating is deferred BY NAME rather than invented. No construct was rewired to consult the stabiliser - the gate and the governor can now be handed a bias, and the join belongs to the caller, as every join in this family has since slice 41. No motif-grain hysteresis, per DECISION-3. And OBSERVATION-9 stands exactly where slice 42 left it: the stabiliser is not a watchdog and counts nothing.

Gates at this slice: the new stabiliser pack ships EIGHTEEN Prolog Unit (PLUnit) tests and archetype grows from forty-two to fifty (the other thirty packs unchanged; SEVEN HUNDRED AND THIRTY-NINE in total across THIRTY-TWO packs, all green); the naming gate reports zero violations across all thirty-two packs and passes its own self-test; the layer, layer-binding and reality-discipline gates all report zero violations; the capstone story is byte-identical to main's. Nothing routes to either change order: a scalar published inside one runtime and read by two constructs in the same runtime travels to nobody and fails the consumer-versus-ontology test of the Second Commandment, and no language feature was missing. Nothing in this slice releases the held sections of the causalontology change order. No new wall was hit; nothing in PrologAI or Causalontology was edited, and conformance was not re-measured because neither cousin was touched.


KONNECTOME REALIZATION LOG - SLICE 45

Slice 45 - The first real watchdog: a windowed history, and the end of a repository in which nothing was watched - 2026-08-09 - pack watchdog (new); no world dict key added, no existing pack edited, no number invented, and the capstone story pinned byte-identical against a main worktree.

WHAT THE PHASE REQUIRED. OBSERVATION-9, opened at slice 42 and named as the first task of this continuation. Slice 42 built the supervisor, which judges readings it is HANDED and keeps no history of its own. The consequence was stated plainly when it was opened: KONNECTOME HAD A WATCHER AND NO WATCHDOGS, so every fault regime in the repository was UNWATCHED in the supervisor's own vocabulary - reported honestly as unwatched rather than dishonestly as clean, which is why the supervisor needed its third outcome before it needed its second reading. The closer OBSERVATION-9 named was the slice that gives a construct a windowed history, or the tick engine a per-construct counter. This is that slice, and it is the first one.

WHY IT COULD NOT HAVE BEEN BUILT EARLIER, WHICH IS ITSELF THE SPECIFICATION'S ANSWER TO A QUESTION IT WAS ASKED TWICE. A watchdog counts events over a WINDOW, and a window is a duration; konnectome had no unit in which to state one until slice 43 declared the ticks-per-second convention as DECISION-2. That is why OBSERVATION-9 stood open through slice 43, which supplied the unit and deliberately closed nothing, and through slice 44, which built the stabiliser and counted nothing. Supplying a unit and building the process that consumes it are different acts, and the build recorded them as different acts rather than collapsing them.

WHAT THIS SLICE SPECIFIES. A NEW PACK, watchdog, AT LAYER ZERO, and nothing else. No existing pack is edited by this slice, which is the strongest available statement that the watchdog is a reader and not a rewiring.

A HISTORY IS A VALUE THE WORLD CARRIES. It is threaded through the caller in the explicit-stateless-value discipline that the eligibility-trace store, the running-average store and the per-territory target store already follow, and it is never hidden inside an engine. That is Theme One's addendum honoured at a new construct rather than promised: a watcher holding nothing but a history can read back the current mode, the span measured, and both readings, WITHOUT RUNNING THE CONSTRUCT the history is about.

THE TWO BOUNDARIES ARE THE CORPUS'S AND NOT KONNECTOME'S. They are the two fault regimes the Layer 8 modes volume's Entry 36 - the mutual-inhibition flip-flop, the Either-Or Latch - supplies for the gate: a switch can fail by flipping too readily, and it can fail the opposite way by settling so hard it will not leave. archetype has carried both by name since slice 42, with their watchdogs named oscillation_watchdog and stuck_switch_watchdog, and this pack is what those two names have been standing in for since they were written.

DECISION-4 IS THE DELIVERABLE OF THIS SLICE AND IT HAS TWO HALVES. FIRST, THE WINDOW LENGTH BELONGS TO THE CALLER THAT OWNS THE WATCHDOG, exactly as the ALLOWANCE already does - so this pack invents no number, and the debt the continuation's first task recorded is paid by refusing the invention rather than by making one. SECOND, AND THIS IS THE HALF WITH TEETH: A WINDOW THE HISTORY DOES NOT YET SPAN IS REPORTED AS NOT-YET-MEASURED, NEVER AS CLEAN AND NEVER AS ZERO. The full reasoning for both halves is in the ledger's DECISION-4 note, together with what the decision does NOT decide.

THE INTERFACE. watchdog_history_new/1, an empty history that has seen nothing and measures nothing. watchdog_observe/4, which records what mode one construct instance stood in at one ordinal tick. Three readers - watchdog_history_observations/2, watchdog_history_span/2 and watchdog_history_current/3 - which make the history externally legible without running anything. watchdog_window_measured/2, the honesty predicate. watchdog_window_ticks/2, which converts a window stated as a wall-clock duration THROUGH the tick engine's single conversion rather than through a second copy of the convention living here. The two readings themselves, watchdog_flips_in_window/3 and watchdog_ticks_held_in_window/3. And watchdog_gate_readings/6, which assembles both under the signatures the gate's own fault block declares, and hands back separately whatever could not honestly be measured.

TIME DOES NOT RUN BACKWARDS AND IT DOES NOT STAND STILL, AND BOTH ARE REFUSALS RATHER THAN ASSUMPTIONS. An observation at a tick not strictly later than the newest one held is refused with domain_error(watchdog_advancing_tick, Tick). The backwards case would rewrite history; the equal case would double-count one tick in every reading taken afterwards. Both would look like ordinary use while corrupting every verdict downstream, which is exactly the class of fault this pack exists to detect in others.

Gates at this slice: the new watchdog pack ships TWENTY-NINE Prolog Unit (PLUnit) tests and no existing pack's count moves (SEVEN HUNDRED AND SIXTY-EIGHT in total across THIRTY-THREE packs, all green); the naming gate reports zero violations across all thirty-three packs and passes its own self-test; the layer, layer-binding and reality-discipline gates all report zero violations; the capstone story is byte-identical to main's. Nothing routes to either change order: a history threaded through one runtime and read by one supervisor in the same runtime travels to nobody and fails the consumer-versus-ontology test of the Second Commandment, and no language feature was missing. Nothing in this slice releases the held sections of the causalontology change order. Nothing in PrologAI or Causalontology was edited, and conformance was not re-measured because neither cousin was touched - the figure is now SIX SESSIONS OLD and must be re-measured before it is quoted.


KONNECTOME REALIZATION LOG - SLICE 46

Slice 46 - The master register widened: a pole, a sub-mode, and the downward assignment - 2026-08-09 - pack master_register (new) and pack neuromodulator_bus (the state channel widened); no number invented, behaviour pinned identical, and the capstone story byte-identical against a main worktree.

WHAT THE PHASE REQUIRED. The mode-register design authority's Part Nine slice SEVEN, which is the LAST NAMED SLICE OF THE FAMILY and the one the other six were building toward: the master register widened past the online-and-offline binary into a named global state plus a sub-mode, and only then the downward assignment that writes per-construct modes.

WHY THE FAMILY ENDS HERE RATHER THAN AT SLICE ONE. Layer 11's outline states it in one sentence: at every layer below, the mode transitions kept being thrown FROM ABOVE - and Layer 11 is the thrower. In the corpus's own vocabulary A GLOBAL STATE IS NOT A FLAG; it is a COORDINATED ASSIGNMENT OF MODES across every cell, junction, motif and region below it, and all twenty-six of its chapters carry a section titled THE DOWNWARD ASSIGNMENT (WHAT IT SETS AT EACH LAYER) to say so. konnectome walked into the consequence at slice 35 without naming it: the global operating state put on the bus then IS the master mode register in its degenerate two-entry form, and it has had nothing beneath it to assign ever since.

WHAT THIS SLICE SPECIFIES. A NEW PACK, master_register, AT LAYER ZERO, and one existing channel widened by two predicates.

DECISION-5 IS THE DELIVERABLE, AND IT HAS TWO HALVES. FIRST, THE BINARY IS PRESERVED AND BECOMES THE POLE, with the sub-mode added BENEATH it rather than replacing it. SECOND, THE DOWNWARD ASSIGNMENT IS KEYED ON THE WHOLE STATE - pole and sub-mode together - so a sub-mode MAY change it, which answers the design authority's Part Seven item 4, a question the corpus explicitly does not settle. Both halves, their three reasons each, and the list of what they do NOT decide are in the ledger's DECISION-5 note.

THE ROSTER IS THE CORPUS'S AND THE CUT IS KONNECTOME'S, AND THE TWO ARE SEPARATED SO NEITHER IS MISTAKEN FOR THE OTHER. Eight entries: the waking family (Layer 11 outline entries 1 through 4) and the sleep family (entries 5 through 8), each carried with the outline's own coined formal name, in the outline's own order. WHAT IS KONNECTOME'S IS THE CUT FROM TWENTY-SIX TO EIGHT: the other eighteen are state MACHINERY (the flip-flops, the two processes, the broadcast service), the arousal and stress states that OVERLAY a base state rather than being one, the ultradian wheel which the corpus itself calls "a whole-night program, not a state", and the engineered and pathological configurations the corpus flags separately. Taking all twenty-six would have put a governor and a disease into a register of operating states, which is slice 42's fault-is-not-a-mode error arriving one grain higher up.

THE DEFAULTS ARE NOT KONNECTOME'S EITHER, AND THAT IS WHY THEY ARE SAFE. Every announcement made anywhere in this repository before this slice names a pole and no sub-mode, and something must say which sub-mode it meant. Waking defaults to Alert Task-Engaged Waking, which the corpus calls the default assumption of nearly every cognitive experiment ever run. Sleep defaults to slow-wave sleep, and that is MEASURED rather than obvious: it is the state whose downward assignment names systems consolidation and synaptic renormalisation, which is precisely and only what konnectome's offline_consolidation pack has done since slice 38. The default is what konnectome's offline phase ALREADY IS, read back from the corpus rather than picked.

THE INTERFACE. master_register_poles/1 and master_register_sub_modes/2, the roster read two ways. master_register_entries/1 and master_register_size/1, the register in the corpus's three-field schema, with its size read from the roster rather than restated. master_register_check/1, the refusal. master_register_settle/2, which reads a bare or unstated announcement as a fully named state. master_register_announce/3 and master_register_current/2, the write and the read against the real bus. master_register_downward_assignment/2 and master_register_assign_downward/3, the assignment and its application. And master_register_throw_rows/3, the assignment walk itself, exported so it can be PROVED against a fixture rather than passing vacuously over an empty table.

THE CHANNEL JUDGES SHAPE AND THE REGISTER JUDGES POLICY, AND THAT DIVISION IS DELIBERATE. neuromodulator_bus_broadcast_global_state/3 refuses a term that is not a widened state and refuses a pole the flip-flop does not have, because both are STRUCTURAL. It does NOT judge whether a sub-mode belongs to its pole, because that is a ROSTER, and a roster is policy belonging to the construct that owns it. This is exactly the division slice 42 drew when mode_register was deliberately not taught that a fault may not be a mode, and both halves of it are pinned by tests.

Gates at this slice: the new master_register pack ships TWENTY-EIGHT Prolog Unit (PLUnit) tests and no existing pack's count moves (SEVEN HUNDRED AND NINETY-SIX in total across THIRTY-FOUR packs, all green); the naming gate reports zero violations across all thirty-four packs and passes its own self-test; the layer, layer-binding and reality-discipline gates all report zero violations; the capstone story is byte-identical to main's. Nothing routes to either change order: a state name broadcast inside one runtime travels to nobody and fails the consumer-versus-ontology test of the Second Commandment, and no language feature was missing. Nothing releases the held sections of the causalontology change order. CONFORMANCE WAS RE-MEASURED THIS SESSION AND IS NO LONGER STALE: 137 of 137 in PrologAI at ebbc3e0 and 137 of 137 in causalontology's Python binding at 292dfca, both run rather than quoted.


KONNECTOME REALIZATION LOG - SLICE 47

Slice 47 - The first caller wired to a watchdog: the sleep-wake flip-flop becomes a register and gets watched - 2026-08-09 - pack two_process_governor (the flip-flop gains a register, a fault block and a watch); no new pack, no number invented in the running machine, and the capstone story byte-identical.

WHAT THE PHASE REQUIRED. A DEBT, NOT A QUEUE ITEM. Slice 45 built the first watchdog, wired it to nobody, and said so in three separate documents: nothing in the running machine kept a history, so the capability was proved in a suite and absent from the machine. It also declined to choose a WINDOW LENGTH - DECISION-4 - on the ground that the caller who owns a watchdog owns its numbers, and named the consequence: the first slice that joins a real construct to a real watchdog must choose a window and say why.

WHY THIS CONSTRUCT AND NOT THE GATE, WHICH IS THE FIRST THING THIS SLICE HAD TO DECIDE AND IS THE REASON IT COULD SUCCEED WHERE SLICE 45 COULD NOT. The watchdog's two boundaries came from the Layer 8 modes volume's Entry 36, the mutual-inhibition flip-flop, and konnectome has TWO constructs answering to that entry: the gate archetype, validated against it block by block at slice 40, and the two-process governor, which is the same motif installed at whole-organism scale and which the corpus catalogues separately as Layer 11's Entry 11, the sleep-wake flip-flop. THE GOVERNOR IS THE RIGHT FIRST CALLER AND THE GATE IS NOT, for one reason that decides it: THE GOVERNOR HAS A CYCLE OF ITS OWN AND THE GATE DOES NOT. A window is a duration and a duration is only meaningful against something; the gate archetype is generic, installed wherever a world wants one, with no timescale of its own - which is exactly why slice 45 could find no honest window for it and was right to refuse.

WHAT THIS SLICE SPECIFIES. Eleven new predicates on an existing pack and no new pack at all, in two halves.

THE FIRST HALF - THE FLIP-FLOP BECOMES A REGISTER, validated against Layer 11's Entry 11 block by block, in the discipline slice 40 established for the gate against Entry 36. Two poles in the corpus's three-field schema, carrying Entry 11's own coined names (The Toggle Up, The Toggle Down). Two per-pole transfer functions, which are Process S's two halves. A transition table of two crossings - and THE AGENCY IS THE ENTRY'S OWN SHARPEST SENTENCE MADE CHECKABLE: Entry 11 says the crossings are "thrown by Process S and Process C, NOT INTERNAL TO THE SWITCH", so NEITHER ROW carries the agency self_selected, and a test proves it over a table it first checks is non-empty. And a fault block that was never empty, because Entry 11 supplies one.

ONE DIVERGENCE FROM THE CORPUS ENTRY IS RECORDED RATHER THAN SMOOTHED OVER, AND IT COSTS A FAULT REGIME. Entry 11's boundary signature is "time spent in the contested mid-band the design exists to forbid". KONNECTOME'S FLIP-FLOP HAS NO MID-BAND TIME TO SPEND: its selection is a comparison returning one of two atoms in the same tick, so there is no third condition it could linger in and nothing to measure. That regime is NOT declared, deliberately, because a fault block listing a boundary nothing can ever cross would report it UNWATCHED forever and would teach every reader that this governor has an unwatched fault when it has no such regime at all.

THE SECOND HALF - DECISION-6, AND IT IS THE DELIVERABLE. THE WINDOW IS NOT A NUMBER; IT IS THE GOVERNOR'S OWN DAY, READ FROM ITS OWN PARAMETER BLOCK. Both allowances are derived from the same day. The full reasoning, and what the decision does NOT decide, are in the ledger's DECISION-6 note.

THE READINGS ARE RE-KEYED RATHER THAN RENAMED AT THE SOURCE, and the reason is a small structural claim worth stating. The watchdog measures FLIPS and TICKS HELD, which are construct-neutral quantities, and files them under the gate's signature names because the gate is what it was built against. This governor's boundaries are named differently, so its watch re-keys the readings onto its own names. WHAT A QUANTITY MEANS IS EACH CONSTRUCT'S OWN CLAIM; what it IS is the watchdog's. Renaming at the source would have made the watchdog know about its callers.

Gates at this slice: two_process_governor grows from twenty Prolog Unit (PLUnit) tests to thirty-five (the other thirty-three packs unchanged; EIGHT HUNDRED AND ELEVEN in total across THIRTY-FOUR packs, all green); the naming gate reports zero violations across all thirty-four packs and passes its own self-test; the layer, layer-binding and reality-discipline gates all report zero violations; the capstone story is byte-identical to main's. Nothing routes to either change order. Nothing releases the held sections of the causalontology change order. Neither cousin was edited; conformance was re-measured earlier this session and stands at 137 of 137 in both, with the honest qualification recorded in the ledger.


KONNECTOME REALIZATION LOG - SLICE 48

Slice 48 - The bus's six named broadcast channels: a roster that declares and does not guard - 2026-08-10 - pack neuromodulator_bus (a roster of six channels with their roles, sources and cognitive tags); no new pack, no existing call changed, no number invented, and the capstone story byte-identical.

WHAT THE PHASE REQUIRED. THE FIRST ITEM OF THE QUEUE'S NEW FRONT. The mode-register family closed at slice 46 and the vision-set analysis went to VERSION 3 on 2026-08-10, moving Family Four - the cognitive cluster - to the front of the whole queue and arguing the move rather than inheriting it. Family Four's first item is the smallest work in the queue and a dependency of two later items in two families: give the broadcast bus the corpus's own six channels by name.

WHAT THIS SLICE SPECIFIES. Six predicates on an existing pack, and no new pack. A ROSTER, neuromodulator_bus_channel/1, enumerable rather than merely checkable, carrying the six the Layer 11 global-states volume's Chapter 16 names in one sentence - the chapter that IS this pack, THE ASCENDING BROADCAST SYSTEM (The State Announcement Service). Three tables beside it, each keyed by channel and each the corpus's own: the ROLE, which is the Layer 10 systems volume's chapter subtitle for that channel (chapters 31 through 36); the SOURCE NUCLEUS, which is Chapter 16's own sentence; and the COGNITIVE TAG, which is the COG or NON-COG marking on those same chapter headings, carried through unaltered so that a later caller reads the corpus's judgment rather than konnectome's. A CHECK, neuromodulator_bus_check_channel/1, which refuses a name the roster does not carry and IS APPLIED TO NOTHING IN THIS FILE. And a RESOLUTION, neuromodulator_bus_channel_named/2, which maps the corpus's own word for a channel onto the name this machine already broadcasts under.

THE SPECIFICATION'S OWN CONSTRAINT, WHICH IS THE UNUSUAL PART. THIS SLICE PROMISES TO REFUSE NOTHING THAT WORKS TODAY, and that promise is what shapes every decision in it. It is why the check guards nothing, why the corpus's spelling resolves rather than aliases, and why no channel is given a default level. A declaration that quietly acquired the power to reject would not be the item the queue scheduled.

DECISION-7 IS THE DELIVERABLE, in two halves and a refusal: THE ROSTER DECLARES AND DOES NOT GUARD, because the modulator keyspace is a NAMESPACE and not a roster; THE CORPUS'S NAME RESOLVES ONTO THE NAME THE MACHINE ALREADY BROADCASTS, because this bus answers an unknown key with ZERO and a second key would make that silence permanent; and NO PER-CHANNEL DEFAULT LEVEL IS DECLARED, because the corpus's apparent defaults are initial values inside worked pseudocode and the corpus's own statement is that the blend, not any single chemical, names the mode. The full reasoning, and the list of what the decision does NOT decide, are in the ledger's DECISION-7 note.

Gates at this slice: neuromodulator_bus grows from thirty-six Prolog Unit (PLUnit) tests to FIFTY-ONE (the other thirty-three packs unchanged; EIGHT HUNDRED AND TWENTY-SIX in total across THIRTY-FOUR packs, all green); the naming gate reports zero violations across all thirty-four packs and passes its own self-test; the layer, layer-binding and reality-discipline gates all report zero violations; the capstone story is byte-identical to main's. Nothing routes to either change order: a roster of channel names read inside one runtime travels to nobody. Nothing releases the held sections of the causalontology change order. Neither cousin was edited; conformance stands where slice 47 re-measured it, at 137 of 137 in both, with the checks-versus-vectors qualification.


KONNECTOME REALIZATION LOG - SLICE 49

Slice 49 - The working-memory blackboard, built to the corpus's own constants - 2026-08-10 - NEW PACK working_memory_blackboard (gated admission, decaying slots, weakest eviction, caller-supplied rehearsal); DECISION-8, the activation grain; OBSERVATION-13, a chapter that disagrees with itself about time; nothing wired into the tick, and the capstone story byte-identical.

WHAT THE PHASE REQUIRED. THE SINGLE HIGHEST-VALUE ITEM IN THE QUEUE, by the vision-set analysis version 3's own words, and the first item of Family Four's remaining front now that slice 48 cleared the roster. The motif-and-workspace design authority's Part B1 states the reason plainly: working memory is not a pack in waiting, it is A PACK ALREADY WRITTEN DOWN TO ITS CONSTANTS, at LAYER_10_SYSTEMS_MANUSCRIPT.txt Chapter 16, THE WORKING MEMORY SYSTEM (Prefrontal-Parietal Persistent-Activity Circuit) (The Present-Thought Blackboard).

WHAT THIS SLICE SPECIFIES. One new pack and eighteen exported predicates. FOUR CONSTANTS, written as the corpus writes them: capacity four items, leakage five hundredths a step, collapse below two tenths, full activation one. THE BOARD ITSELF, a list of item-and-activation pairs held in admission order, made by working_memory_blackboard_new/1, advanced by working_memory_blackboard_step/3, written through working_memory_blackboard_admit/4, erased by working_memory_blackboard_wipe/2, and read by working_memory_blackboard_slots/2, working_memory_blackboard_occupancy/2 and working_memory_blackboard_activation/3. A GRAIN, working_memory_blackboard_parts_per_unit/1 with one conversion in each direction. And FOUR RESTATEMENTS of the chapter's TIME CONSTANTS field in ticks, each computed through the scheduler's single conversion rather than written as a tick literal.

NOT ONE NUMBER IN THE PACK IS KONNECTOME'S INVENTION, which is the constraint the queue entry set and the constraint every decision below serves. Every activation constant is the chapter's; every duration is the chapter's, restated through DECISION-2's conversion; and the one derived number in the pack - seventeen unrehearsed steps to collapse - is computed from the chapter's constants and is never declared.

THE SPECIFICATION'S OWN CONSTRAINT, AND IT IS AN UNUSUAL ONE. THIS SLICE FOUND THE CHAPTER DISAGREEING WITH ITSELF THREE TIMES, and specified konnectome's answer to each in the open rather than picking whichever statement it read last. The three are recorded as OBSERVATION-13 and each is resolved in the pack's own comments with its reason: the collapse boundary, where the chapter's prose and its printed code differ only because of binary floating point; the order of rehearsal and collapse within one step, where the prose and the code genuinely differ; and the STEP RATE, where the chapter's code sample and its own TIME CONSTANTS field imply fades that differ by a factor of between twelve and one hundred and eighteen. The third is not resolved at all: it is reported upward and refused.


KONNECTOME REALIZATION LOG - SLICE 50

Slice 50 - The working-memory mode register - 2026-08-10 - NEW PACK working_memory_mode_register; DECISION-9, a downward assignment may address a singleton by name; OBSERVATION-14, a transition table row that is not a transition.

WHAT THIS ADDS TO THE SPECIFICATION. The specified behaviour of the present-thought blackboard is now stated at two grains rather than one. Slice 49 specified what the surface DOES - four slots, gated admission, decay, collapse, rehearsal, eviction, wipe. This slice specifies what the surface CAN BE: four named modes from Layer 10 Entry 16, each with a posture stating what the contents are doing and what the gate is doing, a transition table naming which changes are the board's own and which are thrown at it from above, and two boundary signatures that are WATCHED and never admitted as modes.

AND IT ADDS A SPECIFIED REFUSAL. No waking global state may assign the board a mode. The corpus gives the waking transition the agency SELF_SELECTED, so a specification that let the global state choose between holding and updating would contradict the chapter it was transcribed from. The silence of the assignment table for online states is a specified behaviour and not an unfilled row.


KONNECTOME REALIZATION LOG - SLICE 51

Slice 51 - The told story that says whether the night was healthy - 2026-08-10 - the demonstration debt closed; no new pack.

WHAT THIS ADDS TO THE SPECIFICATION. The capstone's specified output now includes a verdict on the mind's own night, and the specification of that verdict is that IT MAY BE A REFUSAL. A telling shorter than the governor's own day is specified to report NOT-YET-MEASURED and is specified never to report CLEAN. That is DECISION-4 reaching the demonstration surface, where a reader can see it, rather than living only in the watchdog's own tests.


KONNECTOME REALIZATION LOG - SLICES 52 AND 53

Slices 52 and 53 - The scaler refuses rather than clips, and the durable tier is built as a process - 2026-08-10 - DECISION-10, DECISION-11, OBSERVATION-16; OBSERVATION-15 half-closed; NEW PACK renewal_loop.

WHAT THIS ADDS TO THE SPECIFICATION. Two specified refusals and one specified shape.

THE FIRST REFUSAL. A homeostatic scaling factor that is not strictly positive is refused aloud and never clipped. The specification formerly permitted the bound to silence a weight; it now forbids the bound to erase one, because a weight multiplied to zero can never be lifted off zero and the relative code the multiplicative form exists to protect is destroyed rather than preserved.

THE SPECIFIED SHAPE, AND IT REPLACES A LINE THE QUEUE HAD CARRIED FOR SEVERAL ASSESSMENTS. The durable weight tier is specified as A MAINTAINED PROCESS AND NOT A STORED NUMBER: a level that decays every step against a synthesis that replaces it, where the memory is the fact that the second currently beats the first. There is no second weight field and no consolidated flag.

AND THE SPECIFIED CONSEQUENCE. A durable tier of that shape must be maintained in BOTH shifts of the two-process governor. An offline shift that suspends the refresh does not preserve the durable tier - it erases it, permanently. That is now a specified property of any wiring slice rather than a discovery waiting to be made.
