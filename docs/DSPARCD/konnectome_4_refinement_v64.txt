APPENDIX 4. REFINEMENT

A4.0 THE PURPOSE OF THIS PHASE

The Refinement phase improves the design and the eventual code through iteration: repeated cycles of testing and revision.

Its signature practice is Test-Driven Development, abbreviated TDD, in which one writes a failing test first, then just enough code to make it pass, then cleans up, a cycle called Red-Green-Refactor.

SPARC favours the London School of TDD, which tests each unit in isolation using stand-in fakes for its collaborators, and emphasizes behaviour and interactions.

In SPARC this is the deliverable named refinement.md, together with the growing, tested codebase.

A4.1 THE TEST-DRIVEN LOOP, APPLIED TO A BRAIN

The System is unusually well suited to test-driven development, for a reason worth naming: its acceptance criteria (Appendix 1) are drawn from real neuroscience experiments, and a neuroscience experiment IS a test.

To test the System is to run, in software, the lesion and learning studies that are the evidential basis of the science.

Every test below is a famous experiment rewritten as an automated check.

The loop for each construct and each behaviour is: first write a test that states the expected result and fails because the code does not yet exist (Red); then write the smallest update rule or connection that makes the test pass (Green); then clean up the code without changing its behaviour (Refactor); then move to the next.

A4.2 THE UNIT TESTS: EACH ARCHETYPE IN ISOLATION

Following the London School, each archetype rule is tested in isolation, with its inputs supplied by fakes.

TEST OF THE RELAY: given a fixed input and a fixed gain, the next activation equals input times gain.

A pipe passes what it is given, scaled.

If this fails, the relay is not a relay.

TEST OF THE INTEGRATOR: given a constant input over many ticks, the activation rises and approaches a bound; given no input, it decays toward zero.

A bucket fills and leaks.

TEST OF THE OSCILLATOR: over many ticks with no input, the phase advances at the natural frequency and wraps, and the gain rises and falls once per cycle.

A metronome keeps time.

TEST OF THE ATTRACTOR: given a partial version of a stored pattern, the activation converges to the whole stored pattern; given a pattern near two stored ones, it converges to the nearer.

A memory completes from a fragment.

TEST OF THE GATE: given a sub-threshold drive, the mode does not change; given a supra-threshold drive, it flips and then resists flipping back.

A stiff switch.

TEST OF THE COMPARATOR: given equal expected and actual inputs, the output is zero; given an actual input above expectation, the output is positive; below, negative.

A scale reports the difference.

This is also, precisely, the test that the dopamine reward-prediction-error signal behaves correctly.

A4.3 THE INTEGRATION TESTS: CONSTRUCTS WORKING TOGETHER

Integration tests check that constructs wired together produce the right chain of behaviour.

TEST OF PROPAGATION: a signal presented at a sensory region appears at its cortical target after exactly the number of ticks dictated by the interface delays, and nowhere sooner.

This tests the connection graph and the scheduler together.

TEST OF THE DECISION CHAIN: a valued stimulus presented to the sensory regions leads, through the valuation regions and the action selector, to the selection of the appropriate action, in the correct order.

This tests the perception-to-action arc.

TEST OF THE COMPUTATIONAL INTERFACE: the inferior olive, given an intended movement and a differing outcome, emits an error signal that is neither of its inputs.

This tests that a computational interface computes rather than conveys, the Perfect Wire Test made into an assertion.

A4.4 THE SYSTEM TESTS: THE FAMOUS EXPERIMENTS

The system-level tests are the acceptance criteria of Appendix 1, and each reproduces a landmark result.

THE PARKINSON TEST (AC-4): deplete dopamine to a low fraction of normal; action selection slows and then halts; restore dopamine; it recovers.

This reproduces the cardinal signature of Parkinson's disease and the effect of dopamine-replacement therapy, in software, from first principles.

Passing it is strong evidence the System's action selection is faithful.

THE LEARNING TEST (AC-5): pair a stimulus with reward repeatedly; the connecting weights strengthen by the three-factor rule until the stimulus predicts the reward; then present the stimulus alone; the weights weaken and the prediction extinguishes.

This reproduces Pavlovian conditioning and extinction, the foundation of the psychology of learning.

THE H.M. TEST: disable the plasticity engine; the System continues to perceive, act, and use what it already knows, and forms no new memories.

This reproduces the amnesia of patient H.M., the case on which this whole book centres, and it is the test that proves the plasticity engine is the seat of new memory.

THE DRIVE-CONFLICT TEST (AC-6): raise two drives at once; the override hierarchy resolves them in the specified order; and no amount of deliberative input can suppress the respiration drive to self-termination.

This reproduces the vitality ranking of Chapter 12 and enforces the System's one inviolable safety property.

THE SOCIAL-PAIN TEST: deliver a social-rejection signal; the same circuitry that registers physical pain activates.

This reproduces one of the more striking findings of social neuroscience and tests that the social-connection drive is a genuinely defended set-point.

A4.4bis THE NETWORK, PATHWAY, AND GLIAL TESTS

Because the body names constructs above the single region, the test suite adds checks at that level.

THE NETWORK TEST: engage a task that recruits the dorsal attention and frontoparietal networks; the default mode network's mean activation must fall as theirs rises, reproducing the task-positive-versus-task-negative anticorrelation of Section 2.10 (Layer 10, chapters 21 through 30). A network that does not deactivate on task has not been coupled correctly.

THE PATHWAY TEST: drive the direct pathway and observe an action released (go); drive the indirect pathway and observe it suppressed (no-go); drive the hyperdirect pathway and observe a fast global cancellation (stop). Then run the Papez circuit and confirm a memory trace injected at the hippocampus circulates through the mammillary bodies, anterior thalamus, and cingulate and returns, and that cutting any node breaks the loop, reproducing diencephalic amnesia.

THE GLIAL TEST: disable the astrocytic gating of the tripartite synapse and confirm that plasticity is impaired even though every neuronal construct is intact, reproducing the finding that the glia gate learning. This is the tripartite-synapse counterpart of the H.M. test: memory fails not because a region is lost but because its plasticity partner is silenced.

A4.5 OPTIMIZATION AND THE HARDENING LOOP

Once the tests pass, Refinement optimizes.

The candidate optimizations, in order of value, are: caching the neuromodulatory bridge closures so the bus is not recomputed needlessly; vectorizing the construct update so a tick over one hundred constructs is one batched operation rather than one hundred calls; and profiling to find the few constructs whose rules dominate the tick cost and simplifying them.

None of these may change the System's behaviour: an optimization that changes a test result is a bug, by definition.

The hardening loop then runs the System for long durations and looks for the failure modes the science predicts and warns against: runaway excitation (a seizure, which the homeostatic scaling must prevent), weight explosion (which the scaling must also bound), and a frozen action selector (which indicates a stuck gate).

Each discovered failure becomes a new test, so that the System can never regress into it again.

A4.6 THE KNOWN TRADE-OFFS

Refinement is where trade-offs are made explicit, and honesty requires stating them.

THE GRAIN TRADE-OFF: the regional grain buys tractability and inspectability at the cost of fidelity.

A finer grain would be more faithful and far more expensive.

The System accepts the coarser grain for version one and provides the path to refine it.

This is the largest trade-off in the work.

THE ARCHETYPE TRADE-OFF: six archetypes buy simplicity and uniformity at the cost of some accuracy for constructs that fit no archetype cleanly.

The System accepts a small loss of accuracy for a large gain in buildability, and extends the archetype set only when a construct genuinely resists all six.

THE DETERMINISM TRADE-OFF: strict reproducibility (NFR-2) buys scientific rigour at the cost of the biological realism of noise, which the brain uses.

The System resolves this by making randomness seeded and recorded, so that it has controlled noise that is also exactly replayable, keeping both rigour and realism.

THE SPEED-VERSUS-CLARITY TRADE-OFF: the two-pass synchronous update is slower than an in-place update would be, but it is correct where the faster version is subtly wrong.

The System pays the speed to keep the correctness, and this is not negotiable.

A4.7 REFLECTION

The Refinement phase reveals a property of this System that few software projects enjoy: its tests are not invented by its builders but inherited from a century of neuroscience.

The Parkinson test, the learning test, the H.M. test, the drive-conflict test, and the social-pain test are not checks a programmer dreamed up to exercise the code; they are the actual experiments that established the science the code implements.

This gives the System an unusually strong form of validation.

It is correct not merely when it does what its author intended, but when it reproduces what nature does, as measured by experiments its author did not run.

A model that can be given Parkinson's disease by depleting a single simulated molecule, and cured by restoring it, has passed a test no amount of ordinary software testing could design.



KONNECTOME REALIZATION LOG

Slice 1 - the heartbeat - 2026-07-20 - pack tick_engine, version 0.1.0.

The scheduler test of Sections A2.9 and A4.3 is realized as the PLUnit suite test_tick_engine.pl: a run of N ticks records exactly N numbered snapshots, the two passes never interleave, no construct reads a neighbour's future within a tick, and the run is reproducible.

Gates at this slice: 7 of 7 tick_engine Prolog Unit (PLUnit) tests pass; the naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited; konnectome reuses them through the library path.

This log grows one entry per code slice, per the Tenth Commandment. The full DSPARCD Fileset (the SPARCD Fileset until the Constitution's version 8) was bumped in lockstep from version 1 to version 2, and the version 1 files were archived under docs/archive/ in the same change, per the Ninth Commandment.


KONNECTOME REALIZATION LOG - SLICE 2

Slice 2 - the six archetype rules and the first Causalontology thought - 2026-07-20 - packs archetype (0.1.0) and observer (0.1.0).

The six archetype isolation tests of Section A4.2 are realized as the archetype PLUnit suite (relay scales, integrator fills and leaks, oscillator keeps time, attractor completes from a fragment, gate resists flipping, comparator reports the difference), plus a dispatch-routing test; the observer suite verifies the token_occurrence is valid, content-addressed, reproducible, and distinct per tick.

Gates at this slice: archetype 7 of 7 and observer 6 of 6 Prolog Unit (PLUnit) tests pass; naming, layer, and layer-binding gates report zero violations; the Causalontology 119-vector conformance suite passes 119 of 119. Nothing in PrologAI or Causalontology was edited; konnectome reuses causal_core through the library path.


KONNECTOME REALIZATION LOG - SLICE 3

Slice 3 - the connection graph - 2026-07-20 - pack connection_graph (0.1.0).

The propagation integration test of Section A4.3 is realized: a signal at a source reaches its target after exactly the number of one-tick hops, and nowhere sooner.

Gates at this slice: connection_graph 4 of 4 Prolog Unit (PLUnit) tests pass (tick_engine 7, archetype 7, observer 6 unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited; connection_graph reuses the archetype pack.


KONNECTOME REALIZATION LOG - SLICE 4

Slice 4 - the reward loop - 2026-07-20 - packs neuromodulator_bus (0.1.0) and drive_system (0.1.0).

The drive-loop tests confirm: the first tick yields no spurious reward, the reward equals the total error reduction and is broadcast as dopamine, moving away from a set-point gives a negative reward, and several drives sum their reductions.

Gates at this slice: neuromodulator_bus 4 of 4 and drive_system 5 of 5 Prolog Unit (PLUnit) tests pass (the earlier four packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 5

Slice 5 - the action selector - 2026-07-20 - pack action_selector (0.1.0).

The action-selection tests confirm: the strongest candidate is released, exactly one is released, the output is always one of the inputs (never invented), ties break deterministically, and a pressing drive biases its action to win.

Gates at this slice: action_selector 6 of 6 Prolog Unit (PLUnit) tests pass (the earlier six packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 6

Slice 6 - the plasticity engine - 2026-07-20 - pack plasticity_engine (0.1.0).

The learning tests confirm the three-factor rule: coincident activity plus dopamine strengthens a weight, no dopamine means no learning, an inactive end means no learning, and a computational interface does not learn.

Gates at this slice: plasticity_engine 5 of 5 Prolog Unit (PLUnit) tests pass (the earlier seven packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 7

Slice 7 - completing the architecture - 2026-07-20 - packs override_controller (0.1.0) and perturbation_interface (0.1.0).

The drive-conflict test of Section A4.4 is realized: the override hierarchy resolves competing drives by vitality, and the respiration drive cannot be suppressed to self-termination; a Parkinson-flavoured trial shows that depleting dopamine halts learning.

Gates at this slice: override_controller 4 of 4 and perturbation_interface 4 of 4 Prolog Unit (PLUnit) tests pass (the earlier eight packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 8

Slice 8 - one running tick - 2026-07-20 - pack cognitive_cycle (0.1.0).

The integration test confirms the whole loop: the reward becomes dopamine and drives learning, a vital drive overrides the normal action, each tick records a distinct thought, and the run is reproducible.

Gates at this slice: cognitive_cycle 4 of 4 Prolog Unit (PLUnit) tests pass (the earlier ten packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 9

Slice 9 - hardening the loop - 2026-07-20 - packs connection_graph (0.2.0), drive_system (0.2.0), cognitive_cycle (0.2.0).

The hardened integration test shows a closed sensorimotor loop: the body converges to its set-point driven by the released action, the reward flows and the connection learns while the body improves, a vital drive still overrides, and the run is reproducible.

Gates at this slice: connection_graph 6 of 6, drive_system 9 of 9, and cognitive_cycle 4 of 4 Prolog Unit (PLUnit) tests pass (the other eight packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 10

Slice 10 - thought combination - 2026-07-20 - pack thought_combination (0.1.0).

The thought-combination tests confirm: an atomic thought is a content-addressed occurrent, a combination mints a CRO over its causes and effect, it is reproducible, causes and effects must be occurrents, an empty cause set is rejected, a modality changes the identity, and effect-to-cause links form a chain of thought.

Gates at this slice: thought_combination 7 of 7 Prolog Unit (PLUnit) tests pass (the earlier eleven packs unchanged); naming, layer, and layer-binding gates report zero violations; the Causalontology 119-vector conformance passes 119 of 119. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 11

Slice 11 - other minds and prediction, the wall-finding build - 2026-07-22 - packs other_minds (0.1.0) and prediction_loop (0.1.0).

The slice's tests are wall-demonstration tests: they assert, by building, precisely what the substrate cannot do. The other_minds suite (8 tests) holds Sally's false belief and konnectome's nested model of it, then asserts the closed kind list has exactly eighteen kinds and no doxastic kind, that causal_core_identify fails on an attitude-shaped record, and that an assertion with a holder field and one without content-address to the identical identifier. The prediction_loop suite (10 tests) runs the object-permanence trial to a signed error, then asserts neither predicted_occurrence nor prediction_error is a kind, that a forecast and a report with the same content share one identifier, and that a predicted marker attached to a token_occurrence leaves that identifier unchanged. A test that passes by demonstrating an impossibility is still a passing test, and it is how this build turns a wall into a recorded finding rather than a guess.

Gates at this slice: other_minds 8 of 8 and prediction_loop 10 of 10 Prolog Unit (PLUnit) tests pass (the earlier twelve packs unchanged, 69 tests); naming, layer, and layer-binding gates report zero violations; the Causalontology 119-vector conformance suite is untouched and green. Two walls were hit, recorded as Wall-1 and Wall-2, and ROUTED to the Causalontology change order (Sections B and A). Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 12

Slice 12 - the closure: minting the new kinds - 2026-07-22 - packs other_minds (0.2.0) and prediction_loop (0.2.0).

The slice's discipline turns slice 11's wall-DEMONSTRATION tests into wall-CLOSURE tests: where they asserted, by building, precisely what the substrate could NOT do, they now assert what it CAN. The other_minds suite (10 tests) no longer asserts an eighteen-kind closed list with no doxastic kind; it asserts the closed list now has exactly twenty-one kinds with attitude among them (and predicted_occurrence and prediction_error present), mints a TRUE belief as an attitude record - holder the agent's token_individual identifier, attitude_type believes, content the believed state_assertion identifier, identifier prefixed attitude - with a deterministic re-mint and an infer-kind round trip; mints the FALSE belief while the world's own state_assertion says otherwise and shows both records coexisting with distinct identifiers and no conflict (Rule 25, doxastic quarantine); mints the NESTED attribution as two attitudes whose outer content is the inner attitude's identifier, their holders wired to konnectome and Sally; and exports the false-belief trial's predicted-search result as a real, shareable attitude record while the runtime belief survives. The tests that remained TRUE are kept unchanged: an assertion still names only the signer, because the holder now lives in the attitude kind - permanent design, not a wall. The prediction_loop suite (16 tests) likewise now mints the predicted_occurrence before arrival, the actual token_occurrence, and the prediction_error carrying the signed discrepancy, rather than asserting their absence. A test that once passed by demonstrating an impossibility now passes by demonstrating the possibility the change orders delivered - the same test-first discipline, on the far side of the wall. The closure's own library wall, Wall-3 (causal_core could not yet mint the new kinds), was routed to the PrologAI change order and closed the same day by causal_core 1.1.0.

Gates at this slice: other_minds 10 of 10 and prediction_loop 16 of 16 Prolog Unit (PLUnit) tests pass (the earlier twelve packs unchanged); naming, layer, and layer-binding gates report zero violations; the Causalontology conformance suite is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. Wall-1, Wall-2, and Wall-3 are CLOSED. Causalontology and PrologAI were extended through their own gated change orders, never edited directly.


KONNECTOME REALIZATION LOG - SLICE 13

Slice 13 - the provenance layer: the mind grades and, when wrong, disowns its own thoughts - 2026-07-26 - pack self_provenance (0.1.0).

The slice was built test-first, in the Red-Green-Refactor loop this appendix prescribes, and its seventeen tests are each a small experiment in self-assessment. The grades test pins the standard's ordered scale: intervention outranks observation, which outranks simulation, which outranks derivation, which outranks a human hint, which outranks an import - and a word that is not one of the six carries no rank at all. The source-identity test proves konnectome's own signing identity is a well-formed Ed25519-shaped key and is deterministic, carrying no secret. Then the load-bearing family, asserting about the mind's own minted records: an assertion ABOUT an attitude distinguishes the modelled HOLDER from the signing SOURCE, the Rule 25 distinction the Causalontology conformance suite pins as vector one hundred thirty-five, here demonstrated over konnectome's own false-belief attitude; an assertion about a forecast is graded 'simulation', because a model-based forecast is model-based evidence; and an assertion about a prediction_error cites the actual observed token_occurrence as its evidence. The self-correction family follows: when the object-permanence screen lifts on nothing - the signed error a minus one - the mind RETRACTS its own forecast assertion, the honest exit, and the retraction is signed by the same source that made the assertion; when the world instead confirms the forecast, the stance is SUPERSEDED from simulation to the stronger observation grade, the old assertion retracted and a new one restated about the same content. A foreign source's assertion cannot be retracted here (Rule 10), and that refusal is tested. The malformed-input guards - an unknown grade, an out-of-range confidence, a malformed subject, a subject of a scheme the standard forbids, and a non-token evidence citation - are red-then-green tested as hard refusals. One final test mechanically documents the honest non-closure: an assertion is content-addressed and reproducible WITHOUT a signature, so a graded thought is shareable data even before the deployment key-holder signs it.

Gates at this slice: self_provenance 17 of 17 Prolog Unit (PLUnit) tests pass (the earlier fourteen packs unchanged, 95 tests; 112 in total); naming, layer, and layer-binding gates report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; the slice-12 honest non-closure is CLOSED; the deferred signature is recorded as an honest non-closure. Nothing in PrologAI or Causalontology was edited; self_provenance reuses causal_core through the library path.


KONNECTOME REALIZATION LOG - SLICE 14

Slice 14 - the capstone: the mind runs, and its own records tell the story - 2026-07-26 - pack capstone_demonstration (0.1.0), and the first runnable entry point, bin/run_capstone_demonstration.sh.

The slice was built test-first, and its nineteen tests divide into five families, one of which deserves this appendix's particular attention. The narration family checks the story's form: a non-empty list of text lines, exactly one line per lived tick, the reward and dopamine named, the regulated variable and its set-point named. The determinism family tells the story twice and requires the two tellings identical - the strongest reproducibility check the substrate offers, since one differing identifier would fail it. The milestone family checks coverage: the confirmed forecast and its supersession, the surprise and its retraction, the chain of combined thoughts, the false belief with both the false place and the true place present, the holder-versus-source identities of Rule 25, and the honest limits named at the close. The guard family red-then-green tests the two hard refusals of a bad tick count. But the family this appendix cares most about is the INDEPENDENT DYNAMICS pair: the tests boot the same world the story boots and run the same mind OUTSIDE the narration, then assert directly that the body reached its set-point and the weight rose above its boot value - so the story cannot grade its own homework. The same discipline lives inside the narration itself, which checks the dynamics before claiming them: a story that could print a comforting line about a body that never settled would be an optimization that changes a test result, and this appendix calls that a bug by definition. One test closes the reproducibility loop from the outside: it re-mints the story's attitude record independently and requires the story to carry that very identifier.

Gates at this slice: capstone_demonstration 20 of 20 Prolog Unit (PLUnit) tests pass (the earlier fifteen packs unchanged, 112 tests; 132 in total); naming, layer, and layer-binding gates report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins, re-measured this slice. No new wall was hit and no new honest non-closure was recorded; the slice-13 deployment-signature non-closure stands, and the story itself narrates it. Nothing in PrologAI or Causalontology was edited; the capstone reads its siblings through the library path.


KONNECTOME REALIZATION LOG - SLICE 15

Slice 15 - Rung Two begins: words grounded in lived experience, and pretend under quarantine - 2026-07-26 - packs symbol_exchange (0.1.0) and pretend_play (0.1.0).

The slice was built test-first, and its thirty tests divide by what they defend. The grounding family defends the rung's defining phrase: a word grounds in a lived condition (green), refuses a condition the mind never lived (red-then-green, the load-bearing refusal), refuses a condition outside the closed vocabulary, and builds the standard vocabulary only from a genuinely lived run of the whole mind. The exchange family defends both directions of symbol use: production finds the word for the condition, recognition hears the word through the reused word bank and finds its condition, and each direction refuses what was never grounded. The pilot family defends the rung's one discriminator as an executable fact: the same heard words draw different replies from a mind in deficit and a mind at its set-point, and the reply is deterministic. The quarantine family defends pretend's honesty: the rollout is sealed into the imagined reality, the observed record is never written by pretending, promotion into expectation works as a deliberate act, promotion into the observed record throws (red-then-green - the konnectome rule), promotion of a never-pretended fact fails rather than inventing it, and reset clears the pretence. The record family defends the export: the pretend thought is content-addressed, reproducible outside the pack byte for byte, and the stance on it is graded simulation and is about the thought itself. One refinement note in this appendix's own spirit: the first run surfaced PLUnit choice-point warnings - the reused language procedure and the new walk both left open choice points - and they were closed (once and a base-case cut) rather than tolerated, because a nondeterministic narration is a latent bug even while every assertion passes.

Gates at this slice: symbol_exchange 18 of 18 and pretend_play 12 of 12 Prolog Unit (PLUnit) tests pass (the earlier sixteen packs unchanged, 132 tests; 162 in total); naming, layer, and layer-binding gates report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; the Section C watch was kept and its result is recorded in the ledger: the per-claim machinery sufficed, no world-level sharing need arose, and Section C remains held. Nothing in PrologAI or Causalontology was edited; the rung reuses the language and imagination packs through the library path, exactly as other_minds reuses theory_of_mind.


KONNECTOME REALIZATION LOG - SLICE 16

Slice 16 - Rung Four deepens: the social-pain test and the empathy trial - 2026-07-26 - pack empathy (0.1.0).

The slice was built test-first, and its fourteen tests are the famous experiment and its consequences, re-run as a suite. The social-pain family is the experiment itself: rejection thwarts the defended set-point (the error rises from zero to one through drive_system_error, unmodified); the equivalence test computes a physical thwarting and a social severance of equal magnitude through the SAME predicate and asserts the two numbers equal - the shared-circuitry claim as an executable assertion; and the affect tests pin the signed consequences, reward minus one and dopamine minus one on rejection, plus one on restoration. The safety family re-proves the inviolable property under social pressure: respiration in distress seizes control from the helping action. The empathy family is the trial: the guarded distress model (a level outside zero to one refused by name), resonance moving the mind's own belonging (floored at zero), and the load-bearing pair - without modelled distress the pressing physical need wins the selection; with it, the resonated social pain outweighs the physical need and the HELPING action is released, applied, and shown restoring belonging to its set-point - plus the never-invented check that the released help is one of the drives' own proposals. The record family mints the distress as a deterministic, content-addressed attitude (fears), grades the attribution at observation, and retracts it when misattributed, Rule 10 kept. One refinement note: the guard style the slice-15 review demanded - named refusals for a missing body variable and a wordy value - was written into the resonance procedure from the first draft; a review's lesson is only learned when the next slice no longer needs it.

Gates at this slice: empathy 17 of 17, drive_system 10 of 10, and symbol_exchange 19 of 19 Prolog Unit (PLUnit) tests pass (the other sixteen packs unchanged; 181 in total), the counts carrying two review-born fixes and one paid debt: drive_system (0.3.0) now caps the homeostatic step at its set-point, so a fractional distance settles exactly and help can never overshoot into harm - pinned by its own regression test; naming, layer, and layer-binding gates report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; the remaining ascent of the rung (the appraisal and regulation pilots, and the capstone joining the new milestones into its telling) is recorded as pending, not hidden. Nothing in PrologAI or Causalontology was edited; empathy reads its siblings through the library path.


KONNECTOME REALIZATION LOG - SLICE 17

Slice 17 - the reality-discipline gate: the rule made checkable - 2026-07-26 - bin/check_reality_discipline.sh (no pack version moved).

The red-green discipline this appendix prescribes for code was applied, this slice, to a GATE - because a gate that has never been seen red is as untested as a test that has never failed. Before the new reality-discipline gate's first clean run was accepted, a deliberate violation was injected into another pack's source - one line calling the reused crossing predicate - and the gate was required to catch it, name the offending file and line, and exit non-zero; the violation was then removed and the gate required to pass. One boundary was tested with equal care in the opposite direction: a plain-English comment line that merely DISCUSSES an imagination predicate is excused, because English Readable Code demands that source files talk about what they must not do, and a gate that punished honest prose would teach the code to stop explaining itself. The suite's test count does not move this slice; what moved is the number of invariants the build proves on every merge.

Gates at this slice: no pack changed and no test count moved - the suite stands at one hundred eighty-one Prolog Unit (PLUnit) tests across nineteen packs, all green. The naming, layer, and layer-binding gates report zero violations, and they are joined by a fourth: the new reality-discipline gate (bin/check_reality_discipline.sh) reports zero violations, and was proven able to fail - a deliberate violation was injected, caught, and removed before its first clean run was accepted. The Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited - the imagination pack remains exactly as reused, and the discipline that fences it is konnectome's own.


KONNECTOME REALIZATION LOG - SLICE 18

Slice 18 - the capstone speaks and feels: Rung Two's first words and Rung Four's deepening join the one runnable story - 2026-07-26 - pack capstone_demonstration (0.2.0).

The test-first discipline this appendix prescribes is exactly how the two new chapters were earned and exactly why they can be believed. The capstone_demonstration pack's suite grows from twenty tests to THIRTY-SIX, and every one of the sixteen new tests is an INDEPENDENT dynamics check rather than a reading of the story's own output: for each new scene, the test re-runs the same sibling packs the narration runs - grounding the same words in the same lived run, severing the same belonging, modelling the same distress - OUTSIDE the narration, and re-mints every identifier from scratch, then asserts that the numbers the story will print are the numbers the machinery independently produces. This is the property that keeps the story from grading its own homework: the mind spoke warm and settled, felt belonging fall, and released help not because the storyteller says so but because a separate harness, holding instruments the storyteller does not, measured it doing so. The scenes were added red-green - a scene claiming a dynamic the run had not yet achieved would have failed its check before it could ever be printed - so the narration remains structurally unable to flatter the mind. The exact ledger of the slice: thirty-six of thirty-six in the capstone pack, the other eighteen packs unmoved at one hundred sixty-one tests, one hundred ninety-seven across nineteen packs in total, all green, with the Causalontology conformance suite re-measured rather than merely inherited and standing at one hundred thirty-seven of one hundred thirty-seven in both cousins.

Gates at this slice: capstone_demonstration thirty-six of thirty-six Prolog Unit (PLUnit) tests pass (the other eighteen packs unchanged, one hundred sixty-one tests; one hundred ninety-seven in total across nineteen packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited - the capstone reads its siblings through the library path.


KONNECTOME REALIZATION LOG - SLICE 19

Slice 19 - Rung Four finished: the appraisal and regulation pilots - 2026-07-26 - packs situation_appraisal (0.1.0) and arousal_regulation (0.1.0).

The evidence is test-first and counted exactly. situation_appraisal ships twelve Prolog Unit (PLUnit) tests that pin the map from error to valence in both directions - a violated set-point read as a threat the mind fears, a met or improving one read as good and desired - together with the ownership of the appraisal as an attitude inside the standard's closed enumeration, its grading through self_provenance at observation, and its retraction under Rule 10 when a later reading corrects the world. arousal_regulation ships twelve Prolog Unit (PLUnit) tests that fix the halving step: a fixed half of the remaining distance closed on each tick, the monotone approach toward the tonic baseline, the exact settle at baseline within a hair of it, and the guarantee that the level never overshoots. The earlier nineteen packs are unchanged, so the repository now stands at two hundred twenty-one tests across twenty-one packs, all green; the four standing gates - naming, layer, layer-binding, and reality-discipline - report zero violations; and the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins.

Gates at this slice: situation_appraisal twelve of twelve and arousal_regulation twelve of twelve Prolog Unit (PLUnit) tests pass (the earlier nineteen packs unchanged; two hundred twenty-one in total across twenty-one packs, all green); the four standing gates - naming, layer, layer-binding, and reality-discipline - report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins; no new wall was hit; and no cousin was edited.


KONNECTOME REALIZATION LOG - SLICE 20

Slice 20 - the pilots' review debts paid: appraisal's rounding boundary and regulation's finiteness - 2026-07-26 - packs situation_appraisal (0.1.1) and arousal_regulation (0.1.1).

Slice 20 pays the review debt slice 19 left on the record. The independent adversarial review of the appraisal and regulation pilots, run after they merged, confirmed three findings, and each is now fixed at its root and pinned by a permanent test. First, the appraisal pilot's change reader compared a difference of two independently-rounded set-point errors against exact zero, so two situations equidistant on opposite sides of a set-point - a mathematically neutral change - were read as a good or bad turn on a rounding residue near one part in ten thousand trillion, and, worse, minted as an owned attitude the neutral case is meant to refuse; a rounding-noise band now absorbs that residue, so an equidistant float change reads neutral and is correctly refused before it can become a false record. Second, the regulation pilot accepted a non-finite arousal level, and an infinite level never shrinks toward baseline, so the return would never terminate; the single gatekeeper now requires a finite level and refuses infinity by name. Third, the regulation pilot's whole-number tick rule had no test for its fractional-count refusal; it has one now. No demonstrated behaviour changed - every earlier claim still holds number for number, and every prior test still passes - and the two pilot packs move to patch version 0.1.1. The lesson the method keeps: a finding confirmed by an adversarial review is fixed at its root and pinned, even when the defect is latent - here reachable only by a fractional body value or a hand-built infinity that no ordinary run produces.

Gates at this slice: situation_appraisal fourteen of fourteen and arousal_regulation fourteen of fourteen Prolog Unit (PLUnit) tests pass (the four new tests pin the three confirmed findings; the other nineteen packs unchanged; two hundred twenty-five in total across twenty-one packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 21

Slice 21 - Rung Three opened: the concrete-reasoning trials - 2026-07-26 - packs concrete_operations (0.1.0) and cause_and_effect (0.1.0).

THE TESTS CAME FIRST, AND THEN THE REVIEW EARNED ITS KEEP. Both packs were written test-first: fifty-one tests in total, twenty-six over concrete_operations and twenty-five over cause_and_effect, each refusal pinned by a test that watches it fire. The packs were then handed to an independent adversarial review instructed to REFUTE the work, across four lenses - the logic pack's correctness, the causal pack's correctness, house-rule compliance, and fidelity to the guiding book - with every finding it raised then verified by a separate agent instructed to reproduce or refute the finding itself. Fourteen findings survived that verification; two were refuted and dropped. This is the largest confirmed-finding count of any slice so far, and every one of them is now fixed at its root and pinned.

THE FINDING THAT MATTERED MOST. The classifier grouped objects by unification rather than equality, and nothing required a property value to be fully instantiated. So an object whose colour was an unbound variable would join EVERY group, and the sorting would silently return a wrong answer - the one failure mode the pack's refuse-by-name discipline exists to prevent. The fix is two-sided: an unbound or half-built value is now refused by name, and group membership is decided by strict equality, so no value can lend itself to a group it does not exactly match.

THE FINDING THAT WAS A SPEED DEFECT, NOT A WRONG ANSWER. The support-cycle check restarted a fresh downward walk from the top of every edge, remembering only the current path - so on a lattice where many roads run down through shared supports, the walking doubled with each level. Measured: sixty-eight support facts took eleven seconds, and ninety would never have returned. The check was sound; it was simply unusable on valid input of modest size. It now carries a shared set of objects already proven cycle-free, so each object is walked once rather than once per road, and a forty-diamond lattice that would have hung the old walk now returns in milliseconds - pinned by a test that builds exactly that lattice.

THE FINDINGS ABOUT HONEST NAMES. A negative addition was refused, but under the wrong name: it was reported as an UNKNOWN transformation, when addition is perfectly well known and only its argument was malformed. A caller catching that error to mean "I do not recognise this transformation" would have been misled by a true refusal with a false reason. Malformed additions and removals now carry their own name. In the same family: several public predicates failed silently or leaked a raw system error where the pack's own discipline promised a named refusal - a collection that was not a list, an unbound start state that let the planner invent a start of its own and answer a question nobody asked, a plan step whose justification slots held garbage. Each now refuses by name, and each refusal has a test.

THE FINDING THAT CLOSED THE PACK'S CENTRAL CLAIM. The causal pack claimed causation is answered BY the minted record - but it checked only that a record was a dictionary with the right keys. A hand-written look-alike, never minted, was therefore accepted as proof, and its invented identifier was returned as the justification. The check now re-derives every offered record's content address from its own content and refuses a forgery by name. The claim and the check now say the same thing.

THE FINDINGS ABOUT OVERREACH. Two findings were not defects in the code at all, but in what the code CLAIMED: the pack header said it "realizes" classification and conservation, where the guiding book asks for more than what was built. Both headers were rewritten to size the claim honestly - classification opened in part, conservation opened schematically - and the same honest sizing is carried into the ledger and the tutorial rather than left in the code alone.

Gates at this slice: concrete_operations twenty-six of twenty-six and cause_and_effect twenty-five of twenty-five Prolog Unit (PLUnit) tests pass (the earlier twenty-one packs unchanged; two hundred seventy-six in total across twenty-three packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 22

Slice 22 - Rung Three finished in its trials: the two deferred halves built - 2026-08-07 - packs category_formation (0.1.0) and quantity_model (0.1.0).

THE TESTS CAME FIRST, AND THE REVIEW WAS TOLD TO REFUTE. Both packs were written test-first - forty tests before review - and then handed to an independent adversarial review instructed to REFUTE the slice across five lenses: logic correctness, forgeability of claims, house rules, fidelity to the guiding book, and test coverage. The review reproduced every finding it kept against the running code rather than arguing from a reading, and returned sixteen findings - two blocking, ten should-fix, four nits. Every confirmed finding is fixed at its root and pinned by a permanent test; the suites closed at fifty-five tests across the two packs.

THE BLOCKING FINDING THAT WAS A SILENTLY WRONG ANSWER. The sorter matches an object against a category by an ordered-set subset test, but nothing required a hand-built category's defining core to BE an ordered set - so a core written in the wrong order matched nothing, and an object genuinely carrying every core pair was silently exiled to outside-every-category. A wrong answer, not a refusal, in a pack whose whole discipline is refuse-by-name. The checker now requires a core to be exactly the duplicate-free sorted set the learner mints, and an unsorted core is refused by name.

THE BLOCKING FINDING THE AUTOMATED GATE MISSED. A helper predicate shipped without its pack's whole-word prefix, built on an abbreviation stem - and bin/check_pack_naming.sh waved it through, because its minority-cluster check only hunts terse and retired prefixes. The helper was renamed on the spot; the gate hole is recorded in the ledger note so it is hunted deliberately in review until the gate itself learns to see it.

THE FORGERY FAMILY, CLOSED AS A CLASS. Slice 21's review taught that a claim answered BY a record is empty if a look-alike passes; this slice's review found the same class five more times and every door is now shut by name: a hand-built category with an EMPTY defining core swallowed every object; a category wearing the reserved outside-every-category name doubled the sentinel group; two categories under one identifier double-counted their members; one experience carrying two values under one property key let the trial's two halves contradict each other on the same collection; and a hand-built model with one unit name doubled was measured as two units. Alongside these, the record predicates themselves were the largest door: both would own ANY quoted text as if enacted. Both now take no text at all - the category record enacts the learning episode and the conservation record enacts the trial, minting only what the enactment produced.

THE HONEST-NAME FINDINGS. An unbound transformation unified into the pour clause and was refused as a malformed appearance - a true refusal with a false reason; it is now refused as unknown before dispatch. A minus-one spill - which would GAIN a unit - was refused under the name nothing-was-lost; it is now refused as a bad amount. And the comparison predicate's contract is now stated plainly at its export: it compares two models by measurement alone and does not know what happened between them - the trial and the record are the predicates that tie a verdict to an enacted transformation, and a test pins that boundary so no reader mistakes it.

THE WORDING DEBTS. Nine English-Readable-Code lapses - code lines sharing a comment with a neighbour - were given their own comments. The facade claim that a learning episode leaves "no hidden state behind" was re-scoped to what is true: the episode owns the engine's store and clears it at both ends, and a bystander's items do not survive. The re-sized concrete_operations header gained its missing redirect at the schematic verdict's own export and predicate, and its manifest title now names both slices.

TWO REVIEW JUDGMENTS RECORDED, NOT FIXED. The review noted that the caller-supplied shared-pair floor tunes the granularity of the induced categories - the mind forms its categories, but how coarse they are is handed to it - and that the represented model's appearance field is written and never read, so no code path even attempts to estimate quantity from looks: the lossy pour is the trial's proof that the verdict sides with the world against the name, meeting the book's letter while the deeper being-fooled-by-looks temptation never arises in this code. Both judgments are carried in the ledger's slice note as honest sizing, not as defects.

Gates at this slice: category_formation twenty-six of twenty-six and quantity_model twenty-nine of twenty-nine Prolog Unit (PLUnit) tests pass, and concrete_operations' twenty-six stay green under its re-sized header (the earlier packs otherwise unchanged; three hundred thirty-one in total across twenty-five packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 23

Slice 23 - The story learns its owed chapters: Rung Three told, and Rung Four finished in the telling - 2026-08-07 - pack capstone_demonstration (0.3.0).

THE TESTS GREW WITH THE STORY. The capstone's suite grew from thirty-six to forty-four: the rung-order test now places Rung Three between Rung Two and Rung Four and the finish between the deepening and the provenance layer; the take-back test was deliberately raised from three distinct retractions to four when the appraisal chapter's honest exit joined the telling, with the raise recorded in the test's own comment as at every raise since slice 14; new narration tests pin the reasoning and finishing chapters' load-bearing fragments (the named steps, the formed categories, the honest exile, conserved at five, changed five to four, the fears attitude, the missing word for satisfaction, the exact landing at baseline); new independent-dynamics tests re-derive the categories, the two conservation verdicts, the appraisals, and the eight-tick settling outside the narration; a reproducibility test re-mints the owned conservation verdict by enacting the same pour and finds its identifier in the story byte for byte; and a determinism test tells the story twice in one process and asserts the two tellings identical.

THE REVIEW, TOLD HONESTLY. The independent adversarial reviewer assigned to this slice was stopped mid-run by the owner before it produced findings. Rather than merge unreviewed or inherit a review that never happened, the builder re-ran the review's own planned reproductions directly, and their results are recorded here as what they are - a builder's verification, a weaker guarantee than an independent refuter, stated plainly: the story told twice in one process is byte-identical (now pinned by test); the category episode's store reset reaches nothing outside category_formation, because no other chapter or pack reads the reused inducer's store; the eight-tick landing is derived from the regulation pack's own published constants (a decay fraction of one half and a snap epsilon of one hundredth) under exact binary halving, not an accident of one machine; the epilogue's surviving lines - the unsigned-records declaration and the closing re-run promise - are intact, and only the rungs line changed; and a line-by-line English-Readable-Code walk of both new chapters found every goal carrying its own comment, the flagged lines all being continuations of single multi-line terms. One narration-integrity judgment is recorded: the fixed English describing the derivation steps and the formed categories is hard-coded prose GUARDED by strict equality over the engines' live output immediately above it - a drifted engine silences the story rather than letting it lie - which matches the guard-then-tell pattern the earlier chapters already use.

Gates at this slice: capstone_demonstration forty-four of forty-four Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; three hundred thirty-nine in total across twenty-five packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 24

Slice 24 - Rung Five opened as groundwork: the body interface, and the sense-act loop through the honestly-named simulated body, the rung itself not claimed - 2026-08-07 - packs simulated_body (0.1.0) and body_interface (0.1.0).

THE TESTS CAME FIRST, AND THE REVIEW WAS INTERRUPTED AGAIN - RECORDED STRAIGHT. Both packs were written test-first (thirty-one tests before review). The independent adversarial reviewer was stopped mid-run by the owner for the second slice running, but this time it left two concrete leads before it stopped: one CONFIRMED English-Readable-Code lapse (a clause head without its own comment), and one open question it was in the middle of reproducing - can a hand-minted, never-enacted record be stanced at the observation grade? The builder took both leads, reproduced them live, finished the review's remaining planned lenses itself, and records the whole disposition here as a mixed review: two findings from the independent reviewer's partial run, the rest a builder's verification, the weaker guarantee named as such.

THE FINDING THAT MATTERED: THE OBSERVATION-GRADE FORGERY. Reproduced exactly as the reviewer suspected: a bare dictionary wearing a record identifier was accepted by the stance predicate and graded observation at 0.8 - the scale's strongest self-grade this build grants, granted to a pass that never ran. The fix closes the door the way slice 21 taught: the stance now PROVES the record was genuinely minted - it re-derives the content address from the record's own content, requires the loop's own designator, and refuses a forgery or a tampered designator by name (body_interface_unminted_record). Two tests pin the door: the hand-built look-alike, and a genuinely minted record whose designator was rewritten.

THE TIE, MADE DELIBERATE. The builder reproduced the one order-dependence in the seam: an empty battery (hunger one) beside a seen obstacle (reflex one) tie at salience one, and the selector keeps the earlier candidate in the fixed order - the drives before the reflexes, so the starving battery wins. That behavior was real but undocumented; it is now stated in the header as a deliberate choice and pinned by a test, so the tie can never silently flip.

THE REST OF THE DISPOSITION. The clause-head comment lapse was fixed on the line the reviewer named. The survival rhythm was extended to six ticks and pinned - the drive machinery's advancing state does not drift the two-tick pattern. The stand-in machine gained two pinned edges: showing something to a malformed body refuses by the same name as every other malformed-body use, and a drain of exactly the full charge legally empties the battery. All thirty-seven final tests pass.

Gates at this slice: simulated_body sixteen of sixteen and body_interface twenty-one of twenty-one Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; three hundred seventy-six in total across twenty-seven packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 25

Slice 25 - The story learns the body seam: Rung Five's groundwork told, and the rung not claimed - 2026-08-07 - pack capstone_demonstration (0.4.0).

THE REFINEMENT THIS SLICE IS. The refinement is one of WORDING under guards - the hardest kind this build does, because the failure mode is not a crash but an overclaim. The chapter's title carries its own size: THE BODY SEAM - RUNG FIVE'S GROUNDWORK, TOLD AND NOT CLAIMED. Its first sentence refuses the real machine it does not have; its last sentence repeats the refusal after the evidence - the rung is NOT claimed - so a reader who skims either end meets the boundary. The epilogue's Rung Five disclaimer is left byte-identical (a pinned fragment), and gains one appended sentence naming the groundwork boundary, so the honest-limits list grew MORE honest, not shorter, when the seam entered the telling. Determinism is preserved: the chapter reads only pure predicates over value terms - no global state - and the byte-identical double-telling test kept passing unchanged.

Gates at this slice: capstone_demonstration fifty-two of fifty-two Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; three hundred eighty-four in total across twenty-seven packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 26

Slice 26 - The naming gate learns to see: the unprefixed-helper hole closed, and the hand-hunt retired - 2026-08-07 - tooling bin/check_pack_naming.sh, with a committed fixture and a self-test.

THE REFINEMENT THIS SLICE IS. A rule that had lived in two places - partly in a script, partly in a ledger mandate for human eyes - now lives in one. The refinement kept the existing five checks byte-compatible (all twenty-seven packs still pass with zero violations, so the sixth check introduced no false positive on the real tree), placed the deliberate violation in a committed fixture rather than an injected-then-removed one (slice 17's proof was transient; this one is permanent and re-runnable), and sized the fixture to fail ONLY the new check - it ships a placeholder test file and a properly-prefixed public predicate, so the self-test proves the sixth check catches the hole rather than some other check happening to fire.

Gates at this slice: the extended naming gate reports zero violations across all twenty-seven packs AND passes its own self-test - the committed fixture's unprefixed helper is caught by name; the layer, layer-binding, and reality-discipline gates report zero violations; three hundred eighty-four Prolog Unit (PLUnit) tests across twenty-seven packs, all green and unchanged by this slice; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 27

Slice 27 - The delay lines: the connectome learns to keep a signal in transit - 2026-08-07 - pack connection_graph (grown).

THE REFINEMENT THIS SLICE IS. The classic red-green shape: seven failing behaviours written first - the all-delays-one identity, the two-tick arrival, the chained five-tick arrival and its four asserted silences, the weight applied at delivery, the value preserved in transit after its source went silent, the computational interface still conveying nothing, the refused zero-tick delay - then just enough code. The tighter-scoped independent adversarial review, with named lenses and a hard reproduction budget, ran to completion for the third slice in a row and confirmed four findings: two REAL of the silent-failure family (an unknown construct kind silently dropped from the state by the step's gathering; a source name absent from the state silently destroying a delay line and the values in transit), and two MINOR (delay validation skipped for computational interfaces; a negative tick count failing without a word). Three were fixed at root and pinned with tests - refusal clauses that throw by name in the plain, modulated, and delayed steps alike, an existence error for the missing source, validation of every interface before the transmissive filter - and the fourth was confirmed as standing house behaviour mirrored from the plain run, recorded, no change needed. Eleven tests grew from the slice: seven for the feature, four pinning the review's fixes.

Gates at this slice: connection_graph seventeen of seventeen Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; three hundred ninety-five in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 28

Slice 28 - The eligibility trace: a reward one tick late still finds the connection that earned it - 2026-08-07 - pack plasticity_engine (grown).

THE REFINEMENT THIS SLICE IS. The classic red-green shape, at its widest yet: twelve failing behaviours written first - the zero-valued store for transmissive interfaces only, the accumulated coincidence, the geometric fade, the manuscript's one-tick-late reward, the withheld dopamine, the untouched computational weight, the refused fading factors at one and below zero, the refused ghost trace at both steps, and the refused unknown kind at both steps - then just enough code. The tighter-scoped independent adversarial review, with named lenses and a hard reproduction budget, ran to completion for the fourth slice in a row and confirmed eight findings: four REAL of the silent-failure family (an unbound interface kind silently bound to transmissive by the checking predicate itself, at both steps; a term that is not an interface silently skipped by the trace step; the same malformed term making the reward step fail silently instead of throwing), three MINOR (duplicate trace keys resolved by first-one-wins; an orphan trace silently discarded; duplicate transmissive interfaces double-counting one coincidence), and one STYLE (an inert cut whose comment overstated it). All eight were fixed at root in one stroke - a shared refusal perimeter, one predicate judging shape, kind, duplicates, ghosts, and orphans for both steps before either touches a number - and pinned with seven review tests; the inert cut was removed. Nineteen tests grew from the slice: twelve for the feature, seven pinning the review's fixes.

Gates at this slice: plasticity_engine twenty-four of twenty-four Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; four hundred fourteen in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 29

Slice 29 - Homeostatic scaling: the bound that keeps the learning from exploding - 2026-08-07 - pack plasticity_engine (grown).

THE REFINEMENT THIS SLICE IS. The classic red-green shape again: seventeen failing behaviours written first - the zero-born store, the two-step exponential approach, the absent-activity silence, the refused smoothing factors at zero and above one, the refused duplicate key, the scale-down of the overactive region and the scale-up of the underactive one, the untouched region at target, the factor floored at zero, the untouched computational interface, the refused ghost average, malformed interface, scaling rates at one and below zero, and negative target, and the fifty-tick fixed-point bound - then just enough code. The tighter-scoped independent adversarial review, with named lenses and a hard reproduction budget, ran to completion for the fifth slice in a row and confirmed four findings: two REAL of the silent-failure family with ONE shared root (a malformed entry in the average store - a bare atom among the pairs - made the average step FAIL silently while the SAME store was silently ACCEPTED by the scaling step, one store drawing two verdicts; and the same malformation let a duplicate key slip past the duplicate check unseen) and two MINOR (an unbound store walked into a stack overflow instead of an instantiation error; the constructor could mint a store its own consumers refuse). All four were fixed at the one root - a shared store judgement, one predicate refusing malformed pairs, unbound tails, and duplicate keys for every keyed store in the pack - and the fix was mirrored into the slice-28 trace store and both constructors, whose laxity shared the root. Seven review pins hold it. Twenty-four tests grew from the slice: seventeen for the feature, seven pinning the review's fixes.

Gates at this slice: plasticity_engine forty-eight of forty-eight Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; four hundred thirty-eight in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 30

Slice 30 - Per-territory neuromodulators: the field learns local concentrations, and delayed time learns the bus - 2026-08-07 - packs neuromodulator_bus and connection_graph (grown).

THE REFINEMENT THIS SLICE IS. Twelve failing behaviours written first across two packs - the territory broadcast read back, the diffuse fallback, the local override beside an untouched neighbour, the empty-bus silence, the newest-wins replacement, the undisturbed global level, the all-delays-one identity between the delayed modulated and plain modulated steps, the gain read at delivery (silent chemistry at emission, risen at arrival), the per-territory relay beside a diffuse-fallback twin, the same territory semantics in undelayed time, and the refused unknown kind and ghost source - then just enough code. The tighter-scoped independent adversarial review, with named lenses and a hard reproduction budget, ran to completion for the sixth slice in a row and confirmed two findings, both MINOR, both in the bus, both one root: the flat key space let a territory-shaped compound posing as a modulator alias - and even silently overwrite - a territory entry, and the reads repeated the slice-28 lesson, silently binding an unbound modulator or territory instead of refusing it. No REAL finding: the identity, the delivery-tick timing, and backward compatibility all survived adversarial reproduction. Both findings fell to one guard - a modulator or territory name must be a plain atom, refused aloud as uninstantiated or by name otherwise, across all four bus predicates - pinned by five review tests. Seventeen tests grew from the slice: twelve feature, five pins.

Gates at this slice: neuromodulator_bus fourteen of fourteen and connection_graph twenty-four of twenty-four Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; four hundred fifty-five in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 31

Slice 31 - The per-territory third factor: each connection learns by the chemistry of the place the signal arrives - 2026-08-07 - pack plasticity_engine (grown at its root).

THE REFINEMENT THIS SLICE IS. Seven failing behaviours written first, all red before the first green - the live step reading a local half concentration over a global full one, the diffuse fallback beside a local neighbour in one step, the explicitly silenced territory refusing a global reward, the reward step spending one trace at a local concentration and another at the diffuse field, the silenced territory spending its trace into nothing, and the compound receiving name refused by the bus's own guard at both steps - then just enough code: the two step predicates handed the bus instead of a pre-read global level, each transmissive interface reading its receiving territory. The tighter-scoped independent adversarial review, with named lenses and a hard reproduction budget, ran to completion for the seventh slice in a row and confirmed three REAL findings with one root, all three reproduced before fixing: the live step - the one the cognitive cycle calls every tick - had never been given the refusal perimeter its three siblings carry, so an unbound kind was silently treated as non-learning, a malformed term made the whole step silently fail, and a duplicate transmissive interface silently learned the same coincidence twice. One fix at the root - the shared interface judgement and duplicate check at the head of the live step - pinned by three review tests. Ten tests grew from the slice: seven feature, three pins.

Gates at this slice: plasticity_engine fifty-eight of fifty-eight Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; four hundred sixty-five in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 32

Slice 32 - The live tick carries the whole learning body: traces, averages, and the slow bound beat in the loop - 2026-08-08 - pack cognitive_cycle (grown; the boot worlds of capstone_demonstration and symbol_exchange grown to carry the stores).

THE REFINEMENT THIS SLICE IS. Five behaviours written first - the trace store rising above zero in the running loop, the averages climbing toward the source's steady activity without overshooting, the at-rest identity under an absurd target, the live rate bending the weight upward for a quiet region, and the gutted world refused aloud key by key - four red against the old cycle before the first green, the fifth (the at-rest identity) green by construction as the backward-compatibility pin. Then just enough code: one world reader that refuses a missing key by name, six keys read, three engine steps called in order, two stores committed back. The tighter-scoped independent adversarial review, with named lenses and reproduction before fixing, ran to completion for the eighth slice in a row and confirmed one REAL finding under the silent-failure lens, reproduced before fixing: the run predicate's tick-count guard CLAIMED refusal in its comment while the code just failed - a negative count failed silently, and a non-numeric count died with a stray arithmetic error instead of a named refusal. One fix at the root - a named tick-count check that refuses the unbound, the non-integer, and the negative aloud - pinned by two review tests. Seven tests grew from the slice: five feature, two pins.

Gates at this slice: cognitive_cycle eleven of eleven Prolog Unit (PLUnit) tests pass, and the grown boot worlds of capstone_demonstration (fifty-two of fifty-two) and symbol_exchange (nineteen of nineteen) re-swept green (the earlier packs unchanged; four hundred seventy-two in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 33

Slice 33 - The per-territory scaling target: each region defends the activity level its own territory owes - 2026-08-08 - pack plasticity_engine (grown at its root; cognitive_cycle grown to read the geography; the boot worlds of capstone_demonstration and symbol_exchange grown to carry it).

THE REFINEMENT THIS SLICE IS. Nine behaviours written first - the region with its own target scaled toward it and not the global one; the unmarked region defending the global target exactly while its marked neighbour defends its own; the empty geography identical, term for term, to the old global step; the malformed pair, the duplicate key, the negative target, and the unbound store each refused by name; and in the live tick, the per-territory target bending its own region's weight harder than the global target, plus the at-rest identity under an absurd territory target at rate zero - all red against the old engine before the first green except the at-rest identity, which is green by construction and said aloud to be so, exactly as slice 32's was. Then just enough code: one store judgement, one lookup with the global fallback, one re-rooted step. The tighter-scoped independent adversarial review, with named lenses and reproduction before fixing, ran to completion for the ninth slice in a row and confirmed one REAL finding under the unbound-wrong-judgement lens, reproduced before fixing: an unbound target or rate was refused as a domain error CARRYING AN UNBOUND VARIABLE - a refusal that cannot name its wrong - where the house convention refuses the unbound as uninstantiated; the root, one number-check pattern shared by the four constant checks, was fixed once in all four and pinned by five review tests. Fourteen tests grew from the slice: nine feature, five pins.

Gates at this slice: plasticity_engine seventy of seventy Prolog Unit (PLUnit) tests pass and cognitive_cycle thirteen of thirteen, with the grown boot worlds of capstone_demonstration (fifty-two of fifty-two) and symbol_exchange (nineteen of nineteen) re-swept green (the earlier packs unchanged; four hundred eighty-six in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 34

Slice 34 - The bound turned live in the told story: the capstone world carries a live rate and a small geography, and the story tells the bound's work beside a rest twin - 2026-08-09 - pack capstone_demonstration (grown; no other pack changed).

THE REFINEMENT THIS SLICE IS. Six behaviours written first - the boot world carrying the live rate and the one-entry geography; the story telling the live bound, the relay's own mark, and the slow rate; the story telling the rest twin and claiming the difference as the bound's own work; the live run's weight standing above the rest twin's, checked outside the narration; the regulation and the weight claim holding at the six-tick minimum; and the story still telling whole at that minimum - all red against the resting world before the first green. Then just enough code: two world constants, one twin run, three closing lines. The tighter-scoped independent adversarial review, with named lenses and reproduction before fixing, ran to completion for the tenth slice in a row: the told-mark-drift candidate was reproduced as tamper-and-catch and judged DEFENDED - a drifted constant breaks a pinned test before it can ship a lying story - and one REAL finding was confirmed under the unbound-wrong-judgement lens, the very lens slice 33 named for future reviews: an unbound tick count was refused as capstone_demonstration_bad_ticks CARRYING AN UNBOUND VARIABLE, a refusal that cannot name its wrong; the root - the integer-then-named-error pattern in the tick gate - was fixed once with the house var guard and pinned by two review tests, the unbound count now refused as uninstantiated and the bound-but-wrong count still refused by its own name. Eight tests grew from the slice: six feature, two pins.

Gates at this slice: capstone_demonstration sixty of sixty Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; four hundred ninety-four in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 35

Slice 35 - The two-shift switchboard laid: the global operating state rides the bus, online by default, and nothing yet reads it - 2026-08-09 - pack neuromodulator_bus (grown; no other pack changed).

THE REFINEMENT THIS SLICE IS. Eight behaviours written first - the fresh bus operating online; the offline announcement read back; the newer announcement replacing the older; the halfway state refused by name; the unbound state refused as uninstantiated; the state broadcast leaving the chemical levels untouched; the chemical broadcasts leaving the state untouched; and the state's own key shape refused at the modulator broadcast - all red against the bus that existed before the first green. Then just enough code: one check, one read, one write. The tighter-scoped independent adversarial review, with named lenses and reproduction before fixing, ran to completion for the eleventh slice in a row, working four lenses: unbound-wrong-judgement, key-shape aliasing, default drift, and behaviour-pinned-identical. The aliasing, drift, and pinned lenses returned DEFENDED on reproduction; the unbound-wrong-judgement lens - the very lens slices 33 and 34 named for future reviews - confirmed one REAL finding, reproduced before fixing: the state read asked about an UNBOUND bus silently bound it into a partial list and answered with an invented, unbound state, against the module's own never-silently-bound law. The root was fixed once with the house var guard at the read's head and pinned by a review test; the same inherited pattern in the older chemical reads was recorded rather than touched, per the do-not-fix-speculatively rule. Nine tests grew from the slice: eight feature and pins written first, one review pin.

Gates at this slice: neuromodulator_bus twenty-three of twenty-three Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; five hundred three in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 36

Slice 36 - The reward learns its address: the tick spends the eligibility traces the drives reward, and capture consumes the spent tags - 2026-08-09 - packs plasticity_engine and cognitive_cycle (grown; no other pack changed).

THE REFINEMENT THIS SLICE IS. Eleven behaviours written first, all red before the first line of code: capture spending the tag into the weight and consuming it; the rewardless bus shipping nothing, weight and tag standing; the computational interface untouched beside a capturing transmissive one; capture reading the receiving territory's local dopamine over the global field; a zero territory over a live global keeping its own tag; the ghost tag and the unknown kind refused through the shared perimeter; the unbound bus refused as uninstantiated at the step's head, the house var guard in Observation-5's exact shape; and in the live loop, twin-world tellings - a standing trace spent into the weight on the first rewarded tick, the twins' trace stores identical after capture consumed both histories, and a body at its set-point spending no tag at all. Then just enough code: one capture helper, one tag-fate helper, one step, one call in the tick. The tighter-scoped independent adversarial review ran to completion for the twelfth slice in a row, working the four named lenses. Key-shape aliasing and default drift returned DEFENDED on reproduction; behaviour-pinned-identical was verified bit-identical against a main worktree on a five-tick rewardless world. The standing unbound-wrong-judgement lens - paying for the fourth slice in a row - confirmed one REAL finding, reproduced before fixing: the zero-dopamine short-circuit skipped the arithmetic that judges its arguments, passing an unbound weight, tag, or learning rate through silently on exactly the commonest path, where the sibling reward step throws. The fix judges the spend's arithmetic on every path and keeps the zero path's answer bit-identical; three review pins hold it, and a fourth pin fixes the review's second finding by pinning the previously untested punishment path: a negative third factor spends the tag downward and consumes it. Fifteen tests grew from the slice: eleven written first, four review pins.

Gates at this slice: plasticity_engine eighty-two of eighty-two and cognitive_cycle sixteen of sixteen Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; five hundred eighteen in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 37

Slice 37 - The night watchman takes the post: the two-process governor rides the live tick and throws the slice-35 switch, its first legitimate thrower - 2026-08-09 - pack two_process_governor (new) and pack cognitive_cycle (grown; the fixture worlds of symbol_exchange and capstone_demonstration seat the watchman; no behaviour elsewhere changed).

THE REFINEMENT THIS SLICE IS. Sixteen behaviours written first, all red before the first line of code: the fresh governor discharged at midnight; the waking rise; the sleeping discharge floored at zero; the phase wrap at the day length; the day wave silent at midnight and peaking at midday; the hold below the sleep threshold; the snap offline when the debt beats the wave; the same debt held online by the day wave that flips a silenced night twin - the corpus's opposition made a twin telling; the hysteresis band holding BOTH states on the same numbers; the snap back online when the debt is paid; the default parameters living a consolidated day, exactly two transitions in thirty ticks; and the perimeter - the unbound governor, the unbound state, the third position, the unbound pressure, the unbound discharge at the floor (the short-circuit probed on purpose), and the inverted band, each refused aloud. In the loop, five more: the default watchman holding a short day online; the governor's debt advancing in the world; a fast-flipping watchman throwing the switch in the live tick; the thrown switch moving no other world piece - a twin telling across every store the tick owns; and the missing governor refused by the world key's name. Then just enough code: one parameter check, one governor check, one state check, one pressure mover, one wave, one selector, one step, and three lines in the tick. The tighter-scoped independent adversarial review ran to completion for the thirteenth slice in a row, working the four named lenses. Key-shape aliasing and default drift returned DEFENDED on reproduction; behaviour-pinned-identical was verified bit-identical against a main worktree on a five-tick world, every value and every content-addressed record identifier equal to the letter. The standing unbound-wrong-judgement lens - paying for the FIFTH slice in a row, and aimed by slice 36's refinement straight at silent-failure shortcuts - confirmed one REAL finding, reproduced before fixing: an unbound day length fell through integer/1, which fails SILENTLY on a hole, into the domain refusal, so the refusal carried the hole itself where every sibling field is refused as uninstantiated. Fixed once with the house var guard ahead of the integer test, pinned by a review test; the review's second note - a hand-built fractional phase dying in mod's own typed refusal rather than a governor-named one - was pinned as deliberately relied-upon loudness rather than papered over, since no public constructor can reach it. Twenty-two tests grew from the slice: sixteen and five written first, one review pin, plus the honesty pin.

Gates at this slice: two_process_governor twenty of twenty, cognitive_cycle twenty-one of twenty-one, symbol_exchange nineteen of nineteen, and capstone_demonstration sixty of sixty Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; five hundred forty-three in total across twenty-eight packs, all green); the naming gate reports zero violations across all twenty-eight packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 38

Slice 38 - The night crew takes the dark hours: the tick reads the switch the watchman throws, and the offline works run - interleaved replay against catastrophic forgetting, and the renormalisation shift - 2026-08-09 - pack offline_consolidation (new), pack cognitive_cycle (grown), and pack neuromodulator_bus (one additive export); the fixture worlds of cognitive_cycle, symbol_exchange, and capstone_demonstration seat the three new keys, and the online day is behaviour-pinned identical.

THE REFINEMENT THIS SLICE IS. Nineteen engine behaviours and ten loop behaviours were written first, all red before the first line of code. At the engine: the fresh store empty; the newest memory appended last; the interleaving of an even store alternating new against old; the odd extra falling to the OLD half; the empty store interleaving quiet rather than wrong; the replay round leaving an empty night bit-identical; the strengthening equal to the Hebbian product times the dose; a computational interface untouched; an absent construct replaying silent; the night step's replay-then-bound ORDER pinned by an exact arithmetic value (a replayed 1.1 scaled by 0.9 to 0.99, which the reverse order could not produce); the bound still applying on a night with nothing remembered; and the perimeter - an unbound store, a malformed pair, a duplicate snapshot key, an unbound snapshot, an unbound rate, and a negative rate, each refused aloud. And the headline: THE CATASTROPHIC-FORGETTING REST-TWIN TELLING, the corpus's flagship law made a controlled experiment in the house style - one cortex, two twins, the same ten nights and the same raised bound, differing only in WHAT is replayed. In the loop: the thrown switch governing exactly the NEXT tick (the slice-37 behaviour-neutral twin telling matured into its slice-38 form - every world piece identical up to the flip, and the very next tick diverging into day and night); every online tick remembering its pattern; the default day living the corpus's proportions LIVE - the night falling exactly on ticks nineteen through twenty-six, and morning at twenty-seven; every offline tick's reward zero; the body lying still and the store not growing through the night; the night works strengthening the remembered day's edge; the lowered night bound refused by name; and the three new world keys each refused aloud by name when missing.

THE INDEPENDENT ADVERSARIAL REVIEW ran to completion for the FOURTEENTH slice in a row, working the four named lenses, and this slice it was the most productive it has ever been: SEVEN real findings, every one reproduced before a line was changed. The behaviour-pinned-identical lens returned DEFENDED - a fifteen-tick all-online run was verified bit-identical against a main worktree, every value and every content-addressed record identifier equal, and it was re-verified after every fix. The key-shape aliasing lens returned DEFENDED across the board: the timing law holds with no tick both announcing and obeying; the stale dopamine that survives the night is provably never spent, because the morning's drive step rebroadcasts before any reader; the day's and night's scaling calls agree argument for argument; and a snapshot key cannot alias a bus key. THE UNBOUND-WRONG-JUDGEMENT LENS PAID FOR THE SIXTH SLICE RUNNING, and paid six times: the night engine judged its memory store and its rate but judged the CORTEX not at all, so an unbound interface store was silently bound to the EMPTY cortex (the night reporting it had consolidated nothing), a partial store was silently truncated with the caller's own tail variable mutated to agree, a non-list failed in silence, and an unbound interface kind fell to the untouched branch so an edge was reported consolidated that was never replayed; a hole at an edge's END was BOUND by the replay's own lookup to the remembered snapshot's first construct, inventing a connection no caller declared and strengthening it; and in the loop, an unjudged operating state let a hole run BOTH whole programs and a third value fail the whole tick silently. Fixed at three roots - a cortex check in the memory store's own shape, a name guard ahead of the replay's lookup, and the bus's own domain check exported and called before the dispatch - and pinned by nine review tests. THE DEFAULT-DRIFT LENS PAID TWICE, once shallow and once deep. The shallow finding: the night's two rates arrive by the same route under the same documented law, but only the bound was judged at the tick's head, so a rotten or NEGATIVE replay rate rode a full eighteen-tick day green and detonated at nightfall, invisible to every short-run test; the rate check is now exported and called on every tick, pinned twice. The DEEP finding is the slice's most valuable: measured end to end, the night was multiplying the fixture's weight BY MORE THAN TEN against a scaling target of one half - the exact inverse of the renormalisation Chapter 7 specifies - because the replay dose was paid once per remembered pattern while the bound remained one fixed step, so a growing memory store simply out-doses the bound, and the guard comparing the night's bound to the DAY's gave false assurance by never comparing it to the replay it was set against. Worse, the suite's own night assertion had enshrined the growth. The dose is now shared across the batch, making it invariant to store size; the live night's multiplier fell from ten and a half to one and a sixth; two new pins hold it - one proving the same pattern remembered four times moves the weight exactly as remembering it once does, one proving an unrefreshed edge is washed DOWNWARD - and the rest-twin telling was rewritten around the honest measured numbers rather than the numbers the defect had produced. Thirty-two tests grew from the slice: twenty-nine written first, plus eleven review pins, less the assertions rewritten.

Gates at this slice: offline_consolidation twenty-seven of twenty-seven and cognitive_cycle thirty-four of thirty-four Prolog Unit (PLUnit) tests pass, with neuromodulator_bus twenty-three of twenty-three, symbol_exchange nineteen of nineteen, and capstone_demonstration sixty of sixty unchanged through the reseated worlds (the earlier packs unchanged; FIVE HUNDRED EIGHTY-THREE in total across twenty-nine packs, all green); the naming gate reports zero violations across all twenty-nine packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 39

Slice 39 - Every construct becomes a hybrid automaton: the register of one, stated proudly - 2026-08-09 - pack mode_register (new), packs connection_graph and tick_engine (rewired so every construct's rule is read THROUGH its own register); no world dict key added, no number changed, and the engines pinned behaviour-identical against a main worktree for every well-formed construct kind.

THE REFINEMENT THIS SLICE IS. Thirty behaviours were written first, red before the first line of the module existed, and they divide into four groups. THE SHAPE: a register of one builds and reports its size as one, which is the whole claim of the slice; the current mode reads back; the register lists its mode names; the current mode names the rule; the transition table is present and empty; the fault block is present and empty. THE STORES, EVERY ONE JUDGED THE SAME WAY: an unbound automaton, an impostor term, an empty register, an unbound register, a PARTIAL register, a malformed entry, a duplicate mode name, a non-atom mode name, an unbound current mode, a current mode absent from the register, an unbound transfer block, a mode with no transfer function, a transfer for an undeclared mode, two transfers for one mode, a malformed transfer row, a malformed transition row, a transition to an undeclared mode, an unbound transition table, a malformed fault entry, and an unbound fault block - each refused aloud and by its own name. THE LOOKUP: an unbound mode key refused, and an undeclared mode refused, because a membership check on a hole binds the hole and answers with somebody else's rule. AND KONNECTOME'S OWN CONSTRUCTS: every one of the six construct kinds has a register; every one of those registers holds exactly one mode; EVERY ONE OF THEM FILES THE CONSTRUCT KIND ITSELF AS THAT MODE'S TRANSFER FUNCTION, which is the bit-identical law stated as a test rather than trusted; every entry carries a formal name, a coined name beginning "the", and a non-empty gloss; the six formal names are all distinct; an unregistered kind has NO register and fails quietly rather than inventing one; the resolver passes such a kind through unchanged; and the resolver refuses an unbound kind. Three more were added at the two consumers, so the wiring itself is pinned where it is used.

THE BEHAVIOUR-PINNED-IDENTICAL PROOF WAS RUN BEFORE THE SLICE WAS BELIEVED, against a git worktree of main, and it is the load-bearing evidence of this slice. The whole capstone story - the mind booted, lived, and told by its own records, every content-addressed identifier included - is BYTE-FOR-BYTE identical between main and this branch. Beside it, a hundred-and-fourteen-line engine probe was run in both trees and diffed: forty heartbeat runs of one to forty ticks over a clock, a copy and a hold; twenty-six plain runs and twenty-six delay-honouring runs of zero to twenty-five ticks over a four-interface graph carrying a negative weight, a three-tick delay and a computational interface; twenty bus-modulated steps in both the plain and the delayed paths; and both refusal terms, the unknown kind and the registered-but-unaccepted kind. Identical, to the last digit and the last error term, with only the anonymous variable serials differing between two separate SWI-Prolog processes.

AND THE PROBE FOUND SOMETHING, WHICH IS WHY IT IS RUN RATHER THAN ASSUMED. There is exactly ONE path on which this slice changes behaviour, it is a path no world can reach, and the change is a strict improvement that must nevertheless be declared rather than buried. On main, a tick-engine construct whose KIND was left unbound unified with all three of the heartbeat's clause heads in turn, so a single construct produced THREE contradictory next values and the committed state silently GREW - a run of one tick over one construct returned a three-entry state naming that construct three times, with different values. That is the unbound-wrong-judgement lens's third refinement, live in the oldest pack in the repository, and routing every construct through the register closes it as a side effect: the resolver refuses an unbound kind at the door, aloud, before any clause head can bind it. The connection graph already threw on the same input, though from further downstream. The other pre-existing hole the probe exposed is NOT closed and is logged instead of fixed, per the do-not-fix-speculatively rule: the tick engine still drops an UNKNOWN construct kind in silence, where the connection graph refuses one aloud by name, so a heartbeat state can shrink without complaint.

THE INDEPENDENT ADVERSARIAL REVIEW ran to completion for the FIFTEENTH slice in a row, working the four named lenses, and returned ONE REAL FINDING in this slice's own new code, reproduced before a line was changed - together with three observations about code this slice did not write. THE KEY-SHAPE ALIASING LENS PAID, and it paid on the one store this slice judged in a second place. Transfer-row mode names were guarded as atoms where they were read; TRANSITION ENDPOINTS were not, so the endpoint check reached its membership test with an unguarded key, and a caller writing two holes in a transition row got the row back BOUND - a transition from the register's first mode to the register's first mode, which the caller never wrote and which need not even be the mode the construct is standing in. That is the default-drift lens's law and the key-shape aliasing lens's mechanism arriving as one defect: two names travelling under one documented law, judged in two places, and the second place not judging. The fix is one guard moved into the ONE place both routes come through, and it is pinned by three review tests - an unbound origin, an unbound destination, and an unbound transfer key. THE UNBOUND-WRONG-JUDGEMENT LENS RETURNED DEFENDED at the stores: all four blocks route through a single shape check, and an unbound, a partial, and a non-list block were each verified to be refused, in every block. THE BEHAVIOUR-PINNED-IDENTICAL LENS RETURNED DEFENDED and went further than this slice's own probe: determinism and solution counts were compared predicate by predicate across both trees for every construct kind, and every one reports the same residual choicepoint and the same single solution on both sides, so the new single-clause wrappers neither add nor remove one; every error term and context matches main exactly; and the renamed predicates were confirmed to have no caller outside their own modules and to appear in no export list. The review also confirmed independently the one divergence this slice declares, and recommended that the slice's claim be worded as bit-identical for every WELL-FORMED construct kind, which is how it is now worded here. Its two remaining findings are pre-existing holes in the tick engine that this slice deliberately does not fix, both routed to Observation-7.

Thirty-six tests grew from the slice: thirty written first at the register, three more at the two consumers, and three review pins.

Gates at this slice: mode_register forty of forty Prolog Unit (PLUnit) tests pass at birth, with connection_graph grown from twenty-four to twenty-six and tick_engine from seven to eight (the earlier packs unchanged; SIX HUNDRED TWENTY-SIX in total across THIRTY packs, all green); the naming gate reports zero violations across all thirty packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 40

Slice 40 - The gate archetype re-expressed as its genuine two-mode register - 2026-08-09 - pack archetype (the gate given a register of TWO, per-mode transfer functions that really differ from each other, and an explicit transition table carrying trigger, direction, timescale and agency); no world dict key added, no number changed, and every well-formed gate answer pinned byte-identical against a main worktree.

WHAT THE PHASE REQUIRED. Slice 39 gave every construct a register of ONE, and the design authority - /home/ccaitwo/konnectome/docs/design/05_mode_register_design_authority_v1.txt - named the slice after it in its Part Nine: THE GATE ARCHETYPE RE-EXPRESSED as its genuine two-mode register, with an explicit transition table, validated against corpus entries whose registers are two. The reason this is the next slice rather than a later one is the design authority's own: of everything konnectome had built in thirty-eight slices, the gate archetype's binary flip is "a genuine two-mode register in the corpus's own sense and is the natural seed of the whole design", and its existing vocabulary is corpus-correct and must not be renamed. A register of one is first-class, but it is also the one register length that cannot catch a mistake, because with a single mode the register block, the transfer block and the current-mode slot have nothing to disagree about. Two modes is where the formalism starts doing work.

WHAT THE REVIEW FOUND, AND WHAT IT DID NOT. The review was the sixteenth straight completion, and it carried all four standing lenses - unbound-wrong-judgement, key-shape aliasing, default drift, behaviour-pinned-identical - over code this slice had just written rather than over code it had inherited. It returned THREE findings, all in the new code, all reproduced before anything changed, and all fixed inside the slice.

FIRST, THE DEPARTURE LOOKUP WAS GUARDED AT EACH ROUTE'S BEGINNING RATHER THAN WHERE THE ROUTES MEET. Two routes reached it, both guarded their own mode name before calling, and the lookup itself would still have bound an unbound key to the table's first row had a third route ever appeared. This is precisely the shape of the defect slice 39's review found, one slice later and in new code, which is the whole argument for carrying a lens forward: the fix moves the lookup behind a judged automaton, so the key it reads cannot be a hole by construction.

SECOND, THE TRIGGER WAS SPELLED TWICE - once in the transition table and once in the lookup that searched it - so renaming it in the table would have left the lookup finding nothing and the gate FAILING IN SILENCE rather than refusing aloud. Default drift's exact law: two names travelling under one rule, maintained in two places. The lookup no longer names the trigger at all, and a mode the table gives no departure from is now refused aloud.

THIRD, THE CLOSED GATE'S LAW NEVER READS ITS INPUT, so a hole arriving at a closed gate would have been answered with a confident zero. The guard is placed once, where both transfer functions come through, rather than inside each law.

WHAT THE REVIEW DELIBERATELY DID NOT DO. Observation-7's two halves in tick_engine stand exactly as slice 39 left them; this slice does not touch that pack, and the do-not-fix-speculatively rule holds. The gate's missing hysteresis is recorded as a new observation rather than built, because the stabiliser is its own slice in the design authority's order and because a slice promising to change no number must not add a second threshold on the way past.

VALIDATED AGAINST A CORPUS ENTRY WHOSE REGISTER IS TWO. The Layer 8 modes volume's Entry 36, the mutual-inhibition flip-flop, coined the Either-Or Latch, is the corpus's own nearest relative of konnectome's gate: "a hysteretic toggle whose modes are simply its two self-holding states". Its register holds Mode A - the First Latch and Mode B - the Second Latch. Its per-mode transfer functions describe what holds while each state is realised. Its transition table carries a bias thrown from above on the circadian and homeostatic timescales and a supra-threshold input to the losing pool that flips it near-instantaneously. Its fault block names two regimes - instability and a stuck switch - and a watchdog. konnectome's gate was read block by block against that entry, and the one place the two shapes differ is DECLARED rather than smoothed over.

THE DECLARED DIFFERENCE: KONNECTOME'S GATE IS A SYMMETRIC TOGGLE, NOT A HYSTERETIC LATCH. The corpus's latch turns on at one boundary and off only at a lower one, and the corpus is explicit that the hysteresis is supplied by a STABILISER that is "not part of the switch" and publishes a stability bias of its own. konnectome's gate compares one switching drive against ONE threshold in both directions, so its two transition rows honestly share a single trigger. This slice does NOT invent the second threshold: the design authority already files the stabiliser as its own later slice, and this slice promises to change no number. The transition table records the shared trigger, the transfer functions record what each mode actually does, and the gap is written in the ledger as an observation rather than papered over here.

Gates at this slice: archetype grown from seven Prolog Unit (PLUnit) tests to twenty-nine, all passing (the other twenty-nine packs unchanged; SIX HUNDRED FORTY-EIGHT in total across THIRTY packs, all green); the naming gate reports zero violations across all thirty packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 41

Slice 41 - The bus throws a transition - 2026-08-09 - packs neuromodulator_bus, archetype and mode_register (a broadcast channel that carries a MODE rather than a level, the gate's transition table given its second agency, and a departure lookup keyed on that agency); no world dict key added, no number changed, and every well-formed archetype answer pinned byte-identical against a main worktree.

WHAT THE PHASE REQUIRED. The mode-register design authority - /home/ccaitwo/konnectome/docs/design/05_mode_register_design_authority_v1.txt - names this slice fourth in its Part Nine: THE BUS THROWS A TRANSITION, so that one write produces many coordinated register transitions. Slice 39 gave every construct a register of one; slice 40 gave the gate a genuine register of two and made its transition table the sole authority on direction, and it said in as many words that when a broadcast throws a transition the throw would be a ROW in that same table rather than a second path into the gate. This slice is where that promise is kept. It is also where the design authority's Part Seven item 1 - the COMMAND VERSUS BIAS question - had to be answered, because the whole broadcast semantics of three later families hangs on it.

WHAT THE REVIEW FOUND, AND WHAT IT DID NOT. The review was the seventeenth straight completion and carried all four standing lenses - unbound-wrong-judgement, key-shape aliasing, default drift, behaviour-pinned-identical.

THE DEFAULT-DRIFT LENS PAID OUT BEFORE IT COST ANYTHING, AND THIS IS THE SLICE'S MOST PORTABLE LESSON. Slice 40's departure lookup took the first row leaving a mode. That was correct while every row shared one agency, and it would have STAYED correct the moment this slice added two more rows - purely because the new rows are declared after the old ones. It would have become wrong the first time anyone reordered the table, and it would have become wrong in silence, answering with a real row from the wrong governor. THE GENERAL SHAPE: A LOOKUP WHOSE CORRECTNESS RESTS ON DECLARATION ORDER IS NOT CORRECT, IT IS LUCKY, and the moment a second value enters the column it was ignoring is the moment to name the column. The lookup now names its agency, and the pin for it is a test that asserts the gate's self-selected flip is unchanged.

THE UNBOUND-WRONG-JUDGEMENT LENS PAID OUT FOR THE NINTH SLICE RUNNING, in three places, all of them new code. An unbound AGENCY at the departure lookup would have matched the first row of any agency and answered confidently. An unbound CONSTRUCT KIND at the throw read would have been bound by the lookup to whichever kind the bus happened to list first, handing back that kind's command to a caller who addressed nobody. And an unbound BUS at the same read would have invented a bus and a command with it. All three refuse.

THE KEY-SHAPE ALIASING LENS RETURNED NOTHING TO FIX AND ONE THING TO PIN. mode_throw/1 shares neither name nor arity with global/1 or territory/2, and the bus's atom guard keeps a plain modulator out of every compound shape. That is an argument, and an argument is not a measurement, so the slice pins it with a test that writes all four channels on one bus with the throw deliberately aimed at a kind whose NAME IS A MODULATOR ALREADY ON THAT BUS - the arrangement that would expose a collision if one existed - and reads all four back.

WHAT THE REVIEW DELIBERATELY DID NOT DO. Observation-7's two halves in tick_engine stand exactly as they were; this slice does not touch that pack and the do-not-fix-speculatively rule holds. Observation-5's chemical reads are untouched for the same reason. The gate's missing hysteresis is still the stabiliser's slice and is not smuggled in here. And no construct other than the gate was given a thrown row, because a slice that commanded constructs whose registers are of ONE would have had to invent modes for them.

THE COMMAND-VERSUS-BIAS DECISION, MADE HERE AND RECORDED AS KONNECTOME'S OWN. The design authority records that the corpus is genuinely silent: keeps it in the Pinpoint Report, set from above reads as COMMAND, and biases it toward flashing reads as BIAS, sometimes in the same volume. The Theme Two read raised the same question independently for the offline broadcast, and the Theme Four read raised it a third time; the only concrete evidence any of the three found is Layer 8 Chapter 16's "neuromodulators move the circuit between regimes by scaling recurrent gain", which is bias in FORM - a gain scalar, not a mode name - and which arrives at a different grain from the one where the decision must be made. It is therefore evidence and not an instruction. THE CORPUS DOES NOT DECIDE THIS, SO KONNECTOME DOES, AND THE DECISION IS: A THROWN MODE IS A COMMAND AT THE DISCRETE GRAIN, AND BIAS IS THE CHANNEL KONNECTOME ALREADY HAS.

Three reasons, none of them the corpus's, all of them konnectome's. FIRST, IT UN-CONFLATES TWO OPERATIONS THE DESIGN AUTHORITY SAYS ARE CURRENTLY CONFLATED: its Part Eight records that continuous modulation and discrete throw are different operations on the same channel and that konnectome's bus-modulated relay runs them together. konnectome already owns bias, built and running - relay_modulated reads a level off this same bus and scales its gain where it stands. Had the throw also been a bias, konnectome would own two bias mechanisms, no command mechanism, and the conflation would be permanent. SECOND, A COMMAND COSTS NO INVENTED NUMBER AND A BIAS COSTS AT LEAST ONE: a bias moves a threshold, and the corpus supplies konnectome no threshold offset at this grain, so one would have to be made up - in a family whose standing rule is that no number in the pack is konnectome's invention. THIRD, COMMAND IS THE REVERSIBLE CHOICE: a bias channel can later be added BESIDE this one as a second agency writing rows into the same table, and nothing built on the command has to move; the reverse is not true.

AND THE CONFLICT RULE THAT FOLLOWS FROM IT, ALSO KONNECTOME'S OWN. The design authority's Part Seven item 2 records that the corpus shows preemption in one place and negotiated arbitration in another and states no general rule. konnectome takes PREEMPTION: where a construct's self-selected transition and a standing throw disagree, the throw wins. The reason is konnectome's own and is not a claim about biology - preemption is the rule that can be READ without running the construct, which the owner's standing visualization request demands of every register slice in this family. NEGOTIATED ARBITRATION IS DEFERRED BY NAME rather than denied: it is the shape a later slice would need if two sources ever threw at one kind in one tick, and this channel deliberately holds exactly ONE standing throw per kind, the newest replacing the older exactly as every other broadcast on this bus does, so that case cannot arise unnoticed.

THREE FAMILIES INHERIT THIS DECISION, which is why it is written at this length rather than as a line: the remainder of the mode-register family, the Theme Two offline-broadcast work, and the Theme Four cognitive cluster. A later session that wants to revisit it should revisit it as a decision, in the open, and not discover it as an assumption.

Gates at this slice: mode_register grown from forty Prolog Unit (PLUnit) tests to forty-five, neuromodulator_bus from twenty-three to thirty-six, and archetype from twenty-nine to forty (the other twenty-seven packs unchanged; SIX HUNDRED SEVENTY-SEVEN in total across THIRTY packs, all green); the naming gate reports zero violations across all thirty packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the capstone demonstration runs and its story is byte-identical to main's. No new wall was hit; nothing in PrologAI or Causalontology was edited. HONESTLY STATED: the Causalontology conformance suite was NOT re-measured this slice, because this slice touched neither cousin; the last measured figure is Tungsten Sluicewright's one hundred thirty-seven of one hundred thirty-seven, and a session that needs the number should re-measure rather than quote it.


KONNECTOME REALIZATION LOG - SLICE 42

Slice 42 - The supervisor channel: fault regimes watched and published as warnings, never admitted as modes - 2026-08-09 - pack supervisor (new) and pack archetype (the gate's fault block filled from the corpus's own latch entry); no world dict key added, no number invented, and every archetype answer pinned byte-identical against a main worktree.

WHAT THE PHASE REQUIRED. The mode-register design authority's Part Nine slice five, and the closing of an IOU three slices old. The Layer 6 chapter template states the admission rule for a mode register directly: a regime enters the register only if the HEALTHY system uses it, and the ways a component BREAKS are watched by supervisor processes that publish WARNINGS - never listed as regimes the component operates in. konnectome has honoured that since slice 39 by leaving every fault block present and EMPTY, which was correct and was also a debt: the block could not honestly be filled until something existed to do the watching. This slice builds the watcher, and the blocks stop being empty.

WHAT THE REVIEW FOUND, AND WHAT IT DID NOT. The review was the eighteenth straight completion and carried all five lenses - the four standing ones and the DECLARATION-ORDER lens earned at slice 41, now on its first outing.

THE DECLARATION-ORDER LENS PAID ON ITS FIRST OUTING, in code written the slice after it was named. The watch gathers readings by boundary signature, and the obvious implementation takes the first match. Two watchdogs reporting on one boundary with different allowances would then have produced a real verdict drawn from whichever reading the caller happened to list first - a supervisor confidently right by accident. It refuses instead, with domain_error(supervisor_single_reading_per_signature, Signature). A LENS EARNED IN ONE SLICE AND PAID IN THE NEXT IS THE BEST ARGUMENT AVAILABLE FOR CARRYING LENSES FORWARD RATHER THAN RETIRING THEM.

THE UNBOUND-WRONG-JUDGEMENT LENS PAID FOR THE TENTH SLICE RUNNING, in five places, all new code: an unbound READINGS STORE would have been walked as an empty one and reported everything unwatched, confidently; an unbound VALUE and an unbound ALLOWANCE would each have reached the comparison and raised an arithmetic error naming nothing a caller could act on; an unbound SIGNATURE would have been bound by the gather to whichever regime came first; and an unbound CONSTRUCT KIND at the channel read would have handed back another kind's failure report to a caller who addressed nobody. All five refuse, and the two numeric guards sit in the ONE place every reading passes through rather than at each comparison.

THE DEFAULT-DRIFT LENS RETURNED ONE FINDING OF DESIGN RATHER THAN OF CODE, and it is the reason the report carries two blocks instead of one. An earlier shape returned only warnings, which reads beautifully and is a trap: a caller receiving an empty warning list cannot distinguish a construct that was watched and found healthy from one that nobody watched at all, and the second case is the one that kills systems. The unwatched block exists so that silence has to name itself.

WHAT THE REVIEW DELIBERATELY DID NOT DO. No construct other than the gate was given fault regimes, because the gate is the only konnectome construct whose fault regimes the corpus actually supplies; inventing regimes for the others would have been exactly the armchair guessing the Fourth Commandment forbids. No watchdog was built: the supervisor judges readings it is handed and keeps no history of its own, so the process that COUNTS a gate's flips does not exist yet and is named here as the next thing this pack needs. Observation-7's two halves in tick_engine stand untouched, and Observation-5's chemical reads with them.

WHY THE DISTINCTION IS WORTH A PACK RATHER THAN A CONVENTION, stated once and at length because the whole slice rests on it. A mode and a fault look alike from a distance - both are named conditions a construct can be in - and the entire difference is in what the system is entitled to DO about them. A MODE HAS A TRANSFER FUNCTION: while it holds, the construct runs a different machine, on purpose, and its behaviour in that mode is part of the design. A FAULT HAS NO TRANSFER FUNCTION AND NEVER WILL: it is a boundary the construct was not designed to cross, and the only correct response is to say so aloud to somebody who can act. If a fault were admitted as a mode, THE SYSTEM WOULD ACQUIRE A DESIGNED BEHAVIOUR FOR BEING BROKEN, and would then run it silently and indefinitely - which is exactly the failure the corpus's directive exists to prevent. Written as a convention that is a sentence people forget; written as supervisor_faults_are_not_modes/1 it is a refusal that fires, and it fires on the way into every watch so a malformed register can never be reported on as though it were well formed.

THREE OUTCOMES, NOT TWO, AND THE THIRD IS THE POINT. A fault regime is WARNED (a reading crossed its allowance), CLEAN (a reading did not), or UNWATCHED (no watchdog reported at all). A two-outcome design loses the third, and losing it is precisely how a system comes to believe that silence means health. An unwatched regime is reported as unwatched, by name, carrying the watchdog that did not report, beside the warnings rather than hidden among them.

KONNECTOME INVENTS NO THRESHOLD, AND THAT IS A SHAPE RATHER THAN AN OMISSION. Each reading arrives carrying its own ALLOWANCE, supplied by the caller that owns the watchdog. The supervisor compares and reports; it does not decide how much oscillation is too much, because the corpus supplies no such number at this grain and this family's standing rule is that no number in the pack is konnectome's invention. The boundary case is pinned rather than left to the next reader: a value exactly AT its allowance has not crossed it.

THE GATE'S TWO FAULT REGIMES ARE THE CORPUS'S OWN. They are taken from the Layer 8 modes volume's Entry 36, the mutual-inhibition flip-flop coined the Either-Or Latch - the same entry slice 40 validated the gate against block by block, which names exactly two fault regimes and a watchdog. konnectome takes both and invents no third. OSCILLATION is the latch failing by never settling; STUCK SWITCH is the same latch failing the opposite way, by settling and never leaving. Neither signature is a mode name, and that is CHECKED rather than intended.

Gates at this slice: the new supervisor pack ships TWENTY-SEVEN Prolog Unit (PLUnit) tests and archetype grows from forty to forty-two (the other twenty-nine packs unchanged; SEVEN HUNDRED AND SIX in total across THIRTY-ONE packs, all green); the naming gate reports zero violations across all thirty-one packs and passes its own self-test; the layer, layer-binding and reality-discipline gates all report zero violations; the capstone story is byte-identical to main's. WATCH-3 WAS EXAMINED AND REMAINS A WATCH: the design authority's Part Ten filed the supervisor channel as a routing candidate IF a published warning must be a shareable record rather than a konnectome-internal signal. It is not. Every warning this slice produces is computed inside one runtime from readings that runtime was handed, and travels to nobody; it fails the consumer-versus-ontology test and routes nowhere. Nothing in this slice releases the held sections of the causalontology change order. No new wall was hit; nothing in PrologAI or Causalontology was edited, and conformance was not re-measured because neither cousin was touched.


KONNECTOME REALIZATION LOG - SLICE 43

Slice 43 - The ticks-per-second convention: one hundred ticks to the nominal second, declared and refused rather than rounded - 2026-08-09 - pack tick_engine (three exported predicates, no new pack); no world dict key added, no number invented in any construct, and the capstone story pinned byte-identical against a main worktree.

WHAT THE PHASE REQUIRED. Family Four item 2 of the version 2 vision-set analysis, promoted ahead of the stabiliser on the Iridium hand-off's one scheduling suggestion. KONNECTOME HAS NO SECONDS AND THE CORPUS COUNTS IN THEM. Every quantity in the cognitive cluster the corpus specifies is wall-clock - ignition near three hundred milliseconds, admission in the low hundreds, unrehearsed decay in seconds, a board wiped on task switch in under a second - and a settling time read as confidence is wall-clock too, while konnectome's tick is dimensionless-ordinal by the observer's own written declaration. None of that cluster is buildable to the corpus's numbers until a ticks-per-second convention is declared, and the convention is konnectome's own decision, not the corpus's.

WHY IT JUMPED THE QUEUE, recorded because a promotion is itself a decision. The item arrived from THREE independent directions: beneath Theme Four and Theme Five in the version 2 analysis, and again from a direction nobody predicted when slice 42 found that a watchdog cannot have a window without a unit to measure one in - which is why OBSERVATION-9 was left open rather than closed. The stabiliser, the queue's front, publishes a scalar whose grains must be stated, and a grain argued in seconds cannot be stated in a build with no seconds. THE RULE THIS APPLIES: A CHEAP ITEM THAT ARRIVES FROM A THIRD INDEPENDENT DIRECTION HAS STOPPED BEING A QUEUE ITEM AND HAS BECOME A DEPENDENCY.

WHAT THIS SLICE DELIVERS, AND IT IS DELIBERATELY SMALL. No new pack. tick_engine gains three exported predicates: tick_engine_ticks_per_second/1, the convention stated once and in one place, and the two conversions tick_engine_ticks_from_milliseconds/2 and tick_engine_milliseconds_from_ticks/2. This is a DECISION RECORDED WITH TESTS ASSERTING IT, in the shape of slice 39's naming-collision resolution and slice 41's DECISION-1 - a decision closes a question, where a mechanism answers one.

DECISION-2: ONE HUNDRED TICKS TO THE NOMINAL SECOND, SO ONE TICK IS TEN NOMINAL MILLISECONDS, AND THE SCHEDULER STAYS DIMENSIONLESS. It is konnectome's own decision and must never be presented as the corpus's instruction. The corpus says nothing whatever about how often a synthetic mind should update, because it describes a continuous system and not a scheduler: this is not a silence to be resolved by reading harder, it is a question the source does not contain.

THE THREE REASONS, ALL KONNECTOME'S OWN. FIRST, EVERY CORPUS CONSTANT BECOMES A WHOLE NUMBER OF TICKS - three hundred milliseconds is thirty ticks exactly, one hundred is ten, one second is one hundred, half a second is fifty. A rate leaving a constant between two ticks would force a rounding at every site that used it, AND A ROUNDING IS AN INVENTED NUMBER WEARING A SMALL COAT: the constant leaves the corpus as the corpus's and arrives in the pack as konnectome's, with nobody having decided anything. SECOND, IT IS THE COARSEST RATE THAT ACHIEVES THE FIRST, and coarsest is the honest choice rather than the convenient one, because the corpus states these quantities to a hundred-millisecond grain and a millisecond tick would claim a precision the source does not have. THIRD, IT COSTS THE RUNNING MACHINE NOTHING AND IS THEREFORE REVERSIBLE: no existing behaviour moves by a single count, and every duration konnectome ever expresses in real units passes through ONE conversion predicate, so a session that must restate the rate has one predicate to change and one test to re-read rather than a search across thirty-one packs.

THE REFUSAL IS THE LOAD-BEARING HALF. A DURATION THAT IS NOT A WHOLE NUMBER OF TICKS IS REFUSED ALOUD with domain_error(whole_tick_duration, Milliseconds) and is NEVER ROUNDED. Rounding is exactly how invented numbers enter a careful build - a caller asks for five milliseconds, is handed one tick without being told, and a number that was the corpus's has silently become konnectome's. The refusal forces that decision into the open where it can be recorded. Both conversions also refuse an UNBOUND argument, the unbound-wrong-judgement lens paying for an ELEVENTH SLICE RUNNING (33 through 43).

WHAT DECISION-2 DOES NOT DECIDE. It does not make konnectome real-time: nothing sleeps, nothing measures, nothing consults a wall clock, and a tick is not ten milliseconds long - ten milliseconds is what a tick is CALLED when a duration must be stated, which is pinned by a test asserting the heartbeat still reads five after five ticks. It does not decide that any particular corpus constant is right. It does not decide the observer's stamp, which keeps its own nominal one-second-per-tick carrier and is now numerically at odds with the convention - recorded as OBSERVATION-10 rather than quietly fixed, because restating that stamp would re-mint every content-addressed token occurrence identifier in the repository. It does not decide a window length for any watchdog: it supplies the UNIT a window can be stated in, and OBSERVATION-9's closer must still choose the number and say why. And it does not decide whether a construct may hold a rate of its own.

Gates at this slice: tick_engine grows from eight Prolog Unit (PLUnit) tests to fifteen (the other thirty packs unchanged; SEVEN HUNDRED AND THIRTEEN in total across THIRTY-ONE packs, all green); the naming gate reports zero violations across all thirty-one packs and passes its own self-test; the layer, layer-binding and reality-discipline gates all report zero violations; the capstone story is byte-identical to main's. No new wall was hit, and nothing routes to either change order: a conversion between two of konnectome's own units is computed inside one runtime and travels to nobody, so it fails the consumer-versus-ontology test of the Second Commandment, and it needed no language feature PrologAI lacks. Nothing in this slice releases the held sections of the causalontology change order. Nothing in PrologAI or Causalontology was edited, and conformance was not re-measured because neither cousin was touched - the figure remains four sessions old and must be re-measured before it is quoted.


KONNECTOME REALIZATION LOG - SLICE 44

Slice 44 - The stabiliser: hysteresis published as a scalar, and the gate becomes the corpus's latch - 2026-08-09 - pack stabiliser (new) and pack archetype (the gate gains a second boundary); no world dict key added, no number invented, no number changed, and the capstone story pinned byte-identical against a main worktree.

WHAT THE PHASE REQUIRED. The mode-register design authority's Part Nine slice six, and the closer OBSERVATION-8 named when it was opened. THE CORPUS IS EXPLICIT THAT HYSTERESIS IS NOT PART OF THE SWITCH: the Layer 11 reading states in one sentence that "this is a stabiliser, not a switch, so it has no poles of its own", and that it publishes a scalar stability bias onto the configuration bus. konnectome's hysteresis was two constants inside the two-process governor's parameter block and one symmetric threshold inside the gate - the stickiness buried in the switch, which is the arrangement the corpus names and rejects. Slice 40 found the gap and recorded honestly that it could NOT be fixed where it was found, because adding a second constant inside archetype_gate/4 would have modelled the corpus wrongly while appearing to fix it.

WHAT THIS SLICE SPECIFIES. A NEW PACK, stabiliser, AT LAYER ZERO, and one existing rule given a second boundary.

WHAT A STABILITY BIAS IS, WITH ITS UNITS STATED, because a scalar without stated units is an invitation to misuse. IT IS HOW FAR APART THE TWO BOUNDARIES SIT: half the width of the hysteresis band, expressed in the units of whatever quantity the switch it steadies compares. A gate comparing a switching drive reads it as an offset on the gate's threshold; a governor comparing a sleep-pressure balance reads it as the half-width of its band about a midpoint. One meaning, two consumers, no conversion.

DECISION-1 IS APPLIED AND NOT RE-OPENED. A hysteresis scalar is a BIAS, so it travels as a LEVEL under a plain-atom key on the channel konnectome has had since slice 4, and never as a thrown mode. That is DECISION-1 applied rather than a new decision, and it is proved rather than asserted: the published value reads back through the bus's ORDINARY level read, with nothing about it needing the stabiliser to interpret it.

AND THE STABILISER'S AGENCY IS STATED, because after slice 41 a publisher has two shapes available and they are not the same thing. Its agency is broadcast_bias, AND IT IS AN AGENCY IN NO TRANSITION TABLE, which is the sharpest available statement of what a bias is. Since slice 40 a construct is moved ONLY by a row of its own register's table. THE STABILISER MOVES NOTHING. It changes where a boundary sits, after which the construct's own row fires, or does not, by its own rule. A test reads the gate's table, checks it is not empty so the test cannot pass vacuously, and asserts the stabiliser's name is absent.

DECISION-3: THE PUBLISHED SCALAR GOVERNS THE CONSTRUCT GRAIN ONLY. The version 2 analysis named this the sharpest ambiguity of its visit, because THE CORPUS ANSWERS THE HYSTERESIS QUESTION DIFFERENTLY AT TWO GRAINS - the Layer 11 reading puts the stickiness OUTSIDE the switch at the construct grain, while Layer 8 Chapter 16 gives winner-take-all's transfer function as a hysteretic max operation with an explicitly hysteretic Commitment Mode, stickiness INSIDE the motif. Both are literal. konnectome's three reasons are its own: the two readings stop conflicting once the grain is named; a bias reaching inside a transfer function would make a motif's own law depend on a global value, which is the coupling this build has spent slices avoiding; and it is the reversible choice, because a roster can be extended where a scalar already reaching everywhere could not be withdrawn. THE DECISION IS A REFUSAL AND NOT A COMMENT: the motif grain is turned away by name.

OBSERVATION-8 IS CLOSED, AND AT A BIAS OF ZERO NOTHING MOVES. archetype_gate_hysteretic/5 gives the gate two boundaries where it had one, and at a bias of zero the two coincide and the latch is the old symmetric toggle exactly, in every combination of mode and drive. Because the bus answers zero for a bias nobody has published, every switch in the repository can consult the stabiliser without any of them changing behaviour - which is what made this slice safe to build and why the capstone story is byte-identical.

THE MAPPING KONNECTOME CHOSE, AND THE ONE IT REJECTED. The corpus states hysteresis for a LEVEL comparator, and archetype's gate compares a SWITCHING DRIVE, which is a push rather than a level; the two do not map by themselves. konnectome chose: LEAVING A MODE COSTS THE RAISED BOUNDARY, WHICHEVER MODE IT IS, so over the whole range between the threshold and the threshold plus the bias both modes are stable and history decides which is realised - the corpus's own definition of the property, quoted almost word for word. THE REJECTED READING IS RECORDED because it is the one a later session will reach for first: making turn-on cost the threshold plus the bias and turn-off cost the threshold MINUS it reads exactly like the corpus's higher-and-lower boundaries, and with a push-drive it makes CLOSED ABSORBING over the whole middle range. A gate that settles and never leaves is the corpus's own STUCK SWITCH fault signature, listed in this pack's fault block since slice 42, and a hysteresis design that manufactures a fault regime is not a hysteresis design.

THE GOVERNOR'S TWO CONSTANTS ARE NOW DERIVED RATHER THAN DECLARED, AND NOT ONE NUMBER CHANGED. Sixteen and two are restated as what they always were - a band about a midpoint, a midpoint of nine and a half-width of seven - because replacing them with different numbers would be konnectome inventing where it had no need to. The suite proves the derivation reproduces the original block field for field AND that a governor standing on the derived band runs an identical twenty-four-tick day, state for state, because identical parameters are only evidence and identical behaviour is the proof.

AN HONEST INTERACTION, PINNED RATHER THAN LEFT TO BE DISCOVERED. The two consumers have DIFFERENT FLOORS. A gate reads a bias of zero as "no hysteresis" and behaves as it always did; the governor cannot, because its own guard has refused a flat or inverted band since it was built, and a flat band chatters. The same scalar is therefore legal at zero for one consumer and refused at zero by the other, and a test pins the governor's refusal by its own name. That is not a defect: it is what it looks like when two constructs share a scalar and each keeps its own domain.

WHAT THIS SLICE DELIBERATELY DID NOT BUILD. THE CORPUS GATES THE STABILISER BY CIRCADIAN PHASE AND HUNGER, AND NEITHER GATING IS BUILT: hunger has no construct in konnectome at all, and gating by phase would require a function the corpus does not supply at this grain, so the scalar is published by a caller who chooses it and the gating is deferred BY NAME rather than invented. No construct was rewired to consult the stabiliser - the gate and the governor can now be handed a bias, and the join belongs to the caller, as every join in this family has since slice 41. No motif-grain hysteresis, per DECISION-3. And OBSERVATION-9 stands exactly where slice 42 left it: the stabiliser is not a watchdog and counts nothing.

Gates at this slice: the new stabiliser pack ships EIGHTEEN Prolog Unit (PLUnit) tests and archetype grows from forty-two to fifty (the other thirty packs unchanged; SEVEN HUNDRED AND THIRTY-NINE in total across THIRTY-TWO packs, all green); the naming gate reports zero violations across all thirty-two packs and passes its own self-test; the layer, layer-binding and reality-discipline gates all report zero violations; the capstone story is byte-identical to main's. Nothing routes to either change order: a scalar published inside one runtime and read by two constructs in the same runtime travels to nobody and fails the consumer-versus-ontology test of the Second Commandment, and no language feature was missing. Nothing in this slice releases the held sections of the causalontology change order. No new wall was hit; nothing in PrologAI or Causalontology was edited, and conformance was not re-measured because neither cousin was touched.


KONNECTOME REALIZATION LOG - SLICE 45

Slice 45 - The first real watchdog: what the tests found, and the lens that found it - 2026-08-09.

TWENTY-NINE PROLOG UNIT (PLUNIT) TESTS IN A NEW PACK, AND NO EXISTING PACK'S COUNT MOVED. The suite is written test-first in the house discipline, and it is deliberately weighted toward the two things that would make this pack worse than useless: a reading that is wrong, and a reading that is confidently premature.

THE FIVE NAMED LENSES ALL PAID AGAIN, AND ONE OF THEM PAID IN A NEW WAY.

THE UNBOUND-WRONG-JUDGEMENT LENS PAYS FOR A THIRTEENTH SLICE RUNNING, 33 through 45, four times in this slice's new code: an unbound history, an unbound tick, an unbound mode, and an unbound allowance are each refused aloud rather than bound to whatever the walk happened to want. The allowance case is the sharpest: both allowances are judged in the ONE place both readings come through, rather than at the comparison, where a hole would have been answered by an arithmetic error naming nothing useful.

THE DECLARATION-ORDER LENS PAID IN A FORM IT HAS NOT TAKEN BEFORE, AND IT IS WORTH RECORDING BECAUSE IT GENERALISES. The first version of the flip counter had three clauses - the empty list, the one-element list, and the two-or-more list - which is the obvious shape and is correct. It also left a CHOICE POINT behind on every reading, because first-argument indexing sees only the principal functor and cannot tell a one-element list from a longer one. The tests said so out loud: EIGHT of them passed with a warning that they had succeeded with a choice point. The walk was rewritten to split in two, so that each predicate decides on the SHAPE of its first argument alone - empty or not - and the warnings went away. THE GENERAL FORM: A WALK THAT IS DETERMINISTIC ONLY BECAUSE NOBODY ASKED IT FOR A SECOND ANSWER IS NOT DETERMINISTIC, and the difference is invisible until something backtracks into it. That is the declaration-order lens's own argument - right for the wrong reason is not right - arriving at clause selection rather than at a lookup table.

THE KEY-SHAPE ALIASING LENS held: a mode is an atom, a boundary signature is an atom, and a reading is keyed by the signature the fault block declares, so the watchdog cannot file a reading under a name the gate does not recognise. The join tests prove this by construction rather than by inspection - they hand the readings to the real supervisor and read back the real report.

THE DEFAULT-DRIFT LENS is what DECISION-4's second half IS. The default a lazier design would have taken - answer whatever the short history happens to show - is refused by name.

THE BEHAVIOUR-PINNED-IDENTICAL LENS: the capstone story is byte-identical to main's, which it must be, because this slice edits no existing pack and nothing consults the watchdog yet.

THE SIXTH CANDIDATE LENS, THE ABSORBING-STATE LENS, WAS APPLIED AND DID NOT PAY, AND THAT IS RECORDED HONESTLY RATHER THAN DRESSED UP AS A SECOND PAYMENT. Slice 44 earned it by catching a hysteresis mapping that made the closed mode absorbing, and named it as a candidate that had paid ONCE against a standing bar of twice. Here it was turned on the watchdog's own two boundaries and asked whether any history could cross both at once - which would have meant a construct simultaneously flipping too fast and stuck, and therefore a pair of readings that could never be acted on coherently. IT FOUND NOTHING WRONG: over any window, maximum flips forces minimum hold, and the property holds by construction. A lens that is applied and confirms a design has not PAID in this repository's sense, which is that it caught something a careful reader would have missed. So the candidate stands at one payment still, and the next session inherits the same promotion question this one did. Recording an application that found nothing is the only way that bar stays a bar; a lens credited for every use of it would be promoted by usage rather than by value.

WHAT THE TESTS PIN THAT A READER MIGHT OTHERWISE ASSUME. That a span includes both its ends, so one observation spans one tick and a mode just entered has been held one tick. That a hold is measured in TICKS and not in observations, proved with a sampled history observing ticks one, five and nine and reading a hold of nine. That the window TRAILS the present, proved with a history whose flips all sit outside the trailing window and read as zero. That a window of zero is refused, because a window containing no ticks would report every construct clean forever. And that a duration which does not land on a tick boundary is refused by the tick engine's own name, domain_error(whole_tick_duration, 5), which proves the conversion is genuinely delegated rather than copied.

THE CENTRAL TEST OF THE SLICE, NAMED SO IT CAN BE FOUND: an_unmeasured_window_leaves_the_gate_unwatched_at_the_supervisor. A four-tick history, a twenty-tick window, the real gate's automaton and the real supervisor - and the report carries no warnings and BOTH boundaries in its unwatched block, with their watchdogs named. It is the test that proves DECISION-4's second half is a mechanism and not a sentence.

AND ITS TWIN, WHICH IS THE ONE THAT CLOSES OBSERVATION-9: a_watched_gate_within_its_allowances_is_clean_and_no_longer_unwatched. Same gate, same supervisor, a history long enough to have measured its window - and the unwatched block comes back EMPTY. That is a sentence konnectome could not say before this slice.


KONNECTOME REALIZATION LOG - SLICE 46

Slice 46 - The master register widened: what the tests pin - 2026-08-09.

TWENTY-EIGHT PROLOG UNIT (PLUNIT) TESTS IN A NEW PACK, AND NO EXISTING PACK'S COUNT MOVED. The suite is weighted toward the one thing a widening can silently break - every reader written before it - and toward the one thing a roster can silently become, which is a list of names.

THE CENTRAL TEST OF THE SLICE, NAMED SO IT CAN BE FOUND: a_widened_state_still_reads_back_as_the_pole_alone. A fully named sleep state is announced through the register, and the reader every caller has used since slice 35 hands back `offline` - not the compound. Two more pin the other two cases: a BARE announcement is unchanged in both directions, and a SILENT bus still reads as waking. Between them they cover every shape the channel can be in, which is what "behaviour pinned identical" has to mean for a channel rather than for a rule.

THE ROSTER'S CENTRAL REFUSAL IS PINNED TWICE, ON PURPOSE. A sub-mode of the OTHER pole is refused - `global_state(online, slow_wave_sleep)` - and a sub-mode of NO pole is refused, and both raise the same error. Testing only the second would have proved the roster is a spell-checker; testing the first is what proves it is a statement about what the system does.

THE DIVISION BETWEEN CHANNEL AND REGISTER IS PINNED FROM BOTH SIDES, because it is a decision and not an accident. One test writes a roster violation through the RAW channel and reads it back unchanged, proving the channel holds no opinion about rosters. Two others prove the channel does refuse what IS structural: a third pole, and a term that is not a widened state at all. A later session tempted to tidy the roster check down into the channel will trip over the first of those three.

THE EMPTY TABLE IS PINNED OVER ALL EIGHT ENTRIES RATHER THAN OVER A CHOSEN ONE, using forall over the register itself, so that adding a ninth entry without an assignment cannot slip past a test that only ever looked at two.

AND THE MECHANISM IS PROVED AGAINST A FIXTURE, WHICH IS THE TEST THIS SLICE WOULD HAVE BEEN WRONG WITHOUT. A suite that drove only the register's own empty assignment would pass vacuously and would prove that nothing happens. So master_register_throw_rows/3 is exported and handed a fixture assignment of two rows, and both are read back off the real mode-throw channel - proving the walk reaches the channel AND that it does not stop after one row. A second test then reads back a kind the assignment never named and gets the channel's reserved silence, which is slice 41's row-not-a-door property re-proved at the grain that now depends on it.

THE FIVE NAMED LENSES. THE UNBOUND-WRONG-JUDGEMENT LENS PAYS FOR A FOURTEENTH SLICE RUNNING (33 through 46): an unbound pole, an unbound state and an unbound announcement are each refused aloud rather than bound to whichever roster entry is listed first. THE KEY-SHAPE ALIASING LENS is why the widened value went under the EXISTING global/1 key rather than a new one: a second key would have created two places to ask one question and a window in which they could disagree. THE DEFAULT-DRIFT LENS is what the two corpus-sourced defaults are for - the alternative was every historical bare announcement quietly meaning something konnectome chose. THE BEHAVIOUR-PINNED-IDENTICAL LENS is the slice's headline and the capstone story is byte-identical. THE DECLARATION-ORDER LENS is why the roster is read with findall in declaration order and the register's size is COUNTED rather than stated: a size restated as 8 would be right today and lucky tomorrow.

AN HONEST GAP IN THE SUITE, NAMED RATHER THAN LEFT TO BE NOTICED. The bus's two new predicates are exercised only from the master register's suite, not from the bus's own. That is deliberate for now - the tests that matter drive them through the caller that owns their meaning - but it means neuromodulator_bus's own count did not move while its interface did, and a session touching that channel next should either add the local tests or record the same reason again.


KONNECTOME REALIZATION LOG - SLICE 47

Slice 47 - The first caller wired to a watchdog: what the tests pin - 2026-08-09.

TWO_PROCESS_GOVERNOR GROWS FROM TWENTY PROLOG UNIT (PLUNIT) TESTS TO THIRTY-FIVE, and no other pack's count moves. The fifteen new ones are weighted almost entirely toward one thing: proving that what was joined is the REAL machine and not a fixture of it.

THE SUITE DRIVES A REAL GOVERNOR THROUGH A REAL DAY. A helper runs two_process_governor_step/4 tick by tick and records each pole it settles at into a real watchdog history, and every join test below uses that rather than a hand-built list. The distinction is the whole slice: a test that built the history by hand would have proved the join and not the wiring.

THE CENTRAL TEST, NAMED SO IT CAN BE FOUND: a_real_governor_running_a_real_day_is_watched_and_clean. Twenty-four ticks of the real scheduler, judged end to end, and the report comes back with no warnings AND AN EMPTY UNWATCHED BLOCK. That empty block, produced by production constructs rather than by a suite's own fixtures, is what slice 45 could not produce and what this slice exists for.

ITS TWIN IS THE ONE THAT PROVES THE HONESTY SURVIVED THE JOIN: a_governor_four_ticks_into_its_first_day_is_unwatched_not_clean. Four ticks against a twenty-four-tick window, and both regimes come back UNWATCHED by name with the stabiliser named as the watchdog that did not report. DECISION-4's second half reached the running machine WITHOUT A LINE OF CODE ARRANGING FOR IT TO: an unspanned window yields no readings, an empty reading list re-keys to an empty reading list, and the supervisor receiving nothing says so. A property that survives composition unassisted is a property that was designed rather than arranged.

THE ALLOWANCE IS CHECKED FOR MEANING AND NOT ONLY FOR CORRECTNESS, which is a distinction this suite had not drawn before. a_healthy_day_contains_at_most_the_allowed_two_crossings runs a day and a half and asserts the real governor sits AT its allowance rather than comfortably inside it. An allowance nothing ever approaches is a number that has never been tested by the thing it governs, and would have passed every other test in this file.

BOTH FAILURE MODES ARE DEMONSTRATED, AND BOTH HISTORIES ARE CONSTRUCTED RATHER THAN RUN - deliberately, and the reason is worth stating: a governor that chatters and a governor that locks are governors konnectome DOES NOT HAVE. That is the point of a watchdog. It exists to notice a fault the machine is not currently committing, so the only way to test it is to hand it a history the machine would not produce. Twenty-three crossings in a day warns under state_chatter against an allowance of two; a whole day at one pole warns under state_locked against twenty-three.

DECISION-6 IS PINNED IN THE FORM THAT MATTERS: the_window_moves_with_the_day. A governor built with a twelve-tick day is watched over a twelve-tick window and a lock allowance of eleven, with no edit anywhere. That test is the difference between a window that is derived and a window that merely happens to equal twenty-four today.

ENTRY 11'S SHARPEST SENTENCE IS MADE CHECKABLE: neither_crossing_is_self_selected. The corpus says the crossings are thrown by Process S and Process C and are NOT internal to the switch. The test first asserts the transition table is non-empty, so it cannot pass vacuously - the same guard slice 44 used when proving the stabiliser's absence - and then asserts no row anywhere carries self_selected.

AND THE BEHAVIOUR-PINNED-IDENTICAL PROMISE IS CHECKED AGAINST THE DAY ITSELF rather than asserted: the_watched_day_is_the_same_day_the_governor_has_always_run reads back seventeen ticks of waking before the switch snaps at the eighteenth, which is the day slice 37 built and slice 44 re-derived without changing a number. The capstone story is byte-identical.

THE FIVE NAMED LENSES. THE UNBOUND-WRONG-JUDGEMENT LENS PAYS FOR A FIFTEENTH SLICE RUNNING (33 through 47): an empty history and a history standing at a third pole are both refused aloud, by name, before a supervisor can judge a machine standing nowhere. THE KEY-SHAPE ALIASING LENS is what the re-keying is about - two constructs sharing a measurement while keeping their own boundary names. THE DEFAULT-DRIFT LENS is DECISION-6 itself: the alternative was a constant that would silently stop matching the day it was chosen for. THE DECLARATION-ORDER LENS is why the register, the table and the fault block are all gathered in declaration order and the register's size is counted rather than stated. THE BEHAVIOUR-PINNED-IDENTICAL LENS is pinned twice, by the day and by the capstone.

AN HONEST NOTE ON WHAT THE SUITE DOES NOT PROVE. It does not prove the capstone is watched, because the capstone does not call any of this. Every construct in the chain is real; the demonstration that would make a told story say out loud whether the night was healthy is a separate slice, and this suite should not be read as having done it.


KONNECTOME REALIZATION LOG - SLICE 48

Slice 48 - The bus's six named broadcast channels: a roster that declares and does not guard - 2026-08-10 - pack neuromodulator_bus (a roster of six channels with their roles, sources and cognitive tags); no new pack, no existing call changed, no number invented, and the capstone story byte-identical.

THE LENSES, AND WHICH OF THEM PAID.

THE UNBOUND-WRONG-JUDGEMENT LENS PAID AGAIN, SIXTEEN SLICES RUNNING (33 through 48). Both new entry points refuse an unbound name before any lookup could reach it, and the resolution's refusal is the load-bearing one: a lookup over a six-row table would otherwise bind an unbound name to the first channel declared and hand a caller who named nobody the roster's first answer.

THE DECLARATION-ORDER LENS PAID IN THE FORM SLICE 45 FOUND IT IN - AT CLAUSE SELECTION. A four-table roster written as plain facts is the exact shape that leaves a choice point behind on every keyed read. A test asserts that a keyed read is DETERMINISTIC and not merely correct on its first answer, because a walk that is deterministic only because nobody asked it for a second answer is not deterministic.

THE DEFAULT-DRIFT LENS IS WHAT DECISION-7'S REFUSAL IS. Three of the six channels have a number in the corpus that a hurried reader would install as a resting level; all three are initial values inside one chapter's worked pseudocode, and two channels have no such number at all. A default with a borrowed warrant is an invented value that has learned to cite - met as a NUMBER at slice 45, as a MODE NAME at slice 46, and as a LEVEL here.

THE KEY-SHAPE ALIASING LENS DECIDED THE SPELLING QUESTION. The alternative to resolution was a second atom key carrying the corpus's word, which is aliasing in its purest form: two keys, one meaning, and a bus that cannot tell them apart because it was never designed to. The lens's usual verdict is to keep one key; here it also names the cost of not doing so, which is a plausible zero rather than a wrong answer.

THE BEHAVIOUR-PINNED-IDENTICAL LENS IS THE WHOLE SLICE. No existing call changed; the capstone story is byte-identical; and a test broadcasts and reads the stabiliser's non-roster key to prove that the one thing a roster could have broken still works.

AND A SUITE HABIT WORTH KEEPING, IN THE FORM SLICE 47 ESTABLISHED IT. A test asserts not only that the corpus's word RESOLVES to the right channel but that the UNRESOLVED word would have found a plausible zero on a bus carrying a real level. The second assertion is the reason the first exists, and a suite that proved only the mechanism would have left the reason undocumented in the one place a later reader would look.


KONNECTOME REALIZATION LOG - SLICE 49

Slice 49 - The working-memory blackboard, built to the corpus's own constants - 2026-08-10 - NEW PACK working_memory_blackboard; DECISION-8, the activation grain; OBSERVATION-13, a chapter that disagrees with itself about time; nothing wired into the tick, and the capstone story byte-identical.

THE FIVE NAMED LENSES ALL PAID, AND THE SIXTH CANDIDATE DID NOT APPLY.

THE UNBOUND-WRONG-JUDGEMENT LENS IS NOW SEVENTEEN SLICES RUNNING (33 through 49), AND IT PAID FOUR TIMES IN ONE PACK. A board is a keyed list of plain facts, which is precisely the shape that answers a hole with its first row. An unbound BOARD would have unified with the empty board and reported a blank surface; an unbound ITEM read would have answered with whichever slot happened to be listed first; an unbound GATE VERDICT would have opened the gate by accident, since a hole matches anything a comparison offers it; and an unbound REHEARSAL TARGET would have refreshed the first slot on the board and told nobody. All four are refused aloud, and each has its own test.

THE DEFAULT-DRIFT LENS IS THE SLICE'S THIRD REFUSAL AND ITS SHARPEST. The obvious wiring for this pack is one maintenance step per tick, and it is available, cheap, and WRONG AGAINST THE CHAPTER'S OWN NUMBERS. A full slot survives seventeen steps; at one step a tick that is one hundred and seventy nominal milliseconds, while the same chapter's TIME CONSTANTS field states the unrehearsed fade as two to twenty SECONDS. A rate chosen here to reconcile the two would be konnectome's invention wearing the corpus's warrant - a default with a borrowed warrant arriving for the FOURTH time in five slices, met as a NUMBER at slice 45, a MODE NAME at slice 46, a LEVEL at slice 48, and a RATE here. It is refused a fourth time: there is deliberately no predicate in this pack binding a step to a tick, and the absence is documented as the deliberate part.

THE KEY-SHAPE ALIASING LENS DECIDED THE ABSENCE WORD. The rehearsal target must be able to say NOTHING, and the two candidate spellings for nothing were an unbound argument and a named atom. An unbound argument is the aliasing failure in its purest form - the absence and every possible item share one shape - so the pack uses the atom none, AND REFUSES TO ADMIT AN ITEM CALLED none, because a word cannot mean absence and name a slot on the same surface.

THE DECLARATION-ORDER LENS PAID AT EVICTION, in the form slice 45 found it in. Four slots at equal activation is a four-way tie, and a minimum over a list answers a tie with whichever row is first - which is an answer, and would have been the right one, and would have been right by accident. The pack DECIDES the tie in the open (the earliest-admitted of the equally weak leaves, which is what the corpus's own code does, since a minimum over a Python dictionary returns the first key in insertion order), commits to it with a cut so no choice point survives, and a test asserts the eviction is DETERMINISTIC rather than merely correct on its first answer.

THE BEHAVIOUR-PINNED-IDENTICAL LENS IS FREE HERE AND IS STILL CHECKED. The pack is new and wired to nothing, so no existing call could have moved; the capstone story was captured before the first edit and is byte-identical after the last.

THE ABSORBING-STATE LENS, THE SIXTH CANDIDATE, STILL STANDS AT ONE PAYMENT. The empty board looks absorbing and is not: an admission through an open gate leaves it at once. The promotion question passes to the next session unchanged.

ONE REFINEMENT IS RECORDED AS A DIVERGENCE FROM THE CHAPTER'S PRINTED CODE RATHER THAN A CORRECTION OF IT. Re-offering an item the board already holds UPDATES that slot to full and evicts nothing, where the chapter's Python would evict a rival to make room for an item already present. The chapter's own prose is the warrant: it names the basal-ganglia gate as "deciding, under dopamine, which items are ADMITTED OR UPDATED".


KONNECTOME REALIZATION LOG - SLICE 50

Slice 50 - The working-memory mode register - 2026-08-10 - NEW PACK working_memory_mode_register; DECISION-9; OBSERVATION-14.

WHAT THE TESTS FOUND, AND THEY FOUND IT IN TWO PLACES.

FIRST, THE NAMING GATE CAUGHT AN ARCHITECTURAL MISTAKE, WHICH IS NOT WHAT IT IS FOR. The first draft's multifile clauses read, to the gate, as this pack defining a predicate belonging to another pack, and the gate refused them. The refusal was formally about a prefix and substantively about a boundary being crossed, and re-examining the crossing showed the design behind it was wrong. A gate written for one purpose caught a fault of a different kind because both faults have the same shape at the source level.

SECOND, A PASSING TEST IN ANOTHER PACK BECAME A LIE WITHOUT FAILING. master_register's suite asserted that the downward assignment table was empty for every state. That assertion had been true and correct. Under the first draft it would have gone on PASSING while measuring nothing but which packs the suite happened to load. It was replaced rather than deleted, and the three different kinds of emptiness that remain - no individual to address, no chapter has spoken, the corpus assigned this elsewhere - are now pinned separately, because a single test covering all three would have hidden the day one of them changed.

AND THE FIVE NAMED LENSES PAID AGAIN. Unbound-wrong-judgement: an unbound mode key would have returned the first declared mode's rule. Default drift: OBSERVATION-14, and the automaton's starting position, which is pinned apart from what a caller actually gets so it cannot be quietly promoted into a default. Declaration-order: the chapter's order is asserted as an order and not as a set. Key-shape aliasing: a throw naming a foreign mode is refused by this register rather than passed on, because the channel guarantees a well-formed atom and only the register knows which atoms are its modes. Behaviour-pinned-identical: every sibling suite re-run.


KONNECTOME REALIZATION LOG - SLICE 51

Slice 51 - The told story that says whether the night was healthy - 2026-08-10 - no new pack.

WHAT THE TESTS FOUND. The test that matters here is the one asserting an ABSENCE: the short telling must never print the sentence that would claim health over ticks it did not span. A test for the presence of the honest verdict would have passed even if the dishonest one were printed beside it.

AND THE TEST RE-DERIVES THE NIGHT RATHER THAN TRUSTING THE NARRATION. It lives the boot world's governor forward with its own local walk, so it checks the mind's trajectory instead of re-running the very predicate it is meant to be checking.

capstone_demonstration grows from 60 tests to 66; working_memory_mode_register carries 31; master_register grows from 28 to 31. NINE HUNDRED AND ELEVEN tests across THIRTY-SIX packs, all green.


KONNECTOME REALIZATION LOG - SLICES 52 AND 53

Slices 52 and 53 - 2026-08-10 - DECISION-10, DECISION-11, OBSERVATION-16; OBSERVATION-15 half-closed.

WHAT THE TESTS FOUND, AND THE SHARPEST OF IT IS ABOUT A TEST RATHER THAN ABOUT CODE.

A TEST HAD PINNED THE DEFECT, AND IT HAD PASSED FOR NINETEEN SLICES. test(the_scaling_factor_floors_at_zero) drove an over-active region to a negative raw factor and asserted the resulting weight was exactly zero, describing it as "silenced, not inverted". It was testing, faithfully and in detail, a behaviour the corpus forbids in as many words. A TEST THAT PINS A DEFECT IS WORSE THAN NO TEST: it converts the defect into a promise, and it makes the fix arrive looking like the regression. It was replaced rather than deleted and a note stands in its place.

WHAT REPLACED IT IS A PROPERTY RATHER THAN A VALUE. The strongest of the five new tests asserts that two weights into one loud region KEEP THEIR RATIO through a legal scaling. That is the property the corpus says multiplicative scaling exists for, it is the property the old floor destroyed, and it is a better guard than any assertion about a particular weight because it cannot be satisfied by accident.

THE OTHER SIDE OF THE SAME LESSON. A test asserts that a factor of EXACTLY ZERO is refused, because zero was the one value the old code produced deliberately and it is the value that does the damage. A boundary test aimed at the value a previous implementation preferred is worth more than one aimed at a value nobody ever reached.

AND ONE FINDING CAME FROM RUNNING THE CORPUS RATHER THAN KONNECTOME. OBSERVATION-16 was established by executing the chapter's own printed program at both of its own stated gains and observing that one of them does not produce the outcome the chapter claims. Running a source's illustration against its own prose is a cheap check this build had not previously made a habit of, and it found a contradiction on its first use.

plasticity_engine grows from 82 tests to 86; renewal_loop carries 16. NINE HUNDRED AND THIRTY-ONE tests across THIRTY-SEVEN packs, all green.


KONNECTOME REALIZATION LOG - SLICES 54 AND 55

Slices 54 and 55 - 2026-08-10 - DECISION-12; OBSERVATION-15 CLOSED.

WHAT THE TESTS FOUND, AND THE BEST OF IT IS A TEST WRITTEN FOR AN ABSENCE.

THE TEST THAT MATTERS MOST IN THIS PAIR ASSERTS THAT SOMETHING GOES WRONG. It shows that a night which does NOT maintain a durable mark destroys it, and that a full day of maintenance afterwards cannot bring it back. Nothing in the build does that today, because the tier is unwired - so the test is not guarding current behaviour, it is guarding a future mistake.

THAT IS SLICE 51'S LESSON APPLIED BEFORE THE MISTAKE RATHER THAN AFTER IT. Slice 51 found a watchman, a watchdog and their wiring all built, all tested and never called, and recorded that a capability nobody calls is indistinguishable from one that was never built. The defence against repeating that is not to wire the new thing hastily. It is to make the ABSENCE something a reader meets as a passing test.

AND ONE TEST PINS A LOSS ON PURPOSE. Restricting the renormaliser to the excitatory contacts gives up a property uniform scaling had: the excitation-to-inhibition balance is no longer preserved, and the bound can now drive a construct net negative. A test asserts exactly that, so a later session meets it as a decision rather than as a bug to be corrected back.

plasticity_engine grows from 86 tests to 91; offline_consolidation from 27 to 32. NINE HUNDRED AND FORTY-ONE tests across THIRTY-SEVEN packs, all green.


KONNECTOME REALIZATION LOG - SLICES 56 AND 57

Slices 56 and 57 - 2026-08-11 - DECISION-13, DECISION-14, OBSERVATION-17.

WHAT THE TESTS FOUND, AND THE SHARPEST OF THEM ASSERTS THAT THE MECHANISM CANNOT TELL TWO THINGS APART.

THE TEST THAT MATTERS MOST IN THIS PAIR SHOWS A SUCCESS AND A NON-EVENT SCORING IDENTICALLY. A deliberate steer around a real obstacle, which the machine really performed and really predicted, produces the same cue term as holding still in a still world. Both score a total match. A comparator-only sense of agency would therefore be MOST confident of its own authorship at the moment it had done nothing, and the assertion in the suite is a term equality between the two - the cheapest possible statement of a fault that would never have failed.

AND A TEST ASSERTS AN ABSENCE, WHICH IS THE ONLY WAY TO PIN A REFUSAL. Asking the comparator for a verdict of agency throws a named error, and the suite asserts the throw. A refusal written into a comment can be read past; a refusal written into a test is met by the next session as something that passes.

THE SECOND SLICE'S TESTS ARE THE MEASUREMENT THAT FACULTY 13 WAS MISSING, AND THEY MEASURE RATHER THAN DEMONSTRATE. Interleaved replay against catastrophic forgetting has been tested since slice 38 - qualitatively, one weight lower than another. It is now SCORED: the old competence retains 0.945 of itself when the night interleaves it and 0.9 when the night replays only the new day, and the criterion is met by the strict inequality between those two rather than by either number clearing a bar.

AND ONE TEST PINS AN HONEST LOSS BESIDE THE WIN. Measured against no night at all, the old competence's score FALLS even when the night serves it as well as it can be served, because renormalisation is the night's other job. Quoting the benefit without that would be reporting a machine konnectome does not have.

efference_copy ships with NINETEEN tests; offline_consolidation grows from 32 tests to 39. NINE HUNDRED AND SIXTY-SEVEN tests across THIRTY-EIGHT packs, all green.


KONNECTOME REALIZATION LOG - SLICE 58

Slice 58 - 2026-08-11 - DECISION-15, OBSERVATION-18; NEW PACK conflict_monitor.

WHAT THE TESTS FOUND, AND THE SHARPEST OF THEM IS A TEST FOR A WAY THIS MIND COULD KILL ITSELF WITH EVERY OTHER TEST GREEN.

THE TEST THAT MATTERS MOST IN THIS SLICE ASSERTS THAT A RAISED THRESHOLD CAN NEVER SUPPRESS THE INVIOLABLE DRIVE. It was written because building the loop made the hazard visible: the loop raises the boundary a drive must clear, and run far enough on a conflicted mind the boundary passes above respiration's distress. The failure mode is a mind that stops breathing in order to stop feeling conflicted, and NOTHING IN THE SUITE BEFORE THIS SLICE WOULD HAVE FAILED. This is the seventh consecutive finding of that shape, and the answer is a refusal in code rather than a caution in a comment.

AND THE SAFETY TEST IS PINNED AGAINST BEING VACUOUS. A second test puts the SAME drive at an ordinary rank, raises control by the same amount, and asserts that it IS muted. Without that counterfactual the first test could pass because the raise never bit at all; with it, the exemption is shown to be the thing doing the work. A third test asserts that the inviolable rank is the rank respiration actually occupies, so the exemption and the safety property cannot drift apart.

THE ACCEPTANCE TEST IS AN ABLATION AND NOT A THRESHOLD. The Gratton effect is tested by running the IDENTICAL incongruent trial twice - once after a congruent trial, which recruited no control, and once after an incongruent one, which did - and asserting the strict inequality between the two conflicts. Nobody chose a passing number.

AND ONE TEST TURNS A MISSING CONSTANT INTO A PROPERTY. The gain konnectome refuses to choose is quantified over: the same inequality is asserted at FOUR gains spanning two orders of magnitude, which is the statement that the loop's correctness is monotone in a number this build never has to supply. A test that ranges over the unknown is worth more than a test that picks a value and calls it representative.

THE REFUSALS ARE TESTED AS REFUSALS. A gain of zero and a negative gain are each refused BY NAME and the suite asserts the throw, because a silently open loop looks exactly like a closed one from the outside. A non-numeric gain, an unbound threshold and a missing supervisor allowance are refused at the door.

AND THE MEASURE IS TESTED BY ITS PROPERTIES RATHER THAN BY ITS VALUES. Zero below two active drives; zero when all active drives agree; rising with the number of competitors; rising with their strength. Those four are what earn the pairwise form, and they are what any replacement form would have to keep.

conflict_monitor ships with TWENTY-THREE tests. NINE HUNDRED AND NINETY tests across THIRTY-NINE packs, all green.


KONNECTOME REALIZATION LOG - SLICE 60

Slice 60 - The conflict monitor's mode register, and the supervisor leg closed - 2026-08-11 - DECISION-16; OBSERVATION-18 CLOSED.

WHAT WAS TIGHTENED, AND WHAT THE TESTS PIN. The sharpest test in this slice asserts that a mode konnectome very nearly wrote is NOT in the register.

THE TEST THAT MATTERS MOST PINS A REFUSAL THAT WOULD HAVE PASSED REVIEW. Ask this construct for the transfer function of proactive_control and the suite asserts the throw: existence_error, mode_entry, proactive_control. Braver's dual mechanisms framework - proactive against reactive control - is where the current literature on cognitive control lands first, and konnectome's own corpus calls them "two temporal modes" at Chapter 52.3.3. They are the modes of the LATERAL PREFRONTAL CORTEX, which RECEIVES the conflict signal; Chapter 52.2.1 says the cingulate "does not exert control at all". A register built from them would have looked defensible in review, so the wrong answer is written into the suite as a failure rather than into the record as a caution.

AND THAT REFUSAL IS ONLY TESTABLE BECAUSE THE LOOKUP TAKES THE LONG WAY. The transfer lookup goes through the built automaton and the shared checker rather than off the local clause. A direct read would have answered a bare no-match, and a bare no-match is not a refusal - it is silence that looks like one.

THE BORROWING IS PINNED AS A FACT. A test asks the pack whose register it borrowed and asserts the answer is anterior_cingulate_cortex. DECISION-16 grants the borrowing on the condition that it be declared, and a condition recorded only in a comment is a condition nothing enforces.

THE EXPANSION IS PINNED IN BOTH DIRECTIONS, WHICH IS THE UNUSUAL PART. One test asserts that the corpus's four rows expand to exactly nine and no more, so the expansion cannot grow by accident. A second test asserts the ROW THAT IS ABSENT: the alarm does not arrive at itself. Testing the count alone would have let the tenth row in; testing the exclusion alone would have let the table drift. Both are cheap and neither would ever have failed by itself.

AND ONE TEST FINDS THAT SLICE 58 HAD ALREADY BUILT A TRANSITION. The corpus's first row - rising response conflict, Watchtower to Taskmaster, hundreds of milliseconds, self-selected - is the loop slice 58 wrote without knowing there was a register for it to be a row in. A test asserts the row is there. Under DECISION-2's hundred ticks to the nominal second, "hundreds of milliseconds" is tens of ticks and "seconds" is hundreds, both whole numbers, so the timescales needed no rounding.

THE FAULT BLOCK'S REPOINTING IS PINNED AGAINST THE REGISTER. One test asserts the block is the corpus's own - both boundary signatures, the one shared warning condition, the one shared watchdog list. A second asserts that NO FAULT SIGNATURE IS ADMITTED AS A MODE, which is what keeps a thing that is watched from becoming a thing the construct can be in.

AND THE HONEST HALF IS PINNED, WHICH IS THE RESULT WORTH REPORTING. Three tests run the watch: one where conflict did not fall, and the supervisor warns; one where the loop worked, and the supervisor is quiet; and one that asserts the regime konnectome CANNOT MEASURE comes back UNWATCHED AND NOT CLEAN. That third test is DECISION-4 reaching a single construct's own fault block, and it is the first time in this build that the clean-versus-unwatched distinction has separated two regimes of the SAME construct.

THE DOOR REFUSALS ARE UNCHANGED AND STILL TESTED. A missing supervisor allowance is refused; readings that are not numbers are refused. The pack never invents an allowance.

conflict_monitor grows from TWENTY-THREE tests to THIRTY-FIVE. ONE THOUSAND AND TWO tests across THIRTY-NINE packs, all green.

KONNECTOME REALIZATION LOG - SLICES 61 AND 62

Slices 61 and 62 - The judgement-call protocol, and DECISION-15 worked under it - 2026-08-11 - DECISION-17; CONSTITUTION VERSION 26.

WHAT WAS TIGHTENED, AND WHAT THE TESTS PIN. Five new tests, four of which try to produce konnectome's acceptance effect out of something OTHER than the mechanism, and fail to - which is the only way to find out whether an effect was ever the mechanism's.

WHY THESE TESTS WERE WRITTEN AT ALL, WHICH IS THE PART WORTH READING. The first literature search run under the new protocol went looking for DECISION-15's coupling gain and came back with something it was not looking for. James R. Schmidt, "Evidence against conflict monitoring and adaptation: An updated review", Psychonomic Bulletin and Review 26(3) 753-771, 2019, argues that the evidence for conflict monitoring - "primarily, proportion congruent and congruency sequence effects" - is confounded, that "conflict monitoring is merely an illusion", and that the results are better understood as "(relatively) simpler learning/memory biases unrelated to conflict or attention that confound the key paradigms". The confounds it names are contingency learning, temporal learning, binding and feature repetition. AND THE CONGRUENCY SEQUENCE EFFECT IS, IN THAT PAPER'S OWN WORDS, "ALSO SOMETIMES REFERRED TO AS THE GRATTON EFFECT" - which is the effect slice 58 adopted as this pack's acceptance test.

SO THE SUITE STOPPED ASSERTING THAT THE CONFOUNDS ARE ABSENT AND STARTED PINNING IT. DECISION-17's argument is that every confound Schmidt names is a property of the HUMAN EXPERIMENTAL PARADIGM and that none of them exists in konnectome's ablation by construction. An argument of that shape is exactly the kind that reads well and is never checked, so each clause of it is now a test.

THE FOUR CONFOUNDS ARE PINNED AS A READABLE LIST. One test asks the pack which confounds its ablation excludes and asserts the answer is the four: feature_repetition, contingency_learning, temporal_learning, stimulus_response_binding. Written as a comment the list would have been a claim; written as a fact it is a thing a later session can be held to.

NO FEATURE TO REPEAT, PINNED BY IDENTITY RATHER THAN BY RESEMBLANCE. A test builds ONE incongruent trial term and asserts that the measured trial of the conflicted arm and the measured trial of the calm arm are THE SAME TERM. A human study can only arrange two trials that resemble each other, which is precisely where the feature-repetition confound lives; this ablation hands both arms the identical object, so there is no feature that repeats in one condition and not the other.

NOTHING LEARNS, PINNED BY REPETITION. A test measures one trial's conflict once, then measures the same trial twenty more times and asserts every answer equals the first. The measure carries no state, so no distractor can become predictive of a response - the contingency-learning confound has nothing to run on.

NEITHER POSITION NOR RHYTHM, PINNED BY A LONG RUN-UP THAT CHANGES NOTHING. A test runs the measured trial after ONE calm trial and again after ELEVEN, each from a fresh bus, and asserts the final conflicts are EXACTLY equal. A calm turn recruits no control and therefore leaves no trace, so the temporal-learning confound cannot reach the measured trial no matter how long the sequence in front of it is. THE NUMBER ELEVEN IS NOT DECORATION: a difference of one trial could be arithmetic noise, and an order-of-magnitude difference in run-up length that produces bit-identical conflict is the clean result.

AND THE ONE THAT MATTERS IS THE INEQUALITY RUN INSIDE THE CLEARED GROUND. With no feature repeating, nothing learning and nothing timed, the conflicted history still leaves LESS conflict on the measured trial than the calm history does. IF REMOVING EVERY CONFOUND HAD REMOVED THE EFFECT, THE EFFECT WAS THE CONFOUND - and this is the only one of the five whose failure would have been a finding about the loop rather than about the test. It passes.

AND ONE FINDING WAS VERIFIED RATHER THAN ASSUMED, WHICH IS ITSELF A CHECK. The corpus gap recorded for the update run was confirmed by search and not by memory: the strings "contingency learning", "congruency sequence", "feature repetition", "conflict adaptation" and "proportion congruent" return NOTHING anywhere in docs/neuroscience/. The corpus presents the Gratton effect at Chapter 52.2.1 as a successful prediction - "it predicts, and finds" - and does not carry the confound critique. THE CORPUS IS NOT WRONG; everything it says is accurate, and what is missing is a live dispute about what finding the effect means. Recording a gap you have not looked for is how a build ends up promoting something the corpus already said.

WHAT WAS NOT TIGHTENED, AND WHY THAT IS ALSO A RESULT. DECISION-15 gains no test because it gains no value. The search found no published coupling gain, and it found the field's own reference re-implementation - Princeton's PsyNeuLink, which publishes every layer's logistic gain and bias to one decimal place and states that conflict is computed on the response layer's output port WITHOUT GIVING THE FORMULA - carrying no conflict-to-control coupling constant at all. That is a considerably better warrant for the refusal than "konnectome looked". The refusal is upgraded from one that survived a READING to one that survived a SEARCH, and the ledger now says which of the two it holds.

conflict_monitor grows from THIRTY-FIVE tests to FORTY. ONE THOUSAND AND SEVEN tests across THIRTY-NINE packs, all green.

KONNECTOME REALIZATION LOG - SLICES 63 AND 64

Slices 63 and 64 - The evidence back-fill, and MODE represented well - 2026-08-11 - OBSERVATION-19; no code changed.

WHAT THE TESTS FOUND: NOTHING, BECAUSE NO TEST RAN DIFFERENTLY. ONE THOUSAND AND SEVEN tests across THIRTY-NINE packs, all green and every one of them unmoved. Neither slice changed code. What was tightened at these two slices was tightened by READING, and the reading found four defects that no suite in this repository could have caught.

THE FIRST IS THE ONE WORTH THE WHOLE SWEEP: THE TUTORIAL CONTRADICTED ITS OWN GLOSSARY. Its slice-58 and slice-60 chapters describe a mode register as "a short list of the STATES the thing can be in", and repeat that conflation FOUR TIMES - in the one document in this build whose job is to teach the difference between a state and a mode, roughly eight hundred lines below the glossary entry that draws it. Every gate in this repository was green while that sentence sat there, because no gate reads prose for agreement with other prose. IT WAS FOUND ONLY BY READING THE DOCUMENT END TO END, which is the finding about method: a corpus of documents needs the equivalent of a test suite and does not have one.

THE SECOND WAS AN UNDERCOUNT OF THIS BUILD'S OWN HISTORY. docs/design/README.txt said the mode-register family was "built at slices 39 and 40". It ran 39 through 47, planned seven and built nine, with further registers at 50 and 60. A line like that is wrong in the safest possible direction and is still wrong.

THE THIRD IS A MISSING MARKER RATHER THAN A WRONG SENTENCE, AND THE REPAIR IS DELIBERATELY PARTIAL. docs/design/05, the design authority for the whole concept, had never carried a staleness marker, and its Part Five says konnectome has no supervisor channel (slice 42 built it), that the master register is absent (46), that the stabiliser gap is open (44) and that the register is not built (39). A staleness marker was added and NOT ONE WORD OF THE READ WAS CHANGED. THE DISTINCTION IS THE POINT AND IT IS THE REFINEMENT LESSON: a deep read of a frozen corpus does not go stale and must not be withdrawn; only the document's PICTURE OF KONNECTOME went stale, and the marker now says which half is which.

THE FOURTH IS MARKED SUPERSEDED IN PLACE. The vision-set analysis's grain count is out of date, and it is annotated where it stands rather than rewritten, because it is a DATED ASSESSMENT and editing status prose inside one turns a dated record into a rolling one. DECISION-16 overturned its stated REASON - "no region-grain register because there are no regions" - and not just the number; conflict_monitor carries the anterior cingulate's region-grain register, so the count is four.

AND THE FINDING WORTH MORE THAN THE FOUR REPAIRS IS ONE THE SUITE ACTIVELY CANNOT SEE - OBSERVATION-19. konnectome's fault-exclusion rule is STRICTER THAN ITS OWN DESIGN AUTHORITY SAYS IT SHOULD BE. docs/design/05 Part Four states, under a heading reading "TWO QUALIFICATIONS THAT A STRICT VALIDATOR WOULD GET WRONG", that the rule is PER CONSTRUCT AND NOT PER REGIME, that "A TAGGED MINORITY OF FAULTS ARE ADMITTED INTO THE REGISTER ON A USAGE ARGUMENT, always with the reasoning written inline", and that "the schema therefore needs a per-mode admitted-as-fault flag and a rationale slot, NOT A BLANKET EXCLUSION". konnectome built the blanket exclusion, and every test of it passes, because the tests were written to the rule konnectome built.

NOBODY MISREAD ANYTHING, WHICH IS WHY NO REVIEW CAUGHT IT. mode_register.pl has carried this comment since slice 39: the admitted-as-fault flag "belongs with the supervisor channel that does not exist yet". THE SUPERVISOR CHANNEL WAS BUILT AT SLICE 42 - twenty-two slices ago - and in the intervening time the blanket rule was written into three more documents as though it were the authority's own position. THE GENERAL FAULT IS THAT NOTHING WATCHES A DEFERRAL'S CONDITION: a note saying "when X exists, revisit this" is invisible on the day X arrives, because the session building X is looking at X and not at the list of things waiting for it. This is the SECOND time this session met that family, the first being a document abandoned whole when only part of it was superseded.

WHAT WAS NOT TIGHTENED, AND WHY IT IS RIGHT THAT IT WAS NOT. OBSERVATION-19's closer is sized and left: add the flag and a rationale slot to the mode entry schema, make the supervisor's refusal consult the flag rather than ban outright, and require the rationale whenever the flag is set - so an admitted fault must SAY WHY and cannot be admitted silently. THE RATIONALE SLOT IS THE LOAD-BEARING HALF, because it is what keeps the softened rule from becoming a hole. It touches the per-mode CONFIDENCE tag deferred in the same slice-39 sentence, which slice 60 met again when the corpus's four transfer functions each arrived with a confidence grade and mode_entry had nowhere to put them. TWO DEFERRED FIELDS, ONE SCHEMA, BOTH NOW WITH A CALLER - and a schema change with tests is a slice, not a tail-end tidy.

conflict_monitor stands at FORTY tests, unchanged. ONE THOUSAND AND SEVEN tests across THIRTY-NINE packs, all green.


KONNECTOME REALIZATION LOG - SLICES 65 AND 66

Slices 65 and 66 - The second agency cue, and the admitted-as-fault flag - 2026-08-11 - 1,065 tests across 40 packs.

WHAT THE TESTS FOUND, AND THE FIRST ONE WAS FOUND BY A TEST FAILING FOR THE RIGHT REASON. A test was written asserting that a steer_around issued at an ALREADY CLEAR path infers no authorship, on the ground that it too predicts the world that already stands. It failed - not on the verdict but on the enactment, because simulated_body_enact/3 REFUSES a steer with nothing to steer around, by name, and has done since the machine was written. THE DEGENERATE COMMAND CANNOT REACH THE COMPARATOR AT ALL.

THAT IS THE STRONGEST EVIDENCE AVAILABLE THAT THE EXCLUSIVITY READING IS NOT FITTED TO ITS OWN EXAMPLE. A rule invented to explain hold_still and nothing else would have had no opinion about a steer. This one agrees, on the same arithmetic, with a refusal written into the body years of slices earlier by somebody thinking about actuators rather than about agency. The test is kept in both halves: the forward models are compared directly to show the rule's verdict, and the machine's own refusal is pinned beside it.

THE SECOND FINDING WAS A CHECK THAT WOULD HAVE SWALLOWED ANOTHER CHECK. The authorship conjunction judges two things about each condition - that it is PRESENT, and that what it says can be READ - and the first draft judged both in one place, by looking for the condition in exactly its two legal shapes. A condition carrying an unreadable reading was then reported as an ABSENT condition, and the branch that refuses an unreadable reading became unreachable. Two different faults with two different repairs had been given one complaint. They are separated: presence is judged on the functor, the reading is judged on the walk, and both branches are now exercised.

THE THIRD IS OBSERVATION-20, AND IT IS THE NINTH FINDING OF THE WOULD-NOT-HAVE-FAILED SHAPE. Inside one comparator pass the copy is taken before the command reaches the actuator - structurally, because that is what an efference copy IS - so PRIORITY is met in every pass this build can run. A condition that cannot fail carries no information, and the honest reading is that konnectome's three-condition verdict currently rests on two. Every test would have been green. The condition is therefore built as a GENERAL predicate over a recorded order, and its failing branches are exercised against orders that have a real choice in them, which is the only place they can be shown to work at all.

WHAT MADE PRIORITY FREE WAS BELIEVED TO BE A STANDING OBSTACLE, AND SLICE 67 FOUND THAT OBSTACLE MISSTATED. This paragraph as written at slice 65 read that priority becomes informative the moment a thought can arrive from somewhere other than the command path - one tick's thought against another tick's action - "and NO CONSTRUCT IN THIS BUILD CARRIES ANYTHING ACROSS A TICK". [CORRECTED AT SLICE 67: THIS CLAIM WAS FALSE, AND IT WAS FALSE FOR THE WHOLE OF ITS LIFE. It is false of the hold construct, whose own mode gloss reads "keeps its own current value unchanged from one tick to the next"; of the copy construct, a one-tick delay by construction; of connection_graph's delay lines, a purpose-built tick-crossing buffer built and tested since slice 12; and of the RUNNING LOOP, which threads eligibility traces every tick and reads the governor's selection on the tick after it was written. Five hand-offs carried the claim and nothing ever checked it. THE TRUE OBSTACLE IS NARROWER: the tick engine gives each construct one slot per tick holding that construct's own committed value, so the build could carry a READING and had nowhere to park a PREDICTION ABOUT A DIFFERENT QUANTITY - a storage shape rather than a temporal limit, and the thing running_prediction supplies.] The first half of the sentence stands and is the whole of what slice 67 built against; the second half was a claim nobody had held against the code it described.

THE FOURTH FINDING IS THAT NOTHING REGRESSED WHEN THE BAN WAS SOFTENED, AND THAT WAS CHECKED RATHER THAN ASSUMED. Every register konnectome runs - the construct registers of one, the working-memory register, the master register, the conflict monitor's four-block automaton - passes unchanged, because none of them wanted an admitted fault. That is exactly why the over-strictness survived twenty-two slices unexamined: IT HAD NEVER COST ANYTHING. A test now pins the unadmitted case still being refused, so the softening cannot be mistaken for a repeal.

AND THE TAG BLOCK IS TESTED FOR WHAT IT REFUSES RATHER THAN FOR WHAT IT ACCEPTS. An admission with no rationale, an EMPTY rationale, a rationale that is a hole, an unknown confidence grade, two tags of one kind, a misspelt tag name, a tag block that is a hole, a tag block that is not a list, and a tagged mode with no transfer function - nine refusals, each by its own name. The empty rationale is the one worth naming twice: it passes every shape check and says nothing, which is precisely the silent admission the rationale slot exists to prevent.

THE SUITE IS CHOICEPOINT-CLEAN AND WAS WRITTEN TO STAY THAT WAY. Every recursive walk added by these slices takes its list first, which is what keeps first-argument indexing working and is what stops PLUnit reporting "succeeded with choicepoint" as a warning nobody reads.


KONNECTOME REALIZATION LOG - SLICES 67 AND 68

Slices 67 and 68 - The carrier, and the discrimination gate - 2026-08-11 - 1,109 tests across 41 packs.

WHAT THE TESTS FOUND, AND THE FIRST THING WAS FOUND BEFORE A TEST WAS WRITTEN. The slice was scoped to build a tick-crossing carrier because five hand-offs said the build had none. READING THE CODE BENEATH THAT CLAIM FOUND IT FALSE - hold persists, copy delays by one tick, connection_graph has purpose-built delay lines, and the running cognitive cycle threads eligibility traces and reads the governor's selection a tick after it was written. The slice that was going to spend a day on a carrier spent it on a pack instead, because the obstacle was a storage shape and not a temporal limit.

THAT IS THE THIRD INSTANCE THIS SESSION OF ONE FAULT. A condition that cannot fail, a rule that refuses everything, and A CLAIM NOTHING CHECKS are the same defect at three grains. All three are well behaved. All three answer every question put to them. None can be found by running them, because running them is what they survive.

THE SECOND FINDING WAS A NAMING CHOICE THAT WOULD HAVE PASSED REVIEW. The carried order's events were first named thought_at(Tick) and action_at(Tick), which reads correctly and is wrong: two such terms differ even when the ticks are equal, so a thought SIMULTANEOUS with its action would have scored as preceding it. A test asserting that the simultaneous case fails priority caught it, and the repair is that events are named FOR THEIR TICK ALONE, so two events on one tick are one term and "nothing precedes itself" does real work. THE TEST THAT CAUGHT IT WAS WRITTEN BECAUSE THE SESSION WAS LOOKING FOR CONDITIONS THAT CANNOT FAIL, which is the session's own lens paying inside the slice that built the gate for it.

THE THIRD WAS A TYPE GUARD THAT WAS GUARDING THE WRONG THING. apparent_causation_priority/4 required its event names to be ATOMS, which was right when every order was a list of atoms and wrong the moment the carried pass named events by their tick. The guard's real job was never about type: an UNBOUND event would be bound by the position lookup to whichever event was recorded first. It is now a GROUNDNESS check, which catches a partial term like tick(_) that a var check would have let through - and that partial term would have unified with any tick whatever and reported a confident order.

AND THE FOURTH IS THE GATE'S OWN FIRST RUN, WHICH IS THE SLICE'S REAL RESULT. 181 named refusals across the build; 17 had never been shown able to fire. THIRTEEN WERE PROVEN by adding a test that makes each one throw. FOUR COULD NOT BE, AND THAT IS THE FINDING RATHER THAN THE FAILURE.

THE FOUR ARE ALL ONE SHAPE AND IT IS A LEGITIMATE ONE. archetype_gate_transfer_function cannot fire while the gate's transfer table has two closed rows and the law covers both. mode_transition cannot fire while the gate has two modes and the thrown rows are a complete bijection on them. category_formation_unrenamable_concept cannot fire while PrologAI's concept_formation mints every identifier with the same prefix. self_provenance_refused cannot fire while causal_core defines semantic errors for four kinds and neither of this pack's two is among them. EACH GUARDS AGAINST A FUTURE CHANGE IN A TABLE, A MODE SET, OR A REUSED COUSIN - not against an input any caller can supply today.

SO THEY ARE EXEMPTED AND NOT DELETED, AND THE EXEMPTION CARRIES ITS REASON AND ITS TRIGGER. That is OBSERVATION-21, and the shape of the answer is slice 66's, two slices old: the strict rule stands, an exception may stand beside it where it names itself and states its case, and the case is mandatory. THE DIFFERENCE BETWEEN A TRIPWIRE AND DEAD CODE IS ENTIRELY WHETHER SOMEBODY WROTE DOWN WHICH CHANGE WOULD TRIP IT.

WHAT THE GATE DOES NOT REACH IS TESTED BY BEING WRITTEN DOWN RATHER THAN BY BEING TESTED. It would not have caught slice 66's over-strict ban, which had a test and diverged from a document. It would not have caught slice 65's free priority, which returns a term rather than throwing. And it is textual, so a comment satisfies it. Those three limits are in the script's own header, because a gate that overstates its reach is an instance of the fault it polices.

THE SUITE IS CHOICEPOINT-CLEAN. Every recursive walk added by these slices takes its list first.

KONNECTOME REALIZATION LOG - SLICES 69 AND 70

Slices 69 and 70 - The efference copy wired, and the deferral register - 2026-08-11 - three built-and-unwired mechanisms reached from the running loop; the sixth gate.

THE NO-BEHAVIOUR-CHANGE PROMISE WAS MEASURED AND NOT ASSERTED, WHICH IS THE WHOLE OF PRINCIPLE FOUR APPLIED TO A SIGNATURE THAT DID NOT MOVE. body_interface_survival_run/5 keeps its arity and now delegates to the judged loop, discarding the predictions, and the claim that this changes nothing is not left standing on the reviewer's confidence: a test runs the plain form and the judged form over the same start and asserts that the traces and the final bodies are the SAME TERM. The alternative on offer was a second loop kept beside the first, which would have been easier to write and would have let the two drift apart in silence for as long as nobody compared them. There is one loop called two ways, and the sameness is a measurement.

THE TEMPTATION THAT WAS REFUSED WAS THE ONE THAT WOULD HAVE MADE THE SLICE LOOK BETTER IN A SHORT RUN. The corpus fixes the lag at ten ticks, and a run shorter than ten ticks judges nothing at all, so the first demonstration showed a wiring that appeared to do nothing. Two repairs were available and both were cheap - shorten the lag, or release at the next tick so that something visible happens inside six ticks - and both would have been konnectome quietly inventing a number over the corpus's own. The RUN was lengthened instead, and the short case was pinned by a test that asserts what is still being HELD, so that a loop with nothing yet to say reports its waiting rather than resembling a loop that is broken.

THE TESTS READ THE LAG RATHER THAN WRITING IT, AND THAT DISTINCTION IS LARGER THAN IT LOOKS. Writing the literal ten into the test suite would have pinned body_interface to a convention it does not own and never agreed to, and the day DECISION-2's tick rate is restated the suite would have gone quietly wrong while staying green - the tests would have been measuring a number they had copied rather than the number the build uses. Every new test derives its tick counts from running_prediction_lag_ticks/1, so the suite tracks the corpus through the same predicate the code tracks it through, and a restated rate moves both halves together or breaks both halves together.

THE DISCRIMINATION TEST IS THE SLICE'S REAL ACCEPTANCE CRITERION AND NOT ONE ASSERTION AMONG MANY. A wiring that handed hold-still and recharge the SAME verdict would have been entirely green, would have exercised the comparator, the carrier and the three conditions, and would have told nothing apart from nothing - the exact decoration slice 68 built a gate to name. So the test asserts that the two verdicts DIFFER, and it asserts WHY they differ: exclusivity fails for stillness, because a hold-still tick predicts the world exactly as it already stands and that is word for word the null command's prediction. The mind is the author of its recharges and not of its stillness, and that sentence is now a failing condition rather than a description.

THAT VERDICT IS OBSERVATION-17'S DEGENERATE CASE BEING CAUGHT IN THE RUNNING LOOP BY THE CONDITION BUILT FOUR SLICES EARLIER TO CATCH IT. The null command scoring a perfect match was named as a hazard at slice 65 on paper and against directly compared forward models; here it arrives unbidden out of a draining survival run that nobody arranged to produce it, and exclusivity refuses it. A rule that only ever refuses the example it was written from has not been tested; this one refused an example the loop generated on its own.

THE THIRD OUTCOME IS CARRIED AND IS EMPTY, AND IT IS THREADED OUT ANYWAY BECAUSE AN EMPTY LIST AND A SILENCE ARE NOT THE SAME REPORT. running_prediction_release/5 names RELEASED, WAITING and STALE, and this loop releases on every tick, so nothing in it can go stale today. The stale list is still threaded to the surface and asserted empty by a test, so that a clock which ever begins to skip breaks the silence with a list somebody can read rather than with nobody noticing anything at all.

PRIORITY NOW DISCRIMINATES ACROSS A REAL GAP, WHICH IS THE REPAIR OF THE FINDING SLICE 65 REPORTED AGAINST ITSELF. In the carried form the thought's tick and the action's tick stand one full lag apart, so the ordering is two separate recorded facts rather than one structural consequence of how a comparator pass is laid out. The condition that could not fail can now fail, and it can fail because the build finally has somewhere to park a prediction about a different quantity.

TWO FAULTS THE DEFERRAL GATE FOUND IN ITSELF ON ITS FIRST RUN, BOTH AUTO-FIXED UNDER THE SIXTEENTH COMMANDMENT, AND BOTH OF THEM ARE THE REGISTER'S OWN SUBJECT ARRIVING IN THE REGISTER'S OWN FORMAT. The first: the parser matched on the separator alone, and so read the register's own prose ABOUT the separator as three malformed entries - a parser that could not tell a rule from an instance of the rule, which is the identical confusion the gate exists to police one level up. The second: it lost an entry's note into an empty field that was invisible both to a human reader and to a field splitter, a deferral's condition going missing in exactly the way that deferrals' conditions go missing. The fixes are narrow and are about recognition rather than about tolerance: an entry is recognised by its identifier standing in column one, and an absent probe is written as the word none and never as nothing at all.

THE HISTORICAL PROOF WAS MEASURED RATHER THAN CLAIMED, AND THIS IS THE PART OF SLICE 70 THAT COULD MOST EASILY HAVE BEEN AN ASSERTION. It would have cost nothing to write that the gate would have caught the tick claim on the day it was written, and nobody would have asked for the receipt. The probe was instead run against the repository AS IT STOOD AT THE COMMIT OF SLICE 56 - the slice that deferred the efference copy's wiring on that very claim - and the refutation was already sitting there, because the gloss "keeps its own current value unchanged from one tick to the next" entered mode_register at slice 39. The gate would have turned that deferral red seventeen slices before the deferral was written.

THE GATE'S THREE LIMITS ARE WRITTEN ON ITS OWN FACE FOR THE SAME REASON SLICE 68'S WERE. It cannot find a deferral nobody wrote down, and since the register is populated by hand that is the largest residue it carries. It can prove two probes disagree and cannot prove they disagree ABOUT THE RIGHT THING, which only reading the note will ever find. An unprobeable entry is checked for nothing except that it states its reason. A gate that overstated its reach would be an instance of the fault it polices, and this is the second gate in three slices to say so in its own header.

WHAT IS HONESTLY NOT CLAIMED IS STATED IN THE NARROWEST READING AVAILABLE. cognitive_cycle_step/3 still calls NONE of the three mechanisms; the reachability earned here runs through body_interface's survival loop from the capstone story, and that is what "reached from the one running loop" is being taken to mean, deliberately at its smallest. Seven mechanisms were built and left unwired, THREE are now reached, and FOUR are not - the eligibility trace's consumer, the blackboard's rehearsal target, the durable tier, and the conflict loop. Three of four is progress and it is not completion, and the register now holds the remainder rather than a hand-off sentence holding it.

AND THE FUSION RULE IS DELIBERATELY NOT BUILT, WHICH IS WHY IT SITS IN THE REGISTER AS THE ONE UNPROBEABLE ENTRY. Chapter 51.2.1 calls the combination of prediction and delayed evidence formally like a Kalman filter and states no reliability for either side, so there is no weight to be had that konnectome would not be inventing. A COMPARISON IS NOT A FUSION, and this loop compares. The entry carries a mandatory reason and prints on every run, so the thing that cannot be watched is at least never out of sight.


KONNECTOME REALIZATION LOG - SLICES 71 AND 72

Slices 71 and 72 - Observation-22 settled, and the conflict loop wired - 2026-08-11.

SLICE 71 REFINES THE DEFINITION'S OWN PROSE, NOT ANY CODE. The refinement is textual and is recorded on konnectome_0_definition_v2.txt's own face, per that document's rule that a change of concept is a versioned change with its reason attached; nothing here restates it.

SLICE 72's REFINEMENT PASS: the naive first draft threaded a SECOND bus variable (Bus1 unchanged, a new BusConflict carried separately) through the rest of the online program clause, which would have meant STEP FOUR onward silently keeping reading the pre-conflict bus and losing the published raise. REFINED to a single BusOut that STEP FOUR's plasticity_engine_step/5 and reward_capture_step/6 both read, so the raised control is visible to the SAME tick's learning as well as to the next tick's arbitration - closer to the corpus's own claim that raised control affects "cognitive effort" broadly rather than only the next threshold check.

WHAT IS HONESTLY NOT CLAIMED. The learning steps do not currently DO anything differently because the bus carries a raised bias - plasticity_engine reads dopamine and relay gains from the bus, not the stabiliser channel - so this refinement is currently inert beyond making the correct value available. IT IS RECORDED AS A REFINEMENT RATHER THAN A CLAIM OF NEW BEHAVIOUR, because the alternative (keeping two separate bus values) would have been a smaller, more local change that quietly reintroduced the "two running loops" shape one level down - two buses inside one tick, disagreeing about what stands.
KONNECTOME REALIZATION LOG - SLICE 74

Slice 74 - The working-memory blackboard wired, and its maintenance period declared - 2026-08-12.

THE REFINEMENT PASS FOUND ONE REAL DEFECT AND ONE REAL FINDING, AND THE FINDING IS THE LARGER OF THE TWO.

THE DEFECT: the naive wiring passed the world's standing rehearsal_target straight into working_memory_blackboard_step/3, which REFUSES ALOUD a rehearsal aimed at a slot the board does not hold. That refusal is correct for a caller taking one step and wrong for a STANDING pointer inside a loop, because the night erases the board every night and a pointer at an erased board is the ordinary morning rather than anybody's error - so the naive wiring throws on the first maintenance step after the first night, in every world with a pointer set. REFINED so the tick passes the pointer through only while the board still holds it, and the explicit absence otherwise. The pack's refusal is untouched and still fires for its own callers; the reading is declared in the code and in DECISION-23 rather than made silently, and a test pins the whole sequence - a pointer surviving a night, meeting a maintenance step, and rehearsing nothing without throwing.

THE FINDING, WHICH ARRIVED AS FOUR TESTS FAILING AT ONCE: under the two-process governor's DEFAULT parameter block the whole waking day is EIGHTEEN TICKS, so the board's hundred-tick period never arrives and the surface is only ever erased by a night that comes before the first fade. The governor's block is documented as one tick per hour; DECISION-2 fixes a hundred ticks to the nominal second; the two differ by a factor of three hundred and sixty thousand. THE DEFAULTS WERE NOT CHANGED - that is a numbered decision of its own, not an auto-fix taken in passing inside a slice about a blackboard. The test fixtures restate the governor's six parameters in DECISION-2's units instead, with a comment saying why, and OBSERVATION-23 carries the finding.

WHAT IS HONESTLY NOT CLAIMED. The board is REACHED and it is not FED. Nothing admits to it inside the loop and nothing rehearses on its own authority, so in the capstone's own world the surface is empty from the first tick to the last, and the capstone story is byte-identical for exactly that reason. Reachability is claimed at its smallest: the board is maintained and erased by cognitive_cycle_run/4, which is one of the two loops the Definition's standard names.
