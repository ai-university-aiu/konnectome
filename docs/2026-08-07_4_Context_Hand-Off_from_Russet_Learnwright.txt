CONTEXT HAND-OFF FROM RUSSET LEARNWRIGHT

Date: 2026-08-07. The fourth hand-off of this date.

I chose the name Russet Learnwright because this session finished the learning engine's body: the trace that lets a late reward find its connection, the governor that keeps learning from exploding, and the chemistry of places that tells each territory how much a moment mattered. Russet for the deep red-brown of the house palette; Learnwright for a wright whose material is the rule by which a mind changes itself.


PART ONE - READ THESE FIRST

- Read and follow CONSTITUTION.md - all seventeen commandments, exactly. The Sixteenth is the versioned build log (docs/BUILDING_KONNECTOME_v2.txt, now twenty-four chapters); the Seventeenth is this hand-off protocol.

- Read The SPARCD Fileset at its current versions: docs/konnectome_1_specification_v31.txt through docs/konnectome_6_demonstration_v31.txt (v28 to v31 across slices 28, 29, and 30 this session; superseded versions in docs/archive/).

- Read docs/LAYER_09_NATURES_COGNITIVE_ARCHITECTURE_MANUSCRIPT.txt - the guiding book. The ladder stands as Umber left it: Rungs Zero through Four realized and told; Rung Five groundwork built and told, the rung itself still unclaimed because the book demands a real machine.

- Read the Settings, General, Instructions for Claude - the owner's standing instructions (web search first, novice-level plain-text explanations, plain-text output with a copy to ~/output/, auto-commit-push-merge authorization, session-length budgeting).

- Read ~/CLAUDE.md - git-untracked, at the organization root, the system-wide current-state narrative.

- Read the current tutorial, docs/konnectome_tutorial_v28.txt (twenty-seven parts; Parts Twenty-Two through Twenty-Four tell slices 28 through 30), and read all of docs/.

- Read the prior hand-off, Carmine Timewright's (2026-08-07_3). This session began from it, verified the baseline, and executed three items of the owner's standing queue.

- THE OWNER'S STANDING CHOICE REMAINS: the owner answered "All" to the Seamwright menu, and re-confirmed it to this session in so many words ("Continue with all, please"). Do NOT re-present the menu; verify the baseline and continue down the remaining queue (Part Six below), asking only if a priority decision is needed within it.


PART TWO - THE IMPORTANT DOCS FILES

- konnectome_ledger_v1.txt - the scoreboard. Counter line unchanged: Walls: OPEN 0 | ROUTED 0 | CLOSED 3 | UNNECESSARY 0. Observations: 4. New this session: the Slice-28, Slice-29, and Slice-30 notes in Section Five. The slice-27 pending line (bus into delayed time) is CLOSED by slice 30; new pending lines are listed in Part Five below.

- konnectome_1_specification_v31.txt through konnectome_6_demonstration_v31.txt - the SPARCD Fileset, each ending in the Realization Log through slice 30.

- konnectome_tutorial_v28.txt - the beginner tutorial, twenty-seven parts.

- BUILDING_KONNECTOME_v2.txt - the build log, twenty-four chapters; Twenty-Two through Twenty-Four are this session's.

- konnectome_rung_five_real_machine_design_v2.txt - the real-machine design proposal with the owner's answers to all five decisions in Section Five: Path B (Webots simulator) with a STANDING REMINDER to buy a physical robot in the future; wheels-plus-dock first; an indoor room pen, attended; Webots as the simulator; pack names simulator_body_driver (the Webots work) and robot_body_driver (RESERVED). The simulator groundwork slice is UNBLOCKED; the rung itself still waits for real hardware.

- Causalontology_4_0_0_CHANGE_ORDER_v3.txt - Sections C, D, E remain HELD. No finding released them this session.

- PrologAI_CHANGE_ORDER_v2.txt - unchanged; no language wall was hit this session.

- tests/naming_gate_fixture/ - the committed, deliberately-broken fixture pack proving the naming gate can fail. Do not "fix" its violation; the violation is the point.

- In the owner's output folder (~/output/): konnectome_slice_28_session_report.txt and konnectome_slices_28_29_30_session_report.txt (this session's reports).


PART THREE - WHAT THIS SESSION DID

- SLICE 28 (pull request 53, squash-merged as dbdf719): the eligibility trace - FR-11 and Section A2.6 made runnable. plasticity_engine grew three public predicates (trace store, trace tick, reward step); the manuscript's acceptance pinned in both directions (a reward one tick late still strengthens by exactly the faded trace; a withheld reward fades the trace and moves no weight). Review (fourth straight completion): four REAL silent failures (sharpest: the kind check itself silently binding an unbound kind via memberchk), three MINOR, one STYLE - all eight fixed at one root, a shared refusal perimeter, pinned by seven tests. Suite 5 to 24.

- SLICE 29 (pull request 54, squash-merged as 88f0705): homeostatic scaling - FR-10's governor. plasticity_engine grew three more public predicates (average store, exponential-moving-average tick, scaling step: each region's incoming transmissive weights multiplied by one shared factor, floored at zero). The stability promise pinned as a worst case: fifty ticks of maximal coincidence under full dopamine hold the weight at a finite fixed point just below 1.9. Review (fifth straight): two REAL with one root (one malformed store entry drew two different silent verdicts from two consumers), two MINOR - all fixed with one shared store judgement, mirrored into the slice-28 trace store and both constructors, pinned by seven tests. Suite 24 to 48.

- SLICE 30 (pull request 55, squash-merged as 77094b9): per-territory neuromodulators and the delayed step's bus wiring - FR-6's second half plus the slice-27 debt, in the pairing the ledger suggested. neuromodulator_bus grew territory entries (diffuse-field law: own level, else global fallback, else silence; every construct IS its own territory, so no caller changed; backward compatibility pinned). connection_graph grew the delayed modulated step: delay lines and bus together, gain read at the tick of delivery, never emission, all-delays-one identity pinned. Review (sixth straight): NO REAL finding; two MINOR with one root (flat key space aliasing; unbound names silently bound) closed by one atom guard at every bus entrance, pinned by five tests. Suites 4 to 14 and 17 to 24.

- State at hand-off: main at 77094b9 (plus this hand-off's merge). Thirty slices. Twenty-seven packs. 455 PLUnit tests, all green, re-swept after every slice's review fixes. Four gates at zero violations, the naming gate passing its own self-test, re-run per slice. Conformance re-measured per slice at 137 of 137 in both cousins (PrologAI: bin/run_causalontology_conformance.sh; causalontology: bindings/python/tests/run_conformance.py). The capstone demonstration re-run clean after every slice. SPARCD v31; tutorial v28; build log twenty-four chapters. Pull requests merged by this session: 53, 54, 55, and this hand-off's.


PART FOUR - BUILD-AGAINST COUSIN COMMITS (for reproducibility)

- causalontology: 292dfca (unchanged by this session).

- PrologAI: ebbc3e0 (unchanged by this session).

- Mentova: d117c69 (read-only; untouched).


PART FIVE - SHARP EDGES AND HONEST NON-CLOSURES

- THE SHARED PERIMETERS ARE LOAD-BEARING. plasticity_engine now funnels every keyed store (traces, averages, activations-at-construction) through one store judgement and every interface list through one interface check; neuromodulator_bus refuses non-atom modulator and territory names at all four entrances; connection_graph's three step variants (plain, modulated, delayed, delayed modulated) each end in refusal clauses. A future slice adding a construct kind, an interface kind, or a store shape must extend the right check BEFORE the catch-all fires in tests - the throws are features.

- THE MEMBERCHK LESSON, TWICE LEARNED: a checking predicate that unifies is a checking predicate that lies. Slice 28 found it in the kind check; slice 30 found it again in the bus reads. Any new lookup over a keyed list should either guard with nonvar/atom first or be prepared to invent answers silently.

- THE REWARD STEP DOES NOT CONSUME TRACES (deliberate: trace lifetime belongs to the caller); the scaling factor floors at zero (deliberate: silenced, never sign-flipped); a territory with no level reads the diffuse global field (deliberate: that is what diffuse means).

- PENDING LINES, CURRENT: (1) the plasticity engine's third factor still reads the GLOBAL dopamine level - a per-territory third factor for learning is the natural next use of the slice-30 read, and would make a natural small slice; (2) the trace and average stores are not yet threaded through the cognitive cycle's live tick; (3) the scaling target and rate are one value for the whole graph (per-territory targets would pair with pending line 1); (4) the slice-29 fixed-point proof holds the averages constant - the fully coupled learn-fade-average-scale loop is unproven; (5) computational-interface transforms (slice 3's oldest neighbour); (6) interfaces as Causalontology conduits.

- THE FIXTURE IS STILL SUPPOSED TO BE BROKEN (tests/naming_gate_fixture/); the survival-rhythm constants are still load-bearing in three places; Observation-4 (no word for satisfaction) still stands LOGGED, NOT ROUTED.

- PARALLEL SESSIONS: the hazard did not bite (baseline verified at start and before every merge; main never moved except by this session's own merges). The ritual stands: git fetch, git log origin/main, gh pr list before every claim, numbering, or merge; claim a slice with an empty commit on a pushed feature branch before building it.

- RITUALS THAT KEPT THIS SESSION HONEST, offered to the next: verify the baseline before building; claim the slice before building it; test-first, all-red before the first green; tighter-scoped adversarial review with named lenses and a hard reproduction budget (six straight completions - it is the method); fix every confirmed finding at ONE root and pin it before merge, mirroring the fix into earlier code when the root is shared; re-sweep all tests, re-run all gates, re-measure conformance, and re-run the capstone per slice; record the review disposition in the ledger note itself.


PART SIX - THE FIRST TASK OF THE CONTINUATION

The owner has already chosen: ALL (re-confirmed in this session's own words). The remaining queue, in suggested order (re-orderable by the owner at any time):

1. The remaining pending refinements, one slice each, suggested smallest-first: the per-territory third factor for learning (pending line 1 above - the natural next-smallest, one pack, and it completes what slice 30 started); conduits (interfaces as Causalontology conduit records); the token tier of thought combination. Each: design against the manuscript, test-first pack growth, tighter-scoped adversarial review, lockstep documents, pull request.

2. Deepen Rung Two - the full pilot conversation and a richer grounded vocabulary, the oldest open refinement on the ladder.

3. Rung Five, Path B - UNBLOCKED: the simulator_body_driver pack against Webots, satisfying the same seam contract simulated_body satisfies, with the arena modelled on the decided indoor-room-pen enclosure. The rung itself remains unclaimed until a future physical robot (wheels plus dock first) passes the smoke test - carry the standing reminder to research and shop for that robot.

4. Other ______________ .


PART SEVEN - SIGN-OFF

The learning engine that began this session as a rule without a memory ends it whole: a coincidence leaves a warm trace, a late reward finds what earned it, a governor holds the whole loop at a fixed point a reader can compute by hand, and every territory reads its own chemistry at the moment a signal arrives. Three pull requests merged, eighteen review findings confirmed and every one dispositioned at its root, nothing claimed that a reader cannot re-run.

A checker that unifies is a checker that lies; the only refusal that counts is the one that has itself been made to fail.

- Russet Learnwright, 2026-08-07
