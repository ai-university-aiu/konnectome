CONTEXT HAND-OFF FROM GOLD COURIERWRIGHT

Date: 2026-08-09. The fifth hand-off of this date.

I chose the name Gold Courierwright because this session built the delivery. Gold follows Copper, Bronze, and Brass down the warm-metal shelf - and gold is the metal of payment, which is what this slice finally made the loop do: pay. Courierwright because the corpus's own picture of the work is a courier system - a broadcast cargo seized only at tagged addresses - and this session built the courier that carries each tick's reward to exactly the connections that earned it, burning each address as it pays.


PART ONE - READ THESE FIRST

- Read and follow the konnectome constitution at ~/konnectome/CONSTITUTION.md, first and foremost - NINETEEN commandments. Never cite a commandment by number from memory; re-read the constitution. The hand-off protocol is the Nineteenth, the Safety Gate the Fifteenth.

- Read The SPARCD Fileset at its current versions: docs/konnectome_1_specification_v37.txt through konnectome_6_demonstration_v37.txt (bumped by this session's slice 36; the v36 set is archived).

- Read docs/NATURES_COGNITIVE_ARCHITECTURE_MANUSCRIPT.txt (the guiding book), docs/konnectome_vision_set_top_down_analysis_v1.txt (the Eighth Commandment analysis), and docs/konnectome_cognitive_architectures_coverage_v1.txt (the Ninth Commandment survey).

- Read the Settings, General, Instructions for Claude (the owner's standing instructions: web search first, novice-level plain-text explanations, output copies to ~/output/, auto-commit-push-merge authorization, session-length budgeting).

- Read ~/CLAUDE.md - git-untracked, at the organization root, the system-wide current-state narrative.

- Read the current tutorial, docs/konnectome_tutorial_v35.txt (bumped by this session), and all of docs/.

- Read the prior hand-off, Brass Switchwright's (2026-08-09_4). This session began from it, verified the baseline in full, and executed its priority-one queue item: slice family one, item (b).

- THE OWNER'S STANDING CHOICE REMAINS: "All", re-confirmed across sessions. Do NOT re-present any menu; verify the baseline and continue down the queue (Part Six), asking only if a priority decision is needed within it.


PART TWO - THE IMPORTANT DOCS FILES

- konnectome_1_specification_v37.txt through konnectome_6_demonstration_v37.txt - the SPARCD Fileset, Realization Log through slice 36.

- konnectome_tutorial_v35.txt - the beginner tutorial, now thirty-three parts (Part Thirty is slice 36's; the closing runner, ledger, and where-to-look parts renumbered to Thirty-One through Thirty-Three).

- BUILDING_KONNECTOME_v2.txt - the build log, now THIRTY-TWO chapters; Thirty-Two is this session's (the reward learns its address).

- konnectome_ledger_v1.txt - the scoreboard. Counter line unchanged: Walls: OPEN 0 | ROUTED 0 | CLOSED 3 | UNNECESSARY 0. Observations: 5. New this session: the Slice-36 note (inserted before Observation-5, in slice order). Observation-5 STANDS - see Part Five.

- konnectome_vision_set_top_down_analysis_v1.txt - the Eighth Commandment analysis; its Family One list at line 75 is this queue's source.

- konnectome_cognitive_architectures_coverage_v1.txt - the Ninth Commandment coverage survey; seven coverage bugs, each with a named closer.

- The Vision Document Set (thirty-eight documents) in docs/ - see the Copper hand-off's Part Two for the file map. For the next slices, the load-bearing corpus chapters are the Layer 11 state machinery Chapters 11 through 16 (the two-process governor's design authority, for item (c) - read in full by the Brass session for slice 35, re-read them) and the Layer 11 offline-works chapters plus the Layers 4-5 dossier (for item (d)).

- konnectome_rung_five_real_machine_design_v2.txt - the real-machine design with the owner's five decisions (Path B Webots; wheels-plus-dock; indoor pen; pack names simulator_body_driver and robot_body_driver RESERVED; standing robot-shopping reminder).

- Causalontology_4_0_0_CHANGE_ORDER_v3.txt - Sections C, D, E remain HELD. Nothing this session released them.

- PrologAI_CHANGE_ORDER_v2.txt - unchanged; no language wall hit.

- tests/naming_gate_fixture/ - still deliberately broken; the violation is the point.

- In ~/output/: konnectome_slice_36_reward_capture_report.txt (this session's owner report). The Bronze session's PRIVATE flag about a credential-looking line in the local transcripts file stands for the owner; it is deliberately in no committed document - do not copy that line anywhere.


PART THREE - WHAT THIS SESSION DID

- Verified the Brass baseline in full before claiming anything, at main 853bf83: 503 tests green across twenty-seven packs (counted pack by pack), four gates at zero violations, capstone story clean, conformance 137 of 137 in both cousins (PrologAI bin/run_causalontology_conformance.sh; causalontology bindings/python/tests/run_conformance.py with CAUSALONTOLOGY_SPEC unset). Cousins at their expected commits (causalontology 292dfca, PrologAI ebbc3e0).

- Built SLICE 36 (pull request 74, squash-merged as 390d288): the reward step wired into the live tick, capture consuming the spent eligibility traces - slice family one, item (b). Designed against the Layers 4-5 slow-variables dossier, Part Two (the tagging and capture machinery, Chapters 5 through 8) read IN FULL: Chapter 5's synaptic tag IS the eligibility trace (the dossier's konnectome note names it outright), Chapter 6's cargo is captured only at tagged synapses, and the binding law is that capture CONSUMES - a served address must expire so it cannot capture a later, unrelated shipment. Realized as plasticity_engine_reward_capture_step/6 beside the untouched slice-28 reward step: a nonzero third factor at the receiving territory (local chemistry first, diffuse global fallback - the slice-30/31 law unchanged) spends the trace into the weight AND consumes the tag to zero; a zero third factor ships nothing, weight and tag standing bit-identical. In the tick: capture runs after the fast learning and before the trace fade, on the traces as the tick found them, so a reward one tick late finds the trace that earned it and no coincidence is double-counted within one tick; the scaling bound stays outermost. This closes the pending line every completion document carried since slice 32: the reward step still waits for its caller.

- Test-first: eleven behaviours written red before the first line of code (engine laws plus twin-world tellings in the loop); then one capture helper, one tag-fate helper, one step, one call in the tick. The tighter-scoped independent adversarial review (the twelfth straight completion) worked the four named lenses: key-shape aliasing and default drift DEFENDED on reproduction; behaviour-pinned-identical verified bit-identical against a main worktree on a rewardless five-tick world; one REAL finding under the standing unbound-wrong-judgement lens (four slices in a row), reproduced before fixing - the zero-dopamine short-circuit skipped the judging arithmetic, passing an unbound weight, trace, or learning rate through silently on the commonest path where the sibling reward step throws; fixed once (the spend's arithmetic judged on every path, the zero answer kept bit-identical), pinned by three review tests, plus a fourth pinning the review's second finding, the previously untested punishment path (a negative third factor spends the tag downward and consumes it).

- State at hand-off: main at 390d288 (plus this hand-off's merge). THIRTY-SIX slices. 518 tests across twenty-seven packs, all green, re-swept after the review fixes; four gates at zero; conformance re-measured 137 of 137 in both cousins; capstone re-run clean, its sixty tests untouched through the rewired tick. SPARCD v37; tutorial v35; build log thirty-two chapters. Pull requests merged by this session: 74, and this hand-off's.


PART FOUR - BUILD-AGAINST COUSIN COMMITS (for reproducibility)

- causalontology: 292dfca (unchanged).

- PrologAI: ebbc3e0 (unchanged).

- Mentova: d117c69 (read-only; untouched).


PART FIVE - SHARP EDGES AND HONEST NON-CLOSURES

- THE TICK NOW SPENDS TRACES. Any future slice that reasons about trace values must remember the tick consumes them on every nonzero-reward tick: a trace observed in a world dict after a rewarded tick holds ONLY the fresh coincidence, not history. The older plasticity_engine_reward_step/5 keeps its slice-28 non-consuming meaning for callers who manage trace lifetime themselves; do not "unify" the two speculatively.

- THE PUNISHMENT PATH IS PINNED BUT UNREACHABLE LIVE. A negative third factor spends the tag downward and consumes it (pinned by test), but the current closed loop can never produce a live negative reward, because drive_system_apply_action only moves the body toward set-points. The first slice that adds a perturbation or an adversarial world will make it reachable; the semantics are already decided and pinned.

- OBSERVATION-5 STILL STANDS. The older chemical reads (neuromodulator_bus_level/3, neuromodulator_bus_level_territory/4) silently bind an unbound bus. Slice 36's new capture step guards its own door (unbound bus refused as uninstantiated, pinned) but did NOT touch the inherited reads, per the do-not-fix-speculatively rule. The next slice that touches those reads on purpose closes it with the house var guard plus one pin per read, citing Observation-5.

- THE UNBOUND-WRONG-JUDGEMENT LENS KEEPS PAYING (slices 33, 34, 35, and now 36 - four in a row). Its slice-36 refinement: check the SHORT-CIRCUIT paths especially - a zero-case shortcut that skips arithmetic also skips the judging the arithmetic would have done. Carry all four named lenses: unbound-wrong-judgement, key-shape aliasing, default drift, behaviour-pinned-identical.

- THE SWITCH IS STILL WIRED TO NOTHING. Slice 35's operating state exists, reads online, and nothing reads or throws it. Item (c)'s governor is the first legitimate thrower; item (d)'s offline works are where offline behaviour first legitimately diverges. Any earlier reader of the state is scope creep.

- THE CAPSTONE WORLD IS STILL LIVE (slice 34): scaling rate 0.05, geography b-0.8, three closing guards (temperature 37, live weight above 0.5, live above rest-twin). The rewired tick left all sixty capstone tests untouched, but any slice touching the capstone's boot world or heartbeat chapter must keep the guards true or the story refuses to print.

- THE MODE-FLAG NAMING COLLISION (queue item 2) is still unresolved; resolve it before the mode-registers slice family starts.

- DELIBERATE CHOICES A READER MIGHT MISTAKE FOR GAPS: only the capstone's world runs the bound live; the scaling rate is still global; orphan targets deliberately permitted; the naming-gate fixture deliberately broken; Observation-4 (no word for satisfaction) still LOGGED, NOT ROUTED; Observation-5 LOGGED, NOT FIXED; the long-live-run watch is subsumed by item (d), not forgotten.

- PARALLEL SESSIONS: the ritual held - git fetch, git log origin/main, gh pr list before every claim, numbering, or merge. Keep it; main has moved under a session's feet before.


PART SIX - THE FIRST TASK OF THE CONTINUATION

The owner has already chosen: ALL (standing, re-confirmed across sessions). Slice family one continues smallest-first; the remaining queue, in suggested order (re-orderable by the owner at any time):

1. Slice family one, item (c): the two-process governor pack - a rising sleep-pressure process opposed by a circadian process, publishing the flip as the first legitimate thrower of slice 35's switch. Design against the Layer 11 state machinery (Chapters 11 through 16) read in full; test-first; tighter-scoped adversarial review with the named lenses; lockstep documents; pull request. Then item (d), the offline works - interleaved replay plus the raised-rate bound, proven by a catastrophic-forgetting rest-twin telling (subsumes the long-run-proof pending line); later item (e), the write-allocation gate and the per-territory plasticity fence (the Layers 4-5 dossier's CREB gate and perineuronal net chapters are its design authority).

2. The remaining Vision slice families as the owner prefers: edge type registry; mode registers (resolve the MODE-flag naming collision first); working-memory blackboard; workspace pilot (coverage bug three's closers); motif catalogue; the six-broadcast bus naming; template-parser tooling. The survey's importable margin-ideas ride along where they fit (the impasse disturbance with the selector work; the somatic-marker stamp with the appraisal work; the epistemic drive with the drive work).

3. The pre-corpus pending refinements not absorbed above: computational-interface transforms; interfaces as Causalontology conduits; the token tier of thought combination.

4. Deepen Rung Two - the full pilot conversation and richer grounded vocabulary - designed against the Layer 13 Tutor Protocol (this also advances coverage bug six).

5. Rung Five, Path B - UNBLOCKED: simulator_body_driver against Webots per the decided design (this also opens coverage bug five's frontier); the rung itself unclaimed until a physical robot passes the smoke test; carry the standing robot-shopping reminder.

6. Other ______________ .


PART SEVEN - SIGN-OFF

A courier is a small thing beside the treasure it carries - until you notice that without one, every payment stays a promise. For four slices the loop promised its connections a reward and never delivered; tonight the courier walks the map each tick, pays exactly the addresses that earned it, and burns each address behind him so no debt is ever paid twice. The day shift is whole. What the house needs next is a night watchman to decide when the lights go out.

Deliver what was earned, burn the address, and leave the switch for the watchman.

- Gold Courierwright, 2026-08-09
