APPENDIX 2. PSEUDOCODE

WRITTEN IN ENGLISH READABLE CODE (ERC)

A2.0 A WORD ON ENGLISH READABLE CODE (ERC)

This appendix is written in English Readable Code, abbreviated ERC, a controlled form of precise English in which each sentence corresponds to one exactly defined computational operation, defined by D. R. Dison.

ERC reads like ordinary English and yet can be translated, sentence by sentence, into any real programming language, because every sentence has one and only one meaning.

It is chosen for the Pseudocode phase for the reason SPARC prizes pseudocode in the first place: it states the logic in plain language, where a mistake is cheap to see and fix, before any programming language is chosen.

ERC is the ideal notation for a specification meant to be programming-language-agnostic, which this one is (see Appendix 6), because it commits to the logic without committing to a language.

A note on convention.

Each ERC procedure opens with a sentence of the form "The purpose of this procedure is to" and closes with "The procedure is complete." One operation is written per sentence.

Variable names are written as plain spaced English phrases.

Where a procedure takes inputs or returns a value in a way the base ERC catalogue does not fully template, this appendix extends ERC in the obvious readable way and marks the fact; such extensions are noted as ERC EXTENSION.

What follows is not the whole System in ERC, which would run to many hundreds of procedures.

It is the load-bearing core: the main tick loop, the single-construct update, the six archetype rules, the drive loop, and the learning rule.

From these, every remaining construct follows the same pattern, exactly as Chapter 11 established the per-construct block and then applied it uniformly.

A2.1 THE MAIN LOOP: RUNNING THE SYSTEM

The purpose of this procedure is to run the whole System for a chosen number of ticks.

Create a number variable called total ticks with initial value one thousand.
Create a number variable called current tick with initial value zero.
Create a list variable called all constructs with initial value empty list.
Load every region and interface and molecule into all constructs.

While current tick is less than total ticks, repeat the following steps:
    Read the body state and update every drive error, and compute this tick's reward.
    Update all neuromodulator levels for this tick, including broadcasting the reward as phasic dopamine.
    For each construct in all constructs, perform the following operations:
        Compute the next state of the construct for this tick.
    End the loop.
    For each construct in all constructs, perform the following operations:
        Set the current state of the construct to its next state.
    End the loop.
    Select one action from the competing drive proposals.
    Apply all plasticity weight changes for this tick.
    Record the full state of the System for this tick.
    Calculate the sum of current tick and one.
    Set current tick to the sum.
End the loop.

Display a message that the run is complete to the console.
The procedure is complete.

A note on the two separate loops over all constructs.

The first loop computes every construct's next state from the current states of its neighbours; the second loop then commits every next state at once.

Separating them is what makes one tick a synchronous update, in which every construct sees the same past and no construct sees a neighbour's future.

This is a deliberate and load-bearing choice, and an implementer who fuses the two loops will have built a subtly different and wrong machine.

A2.2 UPDATING ONE CONSTRUCT: THE DISPATCH

The purpose of this procedure is to compute the next state of a single construct according to its dynamical archetype.

Read the archetype of the construct.

Check if the archetype is equal to the word relay.
If the condition is true, then perform the following actions:
    Compute the next state using the relay rule.
End the conditional block.

Check if the archetype is equal to the word integrator.
If the condition is true, then perform the following actions:
    Compute the next state using the integrator rule.
End the conditional block.

Check if the archetype is equal to the word oscillator.
If the condition is true, then perform the following actions:
    Compute the next state using the oscillator rule.
End the conditional block.

Check if the archetype is equal to the word attractor.
If the condition is true, then perform the following actions:
    Compute the next state using the attractor rule.
End the conditional block.

Check if the archetype is equal to the word gate.
If the condition is true, then perform the following actions:
    Compute the next state using the gate rule.
End the conditional block.

Check if the archetype is equal to the word comparator.
If the condition is true, then perform the following actions:
    Compute the next state using the comparator rule.
End the conditional block.

The procedure is complete.

This dispatch is the whole of the "one canonical computation, rewired" thesis made into code.

There are six rules, not one hundred.

Every construct is one of six kinds, and its identity is its parameters and its connections, not a bespoke rule.

A2.3 THE SIX ARCHETYPE RULES

The purpose of this procedure is to compute the next activation of a RELAY construct.
Gather the total weighted input arriving from all incoming interfaces.
Create a number variable called total input with that gathered value.
Read the current gain of the construct, which the neuromodulators have set.
Calculate the product of total input and the current gain.
Create a number variable called next activation with the product.
Set the next state activation of the construct to next activation.
The procedure is complete.

The purpose of this procedure is to compute the next activation of an INTEGRATOR construct.
Read the current activation of the construct.
Create a number variable called decayed activation with the current activation.
Multiply decayed activation by a leak factor slightly less than one.
Gather the total weighted input arriving from all incoming interfaces.
Create a number variable called total input with that gathered value.
Calculate the sum of decayed activation and total input.
Create a number variable called next activation with the sum.
Set the next state activation of the construct to next activation.
The procedure is complete.

The purpose of this procedure is to compute the next state of an OSCILLATOR construct.
Read the current phase of the construct.
Read the natural frequency of the construct.
Calculate the sum of the current phase and the natural frequency.
Create a number variable called next phase with the sum.
Wrap next phase back into the range of one full cycle.
Compute the receptivity of the construct from next phase.
Set the next state phase of the construct to next phase.
Set the next state gain of the construct to the computed receptivity.
The procedure is complete.

The purpose of this procedure is to compute the next state of an ATTRACTOR construct.
Read the current activation pattern of the construct.
Gather the input pattern arriving from all incoming interfaces.
Create a variable called combined pattern by blending the current pattern with the input pattern.
Find the stored pattern that is most similar to combined pattern.
Create a variable called nearest pattern with that stored pattern.
Move the activation pattern a small step toward nearest pattern.
Set the next state activation pattern of the construct to the moved pattern.
The procedure is complete.

The purpose of this procedure is to compute the next state of a GATE construct.
Read the current mode of the construct.
Gather the total drive arriving to switch the gate.
Create a number variable called switch drive with that gathered value.
Read the switching threshold of the construct.
Check if switch drive is greater than the switching threshold.
If the condition is true, then perform the following actions:
    Flip the mode of the construct to the opposite mode.
Otherwise, perform the following actions:
    Keep the mode of the construct unchanged.
End the conditional block.
Set the next state mode of the construct to the resulting mode.
The procedure is complete.

The purpose of this procedure is to compute the next activation of a COMPARATOR construct.
Read the expected input of the construct.
Read the actual input of the construct.
Calculate the difference of the actual input and the expected input.
Create a number variable called prediction error with the difference.
Set the next state activation of the construct to prediction error.
The procedure is complete.

These six procedures are the dynamical heart of the System.

Every region in Chapter 2 and every interface in Chapter 3 runs one of them, differing only in its parameters, its stored patterns, its threshold, or its connections.

This is the machine form of the book's oldest claim.

A2.4 THE DRIVE LOOP: WHAT THE SYSTEM WANTS

The purpose of this procedure is to update every drive and compute the reward for this tick.

Create a list variable called all drives with initial value empty list.
Load every homeostatic drive into all drives.
Create a number variable called total reward with initial value zero.

For each drive in all drives, perform the following operations:
    Read the monitored variable of the drive from the body state.
    Read the set point of the drive.
    Calculate the difference of the monitored variable and the set point.
    Create a number variable called new error with the absolute size of the difference.
    Check if the drive has no previous error recorded yet, which is true only on the first tick.
    If the condition is true, then perform the following actions:
        Set the previous error of the drive to new error, so that the first tick yields no spurious reward.
    End the conditional block.
    Read the previous error of the drive.
    Calculate the difference of the previous error and new error.
    Create a number variable called error reduction with the difference.
    Calculate the sum of total reward and error reduction.
    Set total reward to the sum.
    Set the previous error of the drive to new error.
    Bias action selection toward behaviors that reduce this drive, in proportion to new error.
End the loop.

Broadcast total reward as the phasic dopamine signal for this tick.
The procedure is complete.

Notice the last two lines, and what they accomplish.

The reward is defined as the total reduction of drive error across all drives, exactly the homeostatic reinforcement learning unification of Chapter 9.

And that reward is then broadcast as dopamine, which Chapter 10 established is the third factor in learning.

In these two sentences the drives, the reward, and the learning signal become one quantity.

This is the loop of the mind, closed in plain English.

A2.5 THE LEARNING RULE: HOW THE SYSTEM CHANGES ITSELF

The purpose of this procedure is to change the weight of every interface according to the three-factor rule.

Create a list variable called all interfaces with initial value empty list.
Load every interface that can learn into all interfaces.
Read the current dopamine level as the third factor.
Create a number variable called third factor with the current dopamine level.

For each interface in all interfaces, perform the following operations:
    Read the recent activity at the sending end of the interface.
    Create a number variable called sending activity with that value.
    Read the recent activity at the receiving end of the interface.
    Create a number variable called receiving activity with that value.
    Calculate the product of sending activity and receiving activity.
    Create a number variable called coincidence with the product.
    Calculate the product of coincidence and third factor.
    Create a number variable called raw weight change with the product.
    Multiply raw weight change by the learning rate.
    Create a number variable called weight change with the result.
    Read the current weight of the interface.
    Calculate the sum of the current weight and weight change.
    Set the weight of the interface to the sum.
End the loop.

Apply homeostatic scaling so that each region's average activity stays near its target.
The procedure is complete.

Read the three multiplied quantities in the middle of the loop: the sending activity, the receiving activity, and the third factor.

Those are Hebb's two factors and the neuromodulatory third factor of Chapter 10, and their product is the weight change.

A connection strengthens only when its two ends are active together AND dopamine says the moment mattered.

The final line, the homeostatic scaling, is the bound that keeps the learning from exploding, the negative feedback on plasticity itself.

The whole of Chapter 10 is these two ideas, and here they are, as a procedure a machine can run.

A2.6 THE ELIGIBILITY TRACE, IN BRIEF

The purpose of this procedure is to let a reward strengthen a connection that was active a moment ago.

For each interface in all interfaces, perform the following operations:
    Read the current coincidence at the interface.
    Read the current eligibility trace of the interface.
    Multiply the current eligibility trace by a fading factor slightly less than one.
    Calculate the sum of the faded trace and the current coincidence.
    Set the eligibility trace of the interface to the sum.
End the loop.

When a reward arrives, multiply each interface's eligibility trace by the third factor, and add the result to its weight.
The procedure is complete.

The eligibility trace is a fading memory of recent coincidence, so that a reward arriving a second after an action can still find the connection that earned it.

It is the bridge between fast coincidence and slower reward, and Chapter 10 flagged it as well supported experimentally.

A2.7 REFLECTION

Three reflections on the pseudocode.

First, the choice of ERC over a real language is the phase's central decision, and it directly serves the substrate-independence requirement (NFR-3).

Because the logic above commits to no language, it can be translated into Python, into PrologAI, into any language, without rewriting the thinking.

The pseudocode is the contract; the code in each language must honor it.

Second, the pseudocode makes visible how few moving parts the System has.

A reader who feared that modeling a brain requires a hundred bespoke mechanisms can see here that it requires a main loop, six archetype rules, a drive loop, and a learning rule.

The complexity of the System lives in its DATA, the hundred constructs and their connections, not in its CODE.

This is the deepest engineering consequence of the encapsulation thesis, and it is why the System is buildable at all.

Third, an honest note on the ERC extensions, of which there are two kinds, and both are flagged here rather than hidden.

The first is parameter passing: the base ERC catalogue templates procedures without a formal parameter mechanism, and several procedures above take a construct or an interface as their subject in a way that extends the base notation.

The second is composite operations: sentences such as "Gather the total weighted input arriving from all incoming interfaces," "Wrap next phase back into the range of one full cycle," "Find the stored pattern that is most similar to combined pattern," and "Move the activation pattern a small step toward nearest pattern" are not base ERC primitives but named composite operations, each of which stands for a short block of primitive arithmetic, comparison, and list operations that a full implementation must expand, exactly as ERC's own rule requires composites to expand fully to primitives with no residual ambiguity.

They are written at the composite level here for readability, because expanding every weighted sum and every similarity search into primitive loops would bury the logic the pseudocode exists to make plain.

Both kinds of extension are readable and unambiguous, and both are named in the spirit of ERC's rule that a sentence must have one exact meaning.

A full implementation formalizes the parameter convention and expands the composites; the logic is unaffected.

A2.8 NETWORKS, SYSTEMS, AND THE EXTENDED DYNAMICS LABELS

Three notes keep the load-bearing core above consistent with the fuller body of Chapter 2.

First, a large-scale NETWORK (Section 2.10) is not a new kind of update rule; it is a named VIEW over regions that already run their own archetype rules. The System represents a network as a list of member constructs plus its couplings, and computes a network-level quantity (such as its mean activation or its anticorrelation with another network) by reading the members it already updates. No per-network rule is added to the dispatch of Section A2.2.

Second, a cognitive SYSTEM or PATHWAY (Section 2.11) is likewise not a new rule but a named SUBGRAPH or LOOP over the connection graph. The direct, indirect, and hyperdirect pathways are three labelled routes through the existing basal-ganglia constructs; the Papez circuit is a labelled cycle over existing regions and interfaces. The System represents each as an ordered list of the constructs and interfaces it traverses, and runs it by running those constructs and interfaces in order.

Third, the DYNAMICS labels used in the fuller construct profiles (publisher, broadcast publisher, selector, detector, controller, stabilizer, and the compound labels such as integrator-and-publisher or comparator-and-switch) all reduce to the six archetypes of Section A2.3 plus the molecule FIELD kind, applied singly or in sequence. A publisher or broadcast publisher is a construct that, having computed its state by an archetype rule, writes that state to the neuromodulatory or signalling bus (the field kind). A selector is a GATE; a detector is a COMPARATOR or RELAY; a controller and a stabilizer are a GATE or an INTEGRATOR holding a set-point. A compound label names two archetypes run in sequence within one construct. The glia and the tripartite synapse are MODULATORS: they do not carry a signal but adjust the third factor of the learning rule and the persistence of connections, and so enter the System through the plasticity procedure of Section A2.5, not the update dispatch. This preserves the thesis of six rules, not one hundred: nothing above requires a seventh archetype, and, per Assumption 2 of Appendix 1, one is added only if a construct is ever found that resists all six.

A2.9 THE TEST PLAN FOR EACH CORE PROCEDURE

The SPARC Pseudocode phase requires that the pseudocode carry a plan for how each piece will be tested, so the logic and its verification are designed together. Each core procedure above is paired here with the test that verifies it; the tests themselves are written in full in Appendix 4.

THE MAIN LOOP (Section A2.1) is verified by the scheduler test: a run of N ticks updates every construct exactly N times, the two passes never interleave, and no construct reads a neighbour's future within a tick.

THE DISPATCH (Section A2.2) is verified by dispatching a construct of each archetype and asserting the matching rule was called and no other.

THE SIX ARCHETYPE RULES (Section A2.3) are each verified by their isolation test of Appendix 4, Section A4.2: the relay scales, the integrator fills and leaks, the oscillator keeps time, the attractor completes a pattern, the gate resists flipping, and the comparator reports a difference.

THE DRIVE LOOP (Section A2.4) is verified by presenting a monitored variable that departs from and returns to its set-point and asserting that the reward equals the total reduction of drive error and is broadcast as phasic dopamine.

THE LEARNING RULE (Section A2.5) is verified by the learning test: a stimulus paired with reward strengthens the connecting weight by the product of the two activities and the third factor, and homeostatic scaling holds the average activity near target.

THE ELIGIBILITY TRACE (Section A2.6) is verified by rewarding a connection one tick after it was active and asserting the weight still strengthens, and by withholding reward and asserting the trace fades.

Each plan names the single behaviour its test must pin down, in the test-first spirit of Refinement: the procedure and its test are written as a pair, never the procedure alone.



KONNECTOME REALIZATION LOG

Slice 1 - the heartbeat - 2026-07-20 - pack tick_engine, version 0.1.0.

The main-loop pseudocode of Section A2.1, in its two separate passes over all constructs, is realized by tick_engine_run/5 and tick_engine_step/3. The six-archetype dispatch of Sections A2.2 and A2.3 is deferred to the next slice; the heartbeat uses three minimal update kinds (clock, copy, hold) sufficient to exercise the scheduler.

Gates at this slice: 7 of 7 tick_engine Prolog Unit (PLUnit) tests pass; the naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited; konnectome reuses them through the library path.

This log grows one entry per code slice, per the Tenth Commandment. The full SPARCD Fileset was bumped in lockstep from version 1 to version 2, and the version 1 files were archived under docs/archive/ in the same change, per the Ninth Commandment.


KONNECTOME REALIZATION LOG - SLICE 2

Slice 2 - the six archetype rules and the first Causalontology thought - 2026-07-20 - packs archetype (0.1.0) and observer (0.1.0).

The dispatch of Section A2.2 and the six archetype rules of Section A2.3 are realized by the archetype pack (archetype_step/3 plus the six rule predicates).

Gates at this slice: archetype 7 of 7 and observer 6 of 6 Prolog Unit (PLUnit) tests pass; naming, layer, and layer-binding gates report zero violations; the Causalontology 119-vector conformance suite passes 119 of 119. Nothing in PrologAI or Causalontology was edited; konnectome reuses causal_core through the library path.


KONNECTOME REALIZATION LOG - SLICE 3

Slice 3 - the connection graph - 2026-07-20 - pack connection_graph (0.1.0).

The 'gather the total weighted input arriving from all incoming interfaces' composite of Section A2.3 is realized by connection_graph_total_input/4, and a two-pass network step propagates signals across the graph.

Gates at this slice: connection_graph 4 of 4 Prolog Unit (PLUnit) tests pass (tick_engine 7, archetype 7, observer 6 unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited; connection_graph reuses the archetype pack.


KONNECTOME REALIZATION LOG - SLICE 4

Slice 4 - the reward loop - 2026-07-20 - packs neuromodulator_bus (0.1.0) and drive_system (0.1.0).

The drive loop of Section A2.4 is realized by drive_system_step/6 (reward is the total error reduction, with no spurious first-tick reward), and the reward is broadcast as phasic dopamine on the neuromodulator_bus.

Gates at this slice: neuromodulator_bus 4 of 4 and drive_system 5 of 5 Prolog Unit (PLUnit) tests pass (the earlier four packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 5

Slice 5 - the action selector - 2026-07-20 - pack action_selector (0.1.0).

The 'select one action from the competing drive proposals' step of Section A2.1 is realized by action_selector_select/2.

Gates at this slice: action_selector 6 of 6 Prolog Unit (PLUnit) tests pass (the earlier six packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 6

Slice 6 - the plasticity engine - 2026-07-20 - pack plasticity_engine (0.1.0).

The learning rule of Section A2.5 is realized by plasticity_engine_step/5: the weight change is the product of the sending activity, the receiving activity, and the dopamine third factor, scaled by the learning rate.

Gates at this slice: plasticity_engine 5 of 5 Prolog Unit (PLUnit) tests pass (the earlier seven packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 7

Slice 7 - completing the architecture - 2026-07-20 - packs override_controller (0.1.0) and perturbation_interface (0.1.0).

The action selection under the vitality override is completed by override_controller_arbitrate/4, and the perturbation operations (lesion, cut, shift) are realized for experiments.

Gates at this slice: override_controller 4 of 4 and perturbation_interface 4 of 4 Prolog Unit (PLUnit) tests pass (the earlier eight packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 8

Slice 8 - one running tick - 2026-07-20 - pack cognitive_cycle (0.1.0).

The main loop of Section A2.1 is realized end to end by cognitive_cycle_step/3, which runs the drive loop, the two-pass update, action selection, plasticity, and recording in the specified order.

Gates at this slice: cognitive_cycle 4 of 4 Prolog Unit (PLUnit) tests pass (the earlier ten packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 9

Slice 9 - hardening the loop - 2026-07-20 - packs connection_graph (0.2.0), drive_system (0.2.0), cognitive_cycle (0.2.0).

The gain-reading of Section A2.3 (the neuromodulators set the gain), the action biasing of Section A2.4, and the body's response to action are realized by connection_graph_step_modulated/5, drive_system_proposals/3, and drive_system_apply_action/4.

Gates at this slice: connection_graph 6 of 6, drive_system 9 of 9, and cognitive_cycle 4 of 4 Prolog Unit (PLUnit) tests pass (the other eight packs unchanged); naming, layer, and layer-binding gates report zero violations. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 10

Slice 10 - thought combination - 2026-07-20 - pack thought_combination (0.1.0).

Combining input thoughts into one output thought is realized by thought_combination_combine/3: causes and effects are occurrent identifiers, content-addressed via the reused causal_core.

Gates at this slice: thought_combination 7 of 7 Prolog Unit (PLUnit) tests pass (the earlier eleven packs unchanged); naming, layer, and layer-binding gates report zero violations; the Causalontology 119-vector conformance passes 119 of 119. Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 11

Slice 11 - other minds and prediction, the wall-finding build - 2026-07-22 - packs other_minds (0.1.0) and prediction_loop (0.1.0).

The COMPARATOR rule of Section A2.3 computes a prediction error - the signed difference of the actual and the expected input - and this slice confronts that number with a question the pseudocode never had to ask: where is its record kept. The prediction_loop pack runs the comparator to a signed, graded error each tick (a signed minus-one-point-zero when the object-permanence screen lifts on nothing) and mints the actual outcome as a Causalontology token_occurrence, while the other_minds pack holds an attributed, false, and nested belief in the reused theory-of-mind runtime; but neither the expected occurrence nor the attributed belief can be written down as a shared Causalontology record. The comparator's number is computed exactly as Section A2.3 says, and then has nowhere to live beyond the tick.

Gates at this slice: other_minds 8 of 8 and prediction_loop 10 of 10 Prolog Unit (PLUnit) tests pass (the earlier twelve packs unchanged, 69 tests); naming, layer, and layer-binding gates report zero violations; the Causalontology 119-vector conformance suite is untouched and green. Two walls were hit, recorded as Wall-1 and Wall-2, and ROUTED to the Causalontology change order (Sections B and A). Nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 12

Slice 12 - the closure: minting the new kinds - 2026-07-22 - packs other_minds (0.2.0) and prediction_loop (0.2.0).

The COMPARATOR rule of Section A2.3 computes a prediction error - the signed difference of the actual and the expected input - and slice 11 confronted that number with the question the pseudocode never had to ask: where is its record kept. Slice 12 answers it. With Causalontology 4.0.0 released, the prediction_loop pack now mints the expected outcome as a PREDICTED_OCCURRENCE before arrival (on the start_tick dimension), the actual outcome as a token_occurrence on arrival, and the signed comparator result as a PREDICTION_ERROR record pairing the two - so the number the rule produces each tick now has a durable home beyond the tick. The other_minds pack, in the same slice, mints the attributed belief as an ATTITUDE record - holder, attitude_type, and content - including the false belief that coexists with the world's contrary state_assertion and the nested attribution whose outer content is another attitude's identifier. The minting reuses causal_core_identify exactly as the observer and thought_combination procedures already did; the only thing that changed beneath them is that the library now carries identity-field rows for the three new kinds. That library gap was the closure's own wall, Wall-3, routed to the PrologAI change order and closed the same day by causal_core 1.1.0. No pseudocode procedure changed: the logic of Sections A2.1 through A2.6 is untouched; what changed is only that the comparator's number, and the belief the runtime already held, can now be written down.

Gates at this slice: other_minds 10 of 10 and prediction_loop 16 of 16 Prolog Unit (PLUnit) tests pass (the earlier twelve packs unchanged); naming, layer, and layer-binding gates report zero violations; the Causalontology conformance suite is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. Wall-1, Wall-2, and Wall-3 are CLOSED. Causalontology and PrologAI were extended through their own gated change orders, never edited directly.


KONNECTOME REALIZATION LOG - SLICE 13

Slice 13 - the provenance layer: the mind grades and, when wrong, disowns its own thoughts - 2026-07-26 - pack self_provenance (0.1.0).

No core-loop procedure of this appendix changed. The provenance layer is a minting pattern that sits BESIDE the main loop, exactly as thought_combination and the export halves of other_minds and prediction_loop do; it adds no seventh archetype and reorders none of the steps of Section A2.1. What it adds is a small family of PROVENANCE verbs over the records the loop already mints. Recall the COMPARATOR rule of Section A2.3: it computes a signed prediction error - the actual minus the expected - each tick. Slice 12 gave that number a durable home as a prediction_error record. Slice 13 lets the mind take RESPONSIBILITY for it: assert the record under the mind's own source identity, grade the evidence behind the assertion on the standard's ordered scale, and, when a later tick disconfirms the forecast, retract the assertion. In plain terms, four new procedures join the catalogue beside the loop, each expanding to primitive record construction and the reused content-addressing call: 'assert about a record, grading the evidence and stating a confidence'; 'assert and additionally cite the particular token records that ground the claim'; 'retract a prior assertion, signed by the same source'; and 'supersede - retract the old stance and restate it about the same content under a new grade'. The logic of Sections A2.1 through A2.6 is untouched; what is new is that a thought the loop produced can now be vouched for, graded, disowned, and restated - the provenance layer the data model always assumed, written in the same ERC minting idiom the observer and thought_combination procedures already used.

Honest non-closure recorded this slice: the cryptographic Ed25519 signature the provenance model specifies needs a PRIVATE key, a secret barred from code (Constraint 3), so the signing act is deferred to deployment; the assertion and retraction procedures mint content-addressed, schema-valid records carrying the PUBLIC source identity and the evidence grade, which is the shareable substance, and the signature is applied later by the key-holder. Recorded honestly, not routed.

Gates at this slice: self_provenance 17 of 17 Prolog Unit (PLUnit) tests pass (the earlier fourteen packs unchanged, 95 tests; 112 in total); naming, layer, and layer-binding gates report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; the slice-12 honest non-closure is CLOSED. Nothing in PrologAI or Causalontology was edited; self_provenance reuses causal_core through the library path.


KONNECTOME REALIZATION LOG - SLICE 14

Slice 14 - the capstone: the mind runs, and its own records tell the story - 2026-07-26 - pack capstone_demonstration (0.1.0), and the first runnable entry point, bin/run_capstone_demonstration.sh.

No procedure of this appendix changed, and that is the point worth recording. The capstone adds no seventh archetype, no new update rule, and no step to the main loop of Section A2.1; it is a NARRATION over the procedures the catalogue already holds. Its one new idea is a fold: run the loop for N ticks through the cognitive cycle, then walk the per-tick summaries the loop already emits - tick number, reward, released outcome, minted record - turning each into one plain-English line, and follow with the minting calls the earlier slices already catalogued: the object-permanence trial procedure of the prediction loop, the atomic-and-combine procedures of thought combination, the false-belief attribution and export procedures of other minds, and the assert, supersede, and retract procedures of self provenance. Every line of the story is produced by a procedure that existed before this slice; the capstone merely calls them in rung order and formats what they return. The narration's own procedures observe the same ERC discipline as the loop's - one plain-English comment above every line - so the code that tells the story reads like the story it tells. The guard procedure refuses a tick count that is not an integer of at least six, in the same named-error idiom the provenance procedures established.

Gates at this slice: capstone_demonstration 20 of 20 Prolog Unit (PLUnit) tests pass (the earlier fifteen packs unchanged, 112 tests; 132 in total); naming, layer, and layer-binding gates report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins, re-measured this slice. No new wall was hit and no new honest non-closure was recorded; the slice-13 deployment-signature non-closure stands, and the story itself narrates it. Nothing in PrologAI or Causalontology was edited; the capstone reads its siblings through the library path.


KONNECTOME REALIZATION LOG - SLICE 15

Slice 15 - Rung Two begins: words grounded in lived experience, and pretend under quarantine - 2026-07-26 - packs symbol_exchange (0.1.0) and pretend_play (0.1.0).

No procedure of this appendix changed. The two new packs are catalogue entries beside the loop, in the two idioms the catalogue already holds. symbol_exchange adds one genuinely new fold: the experienced-conditions walk, which steps the whole mind one cognitive-cycle tick at a time and collects the condition (deficit or satisfaction, read from the first drive's own error procedure) the mind passes through - the procedure that turns lived experience into the precondition for grounding a word. Its remaining procedures are small total functions over that result: ground (with three named refusals - a word that is not an atom, a condition outside the closed vocabulary, and the load-bearing one, a condition the mind never lived), produce (condition to word), recognize (hear the word through the reused language procedure, then word to condition), and reply (hear, read the drive condition, and compose the childlike answer - two words in deficit, one at the set-point). pretend_play is the delegation idiom other_minds established, applied to the reused imagination procedures: reset, teach a transition, pretend (roll forward, auto-sealed into the imagined reality), test the quarantine, and promote deliberately - plus one konnectome guard clause standing before the delegation, which throws rather than ever promoting a pretend fact into the observed reality. Two hygiene notes for the catalogue: the reused language procedure leaves an open choice point, closed here with once, and the experienced-conditions walk closes its own base case with a cut, so every new procedure is deterministic exactly as the loop's procedures are.

Gates at this slice: symbol_exchange 18 of 18 and pretend_play 12 of 12 Prolog Unit (PLUnit) tests pass (the earlier sixteen packs unchanged, 132 tests; 162 in total); naming, layer, and layer-binding gates report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; the Section C watch was kept and its result is recorded in the ledger: the per-claim machinery sufficed, no world-level sharing need arose, and Section C remains held. Nothing in PrologAI or Causalontology was edited; the rung reuses the language and imagination packs through the library path, exactly as other_minds reuses theory_of_mind.


KONNECTOME REALIZATION LOG - SLICE 16

Slice 16 - Rung Four deepens: the social-pain test and the empathy trial - 2026-07-26 - pack empathy (0.1.0).

No procedure of this appendix changed, and for this slice that sentence is the demonstration. The social-pain test adds NO new pain procedure: the rejection signal is one body-variable write, and everything after it - the error, the step, the signed reward, the dopamine broadcast - is the drive-loop procedure of Section A2.4 running unmodified over a drive named social_connection instead of temperature. The catalogue gains only small total functions: a guarded model constructor (a distress level refused by name outside zero to one); a resonance fold (subtract the modelled level from the mind's own belonging, floored at zero, with the missing-variable and wordy-value refusals the slice-15 review taught this build to write the first time); and the trial composition - resonate, then the existing proposals procedure, then the existing selection procedure, then the existing body-response procedure. The minting procedures reuse the other_minds record builders (individual, content, attitude) and the self_provenance stance builders, unchanged. The comparator spirit of Section A2.3 runs through the whole slice: the same difference computation, fed a social violation, yields the same signed number - which is precisely the shared-circuitry claim, stated in pseudocode terms.

Gates at this slice: empathy 17 of 17, drive_system 10 of 10, and symbol_exchange 19 of 19 Prolog Unit (PLUnit) tests pass (the other sixteen packs unchanged; 181 in total), the counts carrying two review-born fixes and one paid debt: drive_system (0.3.0) now caps the homeostatic step at its set-point, so a fractional distance settles exactly and help can never overshoot into harm - pinned by its own regression test; naming, layer, and layer-binding gates report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; the remaining ascent of the rung (the appraisal and regulation pilots, and the capstone joining the new milestones into its telling) is recorded as pending, not hidden. Nothing in PrologAI or Causalontology was edited; empathy reads its siblings through the library path.


KONNECTOME REALIZATION LOG - SLICE 17

Slice 17 - the reality-discipline gate: the rule made checkable - 2026-07-26 - bin/check_reality_discipline.sh (no pack version moved).

No procedure of this appendix changed, and none was added: the reality-discipline rule is not runtime logic but a build-time invariant, and its pseudocode fits in one plain sentence - outside the pretend_play pack, no line of konnectome code may name an imagination-pack predicate. The gate that enforces it is a search, not an algorithm: gather every imagination reference under packs/, excuse the pretend_play pack (whose whole purpose is the guarded crossing) and every plain-English comment line (because English Readable Code may honestly DISCUSS the predicates it fences), and refuse the merge if anything remains. The catalogue of procedures is untouched; what grew is the set of sentences the build can prove about itself.

Gates at this slice: no pack changed and no test count moved - the suite stands at one hundred eighty-one Prolog Unit (PLUnit) tests across nineteen packs, all green. The naming, layer, and layer-binding gates report zero violations, and they are joined by a fourth: the new reality-discipline gate (bin/check_reality_discipline.sh) reports zero violations, and was proven able to fail - a deliberate violation was injected, caught, and removed before its first clean run was accepted. The Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited - the imagination pack remains exactly as reused, and the discipline that fences it is konnectome's own.


KONNECTOME REALIZATION LOG - SLICE 18

Slice 18 - the capstone speaks and feels: Rung Two's first words and Rung Four's deepening join the one runnable story - 2026-07-26 - pack capstone_demonstration (0.2.0).

No new procedure enters the appendix's catalogue this slice; what grows is the length of one already-catalogued script - the capstone's narration - and its shape is the same plain shape it has always had: a straight-line sequence in which each scene first ASKS the mind to do a thing through a sibling pack's already-written predicate, then CHECKS that the thing genuinely happened, and only then TELLS it. The two new scenes read exactly as the earlier ones read in English. For the first grounded words: ground a word in a condition the run actually passed through, refusing by name any word not so earned; then ask the mind in deficit for its words and ask the mind at its set-point for its words, and show that the two answers differ. For pretend: run a rollout inside the reused imagination machinery's sealed reality, attempt the promotion into the observed record, and show it refused by name. For the social pain: defend belonging with a drive, sever it, and read back the error, the step, and the reward through the very predicates the physical drive uses. For empathy: model another agent's distress, read the mind's own lowered belonging, let the drives propose and the selector release the help, and confirm that respiration would still seize control first. Every one of these lines is a call OUTWARD into a pack an earlier slice already wrote and tested; the capstone's own pseudocode adds no logic of its own beyond the ordering and the checking, which is precisely why the story can be trusted to describe the mind rather than to perform in its place.

Gates at this slice: capstone_demonstration thirty-six of thirty-six Prolog Unit (PLUnit) tests pass (the other eighteen packs unchanged, one hundred sixty-one tests; one hundred ninety-seven in total across nineteen packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited - the capstone reads its siblings through the library path.


KONNECTOME REALIZATION LOG - SLICE 19

Slice 19 - Rung Four finished: the appraisal and regulation pilots - 2026-07-26 - packs situation_appraisal (0.1.0) and arousal_regulation (0.1.0).

Read as English Readable Code, situation_appraisal reduces to a single new rule whose head is an appraisal and whose body reads a signed set-point error, or a change in that error, from drive_system_error and maps it to a valence: when the reading says a set-point is violated the situation is appraised as a threat the mind fears, and when it says the set-point is met or improving the situation is appraised as good, something the mind desires. The valence written is one of the standard's own closed enumeration - fears or desires and nothing else - and the appraisal is stamped with self_provenance at observation and left open to retraction under Rule 10, so a later reading of the world can withdraw it in the mind's own name. arousal_regulation reads almost as plainly: one tick rule takes the current norepinephrine level on the neuromodulatory bus together with the tonic baseline and states the next level as the level moved halfway toward baseline - a fixed half of the remaining distance closed each tick - with one guarding clause that once the level lies within a hair of baseline the next level is baseline exactly, so the sequence approaches rest and never steps past it. Neither pack adds an update rule; each is a reading rule laid in plain sentences over data its siblings already maintain.

Gates at this slice: situation_appraisal twelve of twelve and arousal_regulation twelve of twelve Prolog Unit (PLUnit) tests pass (the earlier nineteen packs unchanged; two hundred twenty-one in total across twenty-one packs, all green); the four standing gates - naming, layer, layer-binding, and reality-discipline - report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins; no new wall was hit; and no cousin was edited.


KONNECTOME REALIZATION LOG - SLICE 20

Slice 20 - the pilots' review debts paid: appraisal's rounding boundary and regulation's finiteness - 2026-07-26 - packs situation_appraisal (0.1.1) and arousal_regulation (0.1.1).

Slice 20 pays the review debt slice 19 left on the record. The independent adversarial review of the appraisal and regulation pilots, run after they merged, confirmed three findings, and each is now fixed at its root and pinned by a permanent test. First, the appraisal pilot's change reader compared a difference of two independently-rounded set-point errors against exact zero, so two situations equidistant on opposite sides of a set-point - a mathematically neutral change - were read as a good or bad turn on a rounding residue near one part in ten thousand trillion, and, worse, minted as an owned attitude the neutral case is meant to refuse; a rounding-noise band now absorbs that residue, so an equidistant float change reads neutral and is correctly refused before it can become a false record. Second, the regulation pilot accepted a non-finite arousal level, and an infinite level never shrinks toward baseline, so the return would never terminate; the single gatekeeper now requires a finite level and refuses infinity by name. Third, the regulation pilot's whole-number tick rule had no test for its fractional-count refusal; it has one now. No demonstrated behaviour changed - every earlier claim still holds number for number, and every prior test still passes - and the two pilot packs move to patch version 0.1.1. The lesson the method keeps: a finding confirmed by an adversarial review is fixed at its root and pinned, even when the defect is latent - here reachable only by a fractional body value or a hand-built infinity that no ordinary run produces.

Gates at this slice: situation_appraisal fourteen of fourteen and arousal_regulation fourteen of fourteen Prolog Unit (PLUnit) tests pass (the four new tests pin the three confirmed findings; the other nineteen packs unchanged; two hundred twenty-five in total across twenty-one packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite is untouched and green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 21

Slice 21 - Rung Three opened: the concrete-reasoning trials - 2026-07-26 - packs concrete_operations (0.1.0) and cause_and_effect (0.1.0).

THE READING RULES OF THIS SLICE, IN ENGLISH READABLE CODE. Slice 21 adds two packs and, between them, five plain rules. Every line of both modules carries its plain-English comment above it, as the Sixth Commandment requires, so the pseudocode and the code are the same document read at two distances.

THE FIRST RULE - RESTING IS ABOVE. To derive that one thing stands above another: if the thing rests directly on the other, that is the answer, and the step is named resting_is_above. Otherwise find what the thing rests on, derive that the middle thing stands above the target, and join the two with a step named transitive_chain. Return the steps themselves, in order, each carrying the facts it consumed and the fact it produced - so the reader is never asked to trust a leap.

THE SECOND RULE - SORTING AND ORDERING. To classify: read each object's value for the chosen property, refuse by name any object that lacks a fully-instantiated value, gather the distinct values in sorted order, and place each object in the group whose value it EXACTLY equals - never merely unifies with. To seriate: read each object's numeric value for the chosen dimension, refuse by name any object without one, and order the objects by that number, smallest first.

THE THIRD RULE - WHAT A TRANSFORMATION DOES TO AN AMOUNT. If the transformation's name is on the closed appearance-only list, the amount is conserved and the verdict says why. If it is an addition or a removal, the amount honestly changes - and a removal larger than what is present is refused by name, as is an addition or removal carrying anything but a non-negative number. Any other name is refused as an unknown transformation.

THE FOURTH RULE - CAUSE, OR MERE CO-OCCURRENCE. Check every offered record is truly minted, by re-deriving its content address from its own content. Then walk forward from the supposed cause through the records that carry it, collecting each record's identifier as justification, until the supposed effect is reached: that is a genuine cause, and the chain of identifiers is the reason given. If no forward chain exists but a backward one does, report the reverse. If neither exists, report co-occurrence only - the honest answer for two events seen together with no record between them.

THE FIFTH RULE - MEANS TO END. Search the known actions breadth-first from the start state, never revisiting a state, until the goal state is reached; the first plan found is a shortest plan. Each step names its action and the states it leaves and reaches. Commit to the plan by owning it as an intention, and refuse by name any step that does not carry its full justification.

Gates at this slice: concrete_operations twenty-six of twenty-six and cause_and_effect twenty-five of twenty-five Prolog Unit (PLUnit) tests pass (the earlier twenty-one packs unchanged; two hundred seventy-six in total across twenty-three packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 22

Slice 22 - Rung Three finished in its trials: the two deferred halves built - 2026-08-07 - packs category_formation (0.1.0) and quantity_model (0.1.0).

THE READING RULES OF THIS SLICE, IN ENGLISH READABLE CODE. Slice 22 adds two packs and, between them, six plain rules. Every line of both modules carries its plain-English comment above it, as the Sixth Commandment requires.

THE FIRST RULE - FORMING A CATEGORY FROM EXPERIENCE. To learn categories: refuse anything that is not a proper list of well-formed experiences, refuse a doubled experience name and a doubled property key by their own names, and refuse a shared-pair floor that is not a positive whole number. Then start the reused inducer from a clean store, observe every experience as a bundle of property-value pairs, cluster them - each still-ungrouped experience seeds a group and pulls in every later one that keeps the group's shared core at or above the floor, and a group of two or more becomes a category - and clean the store again on the way out, so the episode owns the store and nothing leaks in or out. Rename the engine's terse identifiers to whole words at this one boundary, and refuse an identifier the boundary does not recognize rather than failing quietly.

THE SECOND RULE - SORTING INTO ONE'S OWN CATEGORIES. To sort: take the mind's own formed categories, refuse by name any hand-built category that breaks the learner's invariants - an empty defining core, an unsorted core, the reserved outside-every-category name, a doubled identifier - and place each object in the FIRST category whose defining core its properties carry. An object fitting no category is reported outside every category, honestly, never forced.

THE THIRD RULE - THE FLEXIBLE RE-SORT. To re-sort by a different property on request: convert each experience to the sibling classifier's object shape and hand the whole collection to that classifier - the re-sort IS the sibling, reused unmodified, and its refusals surface under its own names.

THE FOURTH RULE - REPRESENTING AN AMOUNT. An amount is a list of individuated units - unit one, unit two, and so on, each name distinct - together with a fully-built appearance. Refuse a negative or fractional amount, a half-built appearance, and any hand-built model whose unit names collide.

THE FIFTH RULE - THE VERDICT IS A MEASUREMENT. A pour, a spread, a reshape, or a rearrangement replaces the appearance and carries every unit across, one by one; an addition mints fresh units past the highest existing name; a removal takes real units away and refuses to take more than are present; a lossy pour does both an appearance change and a real loss, and a lossy pour that loses nothing is refused as a pour wearing the wrong name. The conservation verdict then compares the measured count before against the measured count after - it never reads what the transformation was called. That is why the taller, thinner glass conserves and the lossy pour does not: the model keeps the truth the surface hides.

THE SIXTH RULE - OWNING BY ENACTING. To own a conclusion on the record: enact it. The category record predicate runs the learning episode itself and mints what it formed; the conservation record predicate runs the trial itself and mints what it measured. A hand-written conclusion cannot be owned as if it had been enacted, because the record predicate accepts no quoted text at all.

Gates at this slice: category_formation twenty-six of twenty-six and quantity_model twenty-nine of twenty-nine Prolog Unit (PLUnit) tests pass, and concrete_operations' twenty-six stay green under its re-sized header (the earlier packs otherwise unchanged; three hundred thirty-one in total across twenty-five packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 23

Slice 23 - The story learns its owed chapters: Rung Three told, and Rung Four finished in the telling - 2026-08-07 - pack capstone_demonstration (0.3.0).

THE READING RULES OF THIS SLICE, IN ENGLISH READABLE CODE. Slice 23 adds no pack and no new rule of dynamics; it adds two chapters to the one story, and each chapter reads the same way: check, then tell.

THE RULE OF THE REASONING CHAPTER. Derive the block above the table and refuse to narrate unless the derivation IS the expected three named steps. Seriate the pea, the apple, and the brick and refuse to narrate any order but smallest-first. Form the categories from the four experiences and refuse to narrate unless exactly the two expected categories, with their defining cores and members, came back. Sort the pencil and refuse to narrate unless it landed honestly outside every category. Re-sort by shape and refuse to narrate unless the round group crosses the formed partition. Pour the five units and refuse to narrate anything but conserved at five; spill one on the way and refuse to narrate anything but changed, five to four. Mint the verdict record by running the pour again inside the record predicate. Link the sun to the warm stone by a minted record and refuse to narrate any finding but genuine cause with that record's identifier; ask the same question of the rooster and the sunrise and refuse to narrate anything but co-occurrence only. Plan from the bare camp and refuse to narrate any plan but the shortest three steps. Only then print the chapter.

THE RULE OF THE FINISHING CHAPTER. Appraise the too-warm body and refuse to narrate anything but threatened at distance three; appraise the settled body and refuse anything but satisfied at zero; appraise the cooling and the heating and refuse anything but good two and bad two. Own the threat as fears and the satisfaction as a desire to keep - saying aloud that the enumeration has no word for satisfaction. Grade the stance at observation, withdraw it when the body cools on its own. Arouse the bus to one, step once and refuse anything but one half, regulate to rest and refuse any landing but exactly baseline in exactly eight ticks. Only then print the chapter.

Gates at this slice: capstone_demonstration forty-four of forty-four Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; three hundred thirty-nine in total across twenty-five packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 24

Slice 24 - Rung Five opened as groundwork: the body interface, and the sense-act loop through the honestly-named simulated body, the rung itself not claimed - 2026-08-07 - packs simulated_body (0.1.0) and body_interface (0.1.0).

THE READING RULES OF THIS SLICE, IN ENGLISH READABLE CODE. Slice 24 adds two packs: a small world and the seam onto it.

THE RULES OF THE STAND-IN MACHINE. A body is a camera, a physical state carrying a battery charge, and an ordered log of everything the machine has done. The world may show the camera something new. Steering around clears a seen obstacle - and is refused by name when the path is already clear, because a steer around nothing is a lie. Recharging fills the battery to full, capped there. Holding still changes nothing but the log, because even doing nothing belongs in the history. Time drains the battery and floors at empty - a battery cannot go below empty, and that floor is physics; a NEGATIVE drain, which would charge by stealth, is refused by name. Anything that is not a body is refused wherever a body is expected.

THE RULES OF THE SEAM. To sense: surface every camera reading as a percept, unchanged, and hand the physical state to the drives AS the homeostatic state - the identity, made explicit. To propose: let every drive propose reducing itself at a salience equal to its own error, keep only the proposals at or above the pressing threshold of one half - a mind with nothing pressing does nothing - and let a seen obstacle propose steering at its fixed reflex salience of one, drives before reflexes in a fixed order, so that at an exact tie the starving battery outranks the seen obstacle, deliberately. To act: let the selector release exactly one proposed action or nothing, map the release to the one actuator that enacts it - recharge for the hunger, steering for the obstacle, stillness for nothing - and refuse by name any release no actuator carries. To survive: each tick, drain, then live one whole pass, and write one glass-box trace entry naming the charge before, the outcome, and the charge after. To own: run the pass inside the record predicate and mint only what it released; and before granting the observation grade to any record, re-derive its address from its content and refuse the forgery.

Gates at this slice: simulated_body sixteen of sixteen and body_interface twenty-one of twenty-one Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; three hundred seventy-six in total across twenty-seven packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 25

Slice 25 - The story learns the body seam: Rung Five's groundwork told, and the rung not claimed - 2026-08-07 - pack capstone_demonstration (0.4.0).

THE CHAPTER, IN ENGLISH READABLE CODE. The new chapter follows the standing guard-then-tell shape every capstone chapter keeps: first the guards - boot the stand-in machine; show it the obstacle; require the percept to be exactly the camera's reading; run one closed pass and require the steering release, the logged movement, and the clear camera; read the half-charge hunger and the half-degree thermal error through the one shared predicate and require them equal; live the six-tick survival run and require the exact pinned trace; starve the machine, show it the obstacle, and require the tie to break toward the drives; enact the loop record and require its content to be the enacted release; mint the stance and require the observation grade - and only then the telling, each printed sentence formatted from the guarded values, so a drifted engine silences the story rather than letting it lie. Every line of the chapter and its tests carries its plain-English comment, per the English Readable Code rule.

Gates at this slice: capstone_demonstration fifty-two of fifty-two Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; three hundred eighty-four in total across twenty-seven packs, all green); the naming, layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 26

Slice 26 - The naming gate learns to see: the unprefixed-helper hole closed, and the hand-hunt retired - 2026-08-07 - tooling bin/check_pack_naming.sh, with a committed fixture and a self-test.

THE CHECK, IN ENGLISH READABLE CODE. For each pack: read every name that begins a line of the pack's module file (clause heads sit at the left margin in the house style; directives begin with the neck symbol and are not names; body goals are indented and are not read). For each such name: if it is the pack's own name, or begins with the pack's own name and an underscore, it obeys the rule; if it begins with the sanctioned shared namespace prologai_, it is allowed; otherwise it is an unprefixed helper, and the gate reports it as a violation naming the pack, the helper, and the prefix it should carry. The self-test runs the same gate over the committed fixture tree and demands the failure: if the fixture's deliberate violation is not caught by name, the self-test itself fails, loudly, saying the hole is open again.

Gates at this slice: the extended naming gate reports zero violations across all twenty-seven packs AND passes its own self-test - the committed fixture's unprefixed helper is caught by name; the layer, layer-binding, and reality-discipline gates report zero violations; three hundred eighty-four Prolog Unit (PLUnit) tests across twenty-seven packs, all green and unchanged by this slice; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 27

Slice 27 - The delay lines: the connectome learns to keep a signal in transit - 2026-08-07 - pack connection_graph (grown).

THE DELAY LINE, IN ENGLISH READABLE CODE. To build the lines for a graph: check every interface's delay - an integer of at least one tick, computational interfaces included, because a bad delay is a bad graph whether or not the interface carries a line - then, for each transmissive interface, make a line holding the source, the destination, the weight, and a buffer of delay-minus-one zeros, the silence seeding a new wire. To advance one delayed tick: for each line, read the source's current activation (refusing aloud a source the state does not know); join that emission to the back of the buffer; deliver the value at the front, scaled by the interface's weight; keep the rest in transit. Then, for each construct: a clamped source holds its activation; a fixed-gain relay gathers the total delivered input and applies the relay archetype rule; any other kind is refused by name, so the state can never shrink in silence. Commit all next activations at once, as ever, in the two-pass synchronous style. To run: build the lines, then loop the delayed step for the requested ticks, recording each tick's snapshot. Every line of the code and its tests carries its plain-English comment, per the English Readable Code rule.

Gates at this slice: connection_graph seventeen of seventeen Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; three hundred ninety-five in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 28

Slice 28 - The eligibility trace: a reward one tick late still finds the connection that earned it - 2026-08-07 - pack plasticity_engine (grown).

THE ELIGIBILITY TRACE, IN ENGLISH READABLE CODE. To build a fresh trace store for a graph: for each transmissive interface, record a zero trace under the interface's source-and-destination key; a computational interface carries no trace. To advance the traces one tick: first refuse aloud a fading factor not at least zero and strictly below one, then sweep the whole topology through the shared refusal perimeter - every element a well-formed interface of a known kind, no duplicate transmissive endpoints, no duplicate trace keys, no trace orphaned of its interface - and only then, for each transmissive interface: read its current trace (refusing aloud a trace the store does not know); multiply the trace by the fading factor; read the two ends' activities (a silent absent end counting as zero, exactly as the plain learning step counts it); add their product, the fresh coincidence, to the faded trace; and record the sum as the new trace. To spend the traces when a reward arrives: sweep the same perimeter, read the dopamine level from the neuromodulatory bus as the third factor, and, for each transmissive interface, grow its weight by the trace multiplied by the third factor and the learning rate, leaving every computational weight untouched. Every line of the code and its tests carries its plain-English comment, per the English Readable Code rule.

Gates at this slice: plasticity_engine twenty-four of twenty-four Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; four hundred fourteen in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 29

Slice 29 - Homeostatic scaling: the bound that keeps the learning from exploding - 2026-08-07 - pack plasticity_engine (grown).

HOMEOSTATIC SCALING, IN ENGLISH READABLE CODE. To build a fresh average store: refuse aloud a malformed or duplicated activation entry, then, for each named construct, record a zero running average under the construct's name. To advance the averages one tick: refuse aloud a smoothing factor not above zero and at most one, and a malformed, duplicated, or unbound store; then, for each construct in the store, read its current activity (a silent absent construct counting as zero, as ever), and move its average a smoothing step toward that activity - the old average times one minus the smoothing factor, plus the activity times the smoothing factor. To apply the bound: refuse aloud a negative activity target, a scaling rate not at least zero and strictly below one, and every malformed interface, unknown kind, duplicate, and ghost; then, for each transmissive interface, read the RECEIVING end's running average, form the factor one plus the scaling rate times the target minus that average, floor the factor at zero, and multiply the weight by the factor - all of a region's inputs sharing the one factor, computational interfaces untouched. The judgement of a keyed store - shape, unbound tails, duplicates - is one shared procedure, used by the average store, the trace store, and the constructors alike, so no consumer of a store can ever disagree with another about what a well-formed store is. Every line of the code and its tests carries its plain-English comment, per the English Readable Code rule.

Gates at this slice: plasticity_engine forty-eight of forty-eight Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; four hundred thirty-eight in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 30

Slice 30 - Per-territory neuromodulators: the field learns local concentrations, and delayed time learns the bus - 2026-08-07 - packs neuromodulator_bus and connection_graph (grown).

THE TERRITORY FIELD AND THE DELAYED MODULATED TICK, IN ENGLISH READABLE CODE. To broadcast into a territory: refuse aloud a modulator or territory name that is unbound or not a plain atom; drop any existing level for that modulator in that territory, so the newest broadcast wins; add the new territory entry beside the global pairs and keep the bus in canonical order. To read a territory's level: refuse the same wrongs aloud; if the territory carries its own entry for the modulator, answer that level; otherwise answer the global broadcast, the diffuse field; and the global read already answers zero when the field itself is silent. To advance one delayed modulated tick: advance every delay line exactly as the delayed step does - the source's emission joins the back, the value whose time has come is delivered, weighted, and a ghost source is refused aloud; then, for each construct: a clamped source holds its activation; a fixed-gain relay gathers its delivered input and applies the relay rule; a bus-modulated relay gathers its delivered input, reads its modulator's level IN THE TERRITORY OF ITS OWN NAME at this tick of delivery, scales its base gain by that level, and applies the relay rule; any other kind is refused by name. Commit all next activations at once, in the two-pass synchronous style. Every line of the code and its tests carries its plain-English comment, per the English Readable Code rule.

Gates at this slice: neuromodulator_bus fourteen of fourteen and connection_graph twenty-four of twenty-four Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; four hundred fifty-five in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 31

Slice 31 - The per-territory third factor: each connection learns by the chemistry of the place the signal arrives - 2026-08-07 - pack plasticity_engine (grown at its root).

THE TERRITORY-READ LEARNING TICK, IN ENGLISH READABLE CODE. To learn for one tick: first refuse aloud every wrong interface - a term that is not an interface, an unbound or unknown kind, a duplicate transmissive endpoint pair - before a single weight moves. Then, for each transmissive interface: read the sending and receiving activities, an absent construct counting as silent; read the dopamine level IN THE TERRITORY OF THE RECEIVING END through the diffuse-field law - the territory's own concentration if it has one, the global broadcast otherwise, silence with neither; multiply the two activities and the level and the learning rate, and add the product to the weight. A computational interface keeps its weight. To spend a reward: judge the whole topology aloud as before - the malformed, the unknown, the duplicate, the ghost trace, the orphan trace - then, for each transmissive interface, read its trace, read the dopamine level in the territory of its receiving end at this moment of spending, and add trace times level times learning rate to the weight. Every line of the code and its tests carries its plain-English comment, per the English Readable Code rule.

Gates at this slice: plasticity_engine fifty-eight of fifty-eight Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; four hundred sixty-five in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 32

Slice 32 - The live tick carries the whole learning body: traces, averages, and the slow bound beat in the loop - 2026-08-08 - pack cognitive_cycle (grown; the boot worlds of capstone_demonstration and symbol_exchange grown to carry the stores).

THE THREADED TICK, IN ENGLISH READABLE CODE. To run one whole tick over a world: read every key the tick needs through one reader that refuses a missing key aloud by name - the tick count, the body, the drives, the bus, the constructs, the activations, the interfaces, the trace store, the average store, the fading factor, the smoothing factor, the scaling target, the scaling rate, the overrides, the threshold, the learning rate, and the simulation start. Let the drives compute the reward and broadcast it as dopamine; gather the proposals; advance every construct in the two-pass synchronous update; release one action under the override controller. Then let the learning body beat in order: the fast three-factor step grows the weights at each receiving territory's chemistry; the trace step fades every trace and adds the tick's fresh coincidence; the average step moves every running average a smoothing step toward the tick's activity; and the scaling step multiplies each region's incoming weights by one plus the scaling rate times its target-minus-average error. Move the body by the released action, advance the tick, record the thought, and commit the new world with the advanced stores inside it. To run many ticks: refuse aloud, by name, a tick count that is unbound, not an integer, or below zero - time never runs backward - then loop the single tick. Every line of the code and its tests carries its plain-English comment, per the English Readable Code rule.

Gates at this slice: cognitive_cycle eleven of eleven Prolog Unit (PLUnit) tests pass, and the grown boot worlds of capstone_demonstration (fifty-two of fifty-two) and symbol_exchange (nineteen of nineteen) re-swept green (the earlier packs unchanged; four hundred seventy-two in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 33

Slice 33 - The per-territory scaling target: each region defends the activity level its own territory owes - 2026-08-08 - pack plasticity_engine (grown at its root; cognitive_cycle grown to read the geography; the boot worlds of capstone_demonstration and symbol_exchange grown to carry it).

THE TARGET GEOGRAPHY, IN ENGLISH READABLE CODE. To scale a graph under a target geography: refuse aloud a global target no region could defend, a rate that would overcorrect, and a geography carrying a malformed pair, a duplicate key, or an indefensible target; then, for each transmissive interface, read the receiving region's running average (refusing a ghost aloud), read the target the receiving territory defends - its own entry if the geography carries one, the global target if not - and multiply the weight by one plus the rate times the target-minus-average error, floored at zero. The old one-target step is one line: the territory step under an empty geography. In the live tick, read one more world key - the target geography, empty meaning every region defends the global target - and pass it to the scaling step; a world without the key is refused by the key's name. Every line of the code and its tests carries its plain-English comment, per the English Readable Code rule.

Gates at this slice: plasticity_engine seventy of seventy Prolog Unit (PLUnit) tests pass and cognitive_cycle thirteen of thirteen, with the grown boot worlds of capstone_demonstration (fifty-two of fifty-two) and symbol_exchange (nineteen of nineteen) re-swept green (the earlier packs unchanged; four hundred eighty-six in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 34

Slice 34 - The bound turned live in the told story: the capstone world carries a live rate and a small geography, and the story tells the bound's work beside a rest twin - 2026-08-09 - pack capstone_demonstration (grown; no other pack changed).

THE LIVE BOUND'S TELLING, IN ENGLISH READABLE CODE. To boot the capstone world: set the diffuse mark at one half, give the relay b its own mark of eight tenths in a one-entry geography, and set the slow rate at five hundredths - the bound live, everything else exactly as before. To tell the bound's work: run the mind for the requested ticks and read the final weight; then boot the same world again, put the bound to rest - rate zero, no geography - and run the same ticks; claim regulation only if the temperature stands at thirty-seven, claim learning only if the live weight stands above its boot value, and claim the bound's work only if the live weight stands above the rest twin's; then say all three aloud - the live weight grown by the fast rule and pulled further by the bound, and the rest weight beside it, so the difference is shown, never merely asserted. To judge a tick count: an unbound count is refused as uninstantiated before any judgement pretends to name it; a bound count must be a whole number of at least six or is refused by its own name. Every line of the code and its tests carries its plain-English comment, per the English Readable Code rule.

Gates at this slice: capstone_demonstration sixty of sixty Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; four hundred ninety-four in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 35

Slice 35 - The two-shift switchboard laid: the global operating state rides the bus, online by default, and nothing yet reads it - 2026-08-09 - pack neuromodulator_bus (grown; no other pack changed).

THE OPERATING STATE, IN ENGLISH READABLE CODE. To read the operating state: refuse an unbound bus as uninstantiated; then, if the bus carries a state entry, answer that entry, and otherwise answer online, the waking default. To announce an operating state: refuse an unbound state as uninstantiated; refuse any value that is not exactly online or offline by the state's own name, because the flip-flop has two positions and spends no time halfway; then drop any older state entry so the newest announcement wins, add the new one under the third key shape, and keep the bus in its one canonical sorted order. The chemical broadcasts do not change by a line: a level written or read beside the state neither sees it nor disturbs it. Every line of the code and its tests carries its plain-English comment, per the English Readable Code rule.

Gates at this slice: neuromodulator_bus twenty-three of twenty-three Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; five hundred three in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 36

Slice 36 - The reward learns its address: the tick spends the eligibility traces the drives reward, and capture consumes the spent tags - 2026-08-09 - packs plasticity_engine and cognitive_cycle (grown; no other pack changed).

THE REWARD CAPTURE, IN ENGLISH READABLE CODE. To capture one interface: if the interface is computational, keep its weight; otherwise read its tag, refusing a ghost aloud; read the dopamine at the receiving end's own territory, local chemistry first and the diffuse field as fallback; judge the spend's arithmetic on every path, so an unbound weight, tag, or learning rate is refused aloud even on a rewardless tick; then, if the dopamine is exactly zero, keep the weight bit-identical, and otherwise add the tag times the signed dopamine times the learning rate. To settle one tag's fate: read the same territory dopamine the weight change read, so the two fates cannot drift apart; a served tag is consumed to zero, an unserved tag stands. To run the step: refuse an unbound bus as uninstantiated before the chemical reads can silently bind it; refuse every malformed interface, unknown kind, duplicate, ghost, and orphan through the shared perimeter; then capture every interface and settle every tag. In the live tick, the capture runs after the fast learning and before the trace fade, on the traces as the tick found them. Every line of the code and its tests carries its plain-English comment, per the English Readable Code rule.

Gates at this slice: plasticity_engine eighty-two of eighty-two and cognitive_cycle sixteen of sixteen Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; five hundred eighteen in total across twenty-seven packs, all green); the naming gate reports zero violations across all twenty-seven packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 37

Slice 37 - The night watchman takes the post: the two-process governor rides the live tick and throws the slice-35 switch, its first legitimate thrower - 2026-08-09 - pack two_process_governor (new) and pack cognitive_cycle (grown; the fixture worlds of symbol_exchange and capstone_demonstration seat the watchman; no behaviour elsewhere changed).

THE NIGHT WATCHMAN, IN ENGLISH READABLE CODE. To build a governor: refuse a parameter block with any hole or any non-number aloud before a governor carries it; refuse a day of no ticks by name, and refuse an unbound day length as uninstantiated, because a silent integer test would otherwise smuggle the hole into the refusal itself; refuse an inverted hysteresis band by name, because a wake threshold above the sleep threshold chatters; then start the day at midnight, phase zero, with the whole debt paid. To step the governor one tick: judge the whole governor first - every field a bound number, on every path, so no branch can shortcut the judging; refuse an unbound operating state as uninstantiated and any third position by name, because the flip-flop has two positions and spends no time halfway; then let Process S move - a waking tick adds the rise, a sleeping tick subtracts the discharge and floors at zero, the floor judged, never skipped; let Process C move - the phase advances one and wraps at the day length, and the day wave reads zero at midnight, the full amplitude at midday, and straight lines between; take the balance, the pressure less the wave; and let the flip-flop select - an online switch snaps offline at or past the sleep threshold and otherwise holds, an offline switch snaps online at or below the wake threshold and otherwise holds. In the live tick, at the tick's end: read the standing selection off the bus, the waking default when the bus has never heard one; step the governor under it; announce the governor's selection back onto the bus for every subscriber; and commit the advanced governor into the world beside the announced bus. Every line of the code and its tests carries its plain-English comment, per the English Readable Code rule.

Gates at this slice: two_process_governor twenty of twenty, cognitive_cycle twenty-one of twenty-one, symbol_exchange nineteen of nineteen, and capstone_demonstration sixty of sixty Prolog Unit (PLUnit) tests pass (the earlier packs unchanged; five hundred forty-three in total across twenty-eight packs, all green); the naming gate reports zero violations across all twenty-eight packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 38

Slice 38 - The night crew takes the dark hours: the tick reads the switch the watchman throws, and the offline works run - interleaved replay against catastrophic forgetting, and the renormalisation shift - 2026-08-09 - pack offline_consolidation (new), pack cognitive_cycle (grown), and pack neuromodulator_bus (one additive export); the fixture worlds of cognitive_cycle, symbol_exchange, and capstone_demonstration seat the three new keys, and the online day is behaviour-pinned identical.

THE NIGHT CREW, IN ENGLISH READABLE CODE. To remember the day: refuse an unbound store, a malformed pair, a non-numeric activity, and a construct named twice, each aloud by name; then append today's pattern at the END, because the store reads oldest first. To interleave the store for the night: count the snapshots; give the older half the odd extra, because the standing cortex outweighs one day's hippocampus; split the store at that place; then alternate the newer half against the older, newest work first, and let whichever half runs long close the batch. To replay one round: refuse a cortex that is a hole, a partial list, a non-list, a malformed edge, an edge with a hole at either end, or an edge of an unknown or unbound kind, each aloud by name, because every one of those used to be answered with a silently wrong cortex; refuse a negative replay rate, because the night strengthens or leaves alone and must never unlearn the day; build the interleaved batch, the store judged whole on the way; SHARE the rate across the batch, so the dose is what one night delivers rather than what one memory costs; then for each batched pattern in order, for each interface, read the two ends' remembered activities - an unnamed construct replaying as silent, exactly as the waking engine reads absence - take the day's own coincidence product with the third factor held at one, scale it by that shared per-pattern dose, and add it to the weight, leaving computational interfaces untouched. To run one whole night tick: replay first, then apply the day's own per-territory homeostatic bound at the night's RAISED rate to the REFRESHED weights, judging the day's standing averages. In the live tick: read the standing selection off the bus at the tick's OPENING, the waking default when the bus has never heard one, and JUDGE it against the bus's own two-position domain so no hole can run both programs and no third value can fail the tick in silence; refuse either night rate aloud before anything moves, on a waking tick exactly as on a sleeping one; if the state is online, run the whole waking tick exactly as before and then remember its updated pattern; if the state is offline, leave the body, the drives, the bus, the activations, the traces, and the averages exactly as the day left them, run the night works over the remembered day, and report a zero reward and an offline works outcome; then, either way, step the watchman and announce its selection for the next tick. Every line of the code and its tests carries its plain-English comment, per the English Readable Code rule.

Gates at this slice: offline_consolidation twenty-seven of twenty-seven and cognitive_cycle thirty-four of thirty-four Prolog Unit (PLUnit) tests pass, with neuromodulator_bus twenty-three of twenty-three, symbol_exchange nineteen of nineteen, and capstone_demonstration sixty of sixty unchanged through the reseated worlds (the earlier packs unchanged; FIVE HUNDRED EIGHTY-THREE in total across twenty-nine packs, all green); the naming gate reports zero violations across all twenty-nine packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 39

Slice 39 - Every construct becomes a hybrid automaton: the register of one, stated proudly - 2026-08-09 - pack mode_register (new), packs connection_graph and tick_engine (rewired so every construct's rule is read THROUGH its own register); no world dict key added, no number changed, and the engines pinned behaviour-identical against a main worktree for every well-formed construct kind.

THE REGISTER, IN ENGLISH READABLE CODE. To build one construct's hybrid automaton: assemble the current mode, the register, the transfer block, the transition table and the fault block into the corpus's own term, and then judge every block before the term is handed back, so an invalid automaton never exists to be read. To judge the register block: judge the store's shape first, refusing a hole, a partial list, or a non-list all the same way, so a hole can never be walked as though it were an empty register; refuse an empty register aloud, because a register of zero is not a register and the corpus records none of zero at any grain; read the formal name out of every entry, refusing anything that is not the corpus's three-field entry and any name that is not an atom; and refuse two entries that share a name, because the transfer lookup would be ambiguous. To judge the current mode: refuse a hole, because a membership check would bind it to the first entry, and refuse a mode the register does not declare, because a construct cannot stand in a mode it does not have. To judge the transfer block: judge its shape exactly as the register's shape was judged, because every store is judged the same way; read the mode name out of every row, refusing a malformed row and a non-atom key; refuse a rule for a mode the register never declared; refuse two rules under one mode name; and refuse a declared mode carrying no rule, because a mode that cannot run is not a mode. THE REGISTER'S NAMES AND THE TRANSFER BLOCK'S NAMES ARE JUDGED AGAINST EACH OTHER IN ONE PLACE, so the two blocks cannot drift apart. To judge the transition table: judge its shape the same way, refuse any row that is not the corpus's four-field row, and refuse either endpoint that the register does not declare, so the machine can never leave its own register. To judge the fault block: judge its shape the same way and refuse any entry that is not the corpus's three-field fault.

To read a register: open the automaton, refusing a hole aloud as an instantiation fault and an impostor term aloud by name, and hand back the block asked for. To read the rule that holds right now: read the current mode, then read the rule filed under it - and refuse an unbound mode key at that lookup, because a hole there would be bound to the store's first entry and answered with another mode's rule.

To give a konnectome construct its register: refuse an unbound kind, because a dispatch on a hole binds itself to the first clause it meets and leaves a choicepoint into the rest; look the kind up in the table of single modes; and build a register of one, filing the construct's OWN rule as that mode's transfer function, with an empty transition table because a register of one has nowhere to go, and an empty fault block because faults are watched and konnectome has no watcher yet. To resolve the rule an engine should apply: refuse an unbound kind at that door too; if the kind has a register, read its current mode's rule out of it; and if it has none, hand the kind through UNCHANGED, so the engine's own refusal stays the authority on what the engine accepts.

And in the two engines: one construct's next value is no longer computed by dispatching on the construct kind. It is computed by reading the rule that holds under the construct's current mode, and then applying that rule - the same clauses as before, one indirection further in. Every line of the code and its tests carries its plain-English comment, per the English Readable Code rule.

Gates at this slice: mode_register forty of forty Prolog Unit (PLUnit) tests pass at birth, with connection_graph grown from twenty-four to twenty-six and tick_engine from seven to eight (the earlier packs unchanged; SIX HUNDRED TWENTY-SIX in total across THIRTY packs, all green); the naming gate reports zero violations across all thirty packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.


KONNECTOME REALIZATION LOG - SLICE 40

Slice 40 - The gate archetype re-expressed as its genuine two-mode register - 2026-08-09 - pack archetype (the gate given a register of TWO, per-mode transfer functions that really differ from each other, and an explicit transition table carrying trigger, direction, timescale and agency); no world dict key added, no number changed, and every well-formed gate answer pinned byte-identical against a main worktree.

WHAT THE PHASE REQUIRED. Slice 39 gave every construct a register of ONE, and the design authority - docs/konnectome_mode_register_design_authority_v1.txt - named the slice after it in its Part Nine: THE GATE ARCHETYPE RE-EXPRESSED as its genuine two-mode register, with an explicit transition table, validated against corpus entries whose registers are two. The reason this is the next slice rather than a later one is the design authority's own: of everything konnectome had built in thirty-eight slices, the gate archetype's binary flip is "a genuine two-mode register in the corpus's own sense and is the natural seed of the whole design", and its existing vocabulary is corpus-correct and must not be renamed. A register of one is first-class, but it is also the one register length that cannot catch a mistake, because with a single mode the register block, the transfer block and the current-mode slot have nothing to disagree about. Two modes is where the formalism starts doing work.

THE PSEUDOCODE OF THE GATE, AS IT NOW READS. The gate rule no longer consults a two-clause flip table. It builds its own register, standing in the mode it says it is in, and then asks the TABLE where a departure lands:

    To decide a gate's next mode, given the mode it is in, a switching drive and a threshold:
      Build this gate instance's hybrid automaton, standing in the given mode.
        (The constructor judges the mode: a hole or a mode the register does not hold is
         refused aloud here, at the door, before any threshold is compared.)
      If the switching drive is above the threshold,
        read the departure the transition table gives for the mode the automaton stands in;
      otherwise the gate stands where it stands.

    To read a departure, given a judged automaton:
      Read the mode the automaton is standing in.
      Read the transition table out of the automaton.
      Find the row leaving that mode - without spelling the trigger a second time, so the
        table cannot drift out from under a lookup that names it twice.
      If there is no such row, refuse aloud rather than failing in silence.

    To apply a mode's own transfer function, given a mode and an arriving reading:
      Judge the arriving reading HERE, in the one place both transfer functions come through.
      Read the rule filed under that mode, through the register rather than beside it.
      Apply that rule: the open gate's law passes what arrives, the closed gate's law passes nothing.
      A rule the register files but this module cannot run is refused aloud, so the transfer
        block and the laws below it cannot drift apart in silence.

THE THREE LISTS THAT MUST AGREE. A two-mode register introduces a hazard a one-mode register does not have: the register block, the transfer block, and the laws that actually run are three lists keyed by the same names, and any two of them can drift apart. The register's own constructor already refuses a transfer for an undeclared mode, two rules under one name, and a declared mode carrying no rule. This slice closes the third seam - a rule the register files but the module cannot run - with a refusal rather than a silent failure, and pins all three lists together with a test that walks every declared mode and applies its law.

VALIDATED AGAINST A CORPUS ENTRY WHOSE REGISTER IS TWO. The Layer 8 modes volume's Entry 36, the mutual-inhibition flip-flop, coined the Either-Or Latch, is the corpus's own nearest relative of konnectome's gate: "a hysteretic toggle whose modes are simply its two self-holding states". Its register holds Mode A - the First Latch and Mode B - the Second Latch. Its per-mode transfer functions describe what holds while each state is realised. Its transition table carries a bias thrown from above on the circadian and homeostatic timescales and a supra-threshold input to the losing pool that flips it near-instantaneously. Its fault block names two regimes - instability and a stuck switch - and a watchdog. konnectome's gate was read block by block against that entry, and the one place the two shapes differ is DECLARED rather than smoothed over.

THE DECLARED DIFFERENCE: KONNECTOME'S GATE IS A SYMMETRIC TOGGLE, NOT A HYSTERETIC LATCH. The corpus's latch turns on at one boundary and off only at a lower one, and the corpus is explicit that the hysteresis is supplied by a STABILISER that is "not part of the switch" and publishes a stability bias of its own. konnectome's gate compares one switching drive against ONE threshold in both directions, so its two transition rows honestly share a single trigger. This slice does NOT invent the second threshold: the design authority already files the stabiliser as its own later slice, and this slice promises to change no number. The transition table records the shared trigger, the transfer functions record what each mode actually does, and the gap is written in the ledger as an observation rather than papered over here.

Gates at this slice: archetype grown from seven Prolog Unit (PLUnit) tests to twenty-nine, all passing (the other twenty-nine packs unchanged; SIX HUNDRED FORTY-EIGHT in total across THIRTY packs, all green); the naming gate reports zero violations across all thirty packs and passes its own self-test; the layer, layer-binding, and reality-discipline gates all report zero violations; the Causalontology conformance suite was re-measured this slice and is green at one hundred thirty-seven vectors (specification 4.0.0) in both cousins. No new wall was hit; nothing in PrologAI or Causalontology was edited.
